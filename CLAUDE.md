# 영어 암기 앱 개발가이드

## 프로젝트 개요

### 기본 정보
- **주제**: PWA 기반 학습 애플리케이션
- **프로젝트명**: Tuk Tak
- **배포 URL**: https://tuk-tak-beta.vercel.app/

### 컨셉트
- 개인의 목적에 맞는 영어를 거침없이 튀어나오게 연습시켜주는 앱

### 목적
- 개인의 목적에 맞는 영문을 확실하게 외워서 영어의 말문을 열게 만드는 앱
- 무료/유료 버전 구분 방향은 아래 "무료 / 유료 버전 구분" 섹션 참고 (2026-08-17 정리)

### 타깃 사용자
- 자신이 어떤 영어를 할것인지 알고 있는 영어학습자
- 적극적으로 자신이 외울 문장들을 수집할 수 있는 학습자

### 톤 & 비주얼 컨셉
- 맑고 청명한 파란색 기반으로 하되 레드계열의 포인트색을 사용해서 튀는 느낌살린다. 또한 파란색과 빨간색의 조화는 한국의 국기나 단청등의 색감을 의미하기도 한다.

### 하지 않을 것 (Non-goals) — 잠정, 진행하며 조정 가능
- 추후 진행하며 정함

### 무료 / 유료 버전 구분 (방향 — 2026-08-17 정리, 유료는 당장 추진하지 않음)
- 무료는 핵심가치("개인화된 문장을 확실하게 암기")에 집중, 유료는 기능 위주로 고도화하는 방향. 유료는 방향만 잡아두는 것이고 실제 개발은 착수하지 않음 — 착수 시점에 재논의
- **무료 (핵심)**
  - 문장 입력: 폼으로 하나씩 입력 / 파일(CSV·TSV·TXT)로 한 번에 가져오기 / 백업 파일로 복원
  - 카드 학습: 한국어 문장 표시 → 확인 → 영어 정답 표시
  - 텍스트로 답변 확인 (음성 답변은 무료에서 다루지 않음 — 아래 유료 참고, 2026-08-17 확정)
  - 문장관리(즐겨찾기·미암기 표시, 수정/삭제), 정렬(랜덤·최신·오래된·못외운것·중요한것 우선), 필터(전체/중요/미암기)
  - 글자 크기, 화면모드(시스템/라이트/다크) 등 학습 편의 설정
- **유료 (기능 고도화, 추후 개발 예정 — 지금은 방향만)**
  - 음성 답변 전체(마이크 입력 → 음성 인식 → 판정): 무료에는 음성 기능 자체가 없고, 유료에서만 제공 (2026-08-17 확정)
    - 인식된 텍스트와 정답 텍스트를 비교해 판정하는 기본 판정과, 발음 자체를 AI로 채점하는 발음 정확도 평가(예: Azure/Google Pronunciation Assessment 등 외부 AI 서비스) 둘 다 유료 범위
  - 문장 자동 변형: 입력한 문장을 AI로 시제/어순 등을 바꿔 추가 문제로 출제 (보류된 아이디어의 "문장 자동 변형 출제"와 동일 항목)
- **유료 버전 기술 방향 (2026-08-17 기록, 미착수)**: 앱을 완전히 새로 만들지 않고 **지금 앱을 그대로 고도화**하는 방향
  - 프론트엔드(HTML/CSS/Vanilla JS, PWA, LocalStorage)는 그대로 유지. Web Speech API도 브라우저 내장이라 그대로 재사용 가능
  - 새로 필요한 것: (1) AI API 키를 클라이언트에 노출하지 않기 위한 서버리스 함수(Vercel Serverless/Edge Functions — 지금 배포도 Vercel이라 같은 프로젝트에 추가 가능, 별도 서버/기술스택 불필요) (2) 유료 사용자 구분을 위한 계정/로그인 + 결제(구독) 시스템 — 지금은 로그인 자체가 없고 기기별 LocalStorage뿐이라 이 부분은 신규 도입 필요
  - 착수 시점에 이 방향을 기준으로 상세 설계 진행

## 작업 규칙

### 1. 승인 원칙
- "시작" 또는 "go"라는 말이 나오기 전까지는 코드를 읽고 분석만 하고, 실제로 파일을 만들거나 수정하지 않을 것.
- 여기서 "단계"는 1~9단계 같은 큰 번호 순서가 아니라 **하나의 완결된 기능 단위**(예: 헤더, 슬라이더, 더보기 메뉴의 "내보내기" 기능 등)를 뜻함.
  - 기능 하나가 UI(예: 3단계)와 실제 동작 로직(예: 5단계)처럼 서로 다른 번호의 체크리스트에 걸쳐 있을 수 있음. 이런 경우 UI만 먼저 만들고 로직은 나중에 따로 만들지 않고, **기능이 실제로 동작하도록 관련 항목을 한 번에 묶어서** 진행하는 것을 기본으로 함 (재작업 비용을 줄이기 위함).
  - 다만 서로 다른 기능을 한 세션에 여러 개 묶어 진행하는 것은 여전히 금지 (절대 금지 사항 참고).
- 승인은 **세션 전체에 한 번이 아니라, 기능 단위마다 매번** 받을 것.
- 한 기능이 끝나면 자동으로 다음 기능으로 넘어가지 말고, 반드시 새로 "시작"/"go"를 기다릴 것.
- 작업을 시작하기 전, 다음 형식으로 계획을 먼저 설명할 것.
  > "이번 단계에서는 [무엇을] 위해 [어떤 파일들]을 [어떻게] 수정하겠습니다."
  - 하나의 기능이 여러 번호의 체크리스트 항목을 함께 완료시키는 경우, 계획 설명 시 어느 항목들이 함께 체크되는지 명시할 것.

### 2. 작업 기록 규칙 (이전 작업 기억하기)
- 새 세션을 시작할 때 먼저 `PROGRESS.md` 파일이 있는지 확인하고, 있으면 내용을 읽을 것.
- 가장 마지막에 기록된 작업 내용을 요약해서 사용자에게 보고할 것.
- "다음 작업 제안"이 적혀 있으면 함께 보고할 것.
- `PROGRESS.md`가 없으면 "이전 작업 기록이 없습니다"라고 알릴 것.
- `PROGRESS.md` 맨 아래에 기록을 추가할 것 (기존 기록은 지우지 말고 누적할 것).
- 작업 순서는 체크리스트를 확인하여 순서대로 진행하되, 번호 순서는 권장 순서일 뿐 절대 규칙은 아님 — 진행 중인 기능이 다른 번호의 항목과 연결되어 있다면 그 항목까지 함께 진행 가능 (위 "1. 승인 원칙" 참고)
- 진행 완료된 체크리스트에 체크 표시 할 것
- 작업 전 항상 계획을 먼저 설명하고 승인을 받을 것
- 파일 수정 시 변경 내용을 먼저 요약할 것
- 각 작업 완료 후 CLAUDE.md 체크리스트를 반드시 업데이트 할 것
- [x] 처리 시 완료 날짜를 괄호로 표기할 것. 예: - [x] 로그인 UI (2026-04-01)

### 3. 검증 규칙 (기능 구현 후 확인)
- 자동화 테스트(로컬 서버/헤드리스 브라우저 등, `localhost`·`file://` 기준)를 통과했다고 해서 실제 환경에서도 정상 동작한다고 단정하지 말 것.
- 기능 구현 완료 후 확인은 2단계로 나눠 진행할 것.
  1. Claude가 자동화 테스트로 1차 확인
  2. 사용자가 실제 기기/네트워크 환경(폰 브라우저, 필요 시 PC를 LAN IP로 접속 등)에서 2차 확인
- 2차 확인이 끝나기 전까지는 다음 기능 단계로 넘어가지 말 것.

## 절대 금지 사항
- 승인 없이 파일 삭제 금지
- 여러 기능을 한 번에 진행 금지 (단, 하나의 기능이 여러 번호의 체크리스트 항목에 걸쳐 있는 경우는 예외 — 위 "1. 승인 원칙" 참고)
- 체크리스트에 없는 기능 임의 추가 금지
- 라이브러리 / 프레임워크 임의 설치 금지

---

## 체크리스트 (잠정 — 대략의 흐름만, 세부 항목은 진행하며 구체화)

> 아직 확정된 사양이 아니며, 순서와 세부 내용은 진행하면서 조정될 수 있음

- [x] 1. 기본 카드 UI: 화면에 한국어 문장을 보여주고 확인할 수 있는 카드 형태 구성 (2026-08-04)
- [x] 2. 텍스트 답변 흐름: 글로 영어 답을 확인하는 방식 (세부 동작은 추후 결정) (2026-08-04)
- [ ] 3. 음성 답변 흐름: 마이크로 영어 답변 → 음성 인식(Web Speech API) → 정답과 비교 판정. **무료에는 포함하지 않는 유료 전용 기능**으로 확정(텍스트 비교 판정·발음 정확도 평가 모두 유료) — 착수는 보류 (2026-08-17 방향 확정, 위 "무료 / 유료 버전 구분" 참고)
- [x] 4. 문서(엑셀/메모장 등)로 문장을 작성 후 파일(CSV/TSV/TXT)로 앱에 가져오기 (2026-08-11)
  - ~~구글시트 "웹에 게시(CSV)" 링크로 실시간 연동~~ (2026-08-11 제거): 게시 갱신 지연·브라우저 캐싱 문제가 반복되고, 매번 링크를 다시 붙여넣어야 해 실효성이 낮다고 판단. 대신 엑셀/구글시트에서 표로 작성 후 CSV로 다운로드 → 파일 가져오기 방식으로 전환 (파일 가져오기는 이미 구현되어 있어 추가 작업 없이 바로 사용 가능)
- [x] 5. 글자 크기 설정: 카드 화면 텍스트(한국어/영어) 크기를 5단계(도트 인디케이터 + `−`/`+` 스테퍼)로 조절. 견본 글자("가")로 실시간 크기 확인 가능 (2026-08-12, 견본은 2026-08-13 추가)
  - ~~별도 "설정" 하단 시트~~ (2026-08-12 제거) → ~~상단바 더보기 드롭다운 안 상시 노출~~ (2026-08-13 제거, 아래 8번 "설정 화면"으로 이동): 더보기 메뉴 항목이 늘어나며 데이터관리처럼 버튼이 여러 개 필요한 그룹이 생겨 전체화면 설정으로 재편
- [x] 6. 화면모드: 시스템/라이트/다크 3단 선택. "시스템" 선택 시 기기 다크모드 설정을 실시간으로 따라감 (2026-08-12 라이트/다크 토글로 최초 구현, 2026-08-13 3단 선택으로 확장)
  - ~~단순 라이트/다크 토글 스위치~~ (2026-08-13 제거): 참고 이미지(`temp/setting.jpg`)를 보고 시스템 연동 옵션을 추가로 요청받아 세그먼트 버튼(시스템/라이트/다크)으로 교체
- [x] 7. 문장 목록 보기 + 수정/삭제: 더보기 메뉴 "문장관리" → 전체화면 목록. 항목 탭 시 기존 "문장 추가" 모달을 재사용해 수정 (2026-08-13)
  - 삭제 방식: ~~각 항목에 항상 보이는 삭제 아이콘 버튼~~ (2026-08-15 변경) → 항목을 길게 누르면(롱프레스) 그 문장만 삭제 확인, 상단 "선택" 버튼으로 체크박스 다중 선택 모드에 진입해 여러 개를 한 번에 삭제. 사용자가 "삭제는 흔치 않은데 버튼이 항상 달려 있을 필요는 없다"고 판단, 이메일 앱 방식(선택모드+체크박스)을 제안 → 자주 쓰는 단일 삭제는 롱프레스로 더 빠르게, 드문 다중 삭제는 선택모드로 분리. 최소 1개는 유지되도록 확인창 및 개수 검증 유지 (2026-08-15)
- [x] 8. 설정 화면 신설: 더보기 메뉴를 문장추가/문장관리/설정 3항목으로 축소. 설정 화면(전체화면)에 글자크기·화면모드(더보기에서 이동) + 데이터 관리(파일가져오기 이동, 백업 내보내기 신규 — TSV 다운로드, 초기화 신규 — 문장 전체 삭제, 확인창 필수) 배치. 하단에 앱 이름·최근 업데이트 날짜 표시 (2026-08-13)
- [x] 9. 출제 순서 / 정렬: 카드 학습 출제 순서 + 문장관리 목록 정렬에 랜덤순/최신순/오래된순/못외운것/중요한것 5가지 기준 적용(설정 화면에 옵션 UI 신설). 아래 3개 기능 단위로 나눠 순서대로 진행 (2026-08-15 계획 확정, 2026-08-16 전체 완료)
  - [x] 9-1. 즐겨찾기(중요 표시): 문장별 별표 토글 데이터(`important`)+UI(문장관리 목록) (2026-08-15, 카드 화면 노출은 아래 참고)
  - [x] 9-2. 미암기 표시: 문장별 미암기 토글 데이터(`unfamiliar`)+UI(문장관리 목록) (2026-08-15)
  - [x] 9-3. 정렬 옵션 적용: 설정 화면에 5가지 기준 선택 UI(세로 라디오 목록) 추가 + 카드 출제 순서·문장관리 목록 정렬 로직에 반영. 기본값은 오래된순(기존 순서와 동일). "못외운것 먼저"/"중요한것 먼저"는 필터가 아니라 우선 정렬(해당 문장을 앞으로, 나머지는 뒤에 최신순으로 이어붙임). 랜덤순은 라디오를 다시 선택할 때만 재섞고 앱 재시작으로는 순서가 바뀌지 않음(LocalStorage에 섞은 결과 저장) (2026-08-16)
  - ~~자가평가(외웠어요/아직이요, 카드 화면에 정답 확인 후 노출)~~ (2026-08-15 제거): 구현 후 사용자가 "카드 화면에 버튼이 갑자기 늘어나 헷갈린다"고 판단, 즐겨찾기 카드 화면 노출도 함께 제거. 문장별 메타데이터(중요/미암기) 조작은 전부 문장관리 목록으로 일원화하고, 자가평가는 "미암기" 토글(목록에서 직접 켜고 끄는 방식)로 대체
- [x] 10. 카드 화면 즉시 표시 + 문장관리 필터: 실사용 중 "못외운 문장을 문장관리에서 다시 찾기 번거롭다"는 문제 발견 → 카드 화면 정답 확인 후 답 아래에 ☆(중요)/⚑(미암기) 아이콘 노출(9-1·9-2 때 제거했던 카드 노출을 아이콘 형태로 재도입, 텍스트 버튼 2개 대신 아이콘이라 예전 "버튼이 많아 헷갈림" 문제는 재발하지 않음), 문장관리 화면 상단에 전체/중요/미암기 필터 탭 추가(정렬과 독립적으로 적용, 필터 상태에서도 롱프레스 삭제·선택모드 다중삭제 그대로 동작). 문장관리 진입 시 필터는 항상 "전체"로 초기화 (2026-08-16)
- [x] 11. 텍스트 붙여넣기로 가져오기: 사진(노트 등)을 ChatGPT로 텍스트 변환한 결과처럼, 파일로 저장하지 않고 텍스트를 앱에 바로 붙여넣어 여러 문장을 한 번에 추가. 설정 > 데이터 관리에 "텍스트 붙여넣기" 버튼 신설, 기존 파일가져오기와 동일한 파서(`parseSentencesText`, 탭/쉼표/연속공백 자동 인식) 재사용 (2026-08-17)
- [x] 12. 카드 화면에서 바로 수정/삭제: 정답 확인 후 노출되는 마킹 아이콘 바(☆/⚑)에 구분선 + 연필(수정)·휴지통(삭제) 아이콘 추가. 손글씨 인식 오류처럼 학습 중 바로 눈에 띈 오탈자를 문장관리 목록을 따로 찾아가지 않고 그 자리에서 고치기 위함. 수정은 기존 "문장 추가/수정" 모달을 그대로 재사용, 삭제는 기존 확인창·최소 1개 유지 규칙(`confirmDeleteSingle`)을 그대로 재사용. 문장관리 화면의 기존 수정(목록 탭)/삭제(롱프레스·선택모드)는 변경 없이 유지 (2026-08-17)
- [x] 13. 문장추가 진입점 통합: "문장추가"(더보기 메뉴, 폼)와 "파일가져오기"·"텍스트 붙여넣기"(설정 > 데이터 관리)가 같은 성격(문장 입력)인데 메뉴가 나뉘어 있던 문제를 해결. "문장추가" 모달 상단에 [폼 입력 / 파일 가져오기 / 텍스트 붙여넣기] 3개 탭(세그먼트 버튼)을 두어 하나로 통합, 설정 > 데이터 관리는 백업 내보내기·초기화 2개만 남김. 문장 수정(문장관리 목록 탭, 카드 화면 연필 아이콘)은 방법을 고를 필요가 없어 같은 모달을 재사용하되 탭은 숨기고 폼만 노출 (2026-08-17)

### 보류된 아이디어 (체크리스트 미등재, 착수 시 재논의)
- **기본문장 100개 제공**(2026-08-17 신규): 사용자가 나중에 내용을 확정하면, 처음부터 다 채워주기보다 원할 때 골라 담는 방식으로 제공(핵심가치인 "개인화된 문장 수집"과 결이 맞음). 진입점은 위 13번에서 통합한 "문장추가" 탭에 4번째 탭("기본문장 사용하기")으로 배치하기로 방향 합의. 기본문장 내용이 아직 미정이라 착수 보류
- **문장 자동 변형 출제**(핵심 고도화 후보, 유료 기능으로 확정 — 2026-08-17, 위 "무료 / 유료 버전 구분" 참고): 시제/수 등을 변형해 추가 문제로 출제. 규칙 기반 vs LLM API 방식 미결정
- **태그/카테고리 필터**(2026-08-15 신규): 문장별 태그(여행/비즈니스/일상 등)를 붙여 목적별로 골라 학습. "개인화된 문장 수집"이라는 핵심가치와 맞닿아 있어 유력하지만, 문장 입력 UI·CSV 가져오기 포맷에 태그 컬럼을 추가해야 해 범위가 커서 9번(출제 순서/정렬) 이후로 보류
- **글씨체 선택**(고딕/궁서/귀여운 등): 기본 폰트 Pretendard가 브랜드 정체성으로 고정돼 있어 충돌, 폰트 파일 확보 작업도 필요해 보류 (2026-08-13)
- **글씨 굵기 선택**: 글자크기 기능과 목적이 겹쳐 우선순위 낮음 (2026-08-13)
- **저장 공간 사용량 표시**: 개인용 소량 데이터라 5MB 한도 도달 가능성이 낮아 정보성 대비 효용 낮음 (2026-08-13)
- **아이콘 하단 고정**(PWA 네이티브 스플래시): Android는 manifest만으로 위치 조정이 불가능해 인앱 배치 vs iOS 전용 커스텀 이미지 중 방향 미정
- **음성 답변 흐름**(체크리스트 3번): 마이너 목표로 후순위 합의. **무료에는 음성 기능 자체를 포함하지 않고 유료 전용으로 확정**(텍스트 비교 판정·발음 정확도 평가 모두 유료) — 착수는 보류 (2026-08-17 방향 확정)
- **앱 내 설명서/도움말 화면**(2026-08-16 신규): "문장 입력 방법"(위 섹션 참고) 등 코드를 안 보면 알기 어려운 사용법(구분자 자동 인식, 백업 파일로 복원하는 법 등)을 앱 안에서 안내. 지금은 입력 경로가 3개뿐이라 전용 화면을 만들기엔 이르다고 판단, 입력 방법이 더 늘어나(특히 문장 자동 변형의 "안내형" 방식이 추가되면) 설명할 지점이 많아질 때 착수하는 게 효율적일 것으로 보임. CLAUDE.md의 "문장 입력 방법" 섹션이 착수 시 초안 역할을 할 예정

---

## 기술 스택
- Frontend: HTML5, CSS3, Vanilla JavaScript
- PWA: Service Worker, Web App Manifest
- Storage: LocalStorage (문장 데이터 캐싱, 학습 진행 상태 저장)
- Backup: TSV 파일 내보내기(설정 > 데이터 관리 > 백업 내보내기) — 복원은 별도 가져오기 로직 없이 기존 "파일가져오기" 기능을 그대로 재사용 (2026-08-13, 데이터 입력을 CSV/TSV로 통일한 방향과 형식을 맞추기 위해 최초 계획의 JSON에서 변경)
- 배포: Vercel
- 버전관리: GitHub
- 답변 입력: 텍스트 확인(무료) + 음성 답변(유료 전용, 착수 보류 — 2026-08-17 확정, 무료/유료 버전 구분 섹션 참고)
- 음성 인식: Web Speech API (SpeechRecognition) — 브라우저 내장 기능으로 영어 음성 답변을 텍스트로 변환 (유료 기능 착수 시 사용 예정)
- 데이터 입력: 앱 내 폼 입력(문장 하나씩) + 파일 가져오기(엑셀/메모장 등에서 작성한 CSV/TSV/TXT 파일을 한 번에 가져오기). 가져온 데이터는 LocalStorage에 저장해 오프라인에서도 사용 가능

---

## 메뉴 기능 정리 (사용자 가이드용 — 추후 앱 내 설명서로 확장 예정)

> 화면별로 각 메뉴/버튼이 하는 일을 정리해둠. 메뉴 구조가 바뀌거나 기능이 추가되면 이어서 갱신할 것 (2026-08-17 최초 작성, 문장추가 탭 통합 반영해 같은 날 갱신)

### 카드 화면
- 이전 / 확인 / 다음: 카드 학습 핵심 조작. "확인"을 누르면 영어 정답이 노출됨
- 정답 확인 후에만 노출되는 마킹 아이콘 바: ☆(중요 표시 토글) · ⚑(미암기 표시 토글) · ✏(현재 카드 문장 수정 — 문장추가 모달을 수정 모드로 재사용) · 🗑(현재 카드 문장 삭제 — 문장관리와 동일한 확인창·최소 1개 유지 규칙 재사용)

### 더보기(⋮) 드롭다운
- **문장추가**: 문장을 새로 넣는 모든 방법을 모달 하나에 탭으로 통합(2026-08-17). 상단 [폼 입력 / 파일 가져오기 / 텍스트 붙여넣기] 3탭 중 선택
  - 폼 입력: 한국어/영어 문장을 하나씩 입력해 추가
  - 파일 가져오기: 엑셀/메모장 등에서 작성한 CSV/TSV/TXT 파일을 업로드해 문장 여러 개를 한 번에 추가
  - 텍스트 붙여넣기: 파일로 저장하지 않고 텍스트를 바로 붙여넣어 문장 여러 개를 한 번에 추가(ChatGPT 등으로 변환한 텍스트를 바로 넣을 때 유용)
  - (다른 입력 경로의 자세한 규칙은 아래 "문장 입력 방법" 참고)
- **문장관리**: 전체 문장 목록. 항목을 탭하면 "문장추가" 모달이 수정 모드로 열림(이때는 탭 없이 폼만 노출), 길게 누르면(롱프레스) 해당 문장만 삭제 확인, 상단 "선택" 버튼으로 다중 선택 모드에 진입해 여러 개 한 번에 삭제. 상단 전체/중요/미암기 필터 탭으로 목록을 좁혀볼 수 있음(정렬과는 독립적인 축)
- **설정**: 아래 설정 화면으로 이동

### 설정 화면
- **글자크기**: 카드 화면 한국어/영어 텍스트 크기를 5단계로 조절(도트 인디케이터 + `−`/`+` 스테퍼, "가" 견본 글자로 실시간 미리보기)
- **화면모드**: 시스템/라이트/다크 3단 선택. "시스템"을 고르면 기기의 다크모드 설정을 실시간으로 따라감
- **출제/정렬 순서**: 카드 학습 출제 순서 + 문장관리 목록 정렬 기준을 랜덤순/최신순/오래된순/못외운것 먼저/중요한것 먼저 중에서 선택
- **데이터 관리** (2026-08-17부터 백업/초기화만 남음 — 파일가져오기·텍스트 붙여넣기는 "문장추가" 탭으로 이동)
  - 백업 내보내기: 현재 저장된 문장 전체를 TSV 파일로 다운로드. 다른 기기로 옮기거나 복원용으로 사용
  - **초기화**: 저장된 문장을 전부 삭제. "문장을 모두 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다" 확인창을 통과해야 실행됨. **삭제되는 건 문장 데이터뿐**이고 글자크기·화면모드·정렬 등 다른 설정값은 그대로 유지됨. 별도 복구 기능은 없어서, 되돌리려면 백업 내보내기로 미리 받아둔 TSV 파일을 문장추가 > 파일 가져오기(또는 텍스트 붙여넣기)로 다시 불러와야 함

---

## 문장 입력 방법 (사용자 가이드용 — 추후 앱 내 설명서로 확장 예정)

> "개인화된 문장 수집"이 이 앱의 핵심가치라, 사용자가 직접 문장을 넣는 방법을 여기에 정리해둠. 새 입력 경로가 추가될 때마다 이 목록에 이어서 번호를 붙여 기록. 추후 앱 안에 도움말/설명서 화면을 만들 때 이 내용을 그대로 가져다 쓸 것 (2026-08-16 최초 작성)

> 2026-08-17부터 아래 1·2·4번은 모두 카드 화면 상단 "더보기"(⋮) > "문장추가" 모달 안 탭 하나로 통합돼 있음(폼 입력/파일 가져오기/텍스트 붙여넣기 탭 전환). 예전에는 파일가져오기·텍스트 붙여넣기가 설정 > 데이터 관리에 있었음.

1. **폼으로 하나씩 입력**: "문장추가" > "폼 입력" 탭 → 한국어/영어 입력 후 추가 버튼. 문장관리 목록에서 항목을 탭하거나 카드 화면의 ✏ 아이콘을 누르면 같은 모달이 "수정" 모드로 열림(이때는 탭 없이 폼만 노출).
2. **파일로 여러 개 한 번에 가져오기**: 엑셀/구글시트/메모장 등에서 "한국어 [구분자] 영어" 형태로 표를 작성 → CSV/TSV/TXT 파일로 저장 → "문장추가" > "파일 가져오기" 탭 → 파일 선택으로 업로드.
   - 구분자는 자동 인식됨: 탭(TSV) 우선 → 없으면 쉼표(CSV) → 그것도 없으면 연속 공백 2칸 이상(메모장 등에서 탭이 스페이스로 바뀌는 경우 대응)
   - 첫 줄이 "한국어"/"kr"/"korean" 같은 헤더로 보이면 자동으로 건너뜀
3. **백업 파일로 복원/이전**: 설정 > 데이터 관리 > 백업 내보내기로 받은 TSV 파일은 별도 복원 기능 없이, 위 2번 "파일 가져오기" 탭으로 그대로 다시 불러오면 됨(다른 기기로 옮길 때도 동일한 방법)
4. **텍스트 붙여넣기로 여러 개 한 번에 가져오기** (2026-08-17 추가): 손글씨 노트를 사진으로 찍어 ChatGPT 등으로 텍스트 변환한 결과처럼, 파일로 저장하지 않고 텍스트를 바로 붙여넣어 가져오기. "문장추가" > "텍스트 붙여넣기" 탭 → 텍스트 입력창에 붙여넣고 "가져오기". 구분자 자동 인식·헤더 자동 스킵 규칙은 2번(파일 가져오기)과 동일(같은 파서 재사용)

---

## 디자인 기획

### 컬러 시스템

## Brand Colors

| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Brand Blue | `--color-brand` | `#378ADD` | 앱 아이콘, 헤더/네비게이션 바, 주요 CTA 버튼, 로고 |
| Brand Navy | `--color-brand-dark` | `#0C447C` | 헤더 타이틀, 본문 강조 텍스트, 진한 배경 위 아이콘 |
| Base White | `--color-base` | `#FFFFFF` | 전체 배경, 카드 배경 (다크모드에서 `#14171C`로 전환되는 테마 배경색 — 브랜드색 배경 위 고정 흰색 텍스트/아이콘에는 이 변수 대신 리터럴 `#FFFFFF` 사용, 아래 UI 레이아웃 규칙 참고) |
| Point Coral | `--color-point` | `#D85A30` | ~~정답/성취 표시, 즐겨찾기·별표, 배지, 보조 CTA~~ **(2026-08-08 사용 보류 — 아래 참고)** |

> **Point Coral 사용 보류 (2026-08-08)**: 지금 단계에서는 버튼 등 UI 요소에 Point Coral을 사용하지 않음. 브랜드 블루/네이비/화이트 + 그레이스케일로 디자인을 진행하고, 진행 중 꼭 필요한 곳이 생기면 그때 다시 적용 여부를 논의. 너무 튀는 느낌이면 아예 제외할 수도 있음. 변수 자체는 삭제하지 않고 유지.

<!-- 그레이스케일 / 다크모드 컬러: TBD -->
### Grayscale
| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Gray 50 | `--color-gray-50` | `#F5F7FA` | 페이지 배경, 섹션 구분 배경 |
| Gray 100 | `--color-gray-100` | `#E7EAEE` | 카드 테두리(연함), 구분선 |
| Gray 200 | `--color-gray-200` | `#D3D8DE` | 입력창 테두리, 비활성 버튼 배경 |
| Gray 300 | `--color-gray-300` | `#B4BAC2` | 플레이스홀더 텍스트, 비활성 아이콘 |
| Gray 400 | `--color-gray-400` | `#8D949E` | 보조 아이콘, 캡션 |
| Gray 500 | `--color-gray-500` | `#6B7280` | 보조 텍스트(설명문) |
| Gray 600 | `--color-gray-600` | `#4B5563` | 라벨, 서브 헤딩 |
| Gray 700 | `--color-gray-700` | `#374151` | 강조 보조 텍스트 |
| Gray 800 | `--color-gray-800` | `#23272F` | 본문 텍스트(기본) |
| Gray 900 | `--color-gray-900` | `#14171C` | 헤딩, 최고 강조 텍스트 |
#### Light Mode

(위 Grayscale 표 값이 라이트 모드 기본값)

#### Dark Mode (2026-08-12 확정, `[data-theme="dark"]`)
| 변수명 | 다크모드 HEX | 비고 |
|---|---|---|
| `--color-base` | `#14171C` | 페이지/카드/모달 배경 |
| `--color-gray-50` | `#1C2027` | |
| `--color-gray-100` | `#262B33` | 구분선 |
| `--color-gray-200` | `#333944` | 입력창 테두리, 비활성 배경 |
| `--color-gray-300` | `#4B5563` | 플레이스홀더 |
| `--color-gray-500` | `#9AA6B2` | 보조 텍스트 |
| `--color-gray-800` | `#E7EAEE` | 본문 텍스트 |
| `--color-gray-900` | `#F5F7FA` | 헤딩/최고 강조 텍스트 |

- `--color-brand`/`--color-brand-dark`/`--color-point`는 다크모드에서도 값 유지(포인트 컬러로 그대로 사용)
- `body`/`.kr-text`/`.modal-title`/`.btn-outline`/`.stepper-btn`처럼 라이트 모드에서 `--color-brand-dark`를 텍스트색으로 쓰는 요소는 다크 배경 위에서 시인성이 떨어져 `[data-theme="dark"]`에서 개별적으로 `--color-gray-900`(밝은 값)로 재지정

#### CSS 변수 선언 규칙
- 모든 CSS 변수는 `css/style.css` 상단의 `:root {}` 블록에만 선언
- 다크모드 변수는 `[data-theme="dark"] {}` 셀렉터로 관리
- 변수명 임의 변경 금지

### 타이포그래피
- **기본 폰트**: Pretendard
- **폰트 파일 위치**: `/public/fonts/pretendard/` (woff2, web/static 서브셋)
- **사용 웨이트**: 400 Regular · 500 Medium · 600 SemiBold · 700 Bold
- **font-family 선언**: `'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif`

### 아이콘
- **라이브러리**: lucide
- **구현 방식**: npm/CDN 설치 없이, 필요한 아이콘만 lucide 공식 SVG를 그때그때 인라인으로 직접 삽입해 사용 (오프라인 PWA 특성상 외부 로드 의존이 생기지 않도록 함). 현재 사용 중: `circle-plus`(문장추가), `list`(문장관리), `settings`(설정), `chevron-left`(전체화면 뒤로가기), `upload`(문장추가 모달의 파일 가져오기 탭), `download`(백업 내보내기), `trash-2`(초기화·문장 삭제), `ellipsis-vertical`(상단바 더보기) (2026-08-13), `star`(문장관리 목록 즐겨찾기), `flag`(문장관리 목록 미암기 표시), `check`(선택 모드 체크박스) (2026-08-15), `star`/`flag`를 카드 화면 마킹 아이콘 바에도 재사용(같은 데이터를 다른 화면에서 조작하는 것이라 동일 아이콘 유지) (2026-08-16), `clipboard-paste`(문장추가 모달의 텍스트 붙여넣기 탭) (2026-08-17), `pencil`(카드 화면 바로 수정), `trash-2`를 카드 화면 바로 삭제에도 재사용(초기화·문장관리 삭제와 동일 아이콘) (2026-08-17)

### UI 레이아웃 규칙
- **카드 화면 상단바**: 화면 좌우 끝까지 꽉 찬 브랜드 블루(`--color-brand`) 바에 왼쪽 로고, 오른쪽 "더보기"(⋮) 버튼 배치. 로고/아이콘은 항상 어두운 배경 위이므로 테마와 무관하게 리터럴 `#FFFFFF` 고정 사용 (2026-08-12, 헤더 색상은 2026-08-16에 네이비→블루로 변경, 아래 색상 스왑 항목 참고)
- **더보기 드롭다운 메뉴**: 문장추가·문장관리·설정 3항목만 플랫하게 배치 (2026-08-13, 항목이 5개로 늘어나며 글자크기/다크모드/데이터관리를 별도 "설정" 전체화면으로 분리 — 항목이 적을 때는 드롭다운이 낫다는 8/12 판단은 유지하되, 데이터관리처럼 버튼이 여러 개 필요한 그룹이 생기면서 전체화면으로 전환)
- **전체화면(문장관리/설정) 공용 헤더**: 브랜드 블루 바에 왼쪽 뒤로가기(`chevron-left`), 가운데 타이틀. `.screen-topbar`/`.screen-back-btn`/`.screen-topbar-title` 클래스를 두 화면이 공유 (2026-08-13)
  - **문장관리 목록 선택 모드**: 상단 "선택" 버튼을 누르면 헤더 배경이 브랜드 네이비로 바뀌고, 뒤로가기 아이콘이 "취소" 텍스트로, 타이틀이 "N개 선택"으로 전환되며 오른쪽에 "삭제" 버튼 노출(0개 선택 시 비활성). 목록 항목도 별/깃발 아이콘 대신 체크박스로 전환 (2026-08-15, 선택모드 강조색은 2026-08-16 헤더 색상 스왑으로 블루→네이비 변경)
- **설정 화면 구성**: 글자크기(도트+스테퍼, "가" 견본 글자로 현재 크기 실시간 미리보기)·화면모드(시스템/라이트/다크 3단 세그먼트 버튼 — "시스템" 선택 시 기기 설정을 실시간으로 따라감) 행에 이어 "데이터 관리" 섹션 제목 아래 파일가져오기/백업 내보내기/초기화 버튼을 세로로 나열 (2026-08-13)
  - **출제/정렬 순서 행 추가** (2026-08-16): 화면모드 행과 "데이터 관리" 섹션 사이에 배치. 5개 옵션(랜덤순/최신순/오래된순/못외운것 먼저/중요한것 먼저)은 화면모드처럼 한 줄 세그먼트로 두면 좁은 폰에서 답답해 보여, 세로 라디오 목록(`.sort-option-list`/`.sort-option-btn`)으로 별도 구현
  - **데이터 관리에서 파일가져오기·텍스트 붙여넣기 제거** (2026-08-17): 아래 "문장추가 모달 탭 통합"으로 이동하며, 데이터 관리에는 백업 내보내기·초기화 2개만 남음
- **문장추가 모달 탭 통합** (2026-08-17): "문장추가"(더보기 메뉴, 폼 입력)와 "파일가져오기"·"텍스트 붙여넣기"(예전엔 설정 > 데이터 관리)가 전부 "문장을 새로 넣는 방법"이라는 같은 성격인데 메뉴만 나뉘어 있던 문제를 해결. "문장추가" 모달 상단에 [폼 입력 / 파일 가져오기 / 텍스트 붙여넣기] 3탭(`#add-modal-tabs`, 화면모드 세그먼트와 같은 스타일)을 두고 탭에 따라 내용만 전환. 문장 수정(문장관리 목록 탭, 카드 화면 ✏ 아이콘)은 방법을 고를 필요가 없으므로 같은 모달을 열되 탭 자체를 숨기고 폼만 노출(`openAddModal(sentence)`가 있으면 탭 숨김, 없으면 탭 노출 + "폼 입력" 탭으로 초기화). 더보기 드롭다운에 파일가져오기·텍스트 붙여넣기를 항목으로 직접 추가하는 대안은 2026-08-13에 해결한 "드롭다운 항목 과다" 문제를 재발시켜 제외
  - **폼 입력 필드 확대** (2026-08-17): 한국어/영어 입력창이 한 줄 `<input>`이라 좁아 보인다는 피드백으로 3줄 높이의 `<textarea rows="3">`로 교체(세로로만 크기 조절 가능). 자동 포커스도 함께 제거해(같은 날 별도 요청) 모달을 열어도 모바일 자판이 자동으로 뜨지 않음
- **카드 화면 하단 구조**: 상단바 이동 후 하단에는 학습 핵심 버튼인 이전/확인/다음(`control-bar`)만 남김 (2026-08-12)
  - **마킹 아이콘 바 추가** (2026-08-16): 정답 확인 후에만 문장 텍스트와 같은 `.card` 블록 안(정답 아래 70px)에 ☆(중요)/⚑(미암기) 아이콘 노출(`#card-mark-bar`). `.card`가 화면 안에서 세로 중앙 정렬되므로, 별도 그리드 행으로 분리하면 화면 크기에 따라 문장과의 거리가 들쭉날쭉해짐 — 문장과 한 블록으로 묶어야 화면 크기와 무관하게 일정한 간격이 유지됨. 정렬 기준이 "중요한것/못외운것 먼저"일 때 토글하면 순서가 바뀔 수 있어, 토글 직후 같은 문장이 화면에 계속 보이도록 `currentIndex`를 그 문장의 새 위치로 맞춰줌(점프 방지)
  - **마킹 아이콘 바에 수정/삭제 추가** (2026-08-17): 손글씨 인식 오류처럼 학습 중 바로 눈에 띈 오탈자를 문장관리를 따로 찾아가지 않고 그 자리에서 고치도록, ☆/⚑ 뒤에 얇은 구분선(`.card-mark-divider`) + 연필(수정)/휴지통(삭제) 아이콘 추가. 수정은 문장관리와 동일한 모달(`openAddModal`), 삭제는 문장관리와 동일한 확인창·최소 1개 유지 로직(`confirmDeleteSingle`)을 그대로 재사용해 로직 중복 없음. 카드에서 삭제 시 남은 문장 수보다 `currentIndex`가 커질 수 있어 `renderCard()`에 범위 보정 로직 추가(마지막 카드를 삭제해도 죽지 않고 이전 카드로 자연스럽게 이동)
- **카드 화면 빈 상태**: 문장이 0개(설정 > 초기화 직후)이면 카드 텍스트 대신 안내 문구를 보여주고 이전/확인/다음 버튼을 비활성화. 문장추가로 다시 채워지면 자동으로 정상 카드 화면으로 복귀 (2026-08-13)
- **문장관리 필터 탭** (2026-08-16): 상단바 아래에 전체/중요/미암기 세그먼트 버튼(`.list-filter-bar`) 추가. 정렬(9-3, 우선 정렬 방식)과는 독립적인 별도 축 — 필터는 조건에 안 맞는 문장을 목록에서 아예 제외. 화면 진입 시 항상 "전체"로 초기화되고, 선택 모드(다중 삭제) 진입 시에는 숨김
- **브랜드색 배경 위 고정 흰색 규칙**: 시작 화면(`.start-logo`, `.btn-primary`)과 상단바처럼 배경이 항상 브랜드 블루/네이비로 고정된 요소는 다크모드 전환과 무관해야 하므로, 테마에 따라 값이 바뀌는 `--color-base` 대신 리터럴 `#FFFFFF`를 사용 (2026-08-12)
- **헤더/상태바 색상 스왑** (2026-08-16): 카드·문장관리·설정 화면의 상단바 기본 배경을 네이비→블루로, 문장관리 선택모드 강조색은 블루→네이비로 서로 맞바꿈(대비는 유지). 동시에 상태바(PWA `theme-color`, `manifest.json`의 `theme_color`)를 블루→네이비로 변경해 헤더와 상태바가 서로 다른 색을 쓰도록 함. 로딩 스플래시 배경(`manifest.json`의 `background_color`)은 기존 블루 그대로 유지


## 파일 구조


## 코딩 규칙

### HTML
- 시맨틱 태그 우선 사용 (header, nav, main, article, section, footer)
- 주석은 한국어로 작성
- 들여쓰기는 2칸

### CSS
- 클래스명은 명확한 의미 전달
- Vanilla CSS 사용 (프레임워크 사용 금지)
- 주석으로 섹션 구분
- 컬러 시스템은 CSS 변수로 관리 (변수명 임의 변경 금지)

### JavaScript
- ES6+ 문법 사용
- 가독성을 위해 함수는 작게 분리
- 주석은 한국어로 작성

---



