# 영어 암기 앱 개발가이드

> **이 문서는 "지금 무엇이 사실인가"만 담습니다.** 왜/어떻게 그 결론에 도달했는지(논의 과정, 버그 발견 서사, 문구 수정 반복, 테스트 상세)는 `PROGRESS.md`에 세션별로 전부 기록돼 있습니다. 특정 기능의 배경이 궁금하면 아래 체크리스트에서 번호/날짜를 확인한 뒤 `PROGRESS.md`에서 검색할 것.
> (2026-09-20 CLAUDE.md 압축 정리 — 내용 삭제 아님, PROGRESS.md와 중복되던 상세 서사만 제거)

## 현재 상황 브리핑 (요약)
- **무료 버전 기능은 전체 완료** — 카드 학습, 문장 입력(폼/파일/텍스트붙여넣기), 문장관리(즐겨찾기·미암기·정렬 7종·필터·검색), 노트형 학습(자동재생 포함), 설정(글자크기·화면모드·데이터관리·저장공간표시), 기본문장 100개 자동제공+관리, 도움말, 문장변형 예시(15-1), 로컬 백업 리마인더(17), 클라우드 백업/동기화(18), 정답 발음듣기 TTS(24), 음성 메모(27), 동시편집 데이터 유실 방지(28), 온보딩, 카드 텍스트 탭 정답토글(29) — 전부 Claude 1차+사용자 2차 확인 완료
- **15-2 AI 자동변형(유료판)**: 2026-09-18 무료판(안내형 프롬프트 복사) 완전 폐지 → 현재 **100% 로그인 필수 + 평생 100개 한도** 전용. "문장변형 > 내 문장 변형하기" 탭에서 AI 아이콘 원터치로 프롬프트 화면 없이 바로 생성. 서버(`api/generate-variations.js`) 구현+실서비스 검증 완료. **결제 게이팅만 미착수**
- **착수 중**: 19번 PC 빠른입력(`input.html`, 유료 번들 편입 확정) — 기능 자체는 구현 완료(키보드흐름/반응형/글자크기/파일가져오기·붙여넣기/조회·수정/페이지네이션 전부 완료), **결제 게이팅 미착수**(별도 세션에서 설계 완료, 착수 대기)
- **유료 가격 정책**: 18(클라우드 백업)+19(PC 빠른입력)+15-2(AI 자동변형) 단일 번들, 1회성 결제 **9,900원**(2026-09-17 확정, 완전 확정은 아니며 출시 직전 재조정 가능 — 근거는 아래 "무료/유료 버전 구분" 참고). 결제 방식은 Google Play 인앱결제(사업자등록 불필요)
- **원칙**: 무료=핵심가치(개인화 문장 암기)에 집중, 유료=고도화("번거로움 제거"). 음성판정(스피킹 연습)은 로드맵에서 완전 제외(Non-goals 참고)
- 상세 배경: Claude 메모리 `project_monetization_strategy`(가격 정책), `project_sentence_variation_feature`(15-2), `project_play_console_launch_pipeline`(스토어 출시) 참고

## 프로젝트 개요

### 기본 정보
- **주제**: PWA 기반 학습 애플리케이션
- **프로젝트명**: Tuk Tak
- **배포 URL**: https://tuk-tak-six.vercel.app/ (2026-08-29 계정 교체로 변경, 옛 주소 `tuk-tak-beta.vercel.app`)

### 컨셉트
개인의 목적에 맞는 영어를 거침없이 튀어나오게 연습시켜주는 앱

### 목적
개인의 목적에 맞는 영문을 확실하게 외워서 영어의 말문을 열게 만드는 앱

### 타깃 사용자
자신이 어떤 영어를 할 것인지 알고 있고, 적극적으로 자신이 외울 문장을 수집할 수 있는 영어 학습자. **영어 전용 앱으로 명시적 확정**(2026-09-19 — 다국어 확장을 검토했다가 "확실성 있는 좁은 시장"을 택하며 철회, `project_goals_and_scope` 메모리의 "좁은 니치 전략" 재확인과 같은 패턴 2회째)

### 톤 & 비주얼 컨셉
맑고 청명한 파란색 기반 + 레드 계열 포인트색(한국 국기·단청의 색감 연상)

### 하지 않을 것 (Non-goals)
- **가로모드 대응**(2026-08-28): 세로로 짧게 드는 카드 암기 앱 특성상 가치 낮음, 방어 코드도 넣지 않음
- **태그/카테고리/폴더 필터**(2026-08-28, 2026-09-06 재확인): 입력 마찰 증가 + "분류 체계는 결국 정리가 안 된다"는 실사용 판단
- **PWA 스플래시 아이콘 위치 조정**(2026-08-28): 원래 의도(앱 소개)는 온보딩으로 이관, 착수 불필요
- **통계/복습 리포트**(2026-09-01, 20번): 스트릭·정답률 등 게이미피케이션이 "목적 뚜렷한 학습자" 성격과 안 맞음 — 코드 구현 후 git revert로 전면 제외
- **테마/폰트 커스터마이징**(2026-09-03, 21번): 꾸미기형 기능은 이 앱(짧게 쓰는 기능형 앱) 성격상 구매 유인 약함
- **스피킹 연습(음성 판정)**(2026-09-11, 3번): 핵심 메커니즘이 recall이지 발화 검증이 아님 — 유료 로드맵에서도 완전 제외. 대신 **정답 발음 듣기(TTS)**가 24번으로 무료 완성됨(방향이 반대라 별개 기능)

### 브레인스토밍 아이디어 (미착수, 논의만 기록)
- 연속학습일수(스트릭): 게이미피케이션이라 비채택 쪽으로 기움
- D-day 계산: 사용자가 이미 가진 목표를 보여주는 것뿐이라 게이미피케이션 아닐 수 있음, 미착수
- 푸시 알림: 서버 인프라 필요(비용 zero 원칙과 충돌) + 설계에 따라 게이미피케이션 함정 가능
- TTS 목소리 선택: 기술적으로 번거로워 보류, **온디바이스 TTS 품질 향상이 쉬워지면 착수 의향 있음**(조건부 확정)
- 문장변형 퀴즈 코너(2026-09-14 논의 후 접음): 기존 카드 학습과 동일 구조라 새 기능 불필요, "진짜 판정" 버전은 이미 제외한 문제들과 겹침

### 무료 / 유료 버전 구분
- **무료(핵심)**: 문장 입력(폼/파일/텍스트붙여넣기/백업복원), 카드 학습(한국어→확인→영어), 텍스트 답변 확인, 문장관리(즐겨찾기·미암기·수정·삭제), 정렬 7종, 필터(전체/중요/미암기), 글자크기, 화면모드
- **유료 — 단일 번들 9,900원**(2026-09-11 통합 확정 → 2026-09-14 한도방식 재확정 → 2026-09-17 가격 재조정):
  - 18. 클라우드 백업/동기화 — **완료**, 현재 무료 공개 중(결제 게이팅은 번들 출시 시점 적용)
  - 19. PC 빠른입력 — **기능 완료**, 현재 무료 공개 중, 결제 게이팅 미착수
  - 15-2. AI 자동변형 — **코드+실서비스 검증 완료**, 100% 로그인+평생 100개 한도, 결제 게이팅 미착수. 모델은 Claude Sonnet 5(`claude-sonnet-5`) 추천이나 최종 확정 아님
  - **한도 방식**(2026-09-14 확정): 월별 리셋 아닌 **평생 누적 100개**, 크레딧 추가 구매 없음 — 월별 한도는 헤비유저의 누적 원가가 무상한선이 되는 구조적 허점 발견으로 변경. 요청당 원가 약 13원 기준 100개=약 1,300원(9,900원 대비 안전)
  - **가격 근거**(2026-09-17): "유료 구매를 고려하는 사람은 이미 앱이 마음에 든 사람이라 가격 차이를 크게 안 느낀다"는 논리 + 구글플레이 프리미엄 앱이 다운로드 13%로 매출 45% 차지한다는 참고 데이터. 9,900원은 charm pricing. **완전 확정 아님, 출시 직전 재조정 가능**
  - 결제 게이팅 설계(2026-09-19): Google Play 구매 영수증 → 서버(Vercel)에서 Google Play Developer API로 검증 → Firestore `users/{uid}.purchased` 플래그 → `input.html`/15-2가 확인. 코드 착수는 별도 세션
  - 결제 방식: Google Play 인앱결제 확정(개인 계정, 사업자등록 불필요)
  - 제외된 후보: 통계/복습 리포트(20), 테마/폰트 커스터마이징(21), 태그/카테고리, 음성 답변 판정 — 전부 Non-goals 참고

## 최종 출시 전 점검사항
- [x] ~~다른 언어 학습자를 위한 여지 남기기~~ — **2026-09-19 철회, 영어 암기 앱으로 명시적 회귀**. 2026-09-11에 "학습할 언어"로 일반화했던 라벨을 15-2 무료판 폐지로 전제가 사라져 전부 "영어"로 원복(`index.html`/`input.html`/`js/app.js`/`js/input.js`/`css/input.css`/`privacy.html`). 다른 언어는 검증 방법이 없어 정확성 담보 불가 — 좁은 니치 집중 재확인. 사용자 실기기 2차 확인 완료(2026-09-19)

## 작업 규칙

### 1. 승인 원칙
- "시작"/"go" 전까지는 읽고 분석만, 파일 생성·수정 금지
- "단계" = 1~9 같은 번호 순서가 아니라 **하나의 완결된 기능 단위**. 한 기능이 여러 체크리스트 번호에 걸쳐 있으면(UI+로직 등) 한 번에 묶어서 진행
- 승인은 세션 전체가 아니라 **기능 단위마다 매번**. 한 기능이 끝나면 자동으로 다음으로 넘어가지 말고 새로 "시작"/"go" 대기
- 작업 전 "이번 단계에서는 [무엇을] 위해 [어떤 파일들]을 [어떻게] 수정하겠습니다" 형식으로 계획 설명, 여러 번호가 함께 체크되면 명시

### 2. 작업 기록 규칙
- 새 세션 시작 시 `PROGRESS.md` 확인 → 마지막 작업 요약 + "다음 작업 제안" 보고. 없으면 "이전 작업 기록이 없습니다"
- `PROGRESS.md` 맨 아래에 누적 기록(기존 기록 삭제 금지)
- 체크리스트 진행 순서는 권장일 뿐 절대 규칙 아님. 완료 시 체크 표시+날짜(`- [x] ... (2026-04-01)`)
- 작업 전 계획 설명+승인, 파일 수정 시 변경 내용 먼저 요약, 완료 후 체크리스트+최상단 브리핑 함께 갱신

### 3. 검증 규칙
- 자동화 테스트(로컬/헤드리스) 통과 ≠ 실제 환경 정상 동작 단정 금지
- 2단계 확인: (1) Claude 자동화 테스트 1차 확인 (2) 사용자 실기기/실환경 2차 확인
- 2차 확인 전까지 다음 기능 단계로 넘어가지 않음

## 절대 금지 사항
- 승인 없이 파일 삭제 금지
- 여러 기능을 한 번에 진행 금지(단, 하나의 기능이 여러 체크리스트 항목에 걸친 경우는 예외)
- 체크리스트에 없는 기능 임의 추가 금지
- 라이브러리/프레임워크 임의 설치 금지

---

## 체크리스트

> 완료 항목은 최종 동작/구조만 기록. 논의 과정·버그 발견 서사·반복 테스트 상세는 `PROGRESS.md` 참고

- [x] 1. 기본 카드 UI (2026-08-04)
- [x] 2. 텍스트로 영어 정답 확인 (2026-08-04)
- [x] ~~3. 음성 답변 흐름~~ — **제외 확정**(2026-09-11, Non-goals 참고)
- [x] 4. 파일(CSV/TSV/TXT) 가져오기 (2026-08-11)
- [x] 5. 글자 크기 설정 5단계 (2026-08-12)
- [x] 6. 화면모드 시스템/라이트/다크 3단 (2026-08-12 최초 → 2026-08-13 3단 확장)
- [x] 7. 문장 목록 보기+수정/삭제 (2026-08-13). 삭제: 롱프레스 단일삭제 확인, "선택"으로 다중선택 일괄삭제. **"최소 1개 유지" 제한 제거**(2026-09-16 — 초기화 버튼과 동일하게 0개까지 삭제 허용, 빈 화면 안내문은 이미 구현돼 있어 문제 없음 확인)
- [x] 8. 설정 화면 신설: 더보기 메뉴 문장추가/문장관리/설정 3항목, 설정에 글자크기·화면모드+데이터관리 배치 (2026-08-13)
- [x] 9. 출제/정렬: 랜덤/최신/오래된/못외운것먼저/중요한것먼저 5종 + 한국어순/영어순(가나다·알파벳) 2종 = **총 7종**, 카드 순서·문장관리 목록 둘 다 반영 (2026-08-16 최초, 2026-09-14 2종 추가 — 26번)
  - [x] 9-1. 즐겨찾기(중요) 토글
  - [x] 9-2. 미암기 토글
  - [x] 9-3. 정렬 옵션 라디오 UI, 랜덤순은 재선택 시에만 재섞음. **"못외운것/중요한것 먼저"는 필터가 아니라 우선정렬**(해당 문장을 앞으로 배치할 뿐 다른 문장을 제외하지 않음) — 필터는 10번 별도 기능
- [x] 10. 카드 마킹 아이콘(☆⚑)+문장관리 필터(전체/중요/미암기), 필터는 정렬과 독립 축 (2026-08-16)
- [x] 11. 텍스트 붙여넣기 가져오기, 파일가져오기와 동일 파서(`parseSentencesText`) 재사용 (2026-08-17). 이후 개선:
  - 파일가져오기/텍스트붙여넣기 **중복 자동 건너뛰기**(2026-09-15, 한/영 완전일치 기준, 폼 입력은 제외)
  - **AI 프롬프트 복사 버튼**("텍스트로" 탭, 2026-09-16) — 손글씨 노트를 사진 찍어 AI에게 보낼 프롬프트를 클립보드로 복사
  - **탭 이름 축약**: "파일 가져오기"→"파일로", "텍스트 붙여넣기"→"텍스트로"(2026-09-16), 탭/안내문 글자크기 14→16px 확대, 도움말 문구도 함께 갱신
  - 제출 버튼 라벨 "폼입력/텍스트로" 두 탭 모두 **"추가하기"로 통일**(2026-09-17)
- [x] 12. 카드 화면에서 바로 수정(연필)/삭제(휴지통), 문장관리와 로직 공유 (2026-08-17)
- [x] 13. 문장추가 진입점 통합: [폼 입력/파일로/텍스트로] 3탭, 수정 시엔 탭 숨기고 폼만 노출 (2026-08-17)
- [x] ~~14. 가로모드 방어적 대응~~ — **착수 자체 제외 확정**(2026-08-28, Non-goals 참고)
- [x] 15. 기본문장 100개 자동 제공(`DEFAULT_SENTENCES`, `source:'default'` 배지) (2026-08-22)
  - [x] 15-1. 문장변형 예시(무료): 고정 10문장×약 89개 변형(시제·인칭·수·부정문·의문문, 문장마다 자연스러운 것만 선별). 더보기 메뉴 "문장변형 > 예시 둘러보기" 탭, 목록(카드형)→상세(태그 붙은 변형 목록, 전체 기본 영어로 시작, 원문일치 항목만 하늘색 고정 강조)로 이동, 탭하면 플립 애니메이션. 각 항목 + 버튼으로 선택 후 하단 "선택한 문장 추가하기 (N개)"로 확정 추가 (2026-08-25~26 구현, 2026-09-17 버튼 시각 다듬기+상세 기본표시를 영어로 변경)
  - [x] 15-2. 개인화 문장 AI 자동 변형 — **2026-09-18 진입구조 재설계+무료판 완전 폐지**로 현재 최종 형태 확정:
    - 더보기 메뉴 "문장변형" > "내 문장 변형하기" 탭: 로그인 필수, 각 문장 옆 AI 아이콘(✦) 탭 한 번 → 서버 호출 → 곧바로 미리보기(체크박스 검토, 기본 전체선택) → 선택분만 **"AI 변형 보관함"**(메인 카드덱과 분리된 영구 로컬 저장소)에 저장 + 그 배치만 카드 화면에서 즉석 학습("방금 변형한 문장·N개" 배너)
    - 보관함 학습 모드에서는 마킹바가 별표/미암기 대신 **승격(➕, 메인 덱에 추가, 탭할 때마다 토글)**/**보관함삭제(🗑)**로 전환(스피커는 공통)
    - 서버(`api/generate-variations.js`, Vercel 서버리스): Firebase ID 토큰 검증 → Firestore `users/{uid}.aiVariationCount`(평생 누적) 확인 → Claude API(`claude-sonnet-5`) 호출, `tools`+`tool_choice` 강제로 JSON 스키마 응답(형식 파싱 실패 방지) → 성공 시에만(변형 0개 결과는 미차감) 트랜잭션으로 카운트 증가
    - 네트워크 실패 시 1초 후 1회 자동 재시도(이미 서버 응답 받은 4xx/5xx나 0개 결과는 재시도 안 함, 한도 이중소모 방지)
    - 프롬프트(`buildAiVariationPrompt`, 무료판·유료판 공유)에 **9종 개별 변형 방식**(현재/과거/미래/현재완료시제, 2인칭, 3인칭단수, 복수, 부정문, 의문문) + 자연스러움 판단 **7가지 규칙**(인사말 고정관용구/비인칭주어/청유문/시간부사충돌/이중부정/복수불가/감탄문) + **8번째 규칙**(즉흥적 욕구·감정·신체상태 표현은 현재완료 부자연스러움, 2026-09-19 추가) + "원어민이 실제로 이렇게 말할지 스스로 판단하라"는 일반 원칙
    - 남은 한도 표시(`#ai-variation-usage-info`, "N/100개 남음") — "내 문장 변형하기" 탭 진입 시 조회(`api/ai-variation-usage.js`)
    - 결제 게이팅 미적용(로그인만 하면 이용 가능) — 위 "무료/유료 버전 구분" 참고
    - 문장관리 목록의 AI 아이콘은 **폐지됨**(진입점은 "문장변형" 메뉴로 통합)
    - 전체 구현+실 Anthropic API 검증+사용자 실기기 2차 확인 완료(2026-09-18), 8번째 규칙 추가는 실 API 검증 완료·**사용자 2차 확인 대기 중**(2026-09-19)
    - **일괄 변형(배치) — 미착수, 설계 원칙만 확정**(2026-09-14): 문장관리에서 여러 문장을 선택해 한 번에 변형 요청하는 기능을 나중에 붙이더라도, **한도는 "버튼 클릭 횟수"가 아니라 "AI에 보낸 원문 문장 개수"로 차감**해야 함(예: 10개 선택 시 100개 중 10개 차감) — 이래야 일괄 처리가 한도 우회 구멍이 되지 않음. 남은 개수보다 많이 선택했을 때 사전 경고할지 처리 중 멈출지는 미확정(사전경고가 더 친절하다는 의견까지만 논의)
  - [x] 15-3. 기본문장 관리: 설정 화면 토글로 숨기기(카드+목록 동시), "기본문장 삭제" 버튼(개인문장 0개 시 안내용 기본문장 1개 자동 유지) (2026-08-22)
- [x] 16. 저장 공간 표시: 설정>데이터관리 상단에 개수+진행률 막대바(5MB 참고치 기준, 최소 3% 시각보정)+용량(Blob 크기 기준), 설정 화면 진입 시마다 재계산(실시간 아님) (2026-08-23)
- [x] 17. 로컬 백업 리마인더: 카드 화면 상단 배너, (경과일≥7일) OR (내 문장 신규 100개 이상) 조건 충족 시 노출, "지금 백업"/닫기(X) 둘 다 체크포인트 리셋 (2026-08-28)
- [x] 18. 클라우드 백업/동기화: Firebase Auth(Google 로그인)+Firestore(프로젝트ID `tuk-tak-2c172`), 설정 화면에 로그인+백업/복원/삭제/로그아웃 버튼. 개인정보처리방침 고지(도움말 화면) 포함 진행단계 1~5 전부 완료 (2026-08-29)
  - 보완 3종(2026-09-12~13): 경고 문구·버튼 색상 구분, "클라우드와 병합" 버튼(비파괴적 병합), "클라우드 백업 삭제" 버튼 — **2026-09-14, 병합 버튼은 28번(저장이 항상 병합) 구현으로 "지금 백업"에 흡수되며 삭제됨**, 병합 로직 자체는 재사용
- [ ] 19. PC 빠른입력 (결제 게이팅 없는 프로토타입 → 기능 완료, 유료 번들 편입 확정, 결제 게이팅만 미착수)
  - 19-1(앱 내 4번째 탭 방식) — **2026-09-08 제거**, 아래 19-2 하나로 일원화
  - 19-2(독립 페이지 `input.html`, 폰 앱 링크 없음, `tuk-tak-six.vercel.app/input.html`): 카드 앱과 완전 분리된 디자인, 같은 `window.CloudSync` 재사용. **완료된 개선**: 키보드 흐름(Enter로 한국어→영어→다음행 자동이동, Tab 삭제버튼 건너뜀, 로그인 시점 자동포커스, 2026-09-12), 반응형 레이아웃(데스크톱 폭 확장, 2026-09-12), textarea 자동높이확장+리사이즈 대응(2026-09-12), 입력 글자크기 조절 버튼(2026-09-12), 파일 가져오기(2026-09-12)+텍스트 붙여넣기(2026-09-14, 파서 `js/sentence-parser.js`로 공유 분리), **클라우드에 저장된 문장 조회·수정·삭제**("변경사항 저장" 시 반영, 기본 접힘 섹션, 2026-09-13), 목록 페이지네이션("더보기", 20개 단위)+저장바 하단 고정(2026-09-14), 중복 자동 건너뛰기(2026-09-15)
  - **동시편집 데이터 유실 방지는 28번으로 통합 구현 완료**(저장 시 항상 병합)
  - 결제 게이팅 설계는 위 "무료/유료 버전 구분" 참고, 코드 착수는 별도 세션
  - 관련 문서: `reference/docs/PC_빠른입력_점검_2026-09-08.docx`(기술적 한계·가격 이력 추적용, 2026-09-14 최종 갱신)
- [x] ~~20. 통계/복습 리포트~~ — **제외 확정**(2026-09-01, git revert로 완전 원복, Non-goals 참고)
- [x] ~~21. 테마/폰트 커스터마이징~~ — **착수 전 제외 확정**(2026-09-03, Non-goals 참고)
- [x] 22. 문장관리 검색: 상단바 돋보기 아이콘 → 필터 "전체"로 리셋+하늘색 폼 박스(돋보기+입력창+X취소)로 전환, 한국어/영어 실시간 부분일치. 검색모드↔선택모드 상호배타 (2026-09-01)
- [x] 23. 필터 학습: 문장관리에서 중요/미암기 필터 선택 시 "이 조건으로 플래시카드 학습" 버튼 노출, 기존 미니학습 인프라(`startMiniSession`) 재사용해 그 조건 문장만 카드 화면 학습 (2026-09-06)
- [x] 24. 정답 발음 듣기(TTS, 무료): 카드 마킹바에 스피커 아이콘(`speakEnglish()`, `lang:'en-US'`), 설정 "발음 자동 재생" 토글(기본 꺼짐). 미니학습(15-2/23번)에도 자동 적용 (2026-09-11). **TTS 워밍업**(2026-09-15): 스크립트 로드 시 무음 더미 문장으로 미리 로딩 트리거해 최초 재생 지연 완화
- [x] 25. 노트형 학습(무료): 문장관리와 분리된 `#note-study-screen` — 관리 아이콘 없이 스피커만, 전체보기/한글만/영어만 3단 토글(재방문 유지), 글자크기 연동+줄바꿈 허용. 문장관리 필터 아래 진입 버튼("전체"에서 1개, "중요/미암기"에서 플래시카드+노트형 2개) (2026-09-11)
  - **탭-확인 인출연습**(2026-09-14): 한글만/영어만 모드에서 문장 탭 시 그 문장만 반대쪽 언어 임시 노출, 재탭 시 원복
  - **자동재생**(2026-09-16): 우하단 알약형 버튼(`speakKorean()`/`speakEnglish()`, 표시모드에 맞는 언어 — 전체보기·한글만은 한국어, 영어만은 영어), 대기시간 스테퍼(2~10초, 기본4초, `localStorage` 저장), 발화 종료 후 대기시간만큼 기다렸다가 정답 노출 없이 자동으로 다음 문장 이동, 마지막 문장에서 자동정지
- [x] 26. 정렬에 한국어순(가나다)/영어순(알파벳) 추가, 기존 `sortMode`/`SORT_MODES` 인프라 재사용 (2026-09-14)
- [x] 27. 음성 메모(무료): 더보기 메뉴 신규, `SpeechRecognition`(`ko-KR` 고정)으로 텍스트 캡처 → 편집 가능 미리보기 → "메모로 저장" → 메모함. 메모 탭 시 문장추가 폼에 한국어 프리필, **실제 추가 성사 시에만 메모 삭제**(취소해도 메모 보존, 유실 방지). 브라우저 미지원 시 마이크 버튼 비활성화+안내 (2026-09-14)
  - **연속 녹음 안정화**(2026-09-16): `interimResults:true`+세션 자동 재시작(브라우저가 임의로 끊어도 이어붙임), 치명적 오류(권한거부 등)만 재시작 중단. 저장/메모탭 이동 시 녹음 정지 누락 버그 수정
- [x] 28. 동시편집 데이터 유실 방지(무료): 폰의 "지금 백업"이 단순 덮어쓰기 대신 **저장 직전 클라우드를 불러와 병합**(한/영 완전일치 기준, 표시는 OR로 병합, 불일치는 신규 추가) 후 재업로드. `input.html`의 "변경사항 저장"도 저장 직전 클라우드를 재조회해 그 사이 다른 기기가 추가한 항목을 보존. **"클라우드 문장으로 교체"만 의도적 전체교체로 유지**(확인창 필수, 2026-09-19 라벨 변경) (2026-09-14)
- [x] 29. 카드 문장 탭으로 정답 토글(무료): 메인 카드 화면 한/영 텍스트 탭 시 "확인" 버튼과 동일 동작(정답 노출+마킹바+자동재생 설정 시 발음재생), 재탭 시 한국어 복귀. "확인" 버튼은 그대로 유지(토글 아님, 항상 노출만) (2026-09-15)

---

## 기술 스택
- Frontend: HTML5, CSS3, Vanilla JavaScript(빌드 없음)
- PWA: Service Worker 없음(오프라인은 LocalStorage 기반 카드학습/문장입력에 한정, "앱 최초 로드" 자체의 오프라인은 보장 안 됨)
- Storage: LocalStorage(문장 데이터, 학습 진행 상태)
- Backup: TSV 파일 내보내기, 복원은 "파일로" 탭 재사용
- 배포: Vercel / 버전관리: GitHub
- 정답 발음 듣기(TTS, SpeechSynthesis, 24번): 무료, 기기 내장 엔진, 기본 수동+설정에서 자동재생
- 음성 메모(STT, SpeechRecognition, 27번): 무료, 판정 없는 순수 텍스트 캡처, `ko-KR` 고정, 미지원 브라우저는 방어적 비활성화
- 클라우드 백업(18번): Firebase Authentication(Google 로그인)+Cloud Firestore(Spark 무료등급, 프로젝트ID `tuk-tak-2c172`, 리전 asia-northeast3). SDK는 CDN 모듈(`js/firebase-init.js`, `type="module"`)로만 로드, `js/app.js`(일반 스크립트)는 `window.CloudSync`로만 접근
- **AI 자동변형(15-2 유료판)**: `api/generate-variations.js`(Vercel 서버리스, Node.js) — 프로젝트 최초 npm 의존성(`package.json`+`firebase-admin@^13.10.0`, `node_modules/`는 gitignore). Firebase ID 토큰 검증 → Firestore `users/{uid}.aiVariationCount`(평생 누적) 트랜잭션 확인/증가 → Claude API(`claude-sonnet-5`) 호출(`tools`+`tool_choice` 강제로 JSON 응답). 프롬프트 생성(`buildAiVariationPrompt`)·결과 파싱(`parseSentencesText`)은 클라이언트 코드 재사용, 서버는 파싱 형식을 모르는 얇은 중계 구조. 결제 게이팅 미적용
- **알려진 인프라 함정**(재발 방지 메모): Vercel 환경변수는 대시보드 붙여넣기 대신 `vercel env add <name> production < 파일`로 등록할 것(여러 줄 JSON이 깨질 수 있음). `firebase-admin` 14.x는 모듈형 API로 바뀌었고 물고 오는 `jose@6` 의존성이 ESM 전용이라 Vercel Node 런타임에서 크래시 — 13.x 유지. Firestore Admin SDK는 앱 보안규칙과 무관하게 서비스계정 IAM 권한("Cloud Datastore 관리자" 등)이 별도로 필요. Claude Sonnet 5는 `temperature` 파라미터 자체를 지원하지 않음(요청 거부)

---

## 메뉴 기능 정리 (사용자 가이드용)

### 카드 화면
- 이전/확인/다음: 핵심 조작. "확인" 시 영어 정답 노출
- **문장 텍스트 탭으로 정답 토글**(29번): 한/영 텍스트 자체를 탭해도 "확인"과 동일 효과, 재탭 시 한국어 복귀
- 마킹 아이콘 바(정답 확인 후 노출): ☆(중요)·⚑(미암기)·🔊(발음듣기, 24번) | ✏(수정)·🗑(삭제)
- 이어보기: 마지막 문장부터 자동 시작(LocalStorage). 초기화는 설정>"처음 문장부터 다시 보기"
- 백업 리마인더 배너(17번): 조건 충족 시 상단바 아래 노출
- 로고 클릭: 미니 학습 중(15-2/23번)일 때만 전체 덱으로 복귀

### 더보기(⋮) 드롭다운
- **문장추가**: [폼 입력/파일로/텍스트로] 3탭. 파일로·텍스트로는 같은 파서 공유, 중복 자동 건너뛰기, "텍스트로"엔 "AI 프롬프트 복사하기" 버튼
- **음성 메모**(27번): 마이크로 텍스트 캡처 → 메모함 → 탭하면 문장추가 폼으로 이어서 완성
- **문장관리**: 전체 목록, 탭=수정모달, 롱프레스=단일삭제, "선택" 아이콘=다중삭제(하단고정 버튼). 검색 아이콘(돋보기)으로 실시간 검색. 필터(전체/중요/미암기) 선택 시 조건 개수+플래시카드/노트형 학습 진입 버튼
- **노트형 학습**(25번, 문장관리에서 진입): 관리 아이콘 없이 스피커만, 3단 표시토글, 한글만/영어만 모드 탭-확인, 우하단 자동재생 버튼
- **문장변형**: [예시 둘러보기(15-1, 고정 10문장)] / [내 문장 변형하기(15-2, 로그인+유료 한도, AI 아이콘 원터치 생성+보관함 학습)] 2탭
- **설정**: 아래 "설정 화면" 참고
- **도움말**: 앱소개+사용법 아코디언(대분류 5개: 카드학습/문장추가/문장관리/설정/개인정보처리방침)
- "처음부터"는 더보기에서 제외되고 설정 화면으로 이동됨(2026-09-08)

### 설정 화면
글자크기(5단계) · 화면모드(3단) · 발음 자동재생 토글(24번) · 출제/정렬 순서(7종+처음부터 버튼) · 기본문장 관리(숨기기/삭제) · 데이터 관리(저장공간 안내+백업내보내기+초기화, 각 "?" 설명버튼) · 클라우드 백업(로그인 상태에 따라 로그인 버튼 또는 클라우드에 백업/클라우드 문장으로 교체/클라우드 백업 삭제/로그아웃 4버튼, 각 "?" 설명버튼)

### 문장변형 — 내 문장 변형하기 (15-2)
1. "내 문장 변형하기" 탭에서 문장 옆 AI 아이콘 탭(비로그인 시 안내 후 중단)
2. 서버 호출 완료 시 곧바로 미리보기(체크박스 검토)
3. 선택분만 "AI 변형 보관함"에 저장 + 즉석 학습
4. 탭 상단 "AI 변형 보관함 학습하기 (N개)" 버튼으로 언제든 재학습
5. 보관함 학습 화면의 승격(➕)은 탭할 때마다 토글(메인 덱 추가/제거), 보관함 자체에서는 승격해도 제거되지 않음
6. 남은 한도("N/100개 남음")는 탭 상단에 표시

## 문장 입력 방법
1. **폼으로 하나씩**: 문장추가>폼 입력
2. **파일로 여러 개**: 문장추가>파일로, 탭(TSV)→쉼표(CSV)→연속공백 순 자동 인식, 헤더 자동 스킵, 중복 자동 건너뛰기
3. **백업 파일 복원**: 설정>백업내보내기로 받은 TSV를 위 2번으로 재업로드
4. **텍스트로 여러 개**: 문장추가>텍스트로, 규칙은 2번과 동일. "AI 프롬프트 복사하기"로 사진→텍스트 변환 프롬프트 제공
5. **PC 빠른입력**: `input.html`에서 행 단위 타이핑(Enter 자동이동, 자동높이확장), 파일가져오기/텍스트붙여넣기 지원, 클라우드 문장 조회·수정·삭제 가능, 저장 시 클라우드와 병합
6. **음성 메모**: 더보기>음성 메모에서 말하면 텍스트로 캡처, 나중에 탭해서 문장추가 폼으로 완성

---

## 디자인 기획

### 컬러 시스템 (2026-08-18 가이드 기준, `reference/tuktak-color-guide.md` 출처)

#### Brand
| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Brand Blue | `--color-brand` | `#378ADD` | 앱 아이콘, 로고, CTA(확인 탭 전) |
| Brand Navy | `--color-brand-dark` | `#0C447C` | 헤더 타이틀, 정답 텍스트, 확인(탭 후) |
| Point Coral | `--color-point` | `#D85A30` | 사용 보류 |

#### Surface
| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Base White | `--color-base` | `#FFFFFF` | 순백 표면(카드) |
| Surface | `--color-surface` | `#F4F6F8` | 기본 화면 배경 |
| Surface Dark | `--color-surface-dark` | `#14171C` | (다크는 아래 Dark Mode 재조정값 참고) |
| Chip Answer | `--color-chip-answer` | `#E6F1FB` | 정답 텍스트 강조 칩(라이트 기준) |

#### Text
| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Text Primary | `--color-text-primary` | `#1E2A38` | 본문, 정답 텍스트 |
| Text Secondary | `--color-text-secondary` | `#4B5D6B` | 보조 설명 |
| Text Muted | `--color-text-muted` | `#7C8A96` | 캡션/힌트 전용 — 본문 금지 |

#### Grayscale (완전 무채색, 라이트)
`--gray-50`~`--gray-900`: `#F7F7F7`/`#ECECEC`/`#D9D9D9`/`#C2C2C2`/`#A6A6A6`/`#8A8A8A`/`#6B6B6B`/`#545454`/`#3C3C3C`/`#242424`

#### Accent (마킹 전용)
| 역할 | 변수명 | HEX |
|---|---|---|
| Accent Star | `--color-accent-star` | `#BA7517` |
| Accent Star Bg | `--color-accent-star-bg` | `#FAEEDA` |

#### 컴포넌트 상태 규칙
- 확인 버튼: 탭 전=Brand Blue, 탭 후=Brand Navy(라이트). 다크모드는 별도 CTA 변수 사용(아래 Dark Mode 참고)
- 이전/다음 버튼: `--gray-600` 아웃라인
- 별표/깃발: 기본 `--gray-600`, 활성 시 accent-star+원형 칩

#### Dark Mode (2026-09-20 독립 팔레트로 재설계, 정식 가이드 아닌 잠정치)
> 이력: 2026-09-19 완전 무채색→남색(slate) 계열로 1차 재조정 → 2026-09-20 "배경에 파란기가 너무 강하다"는 반대 피드백으로, 배경 후보 4종을 실제 컴포넌트 목업으로 비교 후 채도만 낮춘 값으로 2차 재조정 + 라이트모드와 공유하던 색을 다크 전용으로 분리하는 방향으로 확장(사용자 질문 "다크모드가 라이트모드와 공통색상을 반드시 가져야해?"에서 출발)

- **배경/그레이스케일/텍스트**: 색상(H222°)·밝기(L)는 유지하고 채도만 47%→25%로 낮춤(로고·CTA 등 브랜드 블루 계열과의 연결감은 유지하면서 파란기는 절반으로)
  - `--color-base`/`--color-surface`: `#151923`
  - `--color-text-primary`: `#E5E8ED`, `--color-text-secondary`/`--gray-600`: `#9CA4B0`
  - 그레이 10단계 전체 같은 비율(×25/47)로 채도만 재계산
- **CTA 버튼(확인/제출/플래시카드학습/자동재생)**: 기존 "배경에 거의 안 보이게 섞기" 방식이 존재감 없이 밋밋하다는 피드백으로 전면 재설계
  - 탭 전(기본, `--color-cta-fill: #1A2434`/`--color-cta-text: #3993FF`): 눈에 띄는 채움+쨍한 글자로 존재감을 줌, **테두리 없음**
  - 탭 후(정답 노출, `#check-btn.revealed` 전용, `--color-cta-pressed: #202D40`): 다크모드에서는 더 어두워지면 오히려 안 보이므로 반대로 아주 살짝만 밝게 하고, **테두리(`#5B9FE8`)를 새로 둘러** "확인됨" 신호를 줌
  - 나머지 3개 CTA(제출/플래시카드학습/자동재생)는 탭 전/후 상태 구분이 없는 단발성 버튼이라 전부 "탭 전" 스타일(테두리 없음)로 통일
- **액티브 블루(`#5B9FE8`) 신규 도입**: 라이트모드 대비 다크 전용으로 분리한 보조 액센트 — 로고(`.topbar-logo`), 탭 활성 밑줄(`.list-filter-btn.active`/`.add-modal-tab-btn.active`), 정렬 라디오 선택(`.sort-option-btn.active`), 선택모드 강조("취소"/"N개 선택"), 스테퍼 버튼에 적용. 이 요소들은 원래 라이트모드에서 브랜드 네이비(`--color-brand-dark`)를 쓰다가 다크 배경과 대비가 안 나와 "색을 포기하고 밝은 회색으로 대체"했던 곳들 — 이제 실제 색을 되살림(단, `.help-accordion-header`는 heading이라 색 대신 중립 유지)
- **크림색/하늘색 "칩" 배경 톤다운**(2026-09-20 신규 발견·수정): `--color-accent-star-bg`(라이트 전용 밝은 크림 `#FAEEDA`)와 `--color-chip-answer`의 다른 사용처(백업 리마인더 배너/검색 폼 박스/문장변형 하이라이트 — 카드 정답박스 `.en-text`만 2026-09-19에 먼저 고쳐져 있었음)가 다크모드에서 전혀 재정의되지 않아, 새까만 배경 위에 밝은 패치로 튀던 버그를 발견해 수정
  - `--color-accent-star-bg`(다크): `rgba(186,117,23,0.18)`, `--color-accent-star`(다크): `#E0A552` — 변수 단위 수정이라 별표/깃발/미니학습배너/AI보관함 승격 아이콘 등 이 변수를 쓰는 모든 곳에 자동 반영됨
  - `.backup-reminder-banner`/`.list-search-box`/`.variation-item-flipped` 3곳은 배경뿐 아니라 내부 텍스트도 라이트 전용 리터럴 색(`#1E2A38` 등)이라 함께 재정의 필요 — 배경 `rgba(55,138,221,0.16)`, 주 텍스트 `#DCE9FA`, 보조/placeholder 텍스트 `#9DBBE0`으로 통일(`.en-text`와 같은 원리)
- 확정 유지값(다크에서도 라이트와 동일, 큰 채움+흰 아이콘 조합이라 밝은 액티브 블루로 바꾸면 대비가 오히려 나빠짐): `--color-brand`/`--color-brand-dark`(토글 스위치·체크박스·라디오 점 등 "흰 아이콘을 얹는 솔리드 필"), `--color-point`, `--color-text-muted`
- **아웃라인 버튼(`.btn-outline`) + 설정 액션 버튼(`.settings-action-group`) 톤다운**(2026-09-20, 실기기 확인 중 발견): 다크모드에서 배경(`--color-base`)이 페이지와 완전히 같은 값+글자(`--color-brand-dark` 진한 남색)가 어두운 배경 위에서 거의 안 보여, "AI 프롬프트 복사하기"·"백업 내보내기"·"초기화"·"Google로 로그인"·"처음 문장부터 다시 보기" 등 테두리만 겨우 보이고 내용이 안 읽히던 문제 — 테두리는 그대로 두고 배경·글자만 CTA "탭 전" 톤(`#1A2434`/`#3993FF`)으로 통일. 화면모드(시스템/라이트/다크) 선택 버튼의 활성 상태도 "확인 버튼과 같은 스타일로" 요청에 따라 `--color-cta-fill`/`--color-cta-text`를 그대로 재사용하도록 변경
- **더보기(⋮) 드롭다운 메뉴 배경 수정**(2026-09-20, 실기기 확인 중 발견): `.more-menu` 배경이 `--color-base`(페이지와 동일)+어두운 그림자라 다크모드에서 메뉴 영역이 바탕과 전혀 구분되지 않던 버그 — 배경을 `--gray-100`으로 한 톤 밝게, 테두리(`--gray-200`) 추가, 그림자를 더 짙게(`rgba(0,0,0,0.5)`) 교체
- **범위 밖(2026-09-20 기준 미점검)**: `.list-note-study-btn` 등 `.btn-outline`이 아닌 별도 클래스로 브랜드 블루를 직접 쓰는 일부 버튼, 노트형학습/음성메모/문장변형 상세/도움말 화면의 나머지 다크모드 세부 튜닝은 다음 세션에 계속
- Claude 1차 확인(Playwright, 390px, 카드 확인전/확인후/별표활성/문장관리/검색모드/선택모드/더보기메뉴/설정화면전체/문장추가 텍스트로탭 스크린샷+픽셀 색상 직접 추출로 계산값과 렌더값 일치 확인, 콘솔 에러 없음) 완료 — **사용자 실기기 2차 확인 대기 중**

#### CSS 변수 선언 규칙
모든 변수는 `css/style.css` 상단 `:root{}`에만 선언, 다크모드는 `[data-theme="dark"]{}`. 변수명 임의 변경 금지

### 타이포그래피
- 기본 폰트: Pretendard(`/public/fonts/pretendard/`, woff2, `@font-face` 실제 파일 적용 완료 2026-09-08)
- 웨이트: 400/500/600/700
- `font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif`

### 아이콘
lucide SVG 인라인 삽입(오프라인 대응, npm/CDN 미사용). 사용 중: `circle-plus`·`list`·`settings`·`rotate-ccw`·`circle-help`·`chevron-left`·`chevron-down`·`upload`·`download`·`trash-2`·`ellipsis-vertical`·`star`·`flag`·`check`·`clipboard-paste`·`pencil`·`shuffle`·`sparkles`(문장변형 AI 아이콘)·`house`·`x`·`cloud-upload`·`cloud-download`·`log-out`·Google 공식 G 로고·`search`·`volume-2`·`mic`·`play`/`pause`

### UI 레이아웃 규칙
- **`.screen`은 반드시 `height`(뷰포트 고정), `min-height` 금지**(2026-08-27) — `min-height`면 화면 자체가 늘어나 하단 고정 버튼이 밀려남. 내부 스크롤은 `flex:1; overflow-y:auto` 콘텐츠 영역이 담당
- **넓은 화면(PC) 대응**(2026-08-29): `@media(min-width:640px)`에서 `.screen`을 `max-width:480px; margin:0 auto`로 가운데 정렬(모바일 폭 유지). 640px 미만은 기존과 동일
- **화면 콘텐츠 좌우 여백 16px 통일**(2026-09-15): 화면마다 20px/16px로 혼재했던 콘텐츠 패딩을 16px로 통일(설정/도움말/문장추가/문장변형/AI변형 화면 등)
- **카드형 목록 항목 텍스트 인셋 20px로 축소**(2026-09-16): 문장관리(`.sentence-list-item`)·노트형학습(`.note-study-item`) 카드의 바깥 테두리는 16px 유지, 카드 자체 내부 패딩만 16px→4px로 줄여 텍스트 시작점을 32px→20px로 당김(텍스트 가용폭은 오히려 넓어짐)
- **헤더 무채색 전환**(2026-09-15): 카드/문장관리/설정 등 공용 헤더(`.screen-topbar`/`.card-topbar`) 배경을 순백 대신 화면 배경과 같은 `--color-surface`+하단 구분선으로 전환. 로고만 브랜드 블루 유지, 그 외 아이콘(뒤로가기/홈/검색/선택/더보기)은 전부 `--gray-600`. 문장관리 선택모드는 배경 대신 "취소"/"N개 선택" 텍스트 색만 네이비로 전환. 로고/타이틀 최종값: 18px, `--gray-600`, `letter-spacing:0.3px`(로고는 `-1px`), 뒤로가기 아이콘 32px·`stroke-width:1.4`
- **제목 정가운데 고정**(2026-08-27): `.screen-topbar-title`은 `position:absolute; left:0; right:0; text-align:center`로 좌우 버튼 유무와 무관하게 항상 정가운데
- **"홈" 버튼**(2026-08-27): 뒤로가기 2번 이상 눌러야 메인에 닿는 화면(문장변형 상세, AI변형 미리보기 등)에만 `house` 아이콘으로 카드 화면 바로가기 제공
- **카드 이전/확인/다음 버튼 둥글기 20px**(2026-08-15/09-15, `.control-bar .btn`), 다른 화면 버튼은 공용 `.btn` 12px 그대로
- **브랜드색 배경 위 고정 흰색**: 시작화면(`.start-logo`/`.btn-primary`)처럼 배경이 항상 브랜드색으로 고정된 요소는 `--color-base` 대신 리터럴 `#FFFFFF` 사용
- 상태바(PWA `theme-color`)/스플래시 배경(`manifest.json` `background_color`)은 헤더 무채색 전환과 무관하게 브랜드 컬러(네이비/블루) 그대로 유지(의도적 예외)

## 파일 구조

## 코딩 규칙

### HTML
- 시맨틱 태그 우선(header, nav, main, article, section, footer)
- 주석은 한국어, 들여쓰기 2칸

### CSS
- 클래스명은 명확한 의미 전달, Vanilla CSS(프레임워크 금지)
- 주석으로 섹션 구분, 컬러는 CSS 변수로 관리(변수명 임의 변경 금지)

### JavaScript
- ES6+ 문법, 함수는 작게 분리, 주석은 한국어

### 참고 문서(워드 .docx) 작성 규칙 (2026-08-30 확정)
- `reference/` 폴더 워드 문서는 사용자가 직접 인쇄하므로 **인쇄 최적화** 기본
- **색 사용 금지**: 글자색·배경 음영 전부 금지, 검정+흰배경만. 강조는 배경색 대신 **굵게**
- **페이지 번호 필수**: 하단 중앙 "페이지 N / M"(Word 필드 코드 `PAGE`/`NUMPAGES` 사용, 고정 텍스트 금지)
- 신규 문서도 처음부터 이 규칙 적용
- 문서 위치: `reference/docs/`(워드/텍스트 문서), `reference/` 최상위엔 `.md` 참고자료만(`help-content-draft.md`/`tuktak-color-guide.md`/`문장변형_체험판_콘텐츠_초안.md`)
