# 영어 암기 앱 개발가이드

## 현재 상황 브리핑 (요약)
> 새 세션 시작 시 여기부터 훑어보면 전체 그림이 잡힘. 상세 이력은 PROGRESS.md, 전체 항목은 아래 "체크리스트" 참고.

- **완성됨**: 카드 학습, 문장 입력(폼/파일/붙여넣기), 문장관리(즐겨찾기·미암기·정렬·필터·검색·AI 변형 아이콘), 설정(글자크기·화면모드·데이터관리·저장공간표시), 기본문장 100개 자동제공+관리, 도움말 화면, 문장변형(15-1 — 더보기 메뉴명 "문장변형", 10문장 콘텐츠+목록→상세 화면+각 변형을 내 문장으로 추가하는 기능까지 완료), 개인화 문장 AI 변형(15-2 무료판 — 문장관리 각 항목의 AI 아이콘으로 진입, 프롬프트 생성/복사→붙여넣기→미리보기→추가+미니 학습까지 전체 완료, Claude 1차 확인+사용자 2차 확인 모두 완료), 로컬 백업 리마인더(17번 — 카드 화면 상단 배너, 경과일 7일/내 문장 100개 조건 충족 시 노출, Claude 1차 확인+사용자 2차 확인 모두 완료), **클라우드 백업/동기화(18번 — Google 로그인+Firestore 수동 백업/복원, 개인정보처리방침 고지까지 진행 단계 1~5 전부 완료, Claude 1차 확인+사용자 2차 확인 모두 완료, 2026-08-29)**, **문장관리 검색(22번 — 상단바 돋보기 아이콘+하늘색 폼 박스, 한국어/영어 실시간 부분일치 검색, Claude 1차 확인+사용자 2차 확인 모두 완료, 2026-09-01)**, **필터 학습(23번 — 문장관리에서 중요/미암기 필터 선택 시 "이 조건으로 학습하기" 버튼 노출, 기존 미니 학습 인프라 재사용해 그 조건 문장만 카드 화면에서 학습, Claude 1차 확인+사용자 2차 확인 모두 완료, 2026-09-06)**, PC 등 넓은 화면 반응형 레이아웃 버그 수정(2026-08-29, 체크리스트 항목 아님 — 무료 버그 수정), Pretendard 웹폰트 파일 실제 추가(2026-09-08, 체크리스트 항목 아님 — 기기별로 로고 등 텍스트가 다르게 보이던 문제의 근본 원인 해결), **정답 발음 듣기(TTS, 24번 — 카드 마킹 아이콘 바에 스피커 아이콘 추가+설정 화면 "발음 자동 재생" 토글, 기본은 수동 탭·설정에서 자동재생 전환 가능, Claude 1차 확인+사용자 2차 확인 모두 완료, 2026-09-11)**, **노트형 학습(25번 — 문장관리와 완전히 분리된 `#note-study-screen`, 관리 아이콘 없이 스피커만·전체보기/한글만/영어만 토글·글자크기 연동·줄바꿈 허용, 문장관리에는 진입 버튼만 추가, Claude 1차 확인+사용자 2차 확인 모두 완료, 2026-09-11 → 2026-09-14 "한글만"/"영어만" 모드에서 문장 탭 시 그 문장만 반대쪽 언어를 임시로 보여주는 탭-확인 인출연습 보완, Claude 1차 확인+사용자 2차 확인 모두 완료)**, **정렬에 가나다순/알파벳순 추가(26번 — 문장이 많아지면 특정 문장 찾기 어렵다는 문제로 착수, 기존 5개 정렬 옵션에 "한국어순"/"학습어순" 2개 추가·기존 `sortMode` 인프라 재사용해 카드 학습 순서·문장관리 목록 둘 다 반영, Claude 1차 확인+사용자 2차 확인 모두 완료, 2026-09-14)**, **음성 메모(27번 — 더보기 메뉴 신규 항목, SpeechRecognition으로 문장 아이디어를 텍스트로 캡처 후 별도 메모함에 보관, 메모 탭하면 문장추가 폼에 한국어 프리필로 이어서 완성·추가 성사 시에만 메모 삭제해 데이터 유실 방지, 인식 언어 `ko-KR` 고정, Claude 1차 확인+사용자 2차 확인 모두 완료, 2026-09-14)**, **동시편집 데이터 유실 방지(28번 — 19번 점검 문서의 남은 재검토 항목 실구현, 폰의 "지금 백업"이 단순 덮어쓰기 대신 클라우드와 항상 병합하도록 변경(기존 별도 "클라우드와 병합" 버튼은 흡수돼 제거), `input.html`의 "변경사항 저장"도 저장 직전 클라우드를 다시 불러와 그 사이 다른 기기가 추가한 항목을 보존, "클라우드에서 복원"만 의도적 전체교체로 유지, Claude 1차 확인+사용자 2차 확인(폰+PC 양쪽) 모두 완료, 2026-09-14)**, **온보딩(시작 화면 로고 아래 문구 — 최초 방문 시 긴 소개 문단, 재방문부터는 짧은 태그라인 "개인화된 문장 암기 앱"으로 자동 전환. 같은 소개 문단을 도움말 화면 맨 위에도 상시 노출. 2026-09-03 카피 방향 확정 → 2026-09-07 구현 완료 → 2026-09-14 "문구 전환이 헷갈린다"는 피드백으로 설명 이미지를 만들어 범위를 재확인(온보딩 = 이 2가지뿐, 추가 확장 없음으로 확정), Claude 1차 확인+사용자 실기기 2차 확인 모두 완료, 2026-09-14)** — **이로써 무료 버전 전체 완료**(19번 PC 빠른입력만 유료 번들 편입으로 범위 밖), **카드 문장 탭으로 정답 토글(29번 — 메인 카드 화면 한/영 텍스트를 탭하면 "확인" 버튼과 동일하게 정답 노출, 다시 탭하면 한국어로 복귀, 노트형 학습의 탭-확인 인출연습과 같은 패턴을 메인 카드에도 확장, Claude 1차 확인 완료, 2026-09-15)**
- **착수 중(사용자 2차 확인 대기)**: 19번 PC 빠른입력(유료 번들 편입 확정, 결제 게이팅은 미착수, **2026-09-11 전략적 최우선 순위로 재확인**) — **2026-09-08, 19-1(앱 내 문장추가 4번째 탭)은 제거되고 19-2(카드 앱과 분리된 독립 페이지 `input.html`, 폰 앱에서 링크 없음) 하나로 일원화됨**. 코드 구현+Claude 1차 확인+사용자 실사용 확인(19-2, "기능은 성공") 모두 완료(2026-08-29), 유료화 여부는 2026-09-03에 확정(유료 번들 편입). 2026-09-08 세션에서 `input.html`의 기술적 한계(수동 동기화·조회수정 불가·동시편집 충돌 미고려 등)와 유료화 불확실성(구매 의향 미검증)을 점검 문서로 정리(`reference/docs/PC_빠른입력_점검_2026-09-08.docx`, 아직 git 미커밋). **2026-09-11 방향 재확정**: 15-2(AI 자동변형)는 무료 AI 챗의 고도화로 시간이 갈수록 가치가 희석될 위험이 있는 반면, PC 빠른입력은 이 앱 구조 고유의 강점(입력 마찰 해소)이라 다음 실무 우선순위는 15-2 고도화보다 `input.html` 키보드 입력 흐름 개선에 둠 — 논의 끝에 **(1) Enter 키로 한국어→영어(같은 행)→다음 행 한국어로 자동 이동 (2) Tab 순서에서 삭제 버튼 건너뛰기 (3) 페이지 로드 시 첫 입력칸 자동 포커스** 3가지 개선안 확정. **2026-09-12 "시작" 승인으로 착수·구현 완료** — `js/input.js`만 수정(Enter keydown 리스너 2개, `deleteBtn.tabIndex = -1`, `renderAuthView`에 `wasLoggedIn` 비교 로직으로 로그인 전환 시점에만 자동 포커스). Claude 1차 확인(Playwright, `window.CloudSync` 모킹+gstatic 네트워크 차단으로 로그인 시뮬레이션, 11개 검증 항목 전부 통과) 완료 — 사용자 실기기 2차 확인 대기 중이던 상태에서 이어서 **같은 날 "영어" 라벨을 "학습어"로 일반화**(`js/input.js` placeholder, 2026-09-11 언어중립 원칙을 이 페이지에도 마저 적용) + `reference/다른앱의 웹입력화면.png`(Anki 카드추가 창) 참고해 추가 개선 3가지(A 반응형 레이아웃/B 입력 글자크기 조절/C 파일 가져오기) 논의, A→B→C 순서로 확정. **A(반응형 레이아웃) "시작" 승인으로 구현 완료** — `css/input.css`만 수정: 기본(데스크톱) `.page` 최대너비 720px→1100px·패딩 확대, `@media(max-width:1024px)`에서 기존 720px 폭으로 복귀. Claude 1차 확인(Playwright, 5개 뷰포트 1920/1366/768/390/320에서 가로 오버플로우 0 확인+스크린샷) 완료. **이어서 같은 날, 사용자가 "문장은 보통 기니까 한 줄 전체 폭을 주는 게 어때"라고 제안 → "아주 긴 문장은 앞부분이 안 보이게 되지 않냐"는 확인 질문에 `<input>`의 구조적 한계(줄바꿈 불가)를 설명하고 `<textarea>`+자동높이확장으로 해결 가능함을 제안, "시작" 승인으로 구현** — 한국어/학습어 필드를 `<input>`→`<textarea rows="1">`로 교체, 입력마다 `scrollHeight` 기준으로 높이를 늘리는 `autoGrow()` 추가, `.input-row`를 테두리 있는 카드로 감싸 화면 크기와 무관하게 항상 세로 배치(위 A에서 만든 480px 전용 `flex-wrap` 규칙은 이제 불필요해져 삭제), 삭제 아이콘은 카드 우측 상단에 절대위치 고정. 구현 중 **브라우저 창을 리사이즈하면 높이가 재계산 안 돼 텍스트 하단이 잘리는 버그**를 Playwright로 발견해 `window.resize` 리스너(100ms 디바운스)로 전체 재계산하도록 수정. Claude 1차 확인(Playwright, 긴 문장 입력+리사이즈 전후 클리핑 없음 확인, 라이트/다크 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-12, "좋아")** — 19번 키보드흐름 개선 3가지+라벨변경+A 반응형 레이아웃+자동확장 텍스트박스/카드 묶음 모두 확인됨. 이어서 **B(입력 글자 크기 조절) 구현 완료** — `input.html`에 "가-"/"가"(미리보기)/"가+" 버튼 신규, `js/input.js`에서 CSS 변수(`--input-font-size`)를 갱신해 현재+향후 자동생성될 행 모두에 상속되게 함(13~25px, 2px 단위, `localStorage` 저장으로 재방문시 유지, 상하한 도달 시 버튼 비활성화). Claude 1차 확인(Playwright, 상/하한·저장·새로고침 유지·긴 문장 폰트확대 시 클리핑 없음 등 검증) 완료 → 사용자가 "웹은 직접 타이핑하면서 확인되니 별도 미리보기("가")는 불필요하다"고 지적해 같은 날 미리보기 요소 제거(버튼 2개만 남김, Claude 1차 재확인 완료) — **사용자 실기기 2차 확인 완료(2026-09-12)**(최종 버전 기준). 이어서 "UI 정리 + 폰 앱과 화면 디자인 통일" 논의가 나왔으나, 스타일 파일 자체는 분리 유지하되 **화면 디자인 통일은 사용자가 먼저 Figma로 시안을 확정한 뒤 재논의하기로 보류**(착수 시점 미정). **C(파일 가져오기) "진행" 승인으로 구현 완료** — `parseSentencesText`를 `js/app.js`에서 신규 `js/sentence-parser.js`로 분리해 폰 앱·`input.html` 양쪽이 공유(일반 스크립트, 함수 선언이라 모듈인 `input.js`에서도 `window.parseSentencesText`로 접근), `input.html`에 "파일 가져오기(CSV/TSV/TXT)" 버튼 신규 — 불러온 문장은 새 행으로 채워지고 마지막 빈 행은 유지, 즉시 저장 대신 기존 "클라우드에 저장" 버튼으로 검토 후 반영. Claude 1차 확인(Playwright, 폰 앱 회귀 확인 포함 — 파서 분리 후에도 문장 수 정상 증가, `input.html` 헤더줄 스킵+빈행 유지+상태메시지 확인, 라이트/다크 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-12, "확인완료")** — **이로써 A(반응형 레이아웃)+B(입력 글자크기 조절)+C(파일 가져오기) 3가지 개선안 전부 완료**. **2026-09-13 `reference/docs/PC_빠른입력_점검_2026-09-08.docx` 재검토 착수**: 문서 내용을 확인한 결과 가격 정보(5,000원, 18+19번 번들)가 2026-09-11 가격 통합(7,000원, 18+19+15-2)으로 이미 갱신이 필요한 상태임을 확인. 문서 4장 기술적 한계 중 "(2) 조회·수정 불가(웹페이지에서 기존 문장을 볼 수도 고칠 수도 없음)"를 사용자가 "그 기능이 있어야 데스크탑 빠른입력도 의미와 가치가 있을 것 같다"고 판단해 **유료 범위에 포함하기로 결정, 같은 날 구현 완료** — `input.html`에 **"클라우드에 저장된 문장 (N개)" 관리 섹션** 신규: 로그인 시 `CloudSync.restore()`로 클라우드 문장 전체를 불러와 각 항목을 한/영 입력칸(기존 새-문장 입력행과 같은 `.input-row`/`.input-kr`/`.input-en` 스타일 재사용)으로 표시, 텍스트 수정 및 삭제(휴지통 아이콘, confirm 후 목록에서 제거 — 실제 반영은 저장 시점) 가능. 기존 "새 문장 추가" 섹션은 구분선 아래 그대로 유지. 저장 버튼을 "클라우드에 저장"→**"변경사항 저장"**으로 개명해 (관리 섹션의 수정/삭제 결과)+(새로 입력한 문장)을 한 번에 `CloudSync.backup()`으로 저장(id 기준 맵으로 원본의 important/unfamiliar 등 필드 보존), 저장 후 관리 목록은 클라우드에서 다시 불러와 최신화. 관리 섹션 제목 옆 "새로고침" 버튼으로 저장 전 최신 상태를 다시 불러올 수 있음(편집 중 새로고침 시 되돌릴 수 없다는 confirm 경고, 동시편집 충돌을 완전히 막진 못하지만 사용자가 스스로 확인할 수단 제공). 자동높이확장·글자크기 조절 로직은 클래스 공유 덕에 관리 섹션에도 자동 적용, `js/input.js`에 `TRASH_ICON_SVG` 상수로 삭제 아이콘 마크업 통합(중복 제거). 실시간 동기화·동시편집 충돌 "방지"(경고 이상의 로직)는 이번 범위 밖으로 문서에 남겨둠. Claude 1차 확인(Playwright, `CloudSync` 모킹 — 목록 프리필→수정→삭제→새 문장 추가→저장 시 최종 목록에 정확히 반영(삭제 제외/수정 반영/원본 필드 보존/신규 추가) 확인, 저장 후 재조회로 목록 갱신 확인, 새로고침 dirty 경고 확인, 빈 클라우드 안내 확인, 1366px/390px 라이트·다크 스크린샷으로 레이아웃·줄바꿈 확인, 콘솔 에러 없음) 완료 → **사용자 실기기 확인 중 레이아웃 문제 제보(2026-09-13): "로그인화면 모든 문장이 그대로 다 보여. 그러면 새 글을 어디서 입력해?"** — 관리 섹션이 접히지 않은 채 페이지 맨 위에 있어 문장이 많으면 화면을 다 채워 "새 문장 추가" 입력칸을 찾으려면 스크롤을 많이 내려야 하는 문제였음. **같은 날 재배치로 수정 완료**: (1) "새 문장 추가" 섹션을 맨 위로 이동(이 페이지의 핵심 목적이라 항상 펼쳐진 채 노출) (2) "클라우드에 저장된 문장" 관리 섹션은 그 아래 구분선 다음, **기본은 접힌 상태**로 시작 — 제목을 `#manage-toggle-btn` 버튼으로 감싸고 우측에 `chevron-down` 아이콘(펼치면 180도 회전, 도움말 아코디언과 같은 패턴) 추가, 클릭 시 `#manage-content`(안내문+목록+빈 상태 문구를 담은 컨테이너)의 `.hidden` 토글 (3) "변경사항 저장" 버튼은 두 섹션 모두를 포괄해야 해서 맨 아래로 이동. **주의**: 목록 자체는 접혀있어도 로그인 시점에 이미 불러와져 DOM에 남아있음(저장 시 그대로 반영돼야 하므로 지연 로딩 아님) — 다만 `display:none`인 동안 계산된 textarea `scrollHeight`는 0으로 잘못 잡히는 브라우저 특성 때문에, 펼칠 때마다(`manageToggleBtn` 클릭 시 펼치는 경우만) 관리 목록의 textarea 높이를 다시 계산하도록 처리해 접혀있다 펼쳤을 때 텍스트가 잘려 보이는 문제를 방지. Claude 1차 확인(Playwright, 섹션 순서·초기 접힘 상태·펼침/접힘 토글+아이콘 회전·접힌 상태로 저장해도 기존 문장이 그대로 보존되는지·펼쳤을 때 긴 문장 텍스트가 잘리지 않는지, 라이트/다크 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-13)** — 이로써 19번 조회/수정 기능(레이아웃 수정 포함) 전체 완료. 문서의 남은 재검토 항목(폰↔PC 동시편집 충돌을 사용자 주의에 맡길지 방지 로직을 넣을지, 구매의향 검증 방법, Google Play 개발자 계정 심사 현황, 가격 재확정)은 다음 세션에 계속 논의. **2026-09-14 문서 갱신**: 남은 재검토 항목 중 "동시편집 충돌" 방향을 논의 — 사용자가 "실시간 반영은 안 중요하지만 양쪽 입력·수정으로 데이터가 유실되는 건 불만"이라는 기준 제시 → **"저장은 항상 병합(merge), 복원만 의도적 전체교체"로 방향 확정**(코드 구현은 아직 착수 전, 다음 세션 승인 대기). 같은 날 문서의 나머지 stale 정보(가격 5,000원→7,000원, 4장 (2) "조회·수정 불가"→해결됨)도 함께 갱신 완료(`reference/docs/PC_빠른입력_점검_2026-09-08.docx`, git 커밋 완료). **텍스트 붙여넣기 추가(2026-09-14)**: "카메라로 찍어서 AI가 텍스트로 만들어준 문장들을 파일로 안 만들고 바로 복사해서 붙여넣고 싶다"는 요청 — 기존 "파일 가져오기" 옆에 "텍스트 붙여넣기" 버튼 신규, 클릭하면 안내문+textarea+"가져오기" 버튼으로 구성된 패널이 펼쳐짐(다시 누르면 접힘). 파일 가져오기와 붙여넣기 둘 다 같은 파서(`window.parseSentencesText`)를 쓰므로, 두 경로가 공유하던 "파싱 결과를 입력 행에 채워 넣는" 로직을 `insertParsedRows()` 함수로 추출해 중복 제거. 가져온 뒤엔 패널이 자동으로 접히고 textarea가 비워짐(같은 텍스트를 실수로 중복 반영하지 않도록), 문장을 못 찾으면 안내 문구만 표시. Claude 1차 확인(Playwright, `input.html`은 `CloudSync` 모킹으로 로그인 시뮬레이션): 패널 토글 열림/닫힘 확인 → 붙여넣기→가져오기 시 행 정확히 채워짐+상태 메시지 확인 → 가져온 뒤 패널 자동 접힘+textarea 비워짐 확인 → 빈 텍스트로 시도 시 안내 문구 확인 → 기존 파일 가져오기 기능 회귀 없음(공유 함수 추출 후에도 정상 동작, 헤더줄 스킵 포함) 확인 → 라이트 스크린샷 + `colorScheme: 'dark'` 옵션으로 올바르게 확인한 다크 스크린샷(이 페이지는 `data-theme` 속성이 아니라 시스템 다크모드 설정만 따르는 페이지라는 점을 테스트 중 재확인) 모두 확인 → 콘솔 에러 없음(의도적으로 차단한 gstatic 네트워크 에러만 발생) → **사용자 2차 확인 대기 중**. **목록이 길어질 때의 불편 2가지 개선(2026-09-14, "시작" 승인)**: "문장이 길어지니까 저장 버튼이 아래 있는게 불편해", "문장이 매우 많으면 스크롤이 짧아져서 힘들다"는 피드백으로 (1) **저장 버튼 하단 고정**: `input.html`의 "변경사항 저장"+상태 텍스트를 `.save-bar`로 감싸 `position: sticky; bottom: 0`으로 화면 하단에 항상 노출(`css/input.css`만 수정, JS 변경 없음) — `.panel`의 패딩을 상쇄하는 음수 마진으로 카드 바닥에 딱 붙는 형태, 구분선+그림자로 위쪽 콘텐츠와 시각적으로 분리, 반응형 padding/border-radius는 `.panel`과 짝을 맞춤. (2) **관리 목록 페이지네이션("더보기")**: "클라우드에 저장된 문장" 관리 섹션이 문장이 많을 때 전부 한 번에 그려지면 스크롤바 썸이 너무 작아져 다루기 어렵다는 문제로, 처음엔 20개만 보여주고 `#manage-load-more-btn`("더보기 (N개 더 있음)")을 누를 때마다 20개씩 추가로 노출. **핵심 설계 포인트**: 안 보이는 나머지 행도 실제로는 `.hidden` 클래스로 화면에서만 숨길 뿐 DOM에서 제거하지 않음(지연 생성 아님) — "변경사항 저장" 로직이 `manageRowsEl.querySelectorAll('.input-row')`로 전체를 스캔해 화면에 없는 항목을 "삭제된 것"으로 간주하는 기존 로직과 부딪히지 않도록 하기 위함(19번 조회/수정 기능의 저장/삭제 판별 로직에 영향 없이 얹은 것). `loadManageList()`가 20개 초과 인덱스의 행에 `.hidden`을 붙이고 `updateManageLoadMoreBtn()`으로 버튼 노출 여부/잔여 개수를 갱신, 더보기 클릭 시 다음 20개의 `.hidden`만 해제하고 그 행들만 `autoGrow()`를 재실행(접힘→펼침 때와 같은 이유로 숨겨진 동안 `scrollHeight`가 0으로 잘못 계산되는 브라우저 특성 보정). `js/input.js`/`input.html`(버튼 마크업)/`css/input.css`(sticky 스타일) 수정. Claude 1차 확인(Playwright, 45개 목업 문장으로 검증): 초기 20개만 보이고 나머지 25개는 DOM엔 있지만 숨김+더보기 버튼 라벨 확인 → 더보기 2회 클릭으로 45개 전부 노출+버튼 사라짐 확인 → 새로고침 후 다시 20개로 리셋 확인 → **회귀 테스트**: 더보기를 누르지 않아 25개가 숨겨진 채로 보이는 행 1개만 수정 후 저장 시 `CloudSync.backup()`에 45개 전부(숨겨진 항목 포함, 삭제 처리 안 됨) 정확히 전달되는지 확인 → 스크롤 시 저장바가 뷰포트 하단에 계속 붙어있는지(`boundingBox` 좌표) 확인 → 라이트/다크 스크린샷으로 sticky bar 레이아웃·구분선·더보기 버튼 확인 → 콘솔 에러 없음(gstatic 차단 에러 제외) → **사용자 실기기 2차 확인 완료(2026-09-14, "확인 완료")** — 확인 과정에서 "저장 후에도 글자가 남아있다"는 제보가 있었으나 조사 결과 버그 아님(새 문장 입력칸은 저장 시 정상적으로 비워짐, 남아있던 건 그 아래 "클라우드에 저장된 문장" 관리 목록의 정상 데이터였음) — 다만 **"새 문장 추가" 입력 영역과 "클라우드에 저장된 문장" 관리 영역이 시각적으로 잘 구분되지 않는다**는 UI 혼동 포인트로 확인됨. 사용자가 "웹 입력화면 디자인을 아직 확정하지 못했다"며, 아래 "UI 정리 + 폰 앱과 화면 디자인 통일" 논의(2026-09-12, Figma 시안 확정 후 재논의 보류)와 함께 **디자인 확정 시점에 같이 손보기로 결정** — 지금 당장의 코드 수정은 없음
- **유료 가격 정책 전면 재통합(2026-09-11 확정 → 2026-09-14 한도 방식 재확정)**: 기존 "1단계(5,000원, 비용 zero)+2단계(크레딧 소모형)"의 2단계 분리 구조를 폐기하고 **하나의 1회성 결제 7,000원**으로 통합. 최종 번들 = **18(클라우드 백업, 완료)+19(PC 빠른입력, 프로토타입 완료)+15-2 유료판(AI 자동변형, 미착수)** 3개 구성. **2026-09-14, 15-2 한도를 "월 20회"에서 "평생 100개, 크레딧 추가 구매 없음"으로 재확정**(아래 "무료 / 유료 버전 구분" 섹션 참고, 사유는 그 항목에 상세 기록) — 월별 한도는 "수년간 꾸준히 쓰는 헤비유저"에게는 상한이 없다는 구조적 허점이 있어 재검토됨. 15-2 유료판에 쓸 모델은 Claude가 Sonnet 5(`claude-sonnet-5`)를 추천했으나 최종 확정 아님(개발 착수 시점 재확인 필요). 결제 방식은 **Google Play 인앱결제로 확정**(사업자등록 불필요) 그대로 유지. 자세한 내용은 아래 "무료 / 유료 버전 구분" 섹션 참고 — **Claude 메모리(`project_monetization_strategy`)도 이 통합 결정으로 갱신 필요**
- **착수 중(사용자 2차 확인 대기)**: 현재 없음. (참고: "글씨체/글씨굵기"가 예전엔 이 목록에 중복으로 남아있었는데, 2026-09-03에 21번(테마/폰트 커스터마이징)으로 이미 공식 제외 결정된 것과 같은 항목이라 정리함, 위 Non-goals 참고)
- **원칙**: 무료=핵심가치(개인화 문장 암기)에 집중, 유료=고도화(AI변형 — "능력"이 아니라 "번거로움 제거"를 판매하는 구조), 유료 기능은 전부 착수 보류 상태(단, 정답 발음 듣기(TTS)는 24번으로 이미 무료 완성됨). **음성판정(스피킹 연습) 기능은 2026-09-11 앱 성격 재검토로 유료 후보에서 완전히 제외 확정**(아래 Non-goals 참고)

## 프로젝트 개요

### 기본 정보
- **주제**: PWA 기반 학습 애플리케이션
- **프로젝트명**: Tuk Tak
- **배포 URL**: https://tuk-tak-six.vercel.app/ (2026-08-29 계정 교체로 변경 — 옛 주소는 https://tuk-tak-beta.vercel.app/)

### 컨셉트
- 개인의 목적에 맞는 영어를 거침없이 튀어나오게 연습시켜주는 앱

### 목적
- 개인의 목적에 맞는 영문을 확실하게 외워서 영어의 말문을 열게 만드는 앱
- 무료/유료 버전 구분 방향은 아래 "무료 / 유료 버전 구분" 섹션 참고 (2026-08-17 정리)

### 타깃 사용자
- 자신이 어떤 영어를 할것인지 알고 있는 영어학습자
- 적극적으로 자신이 외울 문장들을 수집할 수 있는 학습자

### 톤 & 비주얼 컨셉
- 맑고 청명한 파란색 기반으로 하되 레드계열의 포인트색을 사용해서 튀는 느낌살린다. 또한 파란색과 빨간색의 조화는 한국의 국기나 단청등의 색감을 의미하기도 한다.

### 하지 않을 것 (Non-goals) — 잠정, 진행하며 조정 가능
- **가로모드 대응** (2026-08-28 확정): 방어적 CSS 보정도, "세로로 사용해주세요" 안내 화면도 넣지 않기로 결정. 짧게 세로로 들고 확인하는 카드 암기 앱 특성상 가로 회전은 대부분 의도치 않은 회전 잠금 해제로 발생해 실사용 가치가 낮다고 판단. 자세한 논의는 체크리스트 14번, PROGRESS.md 2026-08-20/2026-08-28 참고
- **태그/카테고리 필터** (2026-08-28 확정, 2026-09-06 재확인): 사용자가 문장마다 태그를 붙이고 태그·카테고리 기준으로 필터링하는 아이디어. 2026-08-15에 정렬/필터 기능(9번) 설계 중 검토됐으나 범위가 커서 보류된 아이디어였고, 2026-08-28에 착수 자체를 접기로 최종 결정. 2026-09-06에 "폴더 생성 후 그 폴더의 문장만 학습" 아이디어로 다시 논의됐으나 같은 개념으로 판단해 재확인 — 가치보다 마찰이 크다는 게 핵심 근거: (1) 문장 추가할 때마다 폴더를 정해야 해 이 앱의 핵심 강점인 "빠르게 문장을 던져 넣는" 입력 흐름에 마찰이 생기고 (2) 사용자가 실제 파일 정리 경험상 "폴더·분류 체계는 결국 정리가 잘 안 된다"고 판단 — 분류형 기능은 이후에도 이 두 기준(핵심 입력 흐름 마찰 여부, 실사용 유지관리 가능성)으로 재검토할 것
- **PWA 스플래시 아이콘 위치** (2026-08-28 확정): 2026-08-08~09에 논의된, 앱 아이콘 탭 시 뜨는 OS 네이티브 스플래시 화면에서 아이콘을 화면 중앙이 아니라 하단에 고정 배치하고 싶다는 아이디어(배경색을 브랜드 블루로 맞추는 것까지는 이미 완료됨). 사용자가 "사실 원했던 건 스플래시 화면으로 잠깐 앱을 소개하는 것"이었다고 밝혔는데, 스플래시는 원래 매우 짧게 지나가는 화면이라 텍스트 전달 용도로 애초에 부적합 — 그 의도는 아래 온보딩 쪽으로 옮겨가는 게 맞다고 보고, 아이콘 위치 조정 자체는 더 이상 필요 없다고 판단해 착수를 접음
- **통계/복습 리포트** (2026-09-01 확정, 체크리스트 20번): 연속학습일수·정답률·진도를 보여주는 화면. 코드 구현(카드 마킹 아이콘 바에 "알아요/아직이에요" 자가평가 아이콘 추가, `#stats-screen` 신규)+Claude 1차 확인(Playwright)까지 완료했으나, 사용자가 "자기동기부여 능력이 없으면 이 앱 자체가 매력 없다"며 스트릭·정답률 같은 게이미피케이션 장치가 이 앱의 성격(이미 목적이 뚜렷한 학습자가 자기 문장을 모아 암기하는 앱)과 맞지 않고 다른 암기 앱을 어설프게 따라하는 느낌이라고 판단 — 전면 제외 결정. 코드는 git revert로 완전히 되돌림. 유사한 "동기부여/게이미피케이션" 성격의 기능(뱃지, 랭킹 등)은 앞으로도 이 판단 기준으로 재검토 필요
- **테마/폰트 커스터마이징** (2026-09-03 확정, 체크리스트 21번): 착수 전 상태였고, 사양도 "글씨체/글씨굵기 정도"로 구체화되지 않은 채 1단계 유료 번들 후보로만 잠정 등재돼 있었음. 20번 제외 이후 "이 기능이 유료 전환을 유도할 만한가?"를 사용자가 다시 묻자, 테마/폰트 커스터마이징 같은 꾸미기형 커스터마이징은 일기·노트 앱처럼 오래 머무르며 자기 공간을 꾸미고 싶은 앱에서 잘 통하는데, 툭탁은 목적이 뚜렷한 학습자가 짧게 들어와 문장을 확인하고 나가는 기능형 앱이라 구매 유인이 약하다고 판단해 착수 전 제외 결정. 코드 자체가 없어 되돌릴 것도 없음(문서 정리만 진행)
- **스피킹 연습(음성 판정) 기능** (2026-09-11 확정, 체크리스트 3번): 마이크로 말한 답을 인식해 정답과 비교하거나 발음 정확도를 채점하는 기능. 2026-08-17 유료 2단계 후보로 확정했고, 2026-09-03엔 "무료판에 아예 없는 능력"이라는 이유로 **유료 전환 유도력 1순위**로까지 판단했었음(비용 추정까지 조사 완료, 시간당 약 150~300원). 그러나 앱 성격을 다시 짚어보며 제외 결정 — 툭탁의 학습 메커니즘은 "한국어 보고 → 확인 → 영어 맞았는지 텍스트로 확인"이라 핵심은 **떠올리기(recall)**이지, 실제로 입 밖으로 소리 내어 말한 것을 앱이 검증하는 구조가 아님. 마이크 판정 기능을 넣으면 "개인화 문장 암기 카드 앱"에서 "회화 연습 앱"으로 앱 성격 자체가 넘어가는 셈이라, 스트릭·정답률(20번)과 마찬가지로 핵심가치와 어긋나는 기능으로 판단해 유료 로드맵에서 완전히 제외. 코드 자체가 없어 되돌릴 것도 없음(문서 정리만 진행)
  - **대체 아이디어로 "정답 발음 듣기"(TTS) 등장 → 같은 날 체크리스트 24번으로 구현 완료**: 같은 논의에서 "카드 학습 중 정답 발음을 들려주면 어떨까"라는 아이디어가 나왔는데, 이건 방향이 반대(텍스트→음성, SpeechSynthesis API)라 위에서 제외한 기능(음성→텍스트, SpeechRecognition)과 무관한 별개 기능. recall 과정에 정확한 소리를 붙여주는 보조 도구라 핵심가치(암기)를 오히려 강화한다고 판단됨 — 기기 내장 TTS 엔진 사용이라 비용도 0원. 무료로 확정, 기본은 스피커 아이콘 수동 탭·설정에서 자동재생 전환 가능한 형태로 바로 구현(위 체크리스트 24번 참고, 사용자 2차 확인 대기)

### 브레인스토밍 아이디어 (착수 여부 미정, 논의만 기록 — 2026-09-06)
> 체크리스트 항목이 아님. 실제 착수하려면 별도로 "시작"/"go" 승인 필요. 학습 지속을 돕는 차원에서 제안된 아이디어들로, 각각 게이미피케이션 여부를 개별 판단해 기록

- **연속학습일수(스트릭)**: 20번(통계/복습 리포트)에서 이미 시도 후 게이미피케이션으로 판단해 전면 제외한 전례와 같은 성격 — 재검토해도 넣지 않는 쪽으로 기울어짐 (위 Non-goals "통계/복습 리포트" 참고)
- **D-day 계산**(예: "시험까지 D-30"): 앱이 사용자를 독려하는 장치가 아니라 사용자가 이미 가진 목표 날짜를 보여주기만 하는 것이라 게이미피케이션이 아니라 "목적이 뚜렷한 학습자" 타깃과 오히려 부합할 수 있음. 다만 "목표/시험 등록" 같은 새 데이터 구조가 필요해 범위가 작지 않음
- **푸시 알림**: 리마인더로서 재방문 유도엔 효과적일 수 있으나 (1) PWA 푸시는 트리거용 서버 인프라가 필요해 1단계 유료 번들의 "비용 zero" 원칙과 충돌 가능 (2) "안 하면 죄책감" 식으로 설계하면 스트릭과 같은 게이미피케이션 함정에 빠질 위험 — 기능 자체보다 구현 방식에 따라 득/실이 갈리는 사례
- ~~**정답 발음 듣기(TTS)**~~ — 2026-09-11 같은 날 바로 착수·구현 완료(체크리스트 24번으로 승격, 사용자 2차 확인 대기). 아래 체크리스트 24번 참고
- **TTS 목소리 선택 (나중에 쉬워지면 검토, 2026-09-11 논의)**: 24번 구현 후 "핸드폰 음성은 목소리 선택도 가능해?"라는 질문에서 시작. 기술적으로는 `speechSynthesis.getVoices()`로 기기별 목소리 목록을 가져와 선택 UI를 만들 수 있으나, 목록이 기기·브라우저마다 다르고 비동기 로딩 등 구현이 번거로워 지금 범위에서는 보류 — 사용자가 "지금은 여기까지 자원을 쓰고 싶지 않다, 기술 발전(온디바이스 TTS 품질 향상)은 코드 변경 없이 무상으로 받아먹을 생각"이라고 확인. **다만 나중에 목소리 선택 구현이 더 쉬워지면(예: 브라우저 API가 표준화되거나 목록 관리가 단순해지면) 그때 기능으로 넣어 서비스할 의향은 있음** — 착수 조건이 명확한 채로 보류된 아이디어라 다른 브레인스토밍 항목과 성격이 조금 다름(다른 항목은 "할지 말지" 자체가 미정인데, 이건 "쉬워지면 한다"로 조건부 확정)
- **문장변형 퀴즈 코너 (2026-09-14 논의 후 접음)**: 유료 문장변형(15-2)에 "실력을 확인하는 퀴즈"를 넣으면 어떨지 논의. 처음엔 20번(통계/복습 리포트)에서 이미 게이미피케이션으로 판단해 제외한 전례와 겹치는지가 쟁점이었으나, 사용자가 의도한 방식("한글 보여주고 영어 맞추기")을 구체화해보니 **기존 카드 학습 화면(한국어→확인→영어 정답)과 완전히 동일한 구조**라는 게 드러남 — 새 "퀴즈" 개념이 필요 없고, 문장변형 결과를 카드처럼 보여주기만 하면 됨(15-2 무료판의 "미니 학습"이 이미 이 역할을 함). 만약 "진짜 판정하는" 퀴즈로 만들려면 (1) 음성으로 답하기 → 2026-09-11에 이미 제외한 "스피킹 연습(음성 판정)"과 동일 (2) 타이핑으로 답하기 → 영어 정답은 표현이 여러 개일 수 있어 정확히 글자가 같아야만 정답 처리하는 방식은 억울한 오답을 만들고, 의미 기준으로 채점하려면 AI를 또 호출해야 해 비용·복잡도가 붙음 — 둘 다 이미 제외했거나 새로운 문제를 만드는 길이라 **착수하지 않기로 결정**

### 무료 / 유료 버전 구분 (방향 — 2026-08-17 정리, 유료는 착수 전)
- 무료는 핵심가치("개인화된 문장을 확실하게 암기")에 집중, 유료는 기능 고도화. 방향만 정하고 착수는 보류, 착수 시점에 재논의
- **무료 (핵심)**
  - 문장 입력: 폼 입력 / 파일(CSV·TSV·TXT) 가져오기 / 백업 파일 복원
  - 카드 학습: 한국어 표시 → 확인 → 영어 정답 표시
  - 텍스트 답변 확인 (음성 답변 판정 기능은 2026-09-11 유료 로드맵에서도 완전히 제외됨 — 위 Non-goals 참고)
  - 문장관리(즐겨찾기·미암기, 수정/삭제), 정렬 5종, 필터(전체/중요/미암기)
  - 글자 크기, 화면모드(시스템/라이트/다크)
- **유료 — 단일 번들로 재통합 (2026-09-11 최종 확정, 2026-09-14 한도 방식 재확정)**: 기존 "1단계(비용 zero)+2단계(크레딧 소모형)" 2단계 구조를 폐기하고 **하나의 1회성 결제(7,000원)**로 통합 — AI 자동변형에 서버 비용이 들긴 하지만 **평생(누적) 100개 한도**로 원가 총액 자체를 상한선 아래로 묶어 별도 크레딧 SKU 없이도 방어 가능하다고 판단(가격은 5,000원→7,000원으로 인상, 한도 신설이 인상의 근거)
  - 클라우드 백업/동기화 (18번, **완료** — 현재는 무료 공개 중, 결제 게이팅은 번들 출시 시점에 적용)
  - PC 빠른입력 (19번, **2026-09-03 편입 확정, 2026-09-11 전략적으로 최우선 순위 재확인** — 프로토타입 구현+사용자 실사용 확인은 완료, 현재는 결제 게이팅 없이 무료 공개 중. "폰 타자가 느려 불편하다"는 실제 pain point 기반이라 유료 철학("번거로움 제거")에 부합한다고 판단해 편입. **2026-09-11 재평가**: 무료 AI(ChatGPT 등)가 계속 고도화되며 15-2(AI 자동변형)의 "번거로움 제거" 가치는 시간이 지날수록 희석될 위험이 있는 반면, PC 빠른입력은 그런 외부 요인에 흔들리지 않는 이 앱 고유의 구조적 강점(입력 마찰 해소)이라 판단 — 다음 실무 우선순위를 15-2 고도화보다 `input.html` UX 개선(아래 19번 항목의 "다음 작업 제안" 참고)에 두기로 함)
  - 문장 자동 변형 자동생성(15-2 유료판, 2026-09-11 가격 통합 확정 → 2026-09-14 한도 방식 재확정): 버튼 한 번으로 API 자동생성. 모델은 Claude Sonnet 5(`claude-sonnet-5`)를 Claude가 추천했으나(품질 대비 원가 균형) 최종 확정은 아직 — 실제 개발 착수 시점에 재확인 필요
    - **한도: "월 20회" → "평생(누적) 100개, 크레딧 추가 구매 없음"으로 변경(2026-09-14)**. 계기는 사용자의 문제제기 — "1회성 결제인데 월 한도를 두면, 다음 달에도 또 20개씩 계속 쓰는 헤비유저가 있으면 결국 무한정 비용이 나가는 거 아니냐"는 지적. 실제로 Sonnet 5 정가(입력 $3/출력 $15 per 1M 토큰, 2026-08-31 할인가 종료)로 요청 1회당 비용을 추정(입출력 각 500토큰 가정, 넉넉하게 잡은 수치)해보니 요청당 약 13원 — **월 20회를 수년간 유지하면 2~3년 시점부터 누적 원가가 7,000원을 넘어서는 것을 확인**(2년=약 6,240원/89% 소진, 3년부터 적자). 월별 한도는 "매달 리셋"되는 구조라 총 사용 기간에 비례해 원가가 무한정 늘어나는 구조적 허점이 있었던 것
    - **"평생 100개"로 바꾼 이유**: 월별이 아니라 누적 총량으로 상한을 두면, 몇 년을 쓰든 총 원가가 100개 × 13원 ≈ 1,300원으로 고정됨(7,000원 대비 여유 충분, 추정이 2~3배 틀려도 안전). "크레딧 추가 구매"(100개 이후 유료 충전)도 검토했으나, 이건 2026-09-11에 이미 "복잡해서 폐기"한 2단계 크레딧 구조를 사실상 다시 여는 것이라 판단해 **채택 안 함** — 100개를 넘으면 그냥 자동변형 기능이 끝나고(무료판 방식인 "직접 AI 챗에 복붙"은 계속 가능), 별도 결제 흐름은 만들지 않기로 확정
    - **100개라는 숫자 자체의 근거**: 비용 관점(위 계산)뿐 아니라 실사용 관점에서도 안전 — 문장 1개를 변형하면 시제 4종+인칭+수+부정문+의문문 등으로 보통 5~9개(평균 7개)의 파생 문장이 나오므로, **100회 요청이면 이미 약 700개의 새 문장이 쌓이는 셈**이라 실제로 이 한도에 도달하는 사용자는 거의 없을 것으로 판단. 즉 100개는 "인색한 제한"이 아니라 "거의 도달할 일 없는 넉넉한 상한"에 가까움
    - **일괄 처리(배치)와의 관계**: 문장관리에서 여러 문장을 선택해 한 번에 변형 요청하는 기능(2026-09-14 별도 논의, 아직 설계만 되고 미착수)을 붙이더라도, **한도는 "버튼 클릭 횟수"가 아니라 "AI에 보낸 원문 문장 개수"로 차감**됨 — 예: 10개 선택해서 일괄 변형하면 100개 중 10개 차감. 이래야 일괄 처리가 한도를 우회하는 구멍이 되지 않음. 남은 개수보다 많이 선택했을 때(예: 8개 남았는데 15개 선택) 사전에 경고할지, 처리하다 중간에 멈출지는 실제 구현 시점에 확정 필요(사전 경고 쪽이 더 친절하다는 의견까지만 논의됨)
  - ~~통계/복습 리포트 (20번)~~ — **제외 확정**(2026-09-01, 앱 성격과 안 맞는 게이미피케이션 기능으로 판단, 위 Non-goals 참고)
  - ~~테마/폰트 커스터마이징 (21번)~~ — **제외 확정**(2026-09-03, 사양 모호+이 앱 성격상 구매 유인 약함, 위 Non-goals 참고)
  - 태그/카테고리 관리는 후보에서 제외(위 Non-goals "태그/카테고리 필터" 결정 유지)
  - ~~음성 답변 전체(마이크 입력 → 인식 → 판정)~~ — **2026-09-11 유료 로드맵에서 완전히 제외 확정**(위 Non-goals "스피킹 연습(음성 판정) 기능" 참고 — 앱 핵심이 recall 중심 암기이지 회화 판정이 아니라는 재검토 결과, 2026-09-03까지 "유료 전환 유도력 1순위"로 판단했던 것도 함께 무효화됨)
  - 결제 방식: **Google Play 인앱결제로 최종 확정**(사업자등록 불필요, `reference/docs/유료화_결제.docx` 참고)
  - 자세한 배경(가격 논의 과정 등)은 Claude 메모리(`project_monetization_strategy`) 참고 — **메모리도 이번 통합 결정으로 갱신 필요**
- **유료 기술 방향 (미착수)**: 기존 앱을 그대로 고도화 — 프론트엔드(HTML/CSS/Vanilla JS, PWA, LocalStorage)는 유지
  - 신규 필요: (1) API 키 보호용 서버리스 함수(Vercel, 15-2 유료판용 — 평생 누적 100개 한도 카운팅 로직 포함, 월별 리셋 없음) (2) 계정/로그인(완료, Firebase Auth) + 결제 시스템(**Google Play 인앱결제, 1회성 — 구독 아님**)

## 최종 출시 전 점검사항 (생기는 대로 추가)

- [x] **다른 언어 학습자를 위한 여지 남기기** (2026-09-11): 툭탁은 언어중립 앱으로 리브랜딩하는 건 아니지만("좁은 니치 전략" 유지, `project_goals_and_scope` 메모리 참고), 코드 점검 결과 핵심 학습 루프(카드학습·정렬·필터·백업 등)는 이미 언어 무관하게 동작하고 있어 최소한의 여지만 남기기로 결정. 문장추가 화면(폼 입력 라벨 + 파일가져오기/텍스트붙여넣기 안내 문구)의 "영어"를 "학습할 언어"로 일반화 — "한국어" 라벨은 타깃 사용자(한국어 화자) 기준 그대로 유지. 문장변형 체험판(15-1)은 영어 전용 고정 콘텐츠(`VARIATION_SENTENCES`)로 그대로 남겨두기로 결정 — 견본 성격의 부가 화면이라 다른 언어 학습자가 자연스럽게 넘어갈 것으로 판단, 별도 라벨링 불필요. AI 자동변형(15-2)의 생성 프롬프트는 여전히 "English sentence" 등 영어 문법 용어가 박혀 있으나, 사용자가 복사한 뒤 자기 AI 챗에서 직접 언어를 바꿔 요청하는 우회가 가능해 손대지 않음

## 작업 규칙

### 1. 승인 원칙
- "시작" 또는 "go"라는 말이 나오기 전까지는 코드를 읽고 분석만 하고, 실제로 파일을 만들거나 수정하지 않을 것.
- 여기서 "단계"는 1~9단계 같은 큰 번호 순서가 아니라 **하나의 완결된 기능 단위**(예: 헤더, 슬라이더, 더보기 메뉴의 "내보내기" 기능 등)를 뜻함.
  - 기능 하나가 UI(예: 3단계)와 실제 동작 로직(예: 5단계)처럼 서로 다른 번호의 체크리스트에 걸쳐 있을 수 있음. 이런 경우 UI만 먼저 만들고 로직은 나중에 따로 만들지 않고, **기능이 실제로 동작하도록 관련 항목을 한 번에 묶어서** 진행하는 것을 기본으로 함 (재작업 비용을 줄이기 위함).
  - 다만 서로 다른 기능을 한 세션에 여러 개 묶어 진행하는 것은 여전히 금지 (절대 금지 사항 참고).
- 승인은 **세션 전체에 한 번이 아니라, 기능 단위마다 매번** 받을 것.
- 한 기능이 끝나면 자동으로 다음 기능으로 넘어가지 말고, 반드시 새로 "시작"/"go"를 기다릴 것.
- 작업을 시작하기 전, 다음 형식으로 계획을 먼저 설명할 것.
  > "이번 단계에서는 [무엇을] 위해 [어떤 파일들]을 [어떻게] 수정하겠습니다."
  - 하나의 기능이 여러 번호의 체크리스트 항목을 함께 완료시키는 경우, 계획 설명 시 어느 항목들이 함께 체크되는지 명시할 것.

### 2. 작업 기록 규칙 (이전 작업 기억하기)
- 새 세션을 시작할 때 먼저 `PROGRESS.md` 파일이 있는지 확인하고, 있으면 내용을 읽을 것.
- 가장 마지막에 기록된 작업 내용을 요약해서 사용자에게 보고할 것.
- "다음 작업 제안"이 적혀 있으면 함께 보고할 것.
- `PROGRESS.md`가 없으면 "이전 작업 기록이 없습니다"라고 알릴 것.
- `PROGRESS.md` 맨 아래에 기록을 추가할 것 (기존 기록은 지우지 말고 누적할 것).
- 작업 순서는 체크리스트를 확인하여 순서대로 진행하되, 번호 순서는 권장 순서일 뿐 절대 규칙은 아님 — 진행 중인 기능이 다른 번호의 항목과 연결되어 있다면 그 항목까지 함께 진행 가능 (위 "1. 승인 원칙" 참고)
- 진행 완료된 체크리스트에 체크 표시 할 것
- 작업 전 항상 계획을 먼저 설명하고 승인을 받을 것
- 파일 수정 시 변경 내용을 먼저 요약할 것
- 각 작업 완료 후 CLAUDE.md 체크리스트를 반드시 업데이트 할 것
- 체크리스트 갱신 시 최상단 "현재 상황 브리핑"도 함께 갱신할 것
- [x] 처리 시 완료 날짜를 괄호로 표기할 것. 예: - [x] 로그인 UI (2026-04-01)

### 3. 검증 규칙 (기능 구현 후 확인)
- 자동화 테스트(로컬 서버/헤드리스 브라우저 등, `localhost`·`file://` 기준)를 통과했다고 해서 실제 환경에서도 정상 동작한다고 단정하지 말 것.
- 기능 구현 완료 후 확인은 2단계로 나눠 진행할 것.
  1. Claude가 자동화 테스트로 1차 확인
  2. 사용자가 실제 기기/네트워크 환경(폰 브라우저, 필요 시 PC를 LAN IP로 접속 등)에서 2차 확인
- 2차 확인이 끝나기 전까지는 다음 기능 단계로 넘어가지 말 것.

## 절대 금지 사항
- 승인 없이 파일 삭제 금지
- 여러 기능을 한 번에 진행 금지 (단, 하나의 기능이 여러 번호의 체크리스트 항목에 걸쳐 있는 경우는 예외 — 위 "1. 승인 원칙" 참고)
- 체크리스트에 없는 기능 임의 추가 금지
- 라이브러리 / 프레임워크 임의 설치 금지

---

## 체크리스트 (잠정 — 대략의 흐름만, 세부 항목은 진행하며 구체화)

> 아직 확정된 사양이 아니며, 순서와 세부 내용은 진행하면서 조정될 수 있음

- [x] 1. 기본 카드 UI: 화면에 한국어 문장을 보여주고 확인할 수 있는 카드 형태 구성 (2026-08-04)
- [x] 2. 텍스트로 영어 정답을 확인하는 방식 (2026-08-04)
- [x] ~~3. 음성 답변 흐름~~ — **착수 전 제외 확정** (2026-09-11). 마이크 → 음성 인식(Web Speech API) → 정답 비교/발음 채점 기능. 2026-08-17 유료 2단계 후보로 확정, 2026-09-03엔 유료 전환 유도력 1순위로까지 판단했었으나, 앱 성격 재검토 끝에 툭탁의 핵심 메커니즘은 "떠올리기(recall)"이지 실제 발화를 검증하는 구조가 아니라고 판단해 완전히 제외. 상세 배경은 위 Non-goals "스피킹 연습(음성 판정) 기능" 참고. 대신 정답 발음 듣기(TTS)는 별도 아이디어로 브레인스토밍 목록에 등재(착수 여부 미정)
- [x] 4. 엑셀/메모장 등에서 작성한 문장을 파일(CSV/TSV/TXT)로 가져오기 (2026-08-11)
- [x] 5. 글자 크기 설정: 카드 텍스트 크기를 5단계(도트+스테퍼)로 조절, 견본 글자("가")로 실시간 확인 (2026-08-12)
- [x] 6. 화면모드: 시스템/라이트/다크 3단 선택. "시스템" 선택 시 기기 다크모드 설정을 실시간으로 따라감 (2026-08-12 최초 구현, 2026-08-13 3단 확장)
- [x] 7. 문장 목록 보기 + 수정/삭제: 더보기 메뉴 "문장관리" → 전체화면 목록. 항목 탭 시 "문장 추가" 모달을 재사용해 수정 (2026-08-13)
  - 삭제: 롱프레스로 단일 삭제 확인, 상단 "선택" 버튼으로 체크박스 다중 선택 후 일괄 삭제. 최소 1개는 항상 유지 (2026-08-15, 삭제 버튼 위치는 2026-08-27에 하단 고정으로 이동 — 아래 참고)
  - **"최소 1개 유지" 제한 제거 (2026-09-16)**: "전체 선택해서 삭제할 때 '최소 1개의 문장은 있어야 합니다' 제한이 걸린다"는 사용자 문의로 재검토 — 설정 화면 "초기화" 버튼은 이미 확인창만 거치면 0개까지 삭제를 허용하고, 카드 화면·문장관리 목록 둘 다 0개 상태를 위한 빈 화면 안내문이 이미 구현돼 있어 정상 동작하는 것을 확인. 즉 이 제한은 지금은 불필요한 예전 안전장치였음 — "제한 자체를 없애 초기화 버튼과 동일하게 동작시키기(추천)" vs "학습대상·삭제대상 모두 아닌 숨겨진 보호용 기본문장을 신규 도입" 두 방식을 제시해 사용자가 전자를 선택. `js/app.js`의 `confirmDeleteSingle()`(개별 삭제)과 `listBulkDeleteBtn` 클릭 핸들러(전체선택삭제) 두 곳에서 "최소 1개" 차단 블록만 제거(`if (sentences.length <= 1) { alert(...); return; }` / `if (remaining < 1) { alert(...); return; }`) — 다른 로직 변경 없음, 카드 화면 12번(카드에서 바로 삭제)도 `confirmDeleteSingle`을 그대로 재사용하므로 자동으로 함께 적용됨. Claude 1차 확인(Playwright, 스크래치패드 정적 서버+`localStorage` 시드): 개별삭제로 마지막 1개까지 차단 없이 삭제→카드/목록 화면 모두 빈 상태로 정상 전환(이전/확인/다음 버튼 비활성화, 빈 상태 안내 노출) 확인 → 전체선택삭제(선택→전체선택→삭제)로도 0개까지 차단 없이 삭제되는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-16, "확인 완료")**
- [x] 8. 설정 화면 신설: 더보기 메뉴를 문장추가/문장관리/설정 3항목으로 축소. 설정 화면에 글자크기·화면모드 + 데이터 관리(파일가져오기, 백업 내보내기, 초기화) 배치. 하단에 앱 이름·최근 업데이트 날짜 표시 (2026-08-13)
- [x] 9. 출제 순서 / 정렬: 카드 출제 순서 + 문장관리 목록 정렬에 랜덤/최신/오래된/못외운것/중요한것 5가지 기준 적용 (2026-08-16 완료)
  - [x] 9-1. 즐겨찾기(중요 표시): 문장별 `important` 토글, 문장관리 목록 UI (2026-08-15)
  - [x] 9-2. 미암기 표시: 문장별 `unfamiliar` 토글, 문장관리 목록 UI (2026-08-15)
  - [x] 9-3. 정렬 옵션: 설정 화면 세로 라디오 목록으로 5가지 기준 선택, 카드·문장관리 정렬 로직에 반영. 기본값 오래된순. "못외운것/중요한것 먼저"는 해당 문장을 앞으로 우선 정렬(필터 아님). 랜덤순은 재선택 시에만 재섞고 LocalStorage에 저장해 유지 (2026-08-16)
- [x] 10. 카드 화면 마킹 아이콘 + 문장관리 필터: 카드 정답 확인 후 답 아래에 ☆(중요)/⚑(미암기) 아이콘 노출, 문장관리 상단에 전체/중요/미암기 필터 탭 추가(정렬과 독립적, 진입 시 항상 "전체"로 초기화) (2026-08-16)
- [x] 11. 텍스트 붙여넣기로 가져오기: 파일 저장 없이 텍스트를 바로 붙여넣어 여러 문장 추가(ChatGPT 변환 결과 등에 유용). 파일가져오기와 동일한 파서(`parseSentencesText`) 재사용 (2026-08-17)
  - **파일 가져오기/텍스트 붙여넣기 중복 검사 추가** (2026-09-15): 2026-09-14 세션에서 사용자가 같은 파일을 반복해서 "파일 가져오기"로 테스트하다가 발견 — 가져온 문장이 기존 문장과 완전히 같은지 전혀 비교하지 않고 그대로 추가돼, 같은 파일/텍스트를 여러 번 가져오면 완전히 동일한 문장이 계속 중복 생성되는 문제였음. 착수 전 범위 확인: (1) 적용 범위는 파일가져오기+텍스트붙여넣기만(폼 입력은 하나씩 직접 타이핑하는 흐름이라 마찰을 더하지 않기 위해 제외) (2) 판정 기준은 한국어+영어 텍스트 trim 후 완전일치(28번 클라우드 병합 로직과 동일 기준 재사용) (3) 중복 발견 시 자동으로 건너뛰고 결과 메시지에만 안내(확인 후 선택하는 UI는 만들지 않음) — 3가지 모두 사용자 확인 후 확정
  - **`js/app.js`(폰 앱)**: `isDuplicateSentence(kr, en)` 함수 신규(`sentences` 배열과 비교) — 파일 가져오기·텍스트 붙여넣기 핸들러에서 `addSentence()` 호출 전에 검사해 중복이면 건너뜀. 결과 alert에 건너뛴 개수가 있으면 "(이미 있는 문장 N개는 건너뛰었어요)"를 덧붙임. `addSentence()`가 검사 직후 바로 `sentences`에 push하므로, 같은 배치(파일 하나) 안에 중복 줄이 있는 경우도 자연히 걸러짐(별도 처리 불필요)
  - **`js/input.js`(PC 빠른입력)**: `isDuplicateInputSentence(kr, en)` 함수 신규 — "클라우드에 저장된 문장" 관리 목록(`manageRowsEl`, 숨겨진 페이지네이션 행 포함)과 아직 저장 전인 "새 문장 추가" 행(`inputRowsEl`) 둘 다와 비교. `insertParsedRows()`가 중복 행은 건너뛰고 `{ addedCount, skippedCount }`를 반환하도록 변경, 결과 문구는 `buildImportStatusText()`로 통합(둘 다 건너뛰어 추가된 게 0개면 "가져온 문장이 모두 이미 있는 문장이라 추가하지 않았어요"로 별도 안내)
  - Claude 1차 확인(Playwright, 스크래치패드 정적 서버 재사용): 폰 앱 — 문장 2개 붙여넣기(정상 추가) → 같은 텍스트 재붙여넣기(전부 건너뜀, 개수 불변) → 1개는 겹치고 1개는 새 문장인 텍스트(겹치는 것만 건너뜀, 신규만 추가) 순으로 3단계 모두 정확한 alert 문구+실제 저장 개수 확인. `input.html` — `CloudSync.restore()`를 클라우드 문장 1개 반환하도록 모킹 후, (클라우드 문장+새 문장) 혼합 텍스트 붙여넣기 시 클라우드 문장만 건너뛰고 새 문장만 행에 추가되는 것 확인 → 같은 텍스트 재붙여넣기 시 이번엔 둘 다(클라우드+직전에 추가된 새-문장 행) 중복으로 잡혀 전부 건너뛰고 "모두 이미 있는 문장" 문구 노출+행 개수 불변 확인 → 콘솔 에러 없음(의도적으로 차단한 gstatic 네트워크 에러만 발생) → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
- [x] 12. 카드 화면에서 바로 수정/삭제: 마킹 아이콘 바(☆/⚑)에 연필(수정)·휴지통(삭제) 아이콘 추가, 학습 중 오탈자를 바로 고치기 위함. 수정은 문장 추가 모달, 삭제는 기존 확인창·최소 1개 유지 규칙(`confirmDeleteSingle`)을 재사용 (2026-08-17)
- [x] 13. 문장추가 진입점 통합: "문장추가" 모달 상단에 [폼 입력 / 파일 가져오기 / 텍스트 붙여넣기] 3탭 통합, 설정 > 데이터 관리는 백업 내보내기·초기화만 남김. 문장 수정 시에는 탭 숨기고 폼만 노출 (2026-08-17)
- [x] 14. 가로모드 방어적 대응: **착수 자체를 접기로 확정** (2026-08-28). 현재 CSS는 세로 전용(카드 버튼 크기가 `vw` 기준, 카드 화면 3단 grid가 세로 높이 기준)이라 가로 회전 시 레이아웃이 깨질 수 있으나, 이 앱 성격상(짧게 세로로 들고 확인하는 카드 암기 용도) 가로 회전은 대부분 의도치 않은 회전 잠금 해제로 발생해 실사용 가치가 낮다고 판단해 방어 코드조차 넣지 않기로 결정. 2026-08-20 한 차례 최소 보정을 구현(버튼 잘림 문제는 해결됨)했으나 사용자가 실효성이 적다고 판단해 되돌린 전례가 있었고, 이번에 재논의 끝에 "안내 화면으로 막기"안도 기각하고 완전히 손대지 않기로 최종 결정. 재검토 필요 시 PROGRESS.md 2026-08-20/2026-08-28 기록 참고
- [x] 15. 기본문장 100개 자동 제공: 최초 실행 시(저장된 문장이 하나도 없을 때만) 일상 대화 위주 기본문장 100개를 자동으로 채워 넣음(`DEFAULT_SENTENCES`). 문장관리 목록에 "기본" 배지로 표시(`source: 'default'`), 사용자가 해당 문장을 수정하면 그 순간 배지가 사라지고 "내 문장"으로 취급됨. 기존 사용자(이미 문장 저장돼 있음)나 초기화 직후에는 재시딩되지 않음 (2026-08-22)
  - [x] 15-1. 문장변형 체험판(무료, 비용 0 전제) — 2026-08-24 정책 확정: 기존 15개 후보 대신 **10개 문장**으로 축소하되 시제·인칭·수·부정문·의문문까지 깊게 다룸(진행형은 문장마다 자연스러운 경우에만 추가하는 선택 카테고리). 단 문장마다 일률적으로 다 만들지 않고 **그 문장에 실제로 자연스러운 변형만** 선별(예: 인사말류는 의문/부정 변형 자체를 안 만듦, 9번 날씨 문장은 인칭·수를 생략해 시제(4종)·부정문·의문문만) — "모든 문장이 똑같이 변형되는 건 아니다"는 것도 학습 포인트. 더보기 메뉴에 "문장관리" 다음 순서로 "문장변형" 항목 추가(2026-08-25엔 "문장변형 체험"이었으나 2026-08-26에 "체험" 제거 — 아래 설명할 "내 문장으로 추가" 기능이 생기며 단순 데모를 넘어섰다고 판단), 문장관리처럼 목록→탭하면 상세(태그 붙은 변형 목록, 원문과 일치하는 항목은 하늘색으로 강조)로 이동하는 구조. 상세 화면에서 각 변형은 카드 화면과 같은 순서로 동작(한국어 먼저 노출 → 탭하면 영어 정답으로 플립·다시 탭하면 원복, 진도저장·이전/다음 넘기기 없는 가벼운 구현) — 처음엔 완전 열람형으로 구현했으나 "학습 체험이 없으면 의미가 약하다"는 피드백으로 항목별 탭-공개 방식으로 전환. 콘텐츠는 `reference/문장변형_체험판_콘텐츠_초안.md`에 초안 작성 후 `js/app.js`의 `VARIATION_SENTENCES` 상수로 이전(2026-08-25, 문장 10개×총 87개 변형 → 2026-08-26 4·9번에 현재완료 항목 추가로 89개). Claude 1차 확인(Playwright, 목록/상세 전환·탭-공개 동작·9번 문장 카테고리 축소·라이트다크 스크린샷) 완료 — 사용자 2차 확인(실기기)도 완료(2026-08-26, "잘 보여")
    - **내 문장으로 추가** (2026-08-26): "문장변형 중에서 카드학습에 추가하고 싶으면 +버튼으로 추가하게 만들 수 있어?"라는 요청으로, 15-1이 "사용자 문장과 무관한 읽기 전용 고정 콘텐츠"였던 기존 원칙에서 한 발 나아가 각 변형 항목에 `circle-plus` 버튼을 추가. 탭하면 기존 `addSentence(kr, en)`을 그대로 재사용해 그 변형 문장을 실제 내 문장 목록(카드 학습 덱)에 추가하고, 아이콘이 체크(`check`)로 바뀌며 즐겨찾기와 같은 accent-star 색(호박색)으로 강조. 렌더링 시점에 `sentences` 배열에 이미 같은 kr/en이 있는지 검사해(기본문장 100개와 겹치는 경우 포함) 있으면 처음부터 체크 상태로 시작 — 중복 추가 방지. 유료판의 AI 기반 개인화 문장 변형(15-2)과는 별개 — "무료판에서 이 체험판 변형을 추가하는 것"과 "유료판에서 AI로 내 문장을 변형하는 것"의 관계는 사용자가 "좀 더 생각해봐야 할 것 같다"고 보류, 15-2 정책은 미변경. Claude 1차 확인(Playwright, 추가 전/후 아이콘 전환·기존 문장과 중복 감지·플립 애니메이션 방해 없음·문장관리 개수 증가 확인) 완료 — 사용자 2차 확인(실기기)도 완료(2026-08-26, "잘 보여")
  - [x] 15-2. 개인화 문장(사용자가 직접 입력한 문장) 자동 변형 (무료판 완료, 2026-08-27) — 2026-08-24 방향 확정: **무료판 = "안내형"** — 앱이 변형 요청 프롬프트(위 12시제표 내용 포함 가능)를 자동 생성해 복사 제공 → 사용자가 본인의 무료 AI 챗에 붙여넣어 결과를 받음 → 기존 "텍스트 붙여넣기" 가져오기로 앱에 다시 붙여넣음(추가 전 미리보기 단계 신설 필요) → 방금 생성된 변형들만 모아 기존 카드 컴포넌트로 즉석 미니 학습 가능. **유료판 = 버튼 한 번으로 API 자동생성**(2단계 크레딧 상품 후보 — "능력"이 아니라 "번거로움 제거"를 판매, 착수 전). 앱 비용은 무료판 기준 0원.
    - **2026-08-27 무료판 구현 완료(Claude 1차 확인 + 사용자 2차 확인 모두 완료, "잘돼") — 최초 구현 후 진입 구조 재설계**: 처음엔 더보기 메뉴에 "AI로 변형하기" 신규 항목 + 전용 "원문 선택" 화면으로 구현했으나, 사용자가 "어차피 문장관리에 문장 목록이 있는데, 거기서 문장마다 중요/미암기/AI 메뉴를 주고 선택하게 하면 어떤가"라고 제안 → Claude가 장단점 검토(전용 선택 화면이 문장관리 목록과 기능적으로 중복된다는 점이 핵심 근거) 후 방향 변경에 동의, 사용자 승인으로 재설계. **최종 구조**:
      1. **진입**(재설계): 더보기 메뉴 항목 없음 — 문장관리(`#list-screen`) 각 항목의 별(⭐)·깃발(⚑) 옆에 구분선(`.sentence-list-actions-divider`) + AI 아이콘(`sparkles`, `.sentence-ai-btn`)을 추가, 탭하면 그 문장으로 바로 프롬프트 화면 진입(`openAiVariationPromptFor()`). 별/깃발은 즉시 토글(화면 이동 없음)인데 AI 아이콘은 새 화면으로 이동하는 무거운 동작이라 모양을 확실히 다르게 하고 구분선으로 시각적으로 분리, 선택 모드(다중삭제)에서는 별/깃발과 함께 자동으로 숨겨짐(`if (!selecting)` 블록 안에서 함께 렌더링하므로 별도 처리 불필요). 아이콘 3개(+구분선)가 한 줄에 들어가도록 `.sentence-star-btn`/`.sentence-unfamiliar-btn`/`.sentence-ai-btn` 크기를 32px→28px로 축소(15-1의 `.variation-item-add-btn`과 동일 크기 규칙 재사용), 화면 여백은 그대로 유지
      2. **프롬프트 생성+복사**(`#ai-variation-prompt-screen`): 시제(현재/과거/미래/현재완료)+인칭(2·3인칭)+수(복수)+부정문+의문문 5종 요청 프롬프트를 자동 생성(15-1과 동일한 카테고리 재사용, 커스텀 선택 UI는 없음). "문장 의미상 부자연스러운 조합은 만들지 말라"는 지시를 명시적으로 포함(역할부여는 하지 않기로 결정 — 이 과제엔 도움될 가능성이 높다는 리서치 결과가 있었으나 사용자가 최종적으로 생략 선택). 결과 형식은 15-1 도움말의 "사진으로 찍은 손글씨 문장을 코드블록+탭 구분으로 정리해달라"는 기존 패턴을 그대로 재사용(코드블록 안에 "한국어 문장\t영어 문장" 한 줄씩). `navigator.clipboard.writeText()`로 복사, 실패 시 텍스트 선택 후 안내. 뒤로가기는 문장관리로 복귀
      3. **결과 붙여넣기+파싱**: 같은 화면 하단에 붙여넣기 textarea, 기존 `parseSentencesText()`(문장추가의 텍스트 붙여넣기 탭과 동일 파서) 그대로 재사용
      4. **미리보기+선택 추가**(`#ai-variation-preview-screen`): 파싱된 항목을 체크박스 목록(한/영 동시 노출, 기본 전체 선택, "전체 선택/해제" 토글)으로 표시 — 15-1의 "탭해서 플립" 방식과 달리 이 화면은 AI 결과를 **검수**하는 목적이라 한/영을 동시에 보여주는 쪽으로 설계(플립은 이미 아는 내용을 학습 체험하는 15-1에만 맞는 인터랙션이라고 판단)
      5. **추가+미니 학습**: 선택 항목만 기존 `addSentence()`로 실제 내 문장 목록에 추가한 뒤, 그 항목들만 카드 화면에서 즉석 학습 — 새 화면을 만들지 않고 기존 카드 화면을 임시 필터링(`miniSessionActive`/`miniSessionIds`, 카드 전용 `getCardOrderedSentences()` 래퍼로 문장관리 등 다른 화면에는 영향 없음). 카드 화면 상단에 "미니 학습 중 · N개 · 종료" 배너(`#mini-session-banner`) 노출, "종료" 클릭 시 전체 덱으로 복귀. localStorage에 저장하지 않는 즉석 상태라 앱을 껐다 켜면 항상 꺼져 있음
      - 15-1의 "내 문장으로 추가" 버튼(플립+체크 전환)과는 재사용 범위를 다르게 가져감 — 논의 결과 개별 항목의 "추가 버튼" 개념만 공유하고, 목록 자체는 문장관리 "선택 모드"(체크박스 다중선택) 쪽 패턴으로 새로 구현하기로 결정(플립 방식은 검수 목적에 안 맞음)
      - Claude 1차 확인(Playwright, 서버 없는 정적 앱이라 스크래치패드에 임시 static 서버+`playwright` 패키지 설치해 구동, 재설계 후 재검증): 더보기 메뉴에 AI 항목이 사라졌는지 → 문장관리 항목마다 아이콘 3개+구분선 1개, 버튼 폭 28px 확인 → AI 아이콘 탭 시 그 문장이 정확히 프롬프트에 반영된 채 진입 → 뒤로가기 시 문장관리로 복귀 → 별 아이콘은 여전히 화면 전환 없이 즉시 토글되는지 → 전체 플로우(붙여넣기→미리보기→추가→미니 학습 배너) 재확인 → 선택 모드에서 AI 아이콘이 별/깃발과 함께 숨겨지는지 확인. 라이트/다크 모두 스크린샷으로 아이콘 3개가 여유 있게 들어가는 것 확인, 콘솔 에러 없음. 사용자 2차 확인(실기기)도 완료(2026-08-27, "잘돼")
  - [x] 15-3. 기본문장 관리(숨기기/삭제): 설정 화면에 "기본문장 관리" 섹션 신설. 토글로 기본문장을 카드 학습·문장관리 목록 양쪽에서 한번에 숨길 수 있음(설정값이라 앱을 껐다 켜도 유지), "기본문장 삭제" 버튼으로 기본문장만 일괄 삭제(확인창 필수). 개인 문장이 하나도 없어 삭제 시 0개가 되는 경우, 기존 "최소 1개 유지" 규칙처럼 삭제를 막는 대신 안내용 기본문장 1개("문장이 없습니다." / "There are no sentences.", `source: 'default'`)를 자동으로 남겨 항상 삭제가 성립하도록 함(2026-08-23 수정) (2026-08-22)
- [x] 16. 저장 공간 표시: 설정 > 데이터 관리 섹션 맨 위에 저장된 문장 개수 + 진행률 막대바 + 사용 용량(예: "약 19.3KB / 5MB 사용") 안내 추가. 문장 데이터를 `Blob` 크기로 계산, 설정 화면에 진입할 때마다 다시 계산해 표시. 실사용량이 5MB 한도 대비 항상 1% 미만이라 막대바가 안 보이는 것처럼 느껴질 수 있어 최소 시각적 폭(3%)을 보정 (2026-08-23)
- [x] 17. 로컬 백업 리마인더: 앱 삭제 시점을 직접 감지할 방법이 없어(PWA/WebAPK 삭제를 알려주는 API 자체가 없음) "삭제되기 직전"이 아니라 "평소 사용 중"에 미리 백업을 권하는 방식으로 구현. 카드 화면 상단(상단바 아래, 미니 학습 배너 위)에 배너(`#backup-reminder-banner`) — (마지막 백업 또는 배너 닫기 시점 이후 경과일 ≥ 7일) **OR** (그 시점 이후 새로 추가된 "내 문장"(기본문장 제외) 수 ≥ 100개, 최초 10개로 논의했으나 "너무 자주 뜨면 귀찮다"는 이유로 100개 확정 — 실기기 2차 확인 동안은 `js/app.js`의 `BACKUP_REMINDER_SENTENCE_COUNT`를 3으로 임시 하향해뒀다가 확인 완료 후 100으로 원복) 조건을 만족하면 노출, 내 문장이 하나도 없으면 절대 노출 안 함. "지금 백업하기"(기존 백업 내보내기 로직 `exportBackup()`으로 분리해 설정 화면 버튼과 공유) 또는 우상단 X(닫기) 둘 다 누르면 체크포인트(`localStorage`의 `tuktak_backup_checkpoint`, `{at, count}`)가 현재 시각·문장수로 리셋되어 다음에 조건이 다시 차야 재노출. 기존 사용자가 업데이트를 받는 순간 갑자기 뜨지 않도록, 체크포인트가 없을 때는 최초 판정 시점(=내 문장이 처음 생기는 시점)을 기준점으로 저장만 하고 그 순간엔 띄우지 않음. Claude 1차 확인(Playwright, 서버 없는 정적 앱이라 스크래치패드에 임시 static 서버+`playwright` 패키지로 구동): 내 문장 0개일 때 항상 숨김 → 내 문장 추가 시 체크포인트 지연 생성 → 경과일 조건·개수 조건 각각 단독으로 노출 트리거 → 닫기(X)·백업 버튼 둘 다 체크포인트 리셋+배너 숨김 → 설정 화면 백업 버튼도 동일하게 체크포인트 갱신 → 콘솔 에러 없음, 라이트/다크 스크린샷으로 배너 가독성 확인 (2026-08-28)
- [x] 18. 클라우드 백업/동기화 (수익화 1단계, 2026-08-28 착수 → 2026-08-29 완료): Google 로그인 + Firestore로 문장 데이터를 클라우드에 백업/복원. 방향은 Claude 메모리(`project_monetization_strategy`)에서 이미 확정: 로그인은 Google(Firebase Auth), 동기화는 수동 "지금 백업"/"클라우드에서 복원" 버튼(실시간 동기화 아님, 기존 TSV 내보내기/가져오기와 같은 멘탈모델), 결제 게이팅은 나중 단계로 분리(우선 로그인+백업 기능만 무료 공개). "지금 백업"=Firestore 덮어쓰기 저장, "클라우드에서 복원"=확인창 후 로컬 문장 전체를 클라우드 백업본으로 교체(파일가져오기의 append와 다르게 overwrite). 기술스택: Firebase Authentication+Firestore(무료 Spark 등급) + 결제 웹훅은 Cloud Functions 대신 기존 Vercel 서버리스 함수+Firebase Admin SDK(Blaze 강제전환 회피). 클라우드 동기화 도입 시점부터 개인정보처리방침 고지 필요(데이터가 기기 밖 구글 서버로 나가게 됨). 진행 단계: (1) Firebase 콘솔에서 프로젝트 생성 **완료(2026-08-29, 프로젝트 ID `tuk-tak-2c172`)** → (2)(3) 앱에 Firebase SDK 연동+로그인 UI+백업/복원 버튼+Firestore 연동 **완료(2026-08-29)** → (4) Firestore 보안 규칙(로그인한 본인 uid 문서만 읽기/쓰기 — `allow read, write: if request.auth != null && request.auth.uid == uid;`) **콘솔에 적용 완료(2026-08-29)**. Claude 1차 확인(Playwright, UI 전환·복원 로직·콘솔 에러 없음)+사용자 실기기 2차 확인(실제 Google 로그인+백업+복원 둘 다 성공) 모두 완료(2026-08-29) → (5) 개인정보처리방침 고지: 설정 화면 "클라우드 백업" 섹션 안내 문구 보강(수집 정보·저장 위치 명시) + 도움말 화면에 "개인정보처리방침" 아코디언 항목 신규(수집 정보/저장 위치/이용 목적/제3자 제공/보관 기간/문의처 `chloepark.labs@gmail.com`), 코드 구현+Claude 1차 확인+사용자 실기기 2차 확인 모두 완료(2026-08-29, "둘다 잘 보여") — **진행 단계 1~5 전부 완료**. 각 단계는 프로젝트 규칙에 따라 별도로 "시작"/"go" 승인 필요
  - **클라우드 동기화 보완 3종 착수** (2026-09-12, `input.html`로 PC 입력을 쓰다 보니 "폰 미백업 상태에서 복원하면 최근 문장이 날아갈 수 있다"는 위험이 재확인돼 순서대로 보완 중): (1) **경고 문구 보완 → 같은 날 재설계 후 완료** — 처음엔 `input.html` 저장 후 안내 문구에도 상세 경고를 넣었으나, 사용자가 "복원 버튼은 폰에 있는데 웹에서 설명하면 더 헷갈린다"고 지적 → `input.html` 문구는 원래 짧은 문구로 되돌리고, **경고는 실제 위험한 동작이 일어나는 폰의 "클라우드에서 복원" 버튼 쪽에 집중**: (a) 클릭 시 `confirm()` 문구에 "최근 추가/수정한 문장이 있다면 먼저 지금 백업"을 추천하는 경고 유지, (b) **버튼 자체를 다른 설정 버튼과 다른 색(즐겨찾기와 같은 accent-star 호박색, `.settings-action-btn-warning`)으로 구분**해 누르기 전에도 "되돌릴 수 없는 동작"임을 암시(2026-09-12 신규). 추가로 "클라우드라는 용어를 모두 잘 아는 건 아니다"는 지적으로 **"지금 백업"/"클라우드에서 복원" 버튼 옆에 `circle-help` 아이콘의 `?` 설명 버튼**(`.settings-info-btn`, `.settings-action-row`로 나란히 배치) 신규 — 탭하면 `alert()`로 쉬운 말 설명 노출. 앞으로 추가될 병합/삭제 버튼도 이 `?` 버튼 패턴을 그대로 따를 예정. Claude 1차 확인(Playwright, 문구 원복 확인+`?` 버튼 2개의 alert 내용 확인+복원 버튼 테두리색이 정확히 accent-star(`#BA7517`)인지 RGB로 검증+백업 버튼은 그대로 브랜드 블루인지 확인, 라이트 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-12)**. (2) **클라우드와 병합 버튼(B안, 2026-09-13 구현 완료, Claude 1차 확인까지 끝남)**: 복원을 "완전 교체"로 유지한 채 **비파괴적 병합 방식의 "클라우드와 병합" 버튼을 별도로 추가**(A안: 기존 복원 자체를 병합으로 대체는 채택 안 함). 위험도 오름차순으로 지금 백업 → **클라우드와 병합**(신규) → 클라우드에서 복원 → 클라우드 백업 삭제 → 로그아웃 순 배치, 데이터를 지우지 않는 동작이라 경고색 대신 "지금 백업"과 같은 기본(브랜드 블루) 스타일 + lucide `merge` 아이콘 신규 적용. 로직(`js/app.js`): `CloudSync.restore()`로 클라우드 문장을 가져와 로컬 `sentences`와 한국어+영어 텍스트(공백 trim 후) 정확히 일치하는 항목을 찾아 **중요/미암기 표시만 OR로 합치고**(중복 추가 안 함), 일치하지 않는 클라우드 문장은 새 문장으로 로컬에 추가 → 병합 결과를 다시 `CloudSync.backup()`으로 클라우드에 올려 로컬↔클라우드를 같은 상태로 맞춤. 클라우드에 백업이 없으면(빈 배열/null) "병합할 내용이 없다"고 안내하고 중단. 되돌릴 수 없는 삭제가 없어 confirm 없이 바로 실행(백업 버튼과 동일 취급), 완료 후 "새 문장 N개 추가, 표시 갱신 M개" alert로 결과 안내. `?` 버튼은 "이 기기 문장은 지워지지 않고 클라우드에만 있던 문장이 추가되며 표시도 합쳐진다"는 설명. Claude 1차 확인(Playwright, `CloudSync.restore`를 매칭 1건+신규 1건을 반환하도록 모킹): 버튼 순서·병합 버튼이 경고색이 아닌 브랜드 블루인 것 확인 → `?` alert 문구 확인 → 병합 실행 후 로컬 저장소가 정확히 3개(기존 유지 1+매칭 병합 1+신규 추가 1)이고 매칭 문장의 important/unfamiliar가 둘 다 true로 합쳐진 것, 무관한 기존 문장은 그대로인 것 확인 → 병합 후 `CloudSync.backup()`이 병합된 3개 문장으로 재호출된 것 확인 → 클라우드 백업이 빈 배열일 때 안내 문구 확인 → 콘솔 에러 없음, 라이트 스크린샷 확인 → **사용자 실기기 2차 확인 완료(2026-09-13)**. (3) **클라우드 백업 삭제 버튼 — 2026-09-13 순서 재조정(3번을 1번으로 앞당김, 구현이 가장 단순해 먼저 처리) 후 구현 완료**: `js/firebase-init.js`에 `deleteDoc` import + `deleteBackup()` 함수 신규(로그인한 유저의 `users/{uid}` Firestore 문서를 통째로 삭제) 추가, `window.CloudSync.deleteBackup`으로 노출. `index.html`의 "클라우드에서 복원"과 "로그아웃" 사이에 `#cloud-delete-btn`(기존 `.settings-action-btn-warning` 재사용 — 복원 버튼과 같은 호박색) + `#cloud-delete-info-btn`(`?` 버튼 패턴) 신규. 클릭 시 `confirm()`(로컬 초기화·클라우드 복원과 같은 톤)으로 재확인 후 삭제, 로컬 문장에는 영향 없음을 `?` alert로 설명 — Google Play "계정 및 데이터 자기서비스 삭제" 정책 대비 목적도 있음. Claude 1차 확인(Playwright, gstatic 차단+`CloudSync` 모킹으로 로그인 시뮬레이션: 버튼 순서(백업→복원→**삭제**→로그아웃), 삭제 버튼 테두리색이 정확히 accent-star `#BA7517`(RGB로 검증)인지, `?` alert 문구, confirm 취소 시 미호출/수락 시 1회 호출+성공 alert, 실패 케이스 에러 alert, 콘솔 에러 없음, 라이트 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-13)**. **클라우드 동기화 안전장치 3종(경고 문구/병합/삭제) 전부 완료** — 다음은 PC 빠른입력 점검 문서 재검토+19번 유료 범위 확정. **2026-09-14, 이 (2)번 "클라우드와 병합" 버튼은 28번(동시편집 데이터 유실 방지) 구현으로 "지금 백업"에 흡수되며 삭제됨** — 저장이 항상 병합을 기본으로 하게 되어 별도 버튼이 중복이 됐기 때문, 병합 로직 자체는 그대로 재사용(아래 체크리스트 28번 참고)
  - **코드 구현 상세(2026-08-29)**: `js/firebase-init.js`(신규, `type="module"`) — Firebase JS SDK 12.18.0을 CDN(`gstatic.com/firebasejs`)에서 모듈로 불러와 초기화, Google 로그인/로그아웃/Firestore 백업(`users/{uid}` 문서에 `sentences` 배열 덮어쓰기)/복원 함수를 `window.CloudSync`로 노출. `js/app.js`는 기존처럼 일반 스크립트(모듈 아님)로 유지 — 모듈 스크립트가 항상 파싱 완료 후(일반 스크립트보다 늦게) 실행되는 점 때문에, app.js는 `cloudsync-ready` 커스텀 이벤트를 기다렸다가 `CloudSync.onAuthChange()`를 등록하는 방식으로 순서 문제 해결. 설정 화면에 "클라우드 백업" 섹션 신규 — 로그아웃 상태(`#cloud-signed-out-view`)면 Google 로그인 버튼(공식 멀티컬러 G 로고), 로그인 상태(`#cloud-signed-in-view`)면 로그인된 이메일 표시 + 지금 백업/클라우드에서 복원(기존 "초기화"와 같은 `confirm()` 패턴 재사용)/로그아웃 3개 버튼. firebaseConfig 값(apiKey 등)은 클라이언트에 공개돼도 되는 값이라 코드에 직접 포함(실제 보안은 Firestore 규칙이 담당)
- [ ] 19. PC 빠른입력 (신규 검토 대상, 2026-08-29 착수 시작 — 결제 게이팅 없는 사용자 본인 검증용 프로토타입): 유료화 논의 중 "PC 키보드로 여러 문장을 빠르게 입력하고 싶다"는 아이디어가 나왔고, 실제로 Playwright로 데스크톱 폭(1600px)을 확인해본 결과 반응형 CSS가 전혀 없어 문장추가 화면 등이 화면 끝까지 늘어나 깨져 보이는 버그를 발견 — 이 버그 수정(무료, `.screen`에 `@media (min-width: 640px)` 적용)과 "PC 전용 빠른 입력 UI"(유료 후보)를 분리하기로 함. 이 아이디어를 두 가지 방식으로 구현함:
  - **19-1. 앱 내 탭 방식** (2026-08-29 최초 구현 → **2026-09-08 제거**): 문장추가 화면에 4번째 탭 "빠른입력" 추가, 한국어/영어 `<input>`이 한 행씩 반복되는 표 형태(엑셀 느낌), 마지막 행에 입력을 시작하면 새 빈 행이 자동 추가(행 추가 버튼 없이 계속 이어 입력 가능), 하단 "모두 추가" 버튼으로 채워진 행 전체를 기존 `addSentence()`로 일괄 추가. 클라우드 동기화는 새로 만들지 않고 기존 로그인+"지금 백업"/"클라우드에서 복원" 버튼을 그대로 재사용(안내 문구로 사용법만 설명) — **2026-09-08, 사용자가 "이 기능은 PC 웹사이트(19-2)에서 입력한 내용을 앱으로 가져오는 것을 말하는 거니까 필요없다"고 판단해 탭 자체를 제거**. PC 빠른입력의 실제 구현체는 아래 19-2(독립 페이지 `input.html`) 하나로 정리됨. `index.html`/`js/app.js`/`css/style.css`에서 관련 마크업·함수(`createQuickInputRow`/`resetQuickInputRows`)·스타일(`.quick-input-*`) 전부 삭제
  - **19-2. 독립 랜딩페이지 방식** (2026-08-29, 사용자가 "앱 화면과 통일시킬 필요 없다"며 카드 앱과 완전히 분리된 별도 웹페이지를 요청 — 폰 앱에서 이 페이지로의 링크는 만들지 않음): 신규 `input.html`(+`css/input.css`, `js/input.js`) — `tuk-tak-six.vercel.app/input.html`로 별도 접속, 카드 앱의 `.screen` 모바일 폭 제약 전혀 없이 랜딩페이지/대시보드 스타일(가운데 정렬된 넓은 패널)로 독립 디자인. `js/firebase-init.js`의 `window.CloudSync`를 그대로 재사용해 Google 로그인 구현(같은 파이어베이스 프로젝트·같은 도메인이라 앱 쪽에서 이미 로그인했다면 이 페이지에서도 인증 상태 공유됨). 19-1과 같은 "행 자동 추가" 입력 방식을 이 페이지 전용으로 다시 구현(스타일은 완전히 독립, 로직만 같은 패턴). **저장 시 기존 클라우드 백업을 먼저 `CloudSync.restore()`로 불러온 뒤 새로 입력한 문장을 뒤에 합쳐서(append) `CloudSync.backup()`으로 다시 저장** — 그냥 덮어쓰면 기존 클라우드 데이터가 날아가므로, 19-1/기존 "클라우드에서 복원"의 덮어쓰기 방식과 달리 이 페이지는 병합 방식으로 데이터 유실을 방지함. 저장 후에도 폰에는 자동 반영되지 않아 폰에서 "클라우드에서 복원"을 눌러야 함을 안내 문구로 명시
  - 결제 게이팅은 두 방식 모두 이번 단계에 없음, 사용자 본인이 먼저 써보고 실제로 유용한지 판단하기 위한 프로토타입 단계. Claude 1차 확인 완료(2026-08-29, Playwright — 19-1: 자동 행 추가·행 삭제(마지막 1행은 비우기)·실제 제출→문장관리 반영·데스크톱/모바일/다크모드. 19-2: 로그아웃/로그인 뷰 전환·행 자동 추가·저장 시 기존 클라우드 데이터에 병합되는지(모킹 검증)·라이트/다크 스크린샷. 둘 다 콘솔 에러 없음) → **사용자 실사용 확인 완료(2026-08-29, 19-2 실제 Google 로그인+입력+저장 플로우 "기능은 성공")** — 개선 작업(세부 UX 다듬기 등)은 다음 세션으로 보류, 프로토타입 단계는 이걸로 마무리. 유료화 여부(가격·결제 검증·환불 정책 등 리스크)는 `reference/docs/유료화_결제.docx`의 "PC 입력 + 자동 동기화" 유료 상품안 섹션 참고 — **2026-09-03, 20·21번이 모두 제외되며 19번이 1단계 유료 번들(18번과 함께)에 최종 편입 확정**됨. 결제 게이팅은 아직 미착수(번들 출시 시점에 적용 예정), 그 전까지는 계속 무료로 공개
  - **19-2 키보드 입력 흐름 개선** (2026-09-11 계획 확정 → 2026-09-12 "시작" 승인으로 구현 완료): `input.html`이 마우스로 칸을 하나씩 클릭해야 해 "빠른입력"이란 이름과 달리 키보드만으로 죽 이어 칠 수 없다는 문제 제기로 3가지 개선 — `js/input.js`만 수정
    1. **Enter 키 자동 이동**: `createInputRow()`의 한국어 입력칸 `keydown`에서 Enter 시 같은 행 영어 칸으로 `focus()`. 영어 칸 `keydown`에서 Enter 시 `row.nextElementSibling`이 있으면 그 행 한국어 칸으로, 없으면(값을 안 채운 채 Enter만 누른 경우 등 아직 자동 새 행이 안 생긴 상태) `createInputRow()`로 새 행을 만들어 그 행 한국어 칸으로 포커스 이동 — 항상 다음 줄로 이어지도록 방어
    2. **Tab 순서 보정**: `deleteBtn.tabIndex = -1`로 삭제 버튼을 Tab 순서에서 제외(클릭/터치는 그대로 동작)
    3. **자동 포커스**: `renderAuthView(user)`에 모듈 스코프 변수 `wasLoggedIn`을 도입해 **로그아웃→로그인으로 전환되는 순간에만** 첫 번째 행 한국어 입력칸에 `focus()` — auth 상태가 반복 갱신될 때마다 타이핑 중인 포커스를 뺏어가지 않도록 전환 시점에만 실행
    - Claude 1차 확인(Playwright, 스크래치패드 임시 static 서버+이전 세션에 캐시된 Playwright 재사용): `input.html`은 실제 Google 로그인 팝업이 필요해 헤드리스로 로그인 불가 → `context.route(/gstatic\.com/, abort)`로 실제 Firebase SDK 로딩을 차단하고 `page.addInitScript()`로 `window.CloudSync`를 모킹(`onAuthChange` 콜백을 저장해뒀다가 테스트에서 직접 호출)해 로그인 상태 시뮬레이션 — 최초 로그인 시 자동 포커스, 로그인 상태 반복 갱신 시 포커스 유지(안 뺏김), 재로그인 시 다시 자동 포커스, 한국어→Enter→영어 이동, 영어 입력 후(자동 새 행 생성 확인) Enter→다음 행 한국어 이동, 값 없이 Enter 눌러도 새 행 생성+포커스 이동(방어 로직), 삭제 버튼 `tabIndex===-1`+실제 Tab 키로 건너뛰는 것까지 총 11개 항목 모두 통과, 콘솔 에러 없음(의도적으로 차단한 gstatic 네트워크 에러만 발생) — **사용자 실기기 2차 확인 대기 중**
- [x] ~~20. 통계/복습 리포트~~ — **착수 후 제외 확정** (2026-09-01). 진도·정답률·연속학습일수(풀세트)를 코드 구현+Claude 1차 확인(Playwright)까지 마쳤으나, 사용자가 앱 성격(자기 목적이 뚜렷한 학습자용)과 스트릭·정답률 같은 게이미피케이션 장치가 맞지 않고 다른 암기 앱을 어설프게 따라하는 느낌이라고 판단해 전면 제외 결정. 코드는 git revert로 완전히 되돌림(`index.html`/`css/style.css`/`js/app.js` 모두 착수 전 상태로 복원). 상세 배경은 위 Non-goals "통계/복습 리포트" 항목, PROGRESS.md 2026-09-01 참고
- [x] ~~21. 테마/폰트 커스터마이징~~ — **착수 전 제외 확정** (2026-09-03). 1단계 유료 번들 후보로 등재만 돼 있던 상태(사양도 "글씨체/글씨굵기 정도"로 미확정)에서, 20번 제외 이후 구매 유인을 다시 검토하며 이 앱 성격(짧게 쓰는 기능형 앱)엔 꾸미기형 커스터마이징이 약한 유인이라고 판단해 착수 자체를 접음. 상세 배경은 위 Non-goals "테마/폰트 커스터마이징" 항목, PROGRESS.md 2026-09-03 참고
- [x] 22. 문장관리 검색 기능 (2026-08-30 등록 → 2026-09-01 착수·완료, 무료/유료 여부 아직 미정): 문장관리 목록에서 한국어/영어 텍스트 부분일치(대소문자 무관)로 문장을 찾는 기능
  - **진입**: 상단바(`.screen-topbar`)에 돋보기 아이콘(`search`, `#list-search-btn`) 신규 — "선택" 버튼 왼쪽에 배치. 탭하면 필터가 자동으로 "전체"로 리셋되며 필터탭(`#list-filter-bar`)이 숨겨짐(선택된 탭이 아니라 전체 문장 대상 검색이라는 사용자 결정, 2026-09-01 대화로 확정) — 검색 결과 항목엔 기존 ☆⚑ 아이콘이 그대로 남아있어 어떤 탭에 속하는지는 계속 구분 가능
  - **폼 박스** (최초엔 배경 없는 텍스트만 타이틀 자리에 얹는 방식이었으나 "검색칸이 안 보인다"는 피드백으로 같은 날 수정, 높이는 이어서 34px→40px로 확대 요청): 타이틀 자리가 `#list-search-box`(하늘색 `--color-chip-answer` 배경의 둥근 박스, 일반 플렉스 흐름 `flex:1`, `height:40px` 명시)로 바뀌고 그 안에 돋보기 아이콘+입력창(`#list-search-input`)+X 취소 버튼(`#list-search-clear-btn`)이 나란히 배치됨. 위 "문장관리 필터 탭" 항목에 스타일 상세 기록
  - **취소**: 최초엔 선택모드처럼 뒤로가기가 "취소" 텍스트로 바뀌는 패턴이었으나, "취소가 폼안에 x버튼으로 넣으면 어때" 요청으로 X 버튼이 취소를 전담하도록 변경 — 뒤로가기 화살표는 검색 중에도 평소처럼 문장관리 화면 자체를 벗어남(카드 화면으로 이동), X 탭 시 `exitSearchMode()`로 검색만 취소하고 화면엔 남음
  - **검색 로직**: `getFilteredSentences()`에 필터 조건과 별개로 검색어 조건을 추가(정렬→필터→검색 순으로 체이닝, 정렬·필터가 독립 축이었던 기존 구조에 검색을 한 축 더 추가한 것). 입력마다(`input` 이벤트) 실시간 필터링, 결과 0개면 "검색 결과가 없습니다." 안내
  - **상호배타**: 검색모드와 선택모드는 동시에 진입 불가 — 검색 중엔 "선택" 아이콘을, 선택 중엔 검색 아이콘을 서로 숨겨서 UI로 강제
  - **"선택" 버튼도 아이콘으로 통일** (2026-09-01): "검색이 아이콘이고 선택은 버튼이니 뭐가 잘 안 맞아 보여"라는 피드백으로, 텍스트 필(pill) 버튼이던 "선택"을 `trash-2` 아이콘 버튼으로 교체(다른 아이콘 후보로 `square-check`/`list-checks`도 검토했으나, 사용자가 "휴지통이 직관적이고 눌러도 바로 삭제되는 게 아니니 불만은 없을 것"이라 판단해 최종 선택). 탭해도 즉시 삭제되지 않고 체크박스 목록+비활성화된 하단 "삭제" 버튼으로 전환되는 기존 흐름 자체가 "아직 안 지워짐"을 보여주므로 별도 안내 UI는 추가하지 않음
  - 구현 중 발견한 버그: 검색 아이콘과 "선택" 버튼 둘 다 `margin-left:auto`를 쓰면 flexbox가 남는 공간을 두 요소에 절반씩 나눠줘서 그룹이 오른쪽 끝이 아니라 중간에 붕 뜨는 문제 발견 → `margin-left:auto`를 별도 클래스(`.screen-topbar-actions-lead`)로 분리해 우측 아이콘 그룹의 맨 앞(검색 아이콘)에만 붙이고, 뒤따르는 아이콘(선택)은 `gap`만으로 붙게 정리 — "선택"이 아이콘으로 바뀌며 같은 함정이 재발할 뻔했던 것을 사전에 방지
  - Claude 1차 확인(Playwright, 2026-09-01, 폼 박스 재조정 후 재검증): 검색 진입 시 필터 리셋+필터탭 숨김+폼 박스 전환 확인 → 한국어("아침")·영어 대문자("MORNING") 검색 모두 부분일치로 매칭 확인 → 검색 결과에 즐겨찾기 별 아이콘 유지 확인 → 결과 없음 안내 문구 확인 → 폼 안 X 탭 시 정상 복귀 확인 → 검색 중 뒤로가기(<) 탭 시 카드 화면으로 바로 이동하는 것(X와 역할이 분리됐는지) 확인 → 선택모드 진입 시 검색 아이콘 숨김(상호배타) 확인 → 360px 좁은 화면·라이트/다크 테마에서도 폼 박스가 겹치지 않고 잘 보이는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-01)로 22번 전체 완료 처리**

- [x] 23. 필터 학습 — 중요/미암기 필터로 걸러진 문장만 학습하기 (2026-09-06 논의·착수·완료): "문장관리에서 중요/미암기로 필터한 것만 공부할 수 있게 해달라"는 요청. 폴더/태그 아이디어와 달리 이미 있는 필터(10번)·마킹(9-1/9-2)·미니 학습(15-2) 인프라를 그대로 조합하는 것이라 범위가 작아 바로 착수. 새 학습 화면을 만들지 않고 **기존 카드 학습 화면을 그대로 재사용**(사용자가 착수 전 "메인 카드 화면과 똑같은 화면이 맞는지" 먼저 확인 후 승인)
  - **진입**: 문장관리 필터탭에서 "중요" 또는 "미암기"를 선택하면 필터 개수 안내(`#list-count-info`, 예: "중요 5개") 아래에 브랜드 블루 알약형 버튼 `#list-filter-study-btn`("이 조건으로 학습하기")이 노출. "전체" 필터에서는 평소 카드 학습과 다를 게 없어 숨김, 필터 결과가 0개일 때도 숨김
  - **동작**: 탭하면 15-2에서 만든 `startMiniSession(ids)`을 그대로 재사용해 그 조건에 해당하는 문장 id만으로 카드 화면 진입(문장관리 등 다른 화면엔 영향 없음, 기존 미니 학습과 동일한 격리 방식). 배너 문구를 15-2의 고정 "미니 학습 중"과 구분하기 위해 `startMiniSession(ids, label)`에 두 번째 인자를 추가해 "중요만 학습 중 · N개"/"미암기만 학습 중 · N개"로 표시(기존 15-2 호출은 인자를 안 넘겨 기본값 "미니 학습 중" 그대로 유지, 하위호환). "종료" 누르면 기존 로직 그대로 전체 덱으로 복귀
  - Claude 1차 확인(Playwright, 2026-09-06, 서버 없는 정적 앱이라 스크래치패드에 임시 static 서버+`playwright` 패키지 설치해 구동): 문장 3개를 중요로 표시 후 "중요" 필터 선택 시 버튼 노출 확인 → "전체" 필터·미암기 0개 상태 각각에서 버튼이 숨는지 확인 → 버튼 탭 시 문장관리→카드 화면 전환+배너 문구("중요만 학습 중 · 3개") 확인 → "다음"을 4번 눌러 정확히 그 3개 문장만 순환(4번째에 첫 문장으로 복귀)하는지 확인해 다른 문장이 섞이지 않음을 검증 → "종료" 시 배너가 사라지고 정상 복귀 확인 → 라이트/다크 스크린샷으로 버튼 가독성 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-06, "잘돼")로 23번 전체 완료 처리**

- [x] 24. 정답 발음 듣기 (TTS, 무료 — 2026-09-11 완료): 카드 학습 중 영어 정답 발음을 들을 수 있는 기능. 위 "스피킹 연습(음성 판정) 기능" Non-goals 제외 논의 중 "정답을 들려주는 건 recall을 강화하는 보조 도구"라는 결론으로 등장한 아이디어(SpeechRecognition이 아니라 SpeechSynthesis, 방향이 반대라 비용 0원). 기본은 **수동**(스피커 아이콘 탭)이고 설정에서 **자동재생**으로 전환 가능하도록 구현
  - **배치**: 카드 마킹 아이콘 바에서 토글류(☆중요·⚑미암기)와 함께 구분선 앞에 스피커 아이콘(`volume-2`, `#card-speaker-btn`) 배치 — `☆ ⚑ 🔊 | ✏ 🗑` 순서(최초엔 구분선 뒤 액션 그룹에 뒀으나, 사용자가 "수정·삭제와 묶여있는 게 별로"라고 피드백 — 수정/삭제는 데이터를 바꾸는 액션인 반면 발음 듣기는 별표/깃발처럼 문장 자체와 상호작용하는 성격이라는 논리로 토글 그룹 쪽으로 이동, 2026-09-11 같은 날). 기존 "정답 확인 후에만 노출" 원칙 그대로 적용(`#card-mark-bar` 안에 포함되므로 별도 처리 불필요)
  - **재생 로직**: `speakEnglish(text)` 함수 신규 — `SpeechSynthesisUtterance`에 `lang: 'en-US'` 고정, 기기 내장 TTS로 재생(서버 비용 없음, 완전 오프라인 동작). 스피커 아이콘 탭 시 현재 카드의 `sentence.en`을 재생, 중복 재생 방지를 위해 재생 전 `speechSynthesis.cancel()` 선호출
  - **자동재생 설정**: 설정 화면 "화면모드" 바로 아래 "발음 자동 재생" 토글 신규(`#auto-play-toggle-btn`, 기존 "기본문장 숨기기"와 같은 `.settings-toggle` 컴포넌트 재사용, 기본값 꺼짐, `localStorage` 키 `tuktak_auto_play_pronunciation`). 켜져 있으면 "확인" 버튼 클릭(정답 최초 공개 시점)마다 자동 재생 — `checkBtn` 클릭 핸들러에만 훅을 걸어 별표/깃발 토글 등으로 `renderCard()`가 재호출될 때는 재생되지 않도록 함(반복 재생 방지)
  - **적용 범위**: 카드 화면(메인 학습)과 이를 재사용하는 미니 학습(15-2 AI변형, 23번 필터 학습) 모두에 자동 적용(같은 컴포넌트 재사용이라 별도 작업 불필요). 문장변형 체험판(15-1)은 범위 밖(원 요청이 "카드 학습 중"으로 한정)
  - **알려진 한계**: 언어는 `en-US`로 고정 — "학습할 언어" 라벨 일반화(위 "최종 출시 전 점검사항" 참고)로 다른 언어쌍을 넣은 사용자에겐 발음이 어색하게 들릴 수 있음, 언어 선택 UI는 이번 범위에서 제외
  - **배포 직후 발견한 버그(그리드 블로우아웃) — 같은 날 발견·수정**: 사용자가 실기기에서 "스피커 추가 이후 화면이 가로로 움직인다"고 제보. 원인 조사 결과 아이콘이 4개(44px)에서 5개+구분선으로 늘면서 마킹 아이콘 바의 min-content 폭(321px)이 `.card`의 `grid-template-columns: 1fr` 트랙 자동 최소 크기를 그 값으로 강제하고, `.card`가 다시 `#card-screen`의 그리드 아이템이라 이 강제된 폭이 상위로 전파돼 **화면 전체(`#card-screen`)가 뷰포트보다 넓어지며 좌우로 밀리는 CSS Grid "blowout" 버그**였음(Playwright로 `document.documentElement.scrollWidth`가 뷰포트 폭과 무관하게 항상 385px로 고정되는 것으로 확인). 아이콘 위치만 바꿔서는 해결되지 않아(위치 무관하게 총 폭은 동일) 근본 수정 진행: `.card`와 `#card-screen`의 `grid-template-columns`를 `1fr`/암시적 단일 컬럼에서 `minmax(0, 1fr)`으로 변경 — 그리드 트랙이 콘텐츠의 min-content로 강제 확장되지 않고 실제 컨테이너 폭을 따르게 해 향후 다른 콘텐츠가 넓어져도 화면 전체가 밀리는 문제가 재발하지 않도록 근본 원인 제거. 이 수정만으로 오버플로우 자체는 사라졌지만(마킹 아이콘 바가 부모 박스보다 넓어도 그리드가 강제로 안 늘어나고, 주변 패딩 여유 공간으로 시각적으로만 살짝 번져 나가는 정도라 실제 뷰포트 폭은 넘지 않음) 임시로 `.card-mark-btn`을 44px→40px로 축소해뒀었음
  - **아이콘이 작아 보인다는 피드백으로 원래 크기 복원 — 같은 날**: 사용자가 "스피커 크기가 좀 작은 느낌"이라고 재차 피드백 → 그리드 수정 덕분에 이미 오버플로우가 해결된 상태라 아이콘 크기를 줄일 필요가 없었다는 걸 확인, Playwright로 44px 원복 시에도 320px까지 전 구간 오버플로우 0인 것을 재검증 → `.card-mark-btn`을 44px(원래 크기)로 되돌리고, 간격만 20px→16px로 소폭 축소(5개+구분선이 된 만큼 최소한의 여백 조정)해 최종 확정. 결과적으로 이번 24번 작업으로 실제 남은 변경은 "간격 20→16px"과 "그리드 `minmax(0, 1fr)` 방어 코드" 뿐, 아이콘 자체 크기는 기존과 동일
  - **버튼 크기는 같은데도 여전히 작아 보인다는 재피드백 → lucide 아이콘 자체의 그림 높이 차이로 확인 — 같은 날**: 사용자가 스크린샷에 가이드선을 그어 다른 아이콘과 위아래가 안 맞는다고 재차 제보 → Playwright `getBBox()`로 각 아이콘의 24×24 viewBox 내 실제 그림 높이를 실측한 결과 별표·깃발·연필·휴지통은 19~20인데 `volume-2`만 16으로 확인(lucide 라이브러리 자체가 이 아이콘을 짧게 그려둔 것) — 버튼/SVG 크기를 다른 아이콘과 똑같이 맞춰도 근본적으로 작아 보일 수밖에 없는 구조였음. `#card-speaker-btn svg`에만 개별로 24px→29px(그림 높이 비율에 맞춘 약 1.22배) 규칙을 추가해 다른 아이콘과 위아래 시각적 높이를 맞춤(렌더 픽셀 기준 top/bottom 좌표 1px 이내 일치 확인), 버튼 자체(44px 원형 클릭 영역)는 변경 없음
  - Claude 1차 확인(Playwright, 2026-09-11, 스크래치패드 정적 서버+기존 설치된 playwright 재사용, `speechSynthesis.speak`를 스텁으로 감지): 스피커 아이콘이 마크바 안에 존재 + 정답 확인 전엔 숨김·확인 후 노출 확인 → 자동재생 기본 꺼짐 상태에서 "확인"만 눌렀을 때 재생 호출 없음(0건) 확인 → 스피커 아이콘 수동 탭 시 현재 문장 텍스트로 정확히 1회 재생 호출 확인 → 설정 화면에서 토글 클릭 시 `aria-checked`/`.active`/`localStorage` 모두 정상 갱신 확인 → 토글 on 상태로 카드 화면 복귀 후 "다음"+"확인" 시 새 문장 텍스트로 자동 재생 호출 확인 → 콘솔 에러 없음 → 위 그리드 블로우아웃 수정 후 320~375px 전 구간 가로 오버플로우 0 확인, 라이트/다크 스크린샷으로 아이콘 5개(☆⚑🔊|✏🗑) 겹침 없이 여유 있게 들어가는 것 확인 → **사용자 실기기 2차 확인 완료(2026-09-11, "확인했어")로 24번 전체 완료 처리**
  - **최초 재생 지연 완화 — TTS 워밍업 (2026-09-15)**: 사용자가 "영어로 읽어주는 기능이 최초 실행 시에는 한참 대기해야 하고 이후부터는 바로바로 읽어준다"고 제보. 원인은 브라우저 내장 `SpeechSynthesis` 엔진이 목소리 데이터를 비동기로 늦게 로딩하는 특성(브라우저/OS가 관리하는 영역이라 완전 제거는 불가능) — 사용자가 스피커를 처음 누르는 순간이 아니라 **앱 스크립트가 로드되는 시점에 무음(volume 0) 더미 문장으로 미리 로딩을 트리거**해 체감 지연을 완화(완전 제거 보장은 아님). `js/app.js` 최상단에 `warmUpSpeechSynthesis()` 함수 신규 — 로드 시 1회 실행 + 일부 브라우저가 목소리 목록을 늦게 채우는 경우를 대비해 `voiceschanged` 이벤트 발생 시 1회 더(`{once:true}`) 재트리거. `speakEnglish()`(실제 재생) 로직 자체는 변경 없음. Claude 1차 확인(Playwright, 스크래치패드 정적 서버+기존 playwright 재사용, `window.speechSynthesis.speak/getVoices/addEventListener`를 네이티브 객체 위에 스텁으로 덮어써 감지 — 통째로 재할당하면 setter가 없어 조용히 실패하는 것을 디버깅으로 확인 후 이 방식 채택): 페이지 로드 직후 무음(volume=0) 워밍업 1회 호출 확인 → `voiceschanged` 이벤트 시뮬레이션 시 워밍업 1회 더(누적 2회) 확인 → 카드 화면 진입 후 실제 스피커 버튼 클릭 시 정상 텍스트+기본 volume으로 재생 호출되어 워밍업과 명확히 구분됨 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확실하게 빨라졌어" — 앱 실행 후 문장을 보고 떠올리는 시간이 자연히 있어 워밍업 타이밍도 문제없다고 확인)로 이 보완 작업 완료 처리**

- [x] 25. 노트형 학습 (무료 — 2026-09-11 완료): 다른 앱 리뷰에서 "플래시카드보다 노트(목록) 형태가 좋다"는 후기를 본 사용자가 "툭탁도 노트형을 선택하게 만들 수 있나" 질문 → Claude가 코드를 확인해 문장관리 화면이 이미 한/영을 동시에 보여주는 목록 구조라 노트형 학습에 가까움을 발견. **최초엔 문장관리 화면 자체를 다듬는 방식으로 구현했으나(안내문+표시토글+2단 레이아웃+스피커를 전부 문장관리에 얹음), 사용자가 실제로 보고 "너무 복잡하다, 이 화면에서 뭘 하라는 건지 모르겠다"고 피드백 → 관리(수정/삭제)와 읽기(학습)를 완전히 분리하는 쪽으로 재설계**
  - **문장관리는 원래 모습 그대로 유지**: 노트형 학습 기능을 얹었다가 다시 걷어냄(한 줄 텍스트+말줄임표, 아이콘 인라인 배치 등 전부 원복). 유일한 변화는 필터 영역에 노트형 학습 진입 버튼이 추가된 것뿐
  - **진입 버튼**: 필터 개수 안내 아래 `#list-study-entry-btns`에 버튼 노출. "전체" 필터에서는 **"노트형 학습하기"**(`#list-note-study-btn`, 아웃라인) 1개만, "중요"/"미암기" 필터에서는 기존 23번 버튼 이름을 **"이 조건으로 플래시카드 학습"**(`#list-filter-study-btn`, 솔리드)으로 바꾸고 그 아래 **"이 조건으로 노트형 학습"**을 나란히 추가. 두 버튼 모두 라벨이 길어(예: "이 조건으로 플래시카드 학습") 옆으로 나란히 두면 좁은 화면에서 넘칠 수 있어 세로로 쌓음
  - **노트형 학습 화면 신규**(`#note-study-screen`, 문장관리와 완전히 분리된 새 화면): 상단바(뒤로가기+제목+홈 버튼 — 문장관리를 거쳐야 하는 2단계 화면이라 홈 버튼 포함), 상시 안내문(`.list-note-intro`, "문장을 쭉 훑어보며 노트처럼 외워보세요") + 전체 보기/한글만/영어만 3단 토글(`#note-display-mode-bar`, 화면모드 3단 선택과 시각적으로 동일하되 `modeSelectBtns` 전역 셀렉터와 겹치지 않도록 완전히 별도 클래스 `.list-display-mode-btn` 재사용) + 문장 목록(`#note-study-list`). 진입한 조건(전체 문장 또는 중요/미암기로 필터링된 id)에 해당하는 문장만 `renderNoteStudyList(ids)`로 렌더링
  - **관리용 아이콘 전혀 없음, 스피커만**: 별표·깃발·AI·체크박스 등 "관리" 성격 아이콘은 전부 제외(사용자가 명시적으로 "아무 아이콘도 넣지 않는 거야" 요청). 다만 스피커(발음 듣기)는 "관리"가 아니라 "암기 보조"라는 다른 성격이라 예외적으로 포함(Claude 의견 제시 후 사용자 승인) — `.note-study-speaker-btn`, 카드 화면(24번)의 `speakEnglish()` 재사용, 각 항목 오른쪽에 작고 은은하게(버튼 26px→32px, `--gray-500`, 2026-09-12 "조금 더 컸으면" 피드백으로 확대) 배치. lucide volume-2 아이콘의 그림 높이가 짧은 문제(24번에서 확인)를 여기서도 선제 반영해 18px→22px로 키움(2026-09-12 함께 확대)
  - **표시 방식은 재방문해도 유지**: 전체보기/한글만/영어만 선택 상태를 `localStorage`(`tuktak_list_display_mode`)에 저장 — 필터처럼 매번 초기화되지 않고 개인 취향으로 유지됨. 항목을 다시 그리지 않고 `#note-study-list`에 `hide-kr`/`hide-en` 클래스만 토글해 즉시 반영
  - **텍스트 줄바꿈 허용 + 글자크기 연동**: `.note-study-kr`/`.note-study-en`이 카드 화면과 같은 `--kr-font-size`를 쓰고 줄바꿈을 허용해 문장이 아무리 길어도 잘리지 않고 전체가 보임(이 화면의 존재 이유 자체)
  - **버그 발견·수정 — 플렉스박스 축소 붕괴**: 최초 구현 시 안내문+토글+목록을 담은 `.note-study-body`를 `display:flex; flex-direction:column`으로 만들었더니, 목록이 길어 전체 콘텐츠가 화면보다 커지는 순간 토글 바(`.list-display-mode-bar`, 둥근 모서리용 `overflow:hidden` 있음)가 거의 0px로 찌그러져 탭이 안 되는 버그 발생 — CSS flexbox 스펙상 `overflow`가 `visible`이 아닌 flex 아이템은 자동 최소 크기가 콘텐츠 기준이 아니라 0이 돼, 공간이 부족할 때 flex-shrink로 우선 짜부라짐. `.note-study-body`를 flex 대신 일반 블록 레이아웃으로 바꿔 이 버그 자체를 제거(`#list-filter-section`이 애초에 flex가 아니라서 겪지 않았던 문제)
  - Claude 1차 확인(Playwright, 2026-09-11, 스크래치패드 정적 서버+playwright 재사용): 문장관리가 원래 모습(한 줄+말줄임표+아이콘 인라인, 스피커 없음)으로 복원된 것 확인 → "전체" 필터에서 노트형 학습 버튼 1개만, "중요"(항목 1개 표시 후) 필터에서 두 버튼 모두 노출+라벨 정확한 것 확인 → 노트형 학습 진입 시 관리 아이콘 없이 스피커만 있는 것, 텍스트가 잘리지 않고 줄바꿈되는 것 확인 → 스피커 탭 시 정확한 문장으로 TTS 호출 확인 → 표시방식 토글(한글만/영어만) 정상 동작+`localStorage` 저장 확인 → 뒤로가기(문장관리로 복귀)·홈 버튼(카드 화면으로 이동) 모두 정상 확인 → AI 변형 미리보기 화면(15-2)도 원복된 공유 클래스로 회귀 없이 정상 동작 재확인 → 라이트/다크 스크린샷 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-11, "확인 완료") + 긴 문장도 잘리지 않고 카드가 길어지며 전체 노출되는 것 추가 확인(scrollHeight===clientHeight)으로 25번 전체 완료 처리**
  - **탭-확인(인출연습) 보완 (2026-09-14)**: "노트형 학습도 결국 인출연습(recall)이라 한글 보고 → 영어 떠올리고 → 확인하는 과정이 있어야 한다"는 사용자 지적으로 보완. "한글만"/"영어만" 모드일 때 문장을 탭하면 **그 문장만** 반대쪽 언어가 임시로 보이고(`.note-study-item.revealed` 클래스), 다시 탭하면 원래대로 복귀 — 카드 화면의 "확인" 동작과 같은 성격의 인출연습이 노트형 목록 안에서도 가능해짐. "영어만" 모드에도 대칭 적용할지 확인 질문 → 사용자가 "둘 다 대칭 적용"으로 확정. "전체보기" 모드에서는 이미 둘 다 보이므로 탭해도 시각적 변화 없음(적용 대상 자체가 없음). 표시모드 전환 시(한글만↔영어만 등) 이전 모드에서 남아있던 `.revealed` 상태는 자동 초기화(`applyListDisplayMode()`에 정리 로직 추가) — 새 모드에서 엉뚱하게 해석되는 것 방지. 스피커(발음 듣기) 버튼은 `textWrap`의 형제 요소라 클릭해도 이 확인 토글에 영향 없음(별도 stopPropagation 불필요, DOM 구조상 자연히 분리됨). Claude 1차 확인(Playwright, 스크래치패드 정적 서버 재사용): 전체보기에서 탭해도 무변화 확인 → 한글만에서 탭 시 영어 노출+한글 숨김, 다시 탭 시 원복 확인 → 탭한 문장 외 다른 문장은 영향 없음 확인 → 영어만으로 전환 시 이전 확인 상태 초기화 확인+대칭 동작(탭 시 한글 노출) 확인 → 스피커 클릭이 확인 상태를 안 건드리는 것 확인 → 라이트/다크 스크린샷으로 한 문장만 확인된 채 나머지는 그대로인 레이아웃 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-14, "확인했어")**
  - **자동재생 (2026-09-16)**: "노트형 학습에서 자동으로 순서대로 넘어가면서 문장을 한번씩 읽어주는 기능"을 요청 — 문장을 읽어주고 조금 기다린 뒤, 기억이 안 나면 그냥 다음으로 넘어가는 핸즈프리 방식. 착수 전 3가지 확인: (1) 읽어줄 언어 — 처음엔 "기존 영어 TTS 재사용"을 추천했으나, 사용자가 "우리 앱은 한국어를 보고 영어를 떠올리는 게 본래 목적이라 한국어를 읽어줘야 한다"고 정정 → **한국어를 읽어주는 것으로 확정**, 신규 `speakKorean()` 함수 추가(기존 24번 `speakEnglish()`와 같은 패턴, `lang: 'ko-KR'`만 다름) (2) 대기시간이 끝난 뒤 정답을 강제로 보여줄지 → 사용자 설명("기억 안 나면 그냥 넘어가는 방식")에 근거해 **정답을 자동으로 보여주지 않고 그냥 다음으로 이동**(표시모드는 그대로 유지, "한글만"으로 켜두면 화면·오디오 모두 한국어만 노출되는 완전한 recall 연습이 됨. 원하면 언제든 평소처럼 직접 탭해서 확인 가능) (3) 대기시간 조절 UI → 기존 글자크기 설정과 같은 "-/+ 스테퍼" 방식으로 확정(2~10초, 1초 단위, `localStorage` 저장으로 재방문 시 유지, 기본 4초)
    - **동작**: 표시모드 토글 바로 아래 "자동재생" 버튼(`#note-autoplay-btn`, play/pause 아이콘+텍스트) 신규. 탭하면 목록 순서대로 첫 문장부터 (1) 그 항목에 파란 인셋 보더+옅은 회색 배경으로 강조(`.note-study-item.auto-current`) + 화면에 스크롤 (2) 한국어를 `speakKorean()`으로 읽어줌 (3) 다 읽고 나면(`SpeechSynthesisUtterance`의 `end` 이벤트) 설정된 대기시간만큼 `setTimeout`으로 대기 (4) 다음 문장으로 이동, 반복. 마지막 문장까지 끝나면 자동으로 정지(반복재생 없음). 재생 중 버튼을 다시 누르면 즉시 정지(타이머 해제+`speechSynthesis.cancel()`+강조 해제). 표시모드(전체보기/한글만/영어만)를 전환하거나 화면을 벗어나면(뒤로가기/홈) 자동재생도 함께 정지 — 맥락이 바뀌는 동작이라 음성 메모의 "화면 이동 시 녹음 정지" 원칙과 동일하게 적용, 백그라운드에서 계속 읽어주는 문제(2026-09-16 음성메모에서 겪은 것과 같은 종류의 버그)를 사전에 방지
    - **강조 스타일**: `--color-chip-answer`처럼 "항상 밝은" 고정 배경 대신 theme-aware한 `--gray-100`+좌측 브랜드컬러 인셋 보더 조합을 사용 — 고정 밝은 배경을 썼다면 다크모드에서 `--color-text-primary`(밝은 텍스트)와 대비가 깨졌을 것이라 판단해 처음부터 theme-aware 톤으로 설계(별도 다크모드 예외 규칙 불필요)
    - `index.html`(버튼+스테퍼 마크업)/`js/app.js`(`speakKorean`, 자동재생 상태·타이머 로직)/`css/style.css`(`.note-autoplay-*`, `.note-study-item.auto-current`) 수정
    - Claude 1차 확인(Playwright, 스크래치패드 정적 서버 재사용, `window.speechSynthesis.speak/cancel/getVoices/addEventListener`를 네이티브 객체 위에 스텁으로 덮어쓰고 `SpeechSynthesisUtterance`를 `EventTarget` 기반 모킹 클래스로 교체 — 통째로 재할당하면 getter 전용이라 조용히 실패하는 것을 디버깅으로 재확인, 24번 TTS 워밍업 테스트와 동일 기법 재사용): 대기시간 스테퍼 기본값 4초+최소(2초)/최대(10초) 한계+`localStorage` 저장 확인 → 자동재생 시작 시 첫 문장 강조+한국어(`ko-KR`)로 정확한 문장 TTS 호출 확인 → 발화 종료 후 대기시간 전에는 다음으로 안 넘어가고, 대기시간 경과 후에만 다음 문장으로 이동+강조도 함께 이동하는 것 확인 → 마지막 문장까지 진행 후 자동 정지(추가 TTS 호출 없음, 버튼 라벨 복귀, 강조 전부 해제) 확인 → 재생 중 수동 정지 시 `cancel()` 호출+타이머 해제(정지 후 기다려도 추가 호출 없음) 확인 → 표시모드 전환 시 자동재생 정지 확인 → 화면 이탈(뒤로가기) 시 자동재생 정지(백그라운드에서 계속 재생되지 않음) 확인 → 라이트/다크 스크린샷으로 재생 중 강조 표시 가독성 확인 → 콘솔 에러 없음 → 총 28개 검증 항목 모두 통과 → 커밋+push
    - **"영어만" 모드 언어 보완(같은 날 재확인)**: 사용자 실기기 확인 중 "영어탭에서도 한글로 읽어주는데"라는 제보 — 착수 전엔 "표시모드와 무관하게 항상 한국어만 읽어줌"으로 설계했었는데, "영어만" 모드(한국어 숨김, 영어만 노출)에서도 한국어를 읽어주면 화면엔 안 보이는 언어를 귀로만 먼저 듣고 이미 보이는 영어로 바로 확인하는 셈이라 앞뒤가 안 맞는 문제였음. "영어만 모드는 어떤 언어를 읽어야 할지" 재확인 질문 → **"화면에 보이는 영어를 읽어줌"으로 확정**(기존 25번 탭-확인 기능이 "영어만" 모드를 "영어 보고 한국어 떠올리기"라는 역방향 recall로 이미 다루고 있던 것과 같은 방향, 2026-09-14 참고). **수정**: `speakEnglish()`도 `speakKorean()`처럼 `utterance`를 반환하도록 변경(기존 호출부는 반환값을 안 쓰므로 하위호환), `playNoteAutoPlayStep()`에서 `listDisplayMode === 'en'`이면 `speakEnglish(sentence.en)`, 그 외(전체보기/한글만)는 기존처럼 `speakKorean(sentence.kr)` 사용하도록 분기. `js/app.js`만 수정. Claude 1차 확인(Playwright, 위 테스트에 이어 검증 항목 5개 추가): "영어만" 모드로 전환 후 자동재생 시작 시 정확히 영어 텍스트+`en-US`로 TTS 호출되는 것, 다음 문장으로 넘어가도 계속 영어로 읽어주는 것 확인 → 콘솔 에러 없음 → 총 33개 검증 항목 모두 통과 → 커밋+push → **사용자 실기기 2차 확인 완료(2026-09-16, "확인완료")**
    - **레이아웃 재배치(같은 날)**: "'문장을 쭉 ~' 이 문구는 삭제하고 자동재생 버튼은 플로팅되어 하단에 있으면 어때?" 요청 — 재생 버튼 모양을 "원형 FAB(아이콘만, 우하단에 띄움)" vs "화면 너비 가득 막대(기존 하단고정 버튼과 동일 패턴)" 중 확인 → **원형 FAB로 확정**(대기시간 스테퍼는 표시모드 토글 아래 그대로 유지). `.list-note-intro`("문장을 쭉 훑어보며 노트처럼 외워보세요") 문단을 `index.html`에서 완전히 삭제(더 이상 쓰이지 않는 CSS도 함께 제거). "자동재생" 버튼을 `.note-study-body`(스크롤 영역) 밖, `#note-study-screen`(`position:relative`로 전환)의 직계 자식으로 이동시켜 `position:absolute; right:20px; bottom:20px(+safe-area)`로 우하단에 고정 — `position:fixed`(뷰포트 기준)를 쓰지 않은 이유는, 이 앱이 넓은 화면(PC)에서 `.screen`을 가운데 정렬된 480px 패널로 제한하는데 `fixed`를 쓰면 패널이 아니라 뷰포트 맨 오른쪽 끝에 떠버렸을 것이기 때문 — `#note-study-screen`을 기준점(`position:relative`)으로 삼아 넓은 화면에서도 패널 안쪽에 자연스럽게 위치. 버튼은 아이콘만 남기고(텍스트 라벨 삭제) `aria-label`로 "자동재생 시작"/"자동재생 정지"를 동적으로 전환(접근성 유지), 재생 중 색상은 기존처럼 accent-star 호박색. FAB가 스크롤 최하단에서 마지막 항목을 가리지 않도록 `.note-study-list`에 하단 여유 패딩(76px, 버튼 56px+여백 20px 기준) 추가. `index.html`/`js/app.js`(라벨 엘리먼트 제거+`aria-label` 토글로 대체)/`css/style.css`(`.note-autoplay-bar` 단순화, `.note-autoplay-btn`→`.note-autoplay-fab`로 교체, `#note-study-screen` position 추가) 수정. Claude 1차 확인(Playwright, 기존 테스트 스위트 33개 항목을 새 마크업(aria-label 기반)에 맞게 재검증 — 전부 통과) + 스크롤 끝까지 내려도 FAB와 마지막 항목이 겹치지 않는 것(좌표 비교로 검증) + 라이트/다크 스크린샷(평소/재생중 상태 모두 FAB가 자연스럽게 보이는 것) 확인 → 콘솔 에러 없음 → **사용자 확인 전에 아래 라벨 복원으로 바로 이어짐(원형 버전은 실사용 확인 없이 알약형으로 대체됨)**
    - **"안내문구가 있어야 할 것 같다" 피드백으로 라벨 복원(같은 날)**: 원형 아이콘 전용 FAB로 바꾼 직후 사용자가 "자동실행 이라는 안내문구는 있어야 할것 같은데. 버튼에 아까처럼 자동실행이 좋을까? 대기시간에 자동실행 버튼 안내를 넣을까?"라고 재문의 — 버튼 자체에 라벨을 되살리는 안(플로팅 위치는 유지)과 대기시간 스테퍼 쪽에 별도 안내문을 추가하는 안 중, Claude가 "설명이 버튼 자체에 바로 붙어있는 게 가장 직관적이고 별도 위치에 안내를 추가하면 화면이 다시 복잡해진다"는 근거로 전자를 추천해 그대로 진행. **원형(56×56px, 아이콘만)** → **알약형(아이콘+"자동재생"/"정지" 텍스트, `border-radius:999px`)**으로 모양만 변경, `position:absolute` 우하단 배치·재생중 accent-star 색상 전환·스크롤 시 마지막 항목을 가리지 않는 여유 패딩 등 나머지 동작은 전부 그대로 유지. `index.html`(라벨 span 복원, `aria-label` 속성은 불필요해져 제거 — 보이는 텍스트가 이미 접근성 라벨 역할)/`js/app.js`(`noteAutoPlayLabelEl` 복원, `updateNoteAutoPlayBtn()`이 다시 `textContent`로 갱신)/`css/style.css`(`.note-autoplay-fab`를 원형→알약형 치수·패딩으로 변경, 아이콘 24px→16px로 축소) 수정. Claude 1차 확인(Playwright, 기존 33개 테스트를 라벨 `textContent` 기반으로 재조정해 재실행 — 전부 통과) + 스크롤 끝까지 내려도 여전히 마지막 항목과 안 겹치는 것(마지막 항목 bottom 608px < 버튼 top 640px) 재확인 + 라이트/다크 스크린샷 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-16, "확인 완료")** — 이로써 노트형 학습 자동재생 기능 전체 완료

- [x] 26. 정렬에 가나다순/알파벳순 추가 (무료 — 2026-09-14 완료): "문장이 많아지니까 특정 문장이 있는지 확인할 때 어려움이 있어. 검색은 특정 문구를 알 때만 쓸 수 있으니, ABC순 정렬을 추가하면 어때?"라는 요청. 기존 정렬 인프라(9번)가 카드 학습 순서와 문장관리 목록 순서 둘 다에 적용되는 `sortMode` 하나로 이미 잘 분리돼 있어, 새 정렬 기준을 라디오 옵션으로 추가하는 것만으로 구현 범위가 작았음
  - **정렬 기준**: 한 문장에 한국어/학습어 두 필드가 있어 "어느 기준으로 정렬할지" 확인 질문 → **둘 다 별도 옵션으로 제공**(한국어순/학습어순 중 택1이 아니라 나열해서 각각 선택 가능) 요청으로 확정
  - **적용 범위**: 기존 5개 정렬 옵션과 같은 설정(`SORT_MODES`, `sortMode`)에 6·7번째로 추가하는 방향으로 확정 — 구조를 그대로 재사용해 구현이 간단하고, 카드 학습 순서에도 가나다순/알파벳순이 적용되는 것을 사용자가 승인(전용 별도 UI를 새로 만들지 않음)
  - **구현**(`index.html`/`js/app.js`): 설정 화면 "출제/정렬 순서" 라디오 목록에 "한국어순 (가나다순)"(`data-sort="kr-alpha"`)/"학습어순 (알파벳순)"(`data-sort="en-alpha"`) 2개 추가, 기존 `.sort-option-btn` 스타일 그대로 재사용(CSS 변경 없음). `SORT_MODES` 배열에 `'kr-alpha'`/`'en-alpha'` 추가, `getOrderedSentences()`에 두 케이스 추가 — 한국어는 `localeCompare(b, 'ko')`(가나다순 로케일 비교), 학습어는 `localeCompare(b, 'en', {sensitivity:'base'})`(대소문자 무시)
  - Claude 1차 확인(Playwright, 2026-09-14, 스크래치패드 정적 서버(node `serve`)+기존 설치된 playwright 재사용): 설정 화면 정렬 옵션이 정확히 7개(라벨 포함)로 늘어난 것 확인 → 한국어순 선택 시 문장관리 목록이 "가방이 무겁다"→"나는 학교에 간다"→"다음에 만나자" 순(가나다순)으로 정렬되는 것 확인 → 학습어순 선택 시 "I go to school"→"See you next time"→"The bag is heavy" 순(알파벳순)으로 정렬되는 것 확인 → 새로고침 후에도 선택한 정렬이 유지되는 것 확인 → 카드 학습 화면에도 같은 정렬이 반영되는 것(kr-alpha 선택 시 "가방이 무겁다"가 첫 카드로 노출) 별도 확인 → 라이트/다크 스크린샷으로 7개 옵션이 겹침 없이 들어가는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-14, "확인")로 26번 전체 완료 처리**

- [x] 27. 음성 메모 (무료 — 2026-09-14 완료): "문장이 갑자기 생각날 때 음성으로 말하면 텍스트로 저장되고, 그 텍스트를 당장이든 나중이든 한국어와 영어로 문장으로 입력하게 해주는 기능"이라는 요청. 사용자가 본인의 다른 앱에서 이미 검증해 쓰고 있는 기능이라 품질은 확인된 상태, 아이폰용 앱 출시 계획이 없어 iOS Safari의 SpeechRecognition 지원 불안정 문제도 리스크에서 제외됨
  - **이전에 제외한 기능과 다름**: 2026-09-11에 로드맵에서 제외한 "스피킹 연습(음성 판정, 체크리스트 3번)"은 카드 학습 중 말한 답을 정답과 비교/채점하는 기능이었음. 이번 기능은 판정이 전혀 없는 **순수 캡처**(음성→텍스트 변환만)라 그 결정과 무관 — 오히려 "입력 방법을 계속 쉽고 다양하게"라는 핵심 우선순위(Claude 메모리 `project_feature_priority.md`)와 같은 방향
  - **배치 논의**: 기존 문장추가 화면(폼/파일가져오기/텍스트붙여넣기, 13번)의 4번째 탭으로 넣는 안도 검토했으나, 그 3개 탭은 전부 "그 자리에서 완성된 한/영 문장 쌍을 만드는" 흐름이라 "일단 붙잡고 완성은 나중에"라는 이 기능의 핵심 목적과 맞지 않는다고 판단 — Claude가 **별도의 "음성 메모함"을 만드는 안**을 추천, 사용자 승인
  - **캡처 화면**(더보기 메뉴 신규 "음성 메모" 항목 → `#voice-memo-screen`): 마이크 버튼(`#voice-memo-record-btn`) 탭 → 녹음 시작(SpeechRecognition, 인식 언어 `ko-KR` 고정) → 인식된 텍스트가 편집 가능한 미리보기 textarea에 채워짐(오타 수정 가능) → "메모로 저장"을 눌러야 실제로 메모함(`voiceMemos`, `localStorage` 키 `tuktak_voice_memos`)에 쌓임. 여러 번 나눠 말해도 이어붙여짐. 마이크 버튼은 브라우저 내장 API라 TTS(24번)와 마찬가지로 비용 0원
  - **완성 화면**(메모함 목록에서 항목 탭): 기존 문장추가 폼(`openAddModal`)을 재사용 — 함수 시그니처를 `openAddModal(sentence, options)`로 확장해 `options.prefillKr`로 한국어 칸에 메모 텍스트를 미리 채운 채 새 문장 추가 모드로 열림(영어 칸은 비워둠, 사용자가 채워서 완성). **메모는 탭하는 즉시 지우지 않고, 실제로 "추가"가 성사되는 순간에만 삭제**(`options.sourceMemoId`를 기억해뒀다가 `addSentence()` 성공 시 `deleteVoiceMemo()`) — 중간에 뒤로가기로 취소하면 메모가 그대로 남아있어 데이터 유실이 없음
  - **`addModalReturnScreen` 계산 방식 개선**: 기존엔 `listScreen`이 보이는지만 확인하는 2방향 삼항연산자였는데(카드/문장관리 두 진입점만 상정), 음성 메모 화면이라는 세 번째 진입점이 생기며 `document.querySelector('.screen:not(.hidden)') || cardScreen`으로 일반화 — 앞으로 새 진입점이 추가돼도 이 로직을 다시 손볼 필요 없음
  - **미지원 브라우저 대응**: `window.SpeechRecognition || window.webkitSpeechRecognition`이 없으면 마이크 버튼을 비활성화하고 "이 브라우저는 음성 인식을 지원하지 않아요" 안내로 대체(방어적 처리, 아이폰 앱 계획이 없어도 PC 브라우저 등 다른 미지원 환경 대비)
  - Claude 1차 확인(Playwright, 2026-09-14, 스크래치패드 정적 서버+playwright 재사용, `SpeechRecognition`을 `EventTarget` 기반 모킹 클래스로 교체해 `start`/`stop`/`result` 이벤트 시뮬레이션): 더보기 메뉴 항목 노출 확인 → 모킹된 API가 있을 때 마이크 버튼 활성 확인 → 녹음 시작 시 상태 문구 전환 확인 → 인식 결과가 미리보기에 채워지는 것 확인 → 메모 저장 후 개수 확인 → 화면을 나갔다 재진입해도 메모가 유지되는 것(localStorage 확인) → 메모 탭 시 문장추가 폼에 한국어 프리필+영어는 빈 채로 열리는 것 확인 → **뒤로가기(취소) 시 메모가 사라지지 않고 유지되는 것 확인(유실 방지 로직 핵심 검증)** → 실제로 영어를 채워 추가 완료 시 메모가 사라지고 문장관리에 새 문장이 반영되는 것 확인 → 휴지통 아이콘 단독 삭제 확인 → 빈 상태 안내 문구 확인 → `SpeechRecognition` 자체를 제거한 별도 테스트로 미지원 브라우저 시 버튼 비활성+안내 문구 확인 → `.btn-primary`(시작 화면 전용 스타일)를 잘못 재사용해 저장 버튼이 흐리게 보이던 문제를 발견해 `.modal-btn.modal-btn-submit`(문장추가 제출 버튼과 같은 클래스)로 교체 → 라이트/다크 스크린샷으로 목록·최신순 정렬·레이아웃 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-14, "확인완료")로 27번 전체 완료 처리**
  - **미리보기 textarea 확대 + 문구 변경 (2026-09-15)**: "인식된 문장이~" 문구가 실제로는 STT 결과를 사람이 다시 확인해서 고치는 대상이라 "인식된"보다 "녹음된"이 더 자연스럽고, 미리보기 칸(`#voice-memo-preview`)이 2줄이라 조금 긴 문장은 스크롤해야 보였던 문제로 3줄로 확대 — `index.html`만 수정(`rows="2"`→`rows="3"`, placeholder "인식된 문장이 여기 나타나요."→"녹음된 문장이 여기 나타나요."). CSS에 고정 높이가 없어 `rows` 속성만으로 반영, JS 변경 없음. Claude 1차 확인(Playwright, 라이트/다크 스크린샷) 완료 → 사용자 2차 확인 대기 중
    - **5줄로 추가 확대 (같은 날)**: "입력폼영역을 5줄로 수정" 요청으로 `rows="3"`→`rows="5"`로 재조정. Claude 1차 확인(Playwright, `rows` 속성값+스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **아이콘 블록 전체를 위로 14px 이동 (같은 날)**: "아이콘부터의 전체 내용을 위로 14픽셀 올려줘" 요청 — 마이크 아이콘·상태문구·미리보기·저장버튼을 감싸는 `.voice-memo-capture` 블록의 `margin-top`을 20px→6px로 줄여(20-14=6) 안내문 바로 아래 블록 전체가 14px 위로 붙도록 조정. Claude 1차 확인(Playwright, `getComputedStyle`+`getBoundingClientRect()`로 안내문 하단~블록 상단 간격이 정확히 6px인 것 확인, 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **마이크 버튼 색상 전환 (2026-09-15, 헤더 무채색 전환과 같은 논의에서 이어짐)**: "마이크 아이콘을 선형의 그레이 계열로 바꿀까?"라는 질문으로 시작 — 평소(비녹음) 상태의 `.voice-memo-record-btn`이 솔리드 브랜드 블루 채움이라 카드 화면의 다른 유틸리티 아이콘(스피커·연필·휴지통 등, 기본 회색·활성 시에만 강조색)과 원칙이 어긋나 있던 것을 정리. `css/style.css`만 수정 — 평소: 흰 배경(`--color-base`)+회색 테두리+회색 아이콘의 아웃라인 원형 버튼, 녹음 중(`.recording`): 기존처럼 accent-star 호박색 솔리드 채움+흰 아이콘 유지(활성 상태 신호는 그대로 보존), 비활성(`:disabled`): 더 옅은 회색 테두리+옅은 아이콘으로 조정
    - **테두리·아이콘 선 색 통일 (같은 날 추가 수정)**: 최초 구현 시 테두리는 `--gray-300`, 아이콘은 `--gray-600`로 서로 다른 톤이었는데, 사용자가 "마이크아이콘의 선과 원테두리의 선을 동일하게 해줘, 마이크아이콘의 선색에 맞추어줘"라고 피드백 → 평소 상태 테두리색을 `--gray-300`→`--gray-600`(아이콘과 동일)으로, 비활성 상태도 같은 원칙으로 테두리·아이콘 모두 `--gray-300`으로 통일
  - Claude 1차 확인(Playwright, 라이트/다크 각각 기본/녹음중 상태 `getComputedStyle`+스크린샷): 기본 상태 배경·테두리·아이콘 색이 라이트(`#FFFFFF`/`#6B6B6B`/`#6B6B6B`, 테두리=아이콘 일치)·다크(`#14171C`/`#9AA6B2`/`#9AA6B2`, 테두리=아이콘 일치) 모두 의도한 값과 정확히 일치, 녹음중 상태는 라이트·다크 동일하게 기존 호박색(`#BA7517`)+흰 아이콘 유지 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **한 단계 밝은 회색으로 교체 (같은 날 추가 수정)**: 사용자가 "지금 아이콘의 선색이 뭐야?"(`--gray-600`, 라이트 `#6B6B6B`/다크 `#9AA6B2`)라고 물은 뒤 "한단계 밝은 그레이로 교체" 요청 → 그레이스케일 팔레트 순서상 한 단계 밝은 `--gray-500`으로 변경(평소 상태의 테두리+아이콘 둘 다, 위에서 통일한 "테두리=아이콘" 원칙 유지). 녹음 중(`.recording`)·비활성(`:disabled`) 상태는 영향 없음. Claude 1차 확인(Playwright, `getComputedStyle`): 라이트 `rgb(138,138,138)`(`#8A8A8A`)/다크 `rgb(120,130,142)`(`#78828E`)로 정확히 `--gray-500` 값과 일치, 테두리·아이콘 색 계속 동일한 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **여백 조정 (2026-09-15)**: "'탭해서 말하기' 아래의 여백을 지금의 두배로 넓혀줘. 그리고 저장된 메모들의 줄간격은 지금의 절반으로 줄여줘" 요청 — `.voice-memo-status`(탭해서 말하기 문구)에 `margin-bottom: 10px` 추가해 기존 부모 flex gap(10px)과 합쳐 아래 여백을 20px(2배)로, `.voice-memo-list`(저장된 메모 카드 목록)의 `gap`을 10px→5px(절반)로 축소. Claude 1차 확인(Playwright, 메모 3개 시드 후 실제 렌더링된 요소 간 픽셀 간격 측정): 상태 문구~입력칸 간격 정확히 20px, 메모 목록 `gap` 정확히 5px 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **메모 항목 패딩 축소 + 좌측 정렬 통일 (같은 날 추가 수정, 이후 아래에서 좌측 패딩만 재조정됨)**: 실기기 스크린샷과 함께 "저장된 메모의 xxx로 두줄 넣어진 줄간격이 아직 너무 멀어(위 gap 5px 조정은 카드 사이 간격이라 체감이 적었고, 실제 여백의 대부분은 각 카드 자체의 상하 패딩 14px이었음을 스크린샷으로 확인), 저 xxx 위와 아래 줄간격을 지금의 절반으로 줄여주고 왼쪽은 저장된메모와 같은 선에서 시작하게 해줘. 화면이 전체적으로 그리드가 너무 들락날락 해보여"라는 피드백 → `.voice-memo-item` `padding: 14px 16px` → `padding: 7px 16px 7px 0`으로 변경(위아래 절반, 왼쪽은 0으로 만들어 텍스트가 "저장된 메모" 타이틀과 정확히 같은 세로선에서 시작하도록). Claude 1차 확인(Playwright, `getBoundingClientRect()`로 실측): 항목 높이 46px(패딩 7+7+삭제아이콘32)로 절반 축소 확인, "저장된 메모" 타이틀과 메모 항목의 `left` 좌표가 정확히 일치하는 것 확인, 항목 간 간격은 기존 5px 그대로 유지 확인 → 콘솔 에러 없음. **주의**: 이후 "저장된 메모" 타이틀 자체가 삭제되고(아래 참고), 화면 좌우 여백 16px 통일 작업(위 "UI 레이아웃 규칙" 섹션 참고) 때 왼쪽 패딩이 다시 16px로 복원돼 최종적으로는 `padding: 7px 16px`(좌우 대칭)이 됨 — 이 항목은 그 중간 과정 기록으로 남겨둠
  - **안내 문구 통합 (같은 날)**: 화면 위쪽 안내문("문장 아이디어가 떠오르면 눌러서 말해보세요. 완성은 나중에 해도 괜찮아요")과 저장된 메모 목록 위의 짧은 안내문("메모를 탭하면 문장추가 화면으로 이어서 완성할 수 있어요")이 따로 떨어져 있어 캡처→완성으로 이어지는 전체 흐름이 한눈에 안 들어온다는 지적 → 사용자가 정확한 대체 문구를 제시해 그대로 반영: `index.html`의 `.voice-memo-intro`를 "문장이 떠오르면 아이콘을 눌러서 말해보세요.<br />텍스트로 저장된 문장을 탭하면 문장추가 화면으로 이어서 완성할 수 있어요."로 교체하고, `.voice-memo-list-hint` 문단은 완전히 삭제(더 이상 쓰이지 않는 `css/style.css`의 `.voice-memo-list-hint` 규칙도 함께 제거). Claude 1차 확인(Playwright): 통합된 문구가 정확히 반영된 것, 목록 위 안내문 요소가 DOM에서 사라진 것, 이전에 조정한 패딩/정렬/간격 수치들에 회귀가 없는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **왼쪽 정렬 + "텍스트로" 삭제 (같은 날 추가 수정)**: "왼쪽정렬로 수정하고 '텍스트로'는 삭제" 요청 — `.voice-memo-intro`의 `text-align: center` → `left`로 변경(다른 좌측 정렬 요소들과 그리드가 통일됨), 문구에서 "텍스트로" 삭제해 "저장된 문장을 탭하면 문장추가 화면으로 이어서 완성할 수 있어요."로 축약. Claude 1차 확인(Playwright, `innerText` 검증+스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **"저장된 메모" 타이틀 삭제 (같은 날 추가 수정)**: 위 안내 문구 통합으로 상단 안내문이 이미 "저장된 문장을 탭하면~"이라고 설명하고 있어, 목록 위 "저장된 메모" 소제목(`.voice-memo-list-title`)이 중복이 됐다고 판단 — "아래의 '저장된 메모'는 삭제" 요청으로 `index.html`에서 해당 문단 삭제, 더 이상 쓰이지 않는 `css/style.css`의 `.voice-memo-list-title` 규칙도 함께 제거. 메모 목록(`#voice-memo-list`)과 빈 상태 안내("아직 저장된 메모가 없어요.")는 그대로 유지. Claude 1차 확인(Playwright, 메모 있음/없음 두 케이스 모두 확인): 타이틀 요소가 DOM에서 사라진 것, 메모 목록이 저장 버튼 바로 아래로 자연스럽게 이어지는 것, 빈 상태 안내 문구는 그대로 노출되는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **연속 녹음이 중간에 끊기는 문제 보완 (2026-09-16)**: 실사용 중 녹음이 짧은 침묵에도 끊기고, 브라우저가 인식을 자체 종료하면 그 뒤로는 이어서 말한 내용이 캡처되지 않는 문제 발견 — `js/app.js`만 수정
    - `voiceMemoRecognition.interimResults`를 `false`→`true`로 변경해 확정 전 중간 결과도 실시간으로 미리보기에 보여줌(placeholder가 오래 남아있지 않도록), `continuous`를 신규로 `true`로 설정해 짧은 침묵에도 인식이 끊기지 않도록 함
    - `result` 이벤트 핸들러를 `continuous` 모드에 맞게 재작성 — 매 이벤트마다 전체 결과가 아니라 `e.resultIndex` 이후의 새 결과만 순회, 확정(`isFinal`)된 구간은 `voiceMemoSessionFinalText`에 누적하고 미확정 구간만 실시간으로 덧붙여 표시
    - **자동 재시작**: `end` 이벤트 시 사용자가 정지 버튼을 누른 게 아니고(`voiceMemoStopRequested`) 치명적 오류도 아니면(`voiceMemoFatalError`), 브라우저가 자체적으로 세션을 끊은 것으로 간주해 `recognition.start()`를 곧바로 재호출 — `voiceMemoRecording`/버튼의 `recording` 클래스는 그대로 유지해 사용자 눈에는 끊긴 것처럼 보이지 않음. `voiceMemoSessionFinalText`는 재시작해도 초기화되지 않아 재시작 전후로 말한 내용이 하나로 이어붙여짐(이번 보완의 핵심)
    - **치명적 오류 구분**: `error` 이벤트에서 `not-allowed`/`audio-capture`/`service-not-allowed`(마이크 권한 거부 등, 재시작해도 소용없는 오류)만 `voiceMemoFatalError`로 표시해 "마이크 권한을 확인해주세요" 안내로 전환하고 자동 재시작을 멈춤. `no-speech` 등 일시적 오류는 별도 처리 없이 `end`에서 자동 재시작됨
    - **여러 번 나눠 녹음할 때의 이어붙이기**: 마이크 버튼을 탭해 새 녹음을 시작하는 시점(사용자가 직접 정지 후 다시 시작하는 경우)에 `voiceMemoBaseText`로 그 시점까지의 미리보기 값을 기억해두고, `voiceMemoSessionFinalText`는 새로 초기화 — 기존 "여러 번 나눠 말해도 이어붙여짐" 동작은 유지하면서 세션 내부(자동 재시작)와 세션 간(수동 재시작) 이어붙이기를 구분
    - **placeholder**: 녹음 시작 클릭 시점에 즉시(이벤트를 기다리지 않고) placeholder를 비워 녹음이 시작됐음을 바로 보여줌, 종료 후 미리보기가 빈 채로 남으면 원래 placeholder로 복원
    - Claude 1차 확인(Playwright, 스크래치패드 정적 서버+`EventTarget` 기반 `SpeechRecognition` 모킹 — `interimResults`/`resultIndex`/`isFinal`까지 실제 API 동작을 재현): 녹음 시작 즉시 placeholder 비워짐+상태 전환 확인 → 최종 결과가 정확히 반영되는 것 확인 → 수동 정지 시 재시작 없이 1회만 `start` 호출되는 것 확인 → interim 결과가 실시간으로 교체되며 표시되다 확정 시 고정되는 것 확인 → **브라우저가 자체적으로 세션을 끊어도 자동 재시작되고(`start` 호출 횟수 증가), 재시작 전후로 말한 내용이 끊기지 않고 하나로 이어붙여지는 것 확인(이번 보완의 핵심 검증 포인트)** → 마이크 권한 거부 등 치명적 오류 시 재시작하지 않고 안내 문구로 전환되며 그 문구가 유지되는 것(정상 문구로 덮어써지지 않음) 확인 → 빈 상태로 종료 시 placeholder 복원 확인 → 메모 저장 흐름 회귀 없음(목록 반영·칸 비움·placeholder 복원 정상) 확인 → 콘솔 에러 없음(의도적으로 차단한 gstatic 에러 제외) → 총 22개 검증 항목 모두 통과 → 커밋+push 후 **사용자 실기기 2차 확인 요청 → "같은 말이 반복적으로 녹음되고, 터치 안 해도 꺼졌다 켜진다"는 실기기 버그 제보(2026-09-16)**
    - **원인 분석 및 수정 (같은 날)**: 신고된 두 증상 모두 `continuous: true` 설정이 원인으로 추정됨 — 안드로이드 일부 기기에서 `continuous: true`를 주면 브라우저가 내부적으로도 세션을 계속 이어가려 자체 시도하는데, 이게 위에서 구현한 "`end` 시 수동으로 `start()` 재호출" 로직과 서로 경합(같은 구간을 두 세션이 동시에 인식 → 같은 말이 반복 캡처됨, 또는 경합으로 한쪽 `start()`가 예외를 던져 조용히 종료 처리(마이크 꺼짐)됐다가 브라우저 자체 내부 세션이 뒤늦게 `start` 이벤트를 발생시켜 다시 켜진 것처럼 보이는 것으로 추정). **수정**: `voiceMemoRecognition.continuous = true;` 할당 자체를 제거(기본값 `false`로 되돌림) — 세션 하나만 항상 살아있도록 해 경합 자체를 없앰. `continuous:false`에서도 매 발화(문장) 종료 시 자연히 `end`가 오는데, 기존에 구현해둔 "`end`마다 즉시 재시작" 로직이 그대로 연속 녹음처럼 동작시켜줌 — 사용자 체감상의 "연속 녹음" 목적은 유지하면서 경합 위험만 제거. 코드는 `js/app.js`만 재수정(로직 변경 없이 `continuous` 할당 줄만 삭제+주석 갱신). Claude 재확인(Playwright, 위 22개 테스트 스위트 전체 재실행) 결과 회귀 없이 전부 통과(모킹 환경은 `continuous` 값과 무관하게 동작해 이 설정 자체를 직접 검증하지는 못함 — 실기기에서만 재현 가능한 버그였음을 감안) → 커밋+push
    - **2차 실기기 제보(같은 날)**: "음성녹음하고 나서 메모로 저장 눌렀는데 저장은 돼. 하지만 아이콘이 계속 녹음중으로 남아있어" — 자동 재시작 로직이 도입되며 생긴 사이드이펙트. 기존엔 뒤로가기 버튼(`voiceMemoBackBtn`)에서만 `stopVoiceMemoRecording()`을 호출했는데, "메모로 저장" 버튼과 "메모 항목을 탭해 문장추가 화면으로 이동"하는 경로에는 이 호출이 빠져 있어, 녹음 중에 이 두 동작을 하면 화면을 벗어나도 마이크가 계속 자동 재시작되며 백그라운드에서 녹음이 이어지는 문제였음. **수정**: `voiceMemoSaveBtn` 클릭 핸들러와 메모 항목(`item`) 클릭 핸들러 맨 앞에 `stopVoiceMemoRecording()` 호출 추가 — 두 곳 모두 "이 녹음 세션은 끝났다"는 의미의 동작이라 뒤로가기와 같은 원칙 적용. `js/app.js`만 수정. Claude 1차 확인(Playwright, 위 테스트에 이어 검증 항목 10개 추가): 녹음 중 저장 시 메모는 정상 저장되면서 `recording` 클래스·인식 인스턴스가 실제로 정지되는 것, 저장 후 잠시 기다려도 자동 재시작이 다시 걸리지 않는 것, 녹음 중 메모 항목을 탭해 문장추가 화면으로 이동해도 녹음이 정지되고(재시작 없음) 화면 전환 자체는 정상 동작하는 것까지 확인 → 콘솔 에러 없음 → 총 32개 검증 항목 모두 통과 → **사용자 실기기 재확인 완료(2026-09-16, "확인완료")** — 반복 녹음/터치 없는 on-off/저장 후 아이콘 미정지 3가지 증상 모두 해결 확인. 이로써 연속 녹음 보완 작업 전체 완료
  - **음성 메모 목록도 같은 방식으로 통일(같은 날)**: "음성메모 목록도 같은 방식으로 통일시켜줘. 버튼과 폼은 그대로 유지" 요청 — 마이크 버튼·미리보기 textarea·"메모로 저장" 버튼은 손대지 않고, 저장된 메모 목록(`.voice-memo-item`)만 `.sentence-list-item`/`.note-study-item`과 동일한 방식으로 좌우 패딩 `16px→4px`로 축소(카드 바깥 테두리 16px 유지, 텍스트 인셋 32px→20px). `css/style.css`만 수정(위아래 패딩 7px는 2026-09-15에 이미 조정된 값이라 유지). Claude 1차 확인(Playwright, 짧은/긴 메모 혼합 시드, 라이트/다크 스크린샷+`getBoundingClientRect()`로 카드 바깥 16px·텍스트 20px 확인): 마이크 버튼·텍스트박스·저장 버튼 레이아웃 변화 없는 것, 메모 목록 텍스트가 카드 가장자리에 붙지 않고 삭제(휴지통) 아이콘도 여유 있게 배치되는 것, 긴 메모가 잘리지 않고 줄바꿈되는 것 확인 + 기존 음성 메모 녹음 테스트 스위트(연속 녹음/저장 시 정지 등, 35개 항목) 재실행 — 전부 통과, CSS 변경이 녹음 로직에 영향 없음 확인 → 콘솔 에러 없음 → **사용자 2차 확인 대기 중**

- [x] 28. 동시편집 데이터 유실 방지 — 저장을 항상 병합으로 통일 (무료 — 2026-09-14 완료): 19번 PC 빠른입력 점검 문서의 남은 재검토 항목("폰↔PC 동시편집 충돌을 사용자 주의에 맡길지 방지 로직을 넣을지") 실제 구현. 같은 날 앞서 논의한 판단 기준 재확인 — "실시간 반영은 안 중요하지만, 양쪽에서 입력·수정하다 데이터가 조용히 사라지는 건 불만"이라는 사용자 기준 → **방법 B(자동 병합)**로 확정했던 방향을 그대로 코드에 반영
  - **핵심 변경**: 폰의 "지금 백업"이 단순 덮어쓰기(`CloudSync.backup(sentences)`)였던 것을, 저장 전에 클라우드를 먼저 불러와 **로컬과 병합한 뒤** 그 결과를 클라우드에 올리는 방식으로 변경. 기존에 별도 버튼이던 "**클라우드와 병합**"(18번 보완 2번, 2026-09-13 구현)의 병합 로직(한국어+영어 텍스트 완전일치 기준, 중요/미암기 표시는 OR로 합침, 불일치 항목은 새 문장으로 추가)을 그대로 흡수 — "저장은 항상 병합"이 기본 동작이 되면서 **별도 병합 버튼은 중복이 되어 제거**(사용자 확인 후 결정, "버튼이 준다시 줄어들어 UI가 깔끔해지고 원칙이 버튼 구조에도 그대로 드러남"). "클라우드에서 복원"만 기존처럼 의도적 전체교체(확인창 필수)로 남김 — 유일하게 남은 "덮어쓰기" 동작
  - **`js/app.js`**: `cloudBackupBtn` 클릭 핸들러를 병합 로직으로 교체(기존 `cloudMergeBtn` 핸들러 코드를 그대로 이식). `cloudMergeBtn`/`cloudMergeInfoBtn` 관련 DOM 참조·핸들러 전부 삭제. "지금 백업" 결과 alert에 병합으로 추가/갱신된 개수가 있으면 "(다른 기기에서 추가된 문장 N개, 표시 갱신 M개 반영)"을 덧붙여 안내, 없으면 기존처럼 "클라우드에 백업했습니다."만 표시. `cloudBackupInfoBtn`의 "?" 설명 문구도 병합 동작을 반영해 갱신
  - **`js/input.js`**: `input.html`의 "변경사항 저장"도 같은 원칙 적용 — 기존엔 페이지를 연 시점에 불러온 관리 목록 스냅샷(`manageSentencesById`)만 기준으로 통째로 `CloudSync.backup()`했는데, 그 사이 폰이 클라우드에 저장한 변경사항이 있으면 조용히 사라지는 문제가 있었음. **저장 버튼을 누르는 시점에 클라우드를 한 번 더 불러와**(`freshCloud`), 이 페이지에서 편집한 항목(`editedById`)·명시적으로 삭제한 항목(`deletedIds`)의 "의도"만 반영하고, **이 페이지가 모르는 항목(그 사이 다른 기기가 추가/변경한 것)은 그대로 보존**하는 방식으로 재구성. 새로 입력한 문장은 기존처럼 그대로 추가
  - **`index.html`**: 설정 화면 클라우드 백업 섹션에서 "클라우드와 병합" 버튼+그 "?" 설명 버튼 마크업 삭제. 버튼 순서가 지금 백업 → 클라우드에서 복원 → 클라우드 백업 삭제 → 로그아웃으로 정리(5개→4개)
  - Claude 1차 확인(Playwright, 2026-09-14, 스크래치패드 정적 서버+playwright 재사용, gstatic 네트워크 차단+`CloudSync` 모킹):
    - 폰 쪽: "클라우드와 병합" 버튼이 화면에서 완전히 사라진 것 확인 → `CloudSync.restore()`가 (로컬과 일치하는 문장 1개+클라우드 전용 신규 문장 1개)를 반환하도록 모킹 후 "지금 백업" 클릭 → alert에 정확한 병합 개수 문구 확인 → `CloudSync.backup()`에 넘어간 최종 목록에 병합된 표시(unfamiliar 등)와 신규 문장이 정확히 반영된 것 확인 → 문장관리 화면에도 병합 결과(총 문장 수 증가)가 그대로 보이는 것 확인
    - PC(`input.html`) 쪽: `CloudSync.restore()`를 호출마다 다른 값을 반환하도록 모킹(1번째=페이지 로드 시 관리 목록 2개, 2번째=저장 직전 시점에 "그 사이 폰이 추가한 문장 1개"가 늘어난 상태)해 실제 레이스 컨디션을 시뮬레이션 → 관리 목록에서 문장 1개 수정+문장 1개 삭제+새 문장 1개 입력 후 저장 → 최종 저장 목록에 **수정 반영+삭제 제외+레이스로 그 사이 추가된 문장 보존(핵심 검증 포인트)+새 문장 포함**이 정확히 일치하는 것 확인
    - 라이트/다크 스크린샷으로 설정 화면 클라우드 섹션 버튼 4개 레이아웃 확인, 콘솔 에러 없음(의도적으로 차단한 gstatic 네트워크 에러만 발생) → **사용자 실기기 2차 확인 완료(2026-09-14, 폰+PC 양쪽 모두 확인, "잘 되는 것 같아")로 28번 전체 완료 처리**
  - `reference/docs/PC_빠른입력_점검_2026-09-08.docx`의 4장 (3)번·6장 체크리스트 항목도 같은 날 "방향 확정" → "해결됨/구현 완료"로 갱신 완료

- [x] 29. 카드 문장 탭으로 정답 토글 (무료 — 2026-09-15 완료): "메인화면에서도 문장을 탭하면 확인버튼 눌렀을때와 같은 영어상태로 보이고 다시 탭하면 한글로 돌아오는 기능 넣어주면 어때"라는 제안. 노트형 학습(25번, 2026-09-14)의 "한글만/영어만 모드에서 문장 탭 시 반대쪽 언어를 임시로 보여주는 인출연습"과 같은 패턴을 메인 카드 화면에도 적용한 것
  - **구현**(`js/app.js`/`css/style.css`): `#kr-text`/`#en-text`에 클릭 리스너 추가 — 탭할 때마다 기존 `revealed` 상태(변수는 이미 "확인" 버튼이 쓰던 것 그대로 재사용)를 토글. 안 보이는 상태에서 탭하면 "확인" 버튼과 동일하게 정답 노출+마킹 아이콘 바 노출+(자동재생 설정 켜져 있으면) 발음 재생까지 동일하게 수행, 보이는 상태에서 다시 탭하면 한국어로 복귀하며 마킹 아이콘 바도 함께 숨김(기존 "정답 확인 후에만 노출" 규칙이 `revealed` 하나에 연동돼 있어 자연히 함께 처리됨). 기존 "확인" 버튼은 그대로 유지(누르면 항상 노출만 함, 토글 아님) — 새 탭 제스처가 추가되는 것뿐. 마킹 아이콘 바(☆⚑🔊✏🗑)는 텍스트의 형제 요소라 클릭 시 서로 간섭하지 않음(별도 `stopPropagation` 불필요). `.kr-text`/`.en-text`에 `cursor: pointer` 추가해 탭 가능함을 암시
  - **적용 범위**: 카드 화면을 재사용하는 미니 학습(15-2 AI변형, 23번 필터 학습)에도 자동 적용(같은 컴포넌트라 별도 작업 불필요)
  - Claude 1차 확인(Playwright, 스크래치패드 정적 서버 재사용): 초기 상태(영어/마킹바 모두 숨김) → 한국어 텍스트 탭 시 영어 노출+마킹바 노출 확인 → 영어 텍스트 탭(재탭) 시 다시 한국어만 남고 마킹바도 숨겨지는 것 확인 → 탭으로 노출한 뒤 "다음" 클릭 시 새 문장에서 정상적으로 리셋되는 것 확인 → 기존 "확인" 버튼도 그대로 정상 동작하는 것 확인 → 자동재생 설정 켜둔 상태에서 텍스트 탭 시에도 정답 확인 버튼과 동일하게 발음이 자동 재생되는 것 확인 → 별표(★) 아이콘 클릭이 텍스트 토글에 영향을 주지 않고(revealed 유지) 정상적으로 즐겨찾기만 토글되는 것 확인(마킹 아이콘과의 이벤트 간섭 없음 검증) → 스크린샷으로 탭 후 레이아웃(정답 칩+마킹바+확인 버튼 네이비 전환까지 기존 "확인" 버튼 결과와 동일) 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**

---

## 기술 스택
- Frontend: HTML5, CSS3, Vanilla JavaScript
- PWA: Service Worker, Web App Manifest
- Storage: LocalStorage (문장 데이터, 학습 진행 상태)
- Backup: TSV 파일 내보내기 — 복원은 "파일가져오기" 기능 재사용 (2026-08-13)
- 배포: Vercel / 버전관리: GitHub
- 답변 입력: 텍스트 확인(무료). 음성 답변(마이크 인식·판정)은 **2026-09-11 유료 로드맵에서 완전히 제외 확정**(위 Non-goals "스피킹 연습(음성 판정) 기능" 참고)
- 정답 발음 듣기(TTS, SpeechSynthesis API, 24번): 2026-09-11 구현 완료(무료, 기기 내장 엔진이라 비용 0원) — 기본 수동(스피커 아이콘)·설정에서 자동재생 전환 가능
- 음성 메모(STT, Web Speech API의 SpeechRecognition, 27번): 2026-09-14 구현 완료(무료, 브라우저 내장 API라 비용 0원). 판정·채점 없는 순수 텍스트 캡처 용도라 2026-09-11 로드맵에서 제외한 "스피킹 연습(음성 판정)"과는 무관. 인식 언어 `ko-KR` 고정, 브라우저별 지원 편차가 있어(iOS Safari 등) 미지원 시 마이크 버튼 비활성화+안내 문구로 방어
- 데이터 입력: 폼 입력 + 파일 가져오기(CSV/TSV/TXT), LocalStorage에 저장해 오프라인에서도 사용 가능
- 클라우드 백업(18번, 2026-08-29 착수): Firebase Authentication(Google 로그인) + Cloud Firestore(무료 Spark 등급, 프로젝트 ID `tuk-tak-2c172`, 리전 asia-northeast3). SDK는 npm 설치 없이 CDN 모듈(`https://www.gstatic.com/firebasejs/12.18.0/...`)로 `js/firebase-init.js`(`type="module"`)에서만 불러오고, 기존 `js/app.js`(일반 스크립트)는 `window.CloudSync`로만 접근 — 앱 전체를 모듈 구조로 바꾸지 않기 위한 최소 격리

---

## 메뉴 기능 정리 (사용자 가이드용 — 추후 앱 내 설명서로 확장 예정)

> 화면별로 각 메뉴/버튼이 하는 일을 정리해둠. 메뉴 구조가 바뀌거나 기능이 추가되면 이어서 갱신할 것

### 카드 화면
- 이전 / 확인 / 다음: 카드 학습 핵심 조작. "확인" 시 영어 정답 노출
- **문장 텍스트 탭으로 정답 토글** (29번, 2026-09-15): 한국어/영어 텍스트 자체를 탭해도 "확인"과 같은 효과 — 안 보이는 상태에서 탭하면 정답 노출, 보이는 상태에서 다시 탭하면 한국어로 복귀(마킹 아이콘 바도 함께 숨김). "확인" 버튼은 그대로 유지(항상 노출만 함, 토글 아님) — 텍스트 탭은 추가된 빠른 제스처
- 정답 확인 후 노출되는 마킹 아이콘 바: ☆(중요 표시 토글) · ⚑(미암기 표시 토글) · 🔊(정답 발음 듣기, 24번, 2026-09-11) | ✏(수정) · 🗑(삭제)
- **이어보기**: "시작하기"를 눌러 카드 화면에 진입할 때마다 마지막으로 보던 문장부터 자동으로 시작(LocalStorage에 저장). 그 문장이 삭제되는 등 찾을 수 없으면 처음부터 시작. 처음부터 다시 보고 싶으면 설정 > 출제/정렬 순서 아래 "처음 문장부터 다시 보기" 사용(2026-09-08, 더보기 메뉴에서 이동 — 아래 "더보기 드롭다운" 참고)
- **백업 리마인더 배너** (2026-08-28): 마지막 백업(또는 배너 닫기) 이후 7일이 지났거나 그 사이 내 문장이 100개 이상 늘었으면 상단바 바로 아래에 "문장을 백업한 지 좀 됐어요" 배너가 뜸. "지금 백업하기"를 누르면 설정 화면과 같은 백업 로직이 바로 실행되고, 우측 X로 닫으면 다음에 조건이 다시 차야 재노출됨
- **로고 클릭으로 미니 학습 종료** (2026-09-06): 미니 학습 중(15-2 AI 변형 결과 학습, 23번 필터 학습)일 때 상단바의 "TUK TAK" 로고를 눌러도 배너의 "종료" 버튼과 똑같이 전체 덱으로 돌아감. 평소(미니 학습이 아닐 때)엔 이미 메인 화면이라 로고를 눌러도 아무 동작 없음

### 더보기(⋮) 드롭다운
- **문장추가**: 문장 입력 방법을 탭으로 통합. [폼 입력 / 파일 가져오기 / 텍스트 붙여넣기] 3탭 중 선택(2026-08-29~2026-09-08엔 "빠른입력" 4번째 탭도 있었으나, PC 빠른입력의 실제 구현체는 별도 페이지 `input.html`(19-2) 하나로 정리하기로 하며 제거 — 아래 19-2 참고)
  - 폼 입력: 한국어/영어 문장을 하나씩 입력해 추가
  - 파일 가져오기: CSV/TSV/TXT 파일 업로드로 여러 문장 한 번에 추가
  - 텍스트 붙여넣기: 파일 저장 없이 텍스트를 바로 붙여넣어 여러 문장 추가
  - (자세한 규칙은 아래 "문장 입력 방법" 참고)
- **음성 메모**(27번, 2026-09-14): 마이크 버튼을 탭해 말하면 텍스트로 변환되는 별도 캡처 화면. 문장추가와 달리 그 자리에서 완성된 한/영 문장 쌍을 요구하지 않고, 인식된 텍스트를 편집 가능한 미리보기에서 확인 후 "메모로 저장"하면 메모함에 쌓임. 메모함 목록에서 항목을 탭하면 문장추가 폼으로 이동하며 한국어 칸에 그 텍스트가 미리 채워짐 — 영어를 채워 실제로 "추가"해야 메모가 사라지고, 중간에 취소하면 메모는 그대로 남음(데이터 유실 방지). 인식 언어는 한국어 고정, 브라우저가 음성 인식을 지원하지 않으면 마이크 버튼이 비활성화되고 안내 문구로 대체됨
- **문장관리**: 전체 문장 목록. 탭하면 수정 모달, 롱프레스는 단일 삭제 확인, "선택" 버튼으로 다중 선택 삭제. 상단 전체/중요/미암기 필터 탭. 최초 실행 시 자동 제공된 기본문장에는 "기본" 배지가 붙음 — 수정하면 배지가 사라지고 내 문장으로 전환됨. 각 항목 우측에 별(중요)·깃발(미암기)에 이어 구분선 + **AI 아이콘**(`sparkles`)이 있어, 탭하면 그 문장을 바로 "AI로 변형하기"(아래 15-2) 흐름의 프롬프트 화면으로 가져감 (2026-08-27). 상단바 "선택" 버튼 왼쪽의 **돋보기 아이콘**(22번, 2026-09-01)을 탭하면 검색 모드로 전환 — 필터가 "전체"로 리셋되고 타이틀 자리가 하늘색 폼 박스(돋보기 아이콘+입력창+X 취소 버튼)로 바뀌어 한국어/영어 부분일치로 실시간 검색(결과에도 ☆⚑ 아이콘 그대로 유지). 검색을 그만두려면 폼 안의 X를 탭(뒤로가기 화살표는 평소처럼 문장관리 화면 자체를 벗어남). "중요" 또는 "미암기" 필터를 선택하면 필터 개수 안내 아래에 **"이 조건으로 플래시카드 학습"** 버튼(23번, 2026-09-06 도입 → 2026-09-11 라벨 변경)이 나타나 그 조건에 해당하는 문장만으로 카드 학습 화면에 바로 진입(기존 미니 학습 인프라 재사용, "전체" 필터에서는 숨김)
  - **노트형 학습 진입**(25번, 2026-09-11): "전체" 필터에서는 "노트형 학습하기" 버튼 1개, "중요"/"미암기" 필터에서는 위 플래시카드 버튼과 함께 "이 조건으로 노트형 학습" 버튼이 추가로 나타나 완전히 별도 화면(아래 "노트형 학습" 참고)으로 이동
- **노트형 학습**(25번, 2026-09-11, 문장관리에서 진입): 카드나 관리 아이콘 없이 문장을 쭉 스크롤하며 훑어보는 화면. 상단에 "전체 보기/한글만/영어만" 3단 토글(재방문해도 유지됨)이 있고, 텍스트는 글자크기 설정과 연동되며 줄바꿈이 허용돼 문장이 잘리지 않음. **"한글만"/"영어만" 모드에서는 문장을 탭하면 그 문장만 반대쪽 언어가 임시로 보이는 인출연습이 가능**(2026-09-14 보완, 다시 탭하면 원복). 각 항목엔 스피커 아이콘(발음 듣기)만 있고 별표·깃발·AI·체크박스는 없음(관리는 문장관리 화면의 몫). 뒤로가기는 문장관리로 복귀, 홈 버튼은 카드 화면으로 바로 이동. **"자동재생"**(2026-09-16): 화면 우하단 원형 플로팅 버튼으로 핸즈프리 학습 시작(대기시간 스테퍼는 표시모드 토글 아래에 별도 배치) — 목록 순서대로 문장을 강조하며 표시모드에 맞는 언어(전체보기/한글만은 한국어, 영어만은 영어)를 읽어주고, 대기시간(2~10초, `-`/`+`로 조절, 기본 4초)만큼 기다렸다가 정답을 보여주지 않은 채 자동으로 다음 문장으로 넘어감(기억 안 나면 그냥 넘어가는 방식). 마지막 문장에서 자동 정지, 버튼을 다시 누르거나 표시모드를 바꾸거나 화면을 벗어나면 즉시 정지
- **문장변형**: 고정된 10개 문장의 시제·인칭·수·부정문·의문문(문장별 진행형 포함 가능) 변형을 볼 수 있는 화면. 목록(카드형)에서 문장을 탭하면 상세 화면(태그 붙은 변형 목록, 원문과 일치하는 항목은 하늘색으로 강조)으로 이동, 각 항목을 탭하면 한/영이 플립 애니메이션으로 전환됨. 항목마다 있는 + 버튼을 누르면 "선택됨"(브랜드 블루) 상태로만 바뀌고, 화면 하단 "선택한 문장 추가하기 (N개)" 버튼을 눌러야 실제로 내 문장 목록(카드 학습)에 한 번에 추가됨(2026-08-27, 기존 탭-즉시-추가에서 변경). 이미 추가된(기본문장과 겹치는 경우 포함) 항목은 체크 표시로 시작하며 재선택 불가. 원문 자체는 수정/삭제 불가, 고정 콘텐츠(`js/app.js`의 `VARIATION_SENTENCES`)
- **설정**: 아래 설정 화면으로 이동
- **도움말**: 앱 소개 + 카드 화면/문장 추가/문장 관리/설정/개인정보처리방침 사용법을 담은 전체화면 안내로 이동. 5개 대분류를 아코디언으로 접어두고 필요한 항목만 펼쳐 보는 구조 (`reference/help-content-draft.md` 초안 반영, 2026-08-19 스크롤형 문서로 최초 구현 → 2026-08-20 아코디언 구조로 전환 → 2026-08-29 "개인정보처리방침" 5번째 항목 추가)
- (2026-09-08) "처음부터"는 더보기 메뉴에서 제외되고 설정 화면으로 이동됨 — 아래 "설정 화면" 항목 참고. 다른 5개 항목은 모두 "화면 이동"인데 처음부터만 유일하게 "화면 이동 없이 즉시 실행되는 액션"이라 성격이 달라 이질감이 있었음

### 문장관리 — AI로 변형하기 (15-2 무료판, 2026-08-27)
내가 직접 넣은 문장을 AI로 변형해 추가하는 기능. 별도 메뉴가 아니라 문장관리 목록 각 항목의 AI 아이콘으로 진입(위 "문장관리" 항목 참고).
1. 문장관리에서 변형하고 싶은 문장의 AI 아이콘(✦) 탭 → 그 문장 기준 프롬프트 생성 화면으로 이동
2. 앱이 자동 생성한 프롬프트를 "복사하기"로 복사해 평소 쓰는 AI 챗(ChatGPT 등)에 붙여넣기 → 받은 결과를 같은 화면 아래 붙여넣고 "미리보기"
3. 파싱된 변형 문장들을 체크박스로 검토(기본 전체 선택, "전체 선택/해제" 토글)
4. 선택한 것만 "선택한 문장 추가하고 학습하기"로 내 문장 목록에 추가하는 동시에 카드 화면에서 그 문장들만 즉석 학습("미니 학습 중 · N개 · 종료" 배너, 종료하면 평소 카드 덱으로 복귀)

문장변형(15-1)과 달리 고정 콘텐츠가 아니라 매번 다른 결과가 나올 수 있는 사용자 개인화 문장 대상 기능

### 설정 화면
- **글자크기**: 카드 텍스트 크기 5단계 조절(도트+스테퍼, "가" 견본으로 실시간 미리보기)
- **화면모드**: 시스템/라이트/다크 3단 선택. "시스템"은 기기 다크모드 설정을 실시간으로 따라감
- **발음 자동 재생** (24번, 2026-09-11): 기본 꺼짐. 켜면 카드 학습 중 "확인"을 눌러 정답을 공개할 때마다 영어 발음이 자동으로 재생됨(꺼두면 카드의 🔊 아이콘을 탭할 때만 재생)
- **출제/정렬 순서**: 랜덤순/최신순/오래된순/못외운것 먼저/중요한것 먼저/한국어순(가나다순)/학습어순(알파벳순, 26번, 2026-09-14) 중 선택 — 카드 학습 순서와 문장관리 목록 순서 둘 다에 반영됨
- **데이터 관리**: 섹션 맨 위에 저장 공간 안내(문장 개수·진행률 막대바·사용 용량, 화면 진입 시마다 갱신) 표시. 백업 내보내기(TSV 저장 — 크롬/엣지 계열 등 File System Access API 지원 브라우저에서는 저장 위치·파일명을 직접 고르는 대화상자가 뜨고, 미지원 브라우저(iOS 사파리 등)에서는 기존처럼 자동 다운로드, 2026-08-27), 초기화(문장 전체 삭제, 확인창 필수 — 문장 데이터만 삭제되고 다른 설정값은 유지. 되돌리려면 백업 TSV를 "문장추가"로 다시 가져와야 함)
- **클라우드 백업** (2026-08-29, 18번): 로그아웃 상태면 "Google로 로그인" 버튼만 노출. 로그인하면 로그인된 이메일과 함께 **"지금 백업"**(2026-09-14부터 단순 덮어쓰기가 아니라 클라우드를 먼저 불러와 병합 후 저장 — 28번, 아래 참고) · "클라우드에서 복원"(확인창 필수 — 지금 기기의 문장 전체를 클라우드 백업본으로 교체, 파일가져오기의 append와 다르게 완전 교체) · **"클라우드 백업 삭제"**(2026-09-13 신규, 확인창 필수 — 이 기기 문장에는 영향 없이 Firestore의 백업 문서만 삭제) · "로그아웃" 버튼 노출. 위험도가 낮은 순으로 배치(백업 → 복원 → 삭제), 백업/삭제 버튼 옆에는 각각 `circle-help` "?" 버튼이 있어 탭하면 쉬운 말 설명이 뜸. 기존 TSV 백업 내보내기와 별개의 수단(실시간 동기화 아닌 수동 버튼). (2026-09-13~14엔 "클라우드와 병합" 버튼이 별도로 있었으나, "지금 백업"이 항상 병합하도록 바뀌며 중복이 되어 제거됨 — 아래 체크리스트 28번 참고)

---

## 문장 입력 방법 (사용자 가이드용 — 추후 앱 내 설명서로 확장 예정)

> "개인화된 문장 수집"이 핵심가치라 사용자가 직접 문장을 넣는 방법을 정리해둠. 새 입력 경로가 추가될 때마다 이어서 기록

1. **폼으로 하나씩 입력**: "문장추가" > "폼 입력" 탭 → 한국어/영어 입력 후 추가. 문장관리 목록 탭 또는 카드 화면 ✏ 아이콘으로 같은 모달을 수정 모드로 열 수 있음
2. **파일로 여러 개 한 번에 가져오기**: 엑셀/구글시트/메모장 등에서 "한국어 [구분자] 영어" 형태로 표 작성 → CSV/TSV/TXT로 저장 → "문장추가" > "파일 가져오기" → 업로드
   - 구분자 자동 인식: 탭(TSV) → 쉼표(CSV) → 연속 공백 2칸 이상
   - 첫 줄이 "한국어"/"kr"/"korean" 같은 헤더면 자동으로 건너뜀
   - **중복 자동 건너뛰기** (2026-09-15): 이미 있는 문장(한국어+영어 완전일치)이면 추가하지 않고 자동으로 건너뜀 — 같은 파일을 실수로 여러 번 가져와도 중복 생성되지 않음. 결과 메시지에 건너뛴 개수도 함께 안내
3. **백업 파일로 복원/이전**: 설정 > 데이터 관리 > 백업 내보내기로 받은 TSV 파일을 위 2번 "파일 가져오기"로 다시 불러오면 됨
4. **텍스트 붙여넣기로 여러 개 한 번에 가져오기**: "문장추가" > "텍스트 붙여넣기" 탭 → 텍스트 입력 후 "가져오기". 구분자·헤더 규칙은 2번과 동일(같은 파서 재사용), 중복 자동 건너뛰기도 동일하게 적용
5. **PC 빠른입력으로 여러 개 행 단위로 입력** (2026-08-29, 19번): 카드 앱과 분리된 별도 페이지 `input.html`(`tuk-tak-six.vercel.app/input.html`)에서 한국어/학습어 칸이 한 행씩 반복되는 카드 형태에 직접 타이핑(Enter 키로 한국어→학습어→다음 행으로 자동 이동, 마지막 행 입력 시 새 행 자동 추가, 긴 문장은 자동으로 줄바꿈되며 높이가 늘어나 잘리지 않음, 2026-09-12) → 저장 시 클라우드 백업에 병합. **메모장 등에서 미리 작성한 CSV/TSV/TXT 파일을 "파일 가져오기" 버튼으로 불러올 수도 있고, 파일로 안 만들고 텍스트를 바로 복사해서 "텍스트 붙여넣기" 버튼으로 붙여넣을 수도 있음**(파일 가져오기는 2026-09-12, 텍스트 붙여넣기는 2026-09-14 추가 — 카메라로 찍은 문장을 AI가 텍스트로 변환해준 결과처럼 파일로 저장하기 애매한 텍스트에 유용. 둘 다 폰 앱과 같은 파서 재사용, 불러온 문장은 새 행으로 채워지고 검토 후 "변경사항 저장"을 눌러야 반영됨, 이미 클라우드에 있거나 이미 입력해둔 문장과 완전히 같으면 자동으로 건너뜀 — 2026-09-15). **로그인하면 화면 위쪽에 "클라우드에 저장된 문장" 목록도 함께 떠서, 이미 저장된 문장을 이 페이지에서 바로 고치거나 지울 수 있음**(2026-09-13 — 새 문장을 던져 넣기만 하던 페이지에서 기존 문장 조회·수정까지 가능한 관리 도구로 확장). 폰 앱에는 이 페이지로의 링크가 없어 URL로 직접 접속해야 함, 자세한 내용은 아래 체크리스트 19-2 참고(2026-09-08, 문장추가 안의 "빠른입력" 탭(19-1)은 제거되고 이 방식 하나로 정리됨). 파일 준비 없이 브라우저에서 바로 여러 문장을 칠 수 있어 PC 키보드 타이핑 속도를 그대로 살릴 수 있음(폰 타자가 느려 불편하다는 피드백에서 착안)
6. **음성으로 말해서 나중에 문장으로 완성하기** (2026-09-14, 27번): 더보기 메뉴 > "음성 메모"에서 마이크 버튼을 탭해 말하면 텍스트로 변환되어 편집 가능한 미리보기에 채워짐 → "메모로 저장"으로 별도 메모함에 보관. 문장이 갑자기 생각났는데 당장 한국어/영어를 다 정리해서 입력할 여유가 없을 때, 일단 텍스트로만 붙잡아두고 나중에 메모함에서 항목을 탭해 문장추가 폼(한국어 칸에 메모 내용이 미리 채워짐)으로 이어서 완성하는 용도. 인식 언어는 한국어 고정, 실제로 문장이 추가될 때만 메모가 지워짐(완성 전에 취소해도 메모는 남아있음)

---

## 디자인 기획

### 컬러 시스템 (2026-08-18 새 가이드로 교체, 같은 날 `css/style.css`·`js/app.js`에 반영 완료)

> 출처: `reference/tuktak-color-guide.md`. 다크모드 값은 가이드에 없어 잠정 추정치로 구현(아래 Dark Mode 참고, 확정 시 교체 필요)

#### Brand
| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Brand Blue | `--color-brand` | `#378ADD` | 앱 아이콘, 헤더/네비게이션 바, CTA 버튼(확인 버튼 탭 전), 로고 |
| Brand Navy | `--color-brand-dark` | `#0C447C` | 헤더 타이틀, 정답 텍스트, 확인 버튼(탭 후), 진한 배경 위 아이콘 |
| Point Coral | `--color-point` | `#D85A30` | 사용 보류(2026-08-08~). 정답/즐겨찾기 표시는 아래 Accent로 대체 |

#### Surface (신규)
| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Base White | `--color-base` | `#FFFFFF` | 순백이 필요한 표면(카드 등). 브랜드색 배경 위 고정 텍스트/아이콘은 변수 대신 리터럴 `#FFFFFF` 사용 |
| Surface | `--color-surface` | `#F4F6F8` | 기본 화면 배경(쿨톤 오프화이트 — 기존 순백/Gray 50 대체) |
| Surface Dark | `--color-surface-dark` | `#14171C` | 다크모드 배경 |
| Chip Answer | `--color-chip-answer` | `#E6F1FB` | 정답 텍스트 강조용 하이라이트 칩 배경 |

#### Text (신규)
| 역할 | 변수명 | HEX | 대비(on Surface) | 용도 |
|---|---|---|---|---|
| Text Primary | `--color-text-primary` | `#1E2A38` | 13.4:1 | 본문, 정답 텍스트 |
| Text Secondary | `--color-text-secondary` | `#4B5D6B` | 6.4:1 | 보조 설명 텍스트 |
| Text Muted | `--color-text-muted` | `#7C8A96` | 3.3:1 | 캡션/힌트 전용 — 소글씨 본문엔 사용 금지 |

#### Grayscale (값 전면 교체 — 완전 무채색)
| 변수명 | HEX | 비고 |
|---|---|---|
| `--gray-50` | `#F7F7F7` | |
| `--gray-100` | `#ECECEC` | |
| `--gray-200` | `#D9D9D9` | |
| `--gray-300` | `#C2C2C2` | |
| `--gray-400` | `#A6A6A6` | |
| `--gray-500` | `#8A8A8A` | |
| `--gray-600` | `#6B6B6B` | 힌트 텍스트, 이전/다음 버튼 테두리·텍스트 (대비 4.92:1, AA 통과) |
| `--gray-700` | `#545454` | |
| `--gray-800` | `#3C3C3C` | |
| `--gray-900` | `#242424` | |

> 브랜드/배경(둘 다 쿨톤)과 색조가 섞이지 않도록 의도적으로 완전 무채색 유지. 변수명이 `--color-gray-*`가 아닌 `--gray-*`인 점 주의(새 가이드 원문 그대로)

#### Accent — 마킹 전용 (신규, Point Coral 대체)
| 역할 | 변수명 | HEX | 용도 |
|---|---|---|---|
| Accent Star | `--color-accent-star` | `#BA7517` | 별표·깃발 활성 아이콘 |
| Accent Star Bg | `--color-accent-star-bg` | `#FAEEDA` | 활성 아이콘 원형 배경(칩) |

> 깃발을 "복습 표시"가 아닌 "오류 신고"용으로 쓸 경우 신고 접수 상태에만 `#E24B4A`(danger red) 적용 검토 — 미정

#### 컴포넌트 상태 규칙 (신규, 2026-08-18 구현 완료)
- **확인 버튼**: 탭 전 = Brand Blue 채움 / 탭 후 = Brand Navy 채움. 색상 전환만 구현, 체크 아이콘 추가는 아직 안 함(별도 작업)
- **이전/다음 버튼**: 텍스트·테두리 모두 `--gray-600` 통일(아웃라인 스타일, 확인 버튼 대비 위계 낮춤)
- **별표/깃발 아이콘**: 기본 `--gray-600`, 활성 시 `--color-accent-star` + `--color-accent-star-bg` 원형 칩
- `--color-chip-answer`는 아직 적용할 요소가 없어 변수만 선언, 미사용

#### Dark Mode
- 새 가이드에 다크모드 값이 없어 `css/style.css`에 **잠정 추정치**로 구현(2026-08-18) — 기존 2026-08-12 다크모드 매핑(옛 그레이스케일 기준)에서 톤을 이어받아 새 변수명에 맞게 확장한 값. 정식 디자인 확정 시 교체 필요
- 확정된 것: `--color-surface-dark`(`#14171C`)가 다크모드 배경, `--color-brand`/`--color-brand-dark`/`--color-point`/`--color-accent-star`/`--color-accent-star-bg`/`--color-text-muted`는 다크모드에서도 라이트 모드와 같은 값 유지

#### CSS 변수 선언 규칙
- 모든 CSS 변수는 `css/style.css` 상단의 `:root {}` 블록에만 선언
- 다크모드 변수는 `[data-theme="dark"] {}` 셀렉터로 관리
- 변수명 임의 변경 금지

### 타이포그래피
- **기본 폰트**: Pretendard
- **폰트 파일 위치**: `/public/fonts/pretendard/` (woff2, web/static 서브셋, `@font-face`로 `css/style.css` 최상단에 선언 — 2026-09-08 실제 파일 추가 완료. 그 전까지는 선언만 있고 파일이 없어 기기별 시스템 폰트로 대체 렌더링되고 있었고, 이게 로고 등 텍스트가 기기마다 다르게 보이던 근본 원인이었음)
- **사용 웨이트**: 400 Regular · 500 Medium · 600 SemiBold · 700 Bold
- **font-family 선언**: `'Pretendard', -apple-system, BlinkMacSystemFont, sans-serif`

### 아이콘
- **라이브러리**: lucide
- **구현 방식**: npm/CDN 설치 없이 필요한 아이콘만 lucide 공식 SVG를 인라인으로 직접 삽입(오프라인 PWA라 외부 로드 의존 없음)
- **사용 중**: `circle-plus`(문장추가) · `list`(문장관리) · `settings`(설정) · `rotate-ccw`(처음부터) · `circle-help`(도움말, 2026-09-12부터 설정 화면 클라우드 백업 섹션의 `.settings-info-btn` "?" 설명 버튼에도 재사용) · `chevron-left`(뒤로가기) · `chevron-down`(도움말 아코디언 토글) · `upload`(파일 가져오기) · `download`(백업 내보내기) · `trash-2`(삭제·초기화, 2026-09-01부터 문장관리 상단바 "선택" 진입 아이콘에도 재사용) · `ellipsis-vertical`(더보기) · `star`(중요, 카드·문장관리 공용) · `flag`(미암기, 카드·문장관리 공용) · `check`(선택모드 체크박스) · `clipboard-paste`(텍스트 붙여넣기) · `pencil`(카드 화면 수정) · `shuffle`(문장변형 메뉴 아이콘, `circle-plus`/`check`는 문장변형 상세 화면의 "내 문장으로 추가" 버튼에서도 재사용) · `sparkles`(15-2 문장관리 목록 각 항목의 "AI로 변형하기" 아이콘, 2026-08-27 — 최초엔 더보기 메뉴 아이콘이었으나 문장관리 인라인 방식으로 재설계되며 위치 변경) · `house`(2026-08-27, "홈" 버튼 — 문장변형 상세·AI변형 프롬프트/미리보기 등 뒤로가기 2번 이상 눌러야 메인에 닿는 화면에서 한 번에 카드 화면으로 이동) · `x`(2026-08-28, 로컬 백업 리마인더 배너 닫기 버튼) · `cloud-upload`(2026-08-29, 설정 화면 "지금 백업") · `cloud-download`(2026-08-29, 설정 화면 "클라우드에서 복원") · `log-out`(2026-08-29, 설정 화면 "로그아웃") · Google 공식 멀티컬러 "G" 로고(2026-08-29, "Google로 로그인" 버튼 — lucide가 아닌 구글 브랜드 가이드 아이콘) · `search`(2026-09-01, 문장관리 상단바 검색 아이콘, 22번) · `volume-2`(2026-09-11, 카드 마킹 아이콘 바의 "정답 발음 듣기" 스피커 아이콘, 24번) · `mic`(2026-09-14, 더보기 메뉴 "음성 메모" 항목 + 캡처 화면의 녹음 버튼, 27번) · `play`/`pause`(2026-09-16, 노트형 학습 "자동재생" 버튼 — 재생중이면 pause로 전환). ~~`merge`(2026-09-13, "클라우드와 병합" 버튼)~~ — 2026-09-14 그 버튼이 "지금 백업"에 흡수되며 제거, 더 이상 사용 안 함(28번)

### UI 레이아웃 규칙
- **`.screen` 공통 규칙(모든 화면이 공유) — 반드시 `height`, `min-height` 금지** (2026-08-27): 모든 화면(`#start-screen`/`#card-screen`/`#list-screen`/설정/도움말/문장변형/AI변형 등)이 `.screen`(flex column, `height: 100vh`/`100dvh`)을 공유. **`min-height`를 쓰면 안 되는 이유**: 내부 콘텐츠 영역(`flex:1; overflow-y:auto` — `.sentence-list`/`.settings-body`/`.help-body`/`.variation-detail-body`/`.ai-variation-body` 등)이 뷰포트보다 길어질 때, 화면이 `height`로 뷰포트에 고정돼 있어야 그 내부 영역만 스크롤되는데, `min-height`면 화면 자체가 콘텐츠 크기만큼 늘어나며 **페이지 전체가 스크롤**돼버려서 그 스크롤 영역 뒤에 있는 하단 고정 버튼(`.list-bulk-delete-footer`/`.variation-detail-footer`/`.ai-variation-preview-footer` 등)이 화면 밖으로 밀려남. 문장관리 "삭제" 버튼을 하단 고정으로 옮긴 직후 "문장이 기니까 버튼이 너무 아래 있다"는 제보로 발견된 근본 원인 — 새 화면에 하단 고정 버튼을 추가할 때마다 반복될 수 있는 버그이므로 `.screen`은 항상 `height`로 유지할 것
  - **넓은 화면(PC 등) 대응** (2026-08-29): 이전까지 반응형 CSS가 전혀 없어서 PC 브라우저로 열면 `.screen`이 뷰포트 전체 폭으로 늘어나 텍스트박스·버튼이 비정상적으로 커 보이는 문제가 있었음(문장추가 화면에서 특히 심각 — textarea가 화면 끝까지 늘어남). `@media (min-width: 640px)`에서 `.screen`에 `max-width: 480px; margin: 0 auto;` + `box-shadow`(테두리+그림자로 경계 표시)를 적용해 모바일 폭 그대로 가운데 정렬되게 함. 640px 미만(실제 모바일)은 기존과 동일하게 전체 폭 사용 — 회귀 없음(Playwright로 모바일 뷰포트 스크린샷 비교 확인). 이건 유료 기능이 아니라 기존 무료 경험의 버그 수정 — "PC에서 여러 문장을 빠르게 입력하는 전용 UI"(유료 후보로 논의 중)와는 별개
- **화면 콘텐츠 좌우 여백 16px로 통일** (2026-09-15): 음성 메모 항목의 좌우 마진이 비대칭(왼쪽 0/오른쪽 16, 삭제된 "저장된 메모" 타이틀에 맞추려던 흔적)인 것을 사용자가 발견해 질문한 데서 시작 — 조사 결과 화면마다 콘텐츠 영역 좌우 패딩이 **16px**(문장관리 `.sentence-list`, 노트형 학습 `.note-study-body`, 음성 메모 `.voice-memo-body`, AI변형 미리보기는 `.sentence-list` 재사용)와 **20px**(문장추가 `.add-body`, 설정 `.settings-body`, 도움말 `.help-body`, 문장변형 목록 `.variation-card-list`·상세 `.variation-detail-body`, AI변형 프롬프트 `.ai-variation-body`) 둘로 나뉘어 있었음. "내용이 많으면 16, 적으면 20" 같은 의도된 원칙이 있는지 확인했으나 문장변형 상세(항목이 최대 9개로 많은데도 20px)가 반례라 **의도된 규칙이 아니라 화면마다 다른 시점에 개별 구현되며 생긴 우연한 불일치**로 결론 → 사용자가 "16으로 통일해줘" 승인(헤더 `.screen-topbar`는 이미 전체가 20px로 통일돼 있어 이번 범위에서 제외, 카드 화면 전용 `.card-topbar`의 24px도 범위 밖). `css/style.css`에서 20px였던 6곳(`.add-body`/`.settings-body`/`.help-body`/`.variation-detail-body`/`.variation-card-list`/`.ai-variation-body`)을 16px로 통일. 덤으로 노트형 학습의 `.list-note-intro`/`.list-display-mode-bar`가 body(16px) 위에 자체 20px를 더 얹어 목록 항목(16+16=32px 인셋)과 어긋나 있던 것(36px)도 16px로 낮춰 같은 화면 안에서 32px로 통일. 음성 메모 항목(`.voice-memo-item`)의 왼쪽 패딩도 0→16px로 복원해 문장관리 항목(`.sentence-list-item`)과 같은 좌우 대칭 구조로 되돌림(그 결정의 근거였던 "저장된 메모" 타이틀이 이미 삭제된 상태였음). Claude 1차 확인(Playwright, 설정/도움말/문장추가/문장변형 목록·상세/노트형학습/음성메모 각 화면 진입 후 `getComputedStyle`로 패딩 16px 일치 확인+스크린샷): 전 화면 좌우 여백이 정확히 16px로 통일된 것, 노트형 학습의 인트로·토글바·목록이 같은 세로선에서 시작하는 것, 음성 메모 항목이 좌우 대칭으로 복원된 것, 텍스트 잘림·오버플로우 없는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **카드형 항목의 텍스트 인셋 20px로 축소(2026-09-16, 후속 보완)**: 노트형 학습 자동재생 작업 중 사용자가 "노트형학습하기 버튼은 더 넓고, 음성메모의 '문장이 떠오르면..' 도 더 넓은데 이유는"이라고 문의 — 위 16px 통일 작업으로 화면 좌우 "바깥" 여백은 전부 16px로 맞춰졌지만, **카드형 항목**(`.sentence-list-item`)은 그 16px 안에 카드 자신의 내부 패딩(16px)이 한 번 더 있어 텍스트가 32px 지점에서 시작하는 반면, 카드가 아닌 일반 버튼·안내문(노트형학습하기 버튼, 음성메모 안내문 등)은 내부 패딩이 없어 16px 지점에서 바로 시작 — 같은 16px 기준을 공유해도 "카드 바깥 테두리 16px"과 "카드 안쪽 텍스트 32px"라는 서로 다른 두 기준이 화면마다 섞여 있었던 것. 사용자가 "시각적으로 16px 또는 20px 정도로 맞출 수 있는지, 텍스트 줄이 너무 짧아지진 않았으면 한다" 요청 → **카드의 바깥 테두리는 그대로 16px 유지**(다른 요소들과 이미 일치), **카드 자신의 내부 좌우 패딩을 16px→4px로 줄여 텍스트 인셋을 32px→20px로 축소**하는 방식 채택 — 바깥 테두리를 안쪽으로 밀지 않고 카드 내부 패딩만 줄이는 방식이라, 텍스트가 쓸 수 있는 가로 폭이 오히려 넓어져(양쪽 12px씩, 총 24px 추가) 사용자가 우려한 "줄바꿈이 늘어 텍스트가 짧아 보이는" 문제와는 반대로 개선됨. `css/style.css`의 `.sentence-list-item`(문장관리 목록+AI변형(15-2) 미리보기가 이 클래스를 공유해 함께 적용됨) `padding: 14px 16px` → `padding: 14px 4px`로 변경. 노트형 학습(`.note-study-item`)·음성 메모(`.voice-memo-item`) 목록은 이번 범위에서 제외(사용자가 "먼저 노트형 학습하기 아래의 목록들"로 범위를 문장관리 목록으로 한정, 나머지는 다음에 이어갈 수 있음). Claude 1차 확인(Playwright, 긴 문장+짧은 문장 혼합 시드, 라이트/다크+선택모드 스크린샷+`getBoundingClientRect()`로 카드 바깥 16px·텍스트 약 20px(테두리 1.5px 포함 21px) 확인): 카드 바깥 테두리가 여전히 16px로 다른 화면 요소와 일치하는 것, 텍스트가 카드 가장자리에 눌리지 않고 선택모드 체크박스·마킹 아이콘(별표/깃발/AI)도 여유 있게 배치되는 것, 긴 문장이 잘리지 않고 말줄임표로 정상 처리되는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-16, "확인완료")**
  - **노트형 학습 목록도 같은 방식으로 통일(같은 날)**: "노트형 학습 화면에서 상단의 버튼은 그대로 두고 역시 텍스트가 써지는 영역만 지금과 같이 통일시켜줘" 요청 — 표시모드 토글(`#note-display-mode-bar`)·대기시간 스테퍼(`.note-autoplay-bar`)는 손대지 않고(사용자가 "상단 버튼은 그대로 두고"로 명시), 문장이 표시되는 `.note-study-item`만 `.sentence-list-item`과 동일한 방식으로 `padding: 14px 16px` → `padding: 14px 4px`로 변경(카드 바깥 테두리 16px 유지, 텍스트 인셋만 32px→20px로 축소). 자동재생 중 강조 표시(`.note-study-item.auto-current`, 좌측 3px 인셋 보더)도 패딩이 줄어든 채로 확인 — 텍스트와 너무 붙지 않고 정상적으로 보임. `css/style.css`만 수정. Claude 1차 확인(Playwright, 기존 자동재생 테스트 스위트 33개 재실행 — 전부 통과, 이 CSS 변경이 자동재생 로직에 영향 없음을 확인) + 라이트/다크, 평소/재생중(강조 표시 포함) 스크린샷으로 레이아웃 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-16, "확인완료")**
  - **한국어 텍스트 색상을 메인 카드와 통일(같은 날)**: 사용자가 "노트형학습의 글자가 메인화면의 글자보다 커보이는 것은 색상때문이야 굵기 때문이야?"라고 질문 → 조사 결과 `font-size`/`font-weight`(600)는 이미 동일하고 `color`만 다름(메인 카드 기본 상태 `.kr-text`는 `--color-text-secondary`, 노트형 학습 `.note-study-kr`은 `--color-text-primary`로 더 진한 색) — 색상 때문이라고 답변 → "메인과 통일시켜줘" 요청으로 `.note-study-kr`의 `color`를 `--color-text-secondary`로 변경. `.note-study-en`은 범위 밖(메인 카드의 `.en-text`는 하이라이트 칩 배경 위 고정 색상을 쓰는 완전히 다른 컴포넌트라 비교 대상이 아니라고 판단, 이번 요청은 한국어 텍스트 비교에서 나온 것으로 한정 해석). `css/style.css`만 수정. Claude 1차 확인(Playwright, 기존 자동재생 테스트 스위트 33개 재실행 — 전부 통과) + 라이트 스크린샷으로 색상 변경 반영 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-16, "확인완료")** — 이로써 노트형 학습 화면의 텍스트 인셋·색상 통일 작업 전체 완료
- **앱 전체 디자인 일관성 점검** (2026-09-15): "큰 작업(19번 PC 빠른입력 남은 재검토)에 신경쓰기 전에 앱 전체를 다듬고 싶다"는 요청 — Claude가 먼저 전체 14개 화면(라이트/다크)을 스크린샷+CSS로 점검해 발견한 불일치를 목록으로 보고한 뒤, 사용자가 4개 중 3개(정렬 통일/색상 대비/4px 여백 정렬) 선택해 수정. 나머지 1개(카드 이전/확인/다음 버튼만 20px 둥글기, 다른 화면 버튼은 12px)는 "오늘 의도적으로 바꾼 것이라 버그는 아님"으로 판단해 보류
  - **안내 문구 정렬 통일**: 문장변형 목록·음성메모 안내문은 왼쪽 정렬인데 도움말 소개문·노트형 학습 안내문은 가운데 정렬로 남아있던 것을 발견 → 최근 세션 방향(왼쪽 정렬)에 맞춰 `.help-intro`/`.list-note-intro`도 `text-align: left`로 통일
  - **카드 정답 확인 후 한국어 텍스트 색상 대비 위반 수정**: `.card.revealed .kr-text`가 컬러 가이드 자체가 "소글씨 본문엔 사용 금지"로 명시한 `--color-text-muted`(대비 3.3:1)를 실제 문장 내용에 쓰고 있던 것을 발견 → `--color-text-secondary`(대비 6.4:1)로 교체
  - **목록 위 요소들의 4px 여백 미스얼라인먼트 수정**: 앞서 "화면 콘텐츠 좌우 여백 16px 통일" 작업(바로 아래 참고) 때 노트형 학습은 고쳤지만 놓친 3곳을 추가로 발견 — 문장관리 필터탭(`.list-filter-bar`)·문장개수 안내(`.list-count-info`)·학습 진입 버튼(`.list-study-entry-btns`), 문장변형 목록 안내문(`.variation-list-intro`), AI변형 미리보기 툴바(`.ai-variation-toolbar`)가 여전히 자체 20px 여백을 가지고 있어 그 아래 16px로 통일된 목록과 4px 어긋나 있던 것을 16px로 맞춤
  - `css/style.css`만 수정. Claude 1차 확인(Playwright, 390px 좁은 화면): 정답 확인 후 한국어 텍스트 색상이 정확히 `--color-text-secondary`(`rgb(75,93,107)`)인 것 확인, 도움말·노트형학습 인트로 `text-align`이 `left`로 반영된 것 확인, 문장관리 노트형학습 버튼과 목록 항목의 좌측 좌표가 정확히 16px로 일치하는 것 확인, 문장변형 인트로 텍스트 시작 위치와 카드 좌측 좌표가 정확히 16px로 일치하는 것 확인, 스크린샷으로 전체 레이아웃 확인, 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
- **카드 화면 상단바**: 화면 좌우 끝까지 꽉 찬 바에 왼쪽 로고, 오른쪽 더보기(⋮). **헤더 무채색 전환**(2026-09-15, 아래 참고)으로 배경은 `--color-surface`+하단 구분선, 로고("TUK TAK")만 브랜드 블루 유지, 더보기 아이콘은 회색(`--gray-600`). **로고 클릭**(2026-09-06): 로고는 카드 화면(메인)에만 있어 평소 클릭은 아무 동작 없음(이미 메인이므로) — 미니 학습 중(15-2/23번)일 때만 기존 "종료" 버튼과 동일하게 전체 덱으로 돌아가는 동작 연결(`#topbar-logo-btn`)
- **더보기 드롭다운**: 문장추가·문장관리·설정 3항목만 플랫하게 배치
- **전체화면(문장관리/설정/도움말) 공용 헤더**: 왼쪽 뒤로가기(`chevron-left`) + 가운데 타이틀. `.screen-topbar`/`.screen-back-btn`/`.screen-topbar-title` 클래스를 세 화면이 공유. 뒤로가기 화살표와 제목 사이 간격(`gap`)은 8px → 14px로 확대(2026-08-27, "화살표+제목이 붙어 있어 마치 그 이름의 화면으로 이동하는 버튼처럼 보인다"는 피드백 — 모든 화면이 `.screen-topbar`를 공유하므로 한 번에 전체 반영됨)
  - **헤더 무채색 전환** (2026-09-15): 사용자가 "화면에 파란색이 너무 많다"고 지적(마이크 아이콘 색상 질문에서 시작된 논의) — 헤더가 거의 모든 화면에서 반복 노출되는 가장 큰 색면이라 이게 핵심 원인이라고 진단, 브랜드 컬러 일반 사용 관례(화면당 주 CTA·활성 상태 등 "신호"로만 아껴 쓰는 것)를 설명 후 "브랜드 인지가 중요하진 않지만 촌스럽지 않고 편했으면 좋겠다"는 목표 확인 → 헤더 배경을 순백 대신(순백은 눈이 피곤할 수 있다는 우려로) 화면 배경과 같은 무채색 `--color-surface`로 전환(다크모드는 `--color-surface-dark`로 이미 테마 대응됨), 하단에 얇은 구분선(`border-bottom: 1px solid var(--gray-200)`)만 추가. `.card-topbar`/`.screen-topbar` 공통 적용. 로고("TUK TAK")만 브랜드 블루(`--color-brand`) 유지 — 흰 배경 위 컬러 워드마크가 오히려 더 또렷한 브랜드 표식 역할(Slack/Notion 등의 흰 헤더+컬러 로고 패턴과 동일). 뒤로가기(`.screen-back-btn`)·홈(`.screen-home-btn`)·검색/선택 아이콘(`.screen-topbar-icon-btn`)·더보기(`.topbar-more-btn`) 등 유틸리티 아이콘은 전부 기본 회색(`--gray-600`)으로 전환(카드 화면 마킹 아이콘 바가 이미 따르던 "기본 회색, 활성 시에만 강조색" 원칙을 헤더에도 확장 적용), 타이틀(`.screen-topbar-title`)은 `--color-text-primary`. 문장관리 **선택 모드**는 기존엔 헤더 배경 자체가 브랜드 네이비로 바뀌었으나, 헤더가 무채색이 된 이후엔 배경은 그대로 두고 "취소"(`.screen-back-btn`)·"N개 선택"(`.screen-topbar-title`) **텍스트 색만** 브랜드 네이비로 바꿔 모드 전환을 표시(`.screen-topbar.selecting` 스코프 규칙) — 다크모드에서는 네이비가 어두운 배경 위에서 시인성이 떨어지는 기존 문제(`.stepper-btn`/`.help-accordion-header`와 동일 패턴)가 있어 다크모드 전용 예외 규칙에 이 요소들도 추가해 `--color-text-primary`(밝은 색)로 전환. "전체 선택" 텍스트 버튼(`.screen-topbar-text-btn`)도 같은 네이비/다크모드 전환 규칙 적용. `css/style.css`만 수정. 시작 화면(`.start-logo`)과 카드 화면 CTA 버튼(확인/제출 등)은 범위 밖 — 브랜드 블루 그대로 유지(단일 주 CTA·인트로 화면은 컬러 유지가 합리적이라는 원칙에 따름). PWA 상태바 색(`theme-color`, 네이비)도 이번 범위에서 제외하고 그대로 유지(헤더가 무채색이 돼도 앱을 열 때 상태바에서 브랜드 컬러가 살짝 보이는 정도는 남겨두기로 함). Claude 1차 확인(Playwright, 스크래치패드 정적 서버 재사용, 라이트/다크 각각 카드·문장관리·설정·검색모드·선택모드 스크린샷+`getComputedStyle`로 배경/텍스트 색 hex 검증): 카드·문장관리·설정 헤더 배경이 정확히 `--color-surface`(라이트 `rgb(244,246,248)`/다크 `rgb(20,23,28)`)인 것, 로고는 라이트·다크 모두 브랜드 블루 유지, 뒤로가기·더보기 아이콘이 회색인 것, 선택 모드에서 배경은 불변인 채 "취소"/"N개 선택" 텍스트만 라이트는 네이비(`rgb(12,68,124)`)·다크는 밝은 텍스트(`rgb(231,234,238)`)로 정확히 전환되는 것, 검색 모드 폼 박스(하늘색 chip-answer)가 무채색 헤더 위에서도 자연스럽게 도드라져 보이는 것 확인 → 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
    - **로고 자간 조정 (같은 날)**: 사용자가 로고 "TUK TAK"의 폰트웨이트(700, Pretendard Bold — `public/fonts/pretendard/`에 실제로 받아둔 4개 웨이트 중 최대치)·자간(기존 0.5px)을 물은 뒤 "자간 -1px로 조정" 요청 → `.topbar-logo`의 `letter-spacing`을 `0.5px`→`-1px`로 변경(글자 간격이 좁아짐). Claude 1차 확인(Playwright, `getComputedStyle`+스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
    - **뒤로가기 아이콘 확대 (같은 날)**: "헤더의 < 아이콘 크기가 지금 얼마야?"(버튼 32×32px, SVG 22×22px) 질문 후 "svg아이콘크기를 32픽셀로 조정" 요청 → `.screen-back-btn svg`를 22px→32px로 확대(버튼 클릭 영역과 같은 크기가 되어 아이콘이 버튼을 꽉 채움). `.screen-topbar`/`.card-topbar`를 공유하는 모든 화면(문장관리·설정·도움말·노트형학습·음성메모·문장변형 등)에 공통 반영. Claude 1차 확인(Playwright, `getComputedStyle`+스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
    - **선 두께 조정 (같은 날)**: "선두께는?" 질문에 SVG `stroke-width` 속성값 2(viewBox 24×24 기준, 32px로 렌더링 시 실제 화면 두께 약 2.67px)라고 답변 → "1.8픽셀" 요청 — 렌더 두께 = `stroke-width` × (렌더크기/24) 공식으로 역산(1.8 = x × 32/24 → x = 1.35), `index.html`에 10곳 반복되던 뒤로가기 아이콘 SVG(`m15 18-6-6 6-6` path, lucide chevron-left)의 `stroke-width="2"`를 전부 `stroke-width="1.35"`로 일괄 변경. Claude 1차 확인(Playwright, `stroke-width` 속성값+렌더 크기로 실제 두께 재계산해 정확히 1.80px 확인, 다른 화면(노트형학습)도 동일 적용 확인, 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **제목 정가운데 고정** (2026-08-27): "화면이름은 보통 정가운데 아니야?"라는 피드백으로, 기존 flex 흐름(화살표 뒤에 이어 붙는 방식이라 우측 버튼 유무·폭에 따라 제목 위치가 화면마다 미묘하게 달랐음)을 버리고 `.screen-topbar-title`을 `position:absolute; left:0; right:0; text-align:center;`로 전환 — 좌우에 어떤 버튼이 있든(문장관리의 "선택" 버튼, 문장변형 상세/AI변형의 "홈" 버튼, 아무것도 없는 화면 등) 항상 상단바 전체 폭 기준 정가운데에 옴. `pointer-events:none`으로 좌우 버튼 클릭을 방해하지 않도록 처리, `.screen-topbar`엔 `position:relative` 추가
  - **"홈" 버튼** (2026-08-27): 뒤로가기를 2번 이상 눌러야 메인(카드) 화면에 닿는 화면(문장변형 상세, AI 변형 프롬프트, AI 변형 미리보기)에만 `.screen-home-btn`(`house` 아이콘, 헤더 우측 `margin-left:auto`)을 추가 — 탭하면 `goToCardScreen()`으로 모든 `.screen`을 숨기고 카드 화면만 바로 노출, 중간 화면들의 뒤로가기 체인은 그대로 둔 채 지름길만 제공. 문장관리·설정·도움말·문장변형 목록처럼 뒤로가기 1번으로 이미 메인에 닿는 화면에는 넣지 않기로 확정(사용자가 "둘 이상 이동해야 하는 화면만" 선택)
- **도움말 화면**: 대분류 5개(카드 학습 화면/문장 추가하기/문장 관리하기/설정/개인정보처리방침 — 마지막 항목은 2026-08-29 18번 클라우드 백업 개인정보처리방침 고지용으로 신규 추가)를 아코디언(`.help-accordion`/`.help-accordion-item`/`.help-accordion-header`/`.help-accordion-content`)으로 감싼 구조. 진입 시 5개 모두 접힌 상태(`hidden`)이며 헤더 클릭 시 해당 항목만 독립적으로 펼침/접힘 토글, 화살표 아이콘(`chevron-down`)이 펼침 상태에서 180도 회전(2026-08-20, 이전의 긴 스크롤형 단일 문서에서 전환). 대분류 안쪽은 기존처럼 하위 섹션(`h3.help-subsection-title`)·목록(`.help-list`)·예시 코드 블록(`.help-example`)으로 구성. 콘텐츠는 `reference/help-content-draft.md`를 그대로 옮긴 것이 출발점(이후 세부 문구를 다듬던 별도 초안 텍스트 파일은 2026-08-31 reference 폴더 정리 시 삭제됨) — 문구를 바꿀 땐 화면 쪽 HTML을 직접 갱신할 것(자동 동기화 아님). 상단 앱 소개 문구(`.help-intro`)는 2026-08-21 삭제 — 추후 앱 시작화면 쪽에 넣을 예정(CSS 클래스는 재사용 가능성 있어 보존)
  - **아코디언 시각 스타일** (2026-08-20 도입, 2026-08-21 배경색 조정): 카드형(테두리 박스)이 아닌 구분선형 — `.help-accordion` 상단 구분선 + 각 `.help-accordion-item` 하단 구분선만 사용. 제목(`.help-accordion-header`)은 18px/bold/`--color-brand-dark`(다크모드에서는 대비 문제로 `--color-text-primary`로 전환, `.stepper-btn`과 같은 다크모드 예외 규칙 공유). 본문(`.help-body p`/`.help-list`)은 16px, 펼친 콘텐츠 영역(`.help-accordion-content`)은 라이트모드에서 `--color-base`(흰색) 배경 + `--gray-200` 테두리 1px(2026-08-21, 기존 `--gray-100` 회색 배경이 화면을 무겁게 보이게 한다는 피드백으로 교체), 다크모드는 기존대로 `--gray-100`(어두운 배경) 유지·테두리 없음
  - **"텍스트 파일 만드는 법" 상세**: `<h3>`이 아니라 시맨틱 `<details>`/`<summary class="help-subsection-title">`로 구현(클릭 시에만 펼쳐짐, 기본 마커는 숨기고 ▸/▾ 커스텀 표시). "3. 파일 가져오기" `<li>` 안에 중첩시켜 인덱트를 그 항목 텍스트보다 왼쪽으로 나가지 않게 함. 내부 텍스트는 전부 14px로 통일(`.help-body .help-details p`/`.help-list`/`.help-note-inline`/`.help-example` — 셀렉터 특정도를 `.help-body p` 등 상위 규칙보다 높여야 실제로 적용됨). 전체에 왼쪽 1px 브랜드 블루 선을 둘러 "상세 정보" 블록임을 표시. 내부 소제목 3개(`.help-mini-title`: 메모장으로 만들기/엑셀·구글시트로 만들기/공통 주의사항)는 위 내용과 20px 간격을 두고 브랜드 블루 점(●, `::before`)을 붙임
  - **문장관리 선택 모드**: "선택" 아이콘(`trash-2`, 2026-09-01부터 텍스트 버튼 대신 아이콘)을 누르면 헤더가 브랜드 네이비로 전환, 뒤로가기 아이콘이 "취소" 텍스트로, 타이틀이 "N개 선택"으로 바뀜. 목록 항목도 체크박스로 전환
    - **하단 고정 삭제 버튼** (2026-08-27): "삭제" 버튼은 처음엔 헤더 우측 구석(취소 옆)에 작은 텍스트 버튼으로 있었으나, 사용자가 "취소와 삭제 버튼 위치가 애매해서 사용하기 불편하다"고 지적 → 15-1/15-2에서 이미 검증된 "하단 고정 액션 버튼" 패턴(`.variation-detail-footer`/`.ai-variation-preview-footer`와 같은 개념)을 재사용해 화면 하단 전체 너비 버튼(`#list-bulk-delete-footer`, `.btn.btn-outline.modal-btn`)으로 이동 — 엄지로 누르기 쉬운 위치+훨씬 큰 탭 영역으로 개선. "취소"는 일반적인 뒤로가기 위치라 그대로 헤더 좌측 유지(사용자가 두 버튼 중 삭제만 옮기는 쪽으로 확정). 선택 개수를 "삭제 (N개)"로 버튼 라벨에 표시, 0개 선택 시 `disabled`(기존 `.btn-outline:disabled` 스타일 재사용, 별도 CSS 불필요)
    - **전체 선택/해제 버튼** (2026-09-12): 체크박스를 하나씩 누르는 게 번거롭다는 이유로 헤더 우측(취소 옆, "N개 선택" 타이틀 반대편)에 `#list-select-all-btn` 텍스트 버튼 신규 — 검색/선택 아이콘과 자리를 공유(선택 모드일 때만 노출, 평소엔 숨김이라 동시에 안 보임). 탭하면 **현재 필터로 보이는 문장 기준**(전체/중요/미암기 중 진입 시점의 필터 그대로 유지됨)으로 전부 선택하고, 이미 전부 선택된 상태면 라벨이 "전체 해제"로 바뀌며 탭 시 전부 해제. 새 CSS 클래스 `.screen-topbar-text-btn`(`.screen-back-btn.text-mode`와 톤 통일)은 검색 아이콘의 `.screen-topbar-actions-lead`(auto-margin 균등분배 버그, 22번에서 발견)와 달리 선택 모드에서 항상 유일한 우측 요소라 `margin-left:auto`를 단독으로 써도 안전
- **설정 화면 구성**: 글자크기 · 화면모드(3단 세그먼트) · "발음 자동 재생" 토글(24번, 2026-09-11) · 출제/정렬 순서(세로 라디오 목록, `.sort-option-list`/`.sort-option-btn` + 그 아래 "처음 문장부터 다시 보기" 버튼) · "기본문장 관리" 섹션(숨기기 토글·기본문장 삭제) · "데이터 관리" 섹션(저장 공간 안내 · 백업 내보내기 · 초기화) · "클라우드 백업" 섹션(계정/백업·복원, 2026-08-29) 순으로 배치
  - **"처음 문장부터 다시 보기" 버튼** (2026-09-08, `#restart-settings-btn`): 원래 더보기 메뉴의 "처음부터" 항목이었으나, 다른 더보기 항목(문장추가/문장관리/문장변형/설정/도움말)은 전부 "화면 이동"인데 이것만 "화면 이동 없이 즉시 실행"이라 성격이 달라 이질감이 있다는 논의로 이동. 정렬 순서(랜덤/최신/오래된 등)와 "카드가 어떤 순서로 보이는가"라는 맥락은 공유하지만, 정렬 순서는 계속 유지되는 상태값(라디오)인 반면 이건 누르는 순간 한 번 실행되는 동작이라 같은 라디오 목록에 섞지 않고 그 아래 별도 버튼(`.settings-action-btn` 재사용)으로 배치. 클릭 시 `goToSentence(0)`으로 첫 문장 위치로 되돌리고 `goToCardScreen()`으로 카드 화면까지 함께 이동(설정 화면 안에서 눌러도 바로 결과를 보도록). 라벨도 "처음부터"에서 "처음 문장부터 다시 보기"로 더 설명적으로 변경(이어보기 기능을 모르면 "처음부터"의 의미가 와닿지 않는다는 지적 반영)
  - **클라우드 백업 섹션** (2026-08-29, 18번): 로그아웃 상태(`#cloud-signed-out-view`)면 안내 문구 + "Google로 로그인" 버튼만 노출. 로그인 상태(`#cloud-signed-in-view`)면 로그인된 이메일 + "지금 백업"/"클라우드에서 복원"/**"클라우드 백업 삭제"**(2026-09-13 신규)/"로그아웃" 4개 버튼 노출(전부 `.cloud-account-view`, 하나만 `.hidden` 토글. 2026-09-13~14엔 "클라우드와 병합" 버튼도 있었으나 28번으로 "지금 백업"에 흡수돼 제거됨). 버튼 스타일은 기존 `.settings-action-btn`(아이콘+라벨 가로 배치) 그대로 재사용해 데이터 관리 섹션의 버튼들과 시각적으로 통일 — 백업은 비파괴적이라 기본(브랜드 블루) 스타일, 복원/삭제는 되돌릴 수 없는 동작이라 `.settings-action-btn-warning`(accent-star 호박색). 백업/삭제 버튼 옆엔 `.settings-info-btn`(`circle-help` "?" 아이콘) — 탭하면 `alert()`로 쉬운 말 설명. Firebase SDK 자체는 `js/firebase-init.js`(신규, `type="module"`)에 격리하고 `js/app.js`는 `window.CloudSync`를 통해서만 접근(아래 "기술 스택" 참고)
    - **버튼 길이 통일 + "?" 아이콘 단순화 1차 시도** (2026-09-15): "설정화면의 클라우드 백업 아이콘들을 길이가 동일하게 만들어줘. 그리고 오른쪽의 물음표 아이콘은 원형물음표 아이콘만 버튼안에 넣어줘"라는 요청 — 백업/복원/삭제 3개 버튼이 각자 `.settings-action-row` 안에서 `width: auto`(내용 길이만큼만 차지)라 라벨 글자 수에 따라 서로 길이가 달랐던 것을 `flex: 1` 추가로 셋 다 동일한 길이로 맞춤(이 시점엔 로그아웃과는 아직 다른 길이). `.settings-info-btn`은 테두리·둥근 사각형·고정폭을 제거해 아이콘만 남도록 단순화. Claude 1차 확인(Playwright) 완료 → 사용자가 "한번 결과보고 다시 수정할께"라며 확인 후 아래 2차 수정 요청
    - **재설계 — "?"를 버튼 안으로 통합 + 로그아웃과 완전히 같은 길이** (같은 날): "물음표를 글자 뒤에 위치 시키고(버튼안) 버튼은 로그아웃과 동일하게 모두 길이 맞추어줘" 요청 — 1차 시도(액션 버튼+"?" 버튼이 나란한 두 개의 시각적 버튼)로는 부족해서, "?"가 라벨 글자 바로 뒤에 **같은 버튼 테두리 안**에 들어가 있고 전체 폭도 로그아웃과 완전히 같아지도록 구조를 다시 짬. `<button>` 안에 `<button>`을 중첩할 수 없는 HTML 제약 때문에, 시각적 버튼 껍데기(테두리·배경·둥근모서리·전체폭)는 새 `.settings-action-group` `<div>`가 담당하고, 그 안에 액션 버튼(`.settings-action-main`, 아이콘+라벨, 테두리·배경 없음)과 설명 버튼(`.settings-info-btn`, 그대로 재사용)이 나란한 별개의 `<button>` 두 개로 들어가 시각적으로는 하나의 버튼처럼 보이게 함. 버튼 ID는 그대로 유지해 `js/app.js`의 기존 클릭 핸들러는 전혀 수정할 필요 없음(HTML 구조와 `css/style.css`만 변경). 눌림 효과(기존 `.btn-outline:active`의 배경 반전)는 `.settings-action-group:has(:active)`로 재현 — 두 내부 버튼 중 어느 쪽을 누르든 그룹 전체 배경이 반전됨. 경고색 변형은 `.settings-action-btn-warning`(버튼 단위) → `.settings-action-group-warning`(그룹 단위)로 이전. `index.html`(백업/복원/삭제 3개 행의 마크업 재구성)+`css/style.css`(새 `.settings-action-group`/`.settings-action-main` 규칙, 기존 `.settings-action-row`/`.settings-action-btn-warning` 대체) 수정. Claude 1차 확인(Playwright, `CloudSync` 모킹): 백업/복원/삭제/로그아웃 4개 버튼의 `getBoundingClientRect().width`가 정확히 동일(448px)한 것 확인 → "?" 아이콘이 라벨 텍스트 오른쪽 끝에서 정확히 8px(그룹의 `gap`)만 떨어져 바로 뒤에 붙어있는 것 확인 → 마우스 다운 상태에서 그룹 배경이 브랜드 블루로 반전되는 것(`:has()` 정상 동작) 확인 → "?" 버튼을 클릭해도 메인 액션(예: "지금 백업"의 `CloudSync.restore()` 호출)이 전혀 실행되지 않고 설명 alert만 뜨는 것(두 버튼이 완전히 독립적으로 동작) 확인 → 라이트/다크 스크린샷으로 레이아웃 확인 → 콘솔 에러 없음(gstatic 차단 에러 제외) → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
    - **버튼 이름·"?" 설명 문구 재작성** (같은 날): 사용자가 세 버튼의 새 이름+설명 초안을 직접 작성해 검수 요청("클라우드에 백업"/"클라우드의 문장으로 복원"/"클라우드의 백업 삭제" + 각 설명). Claude 검수 결과 2가지 지적: (1) 백업 설명의 "웹에서 입력한 문장과 합쳐서"는 부정확 — 실제 병합 대상은 "웹(`input.html`)"으로 한정되지 않고 **클라우드에 이미 저장된 모든 것**(다른 폰 등 포함, 기존 코드 주석 "다른 기기에서만 추가/수정한 문장"과 일치)이라 "클라우드에 이미 저장된 문장"으로 정정 필요 (2) "클라우드에"(백업)와 "클라우드의"(복원/삭제) 조사가 버튼마다 달라 통일 필요. 사용자가 "1번 문구 수정하고 2번처럼 통일하게 진행"으로 승인, 조사 통일 방식은 Claude 판단에 위임 → **최종 결정**: 라벨 1(백업, 목적지를 향하는 동작)은 "에" 유지, 라벨 2·3(복원·삭제, 클라우드에 있는 대상을 다루는 동작)은 "의"를 빼고 명사를 바로 붙이는 방식으로 통일 — 최종 라벨 **"클라우드에 백업"/"클라우드 문장으로 복원"/"클라우드 백업 삭제"**(마지막은 기존 라벨과 동일해 실제 변경 없음). "?" 설명은 사용자가 쓴 문구를 거의 그대로 반영하되 1번만 수정 문구로 교체. 버튼 이름이 바뀌며 그 이름을 인용하던 "클라우드에서 복원" 확인창(`confirm()`) 문구의 "지금 백업" 인용도 "클라우드에 백업"으로 함께 갱신(안 그러면 화면에 없는 옛 버튼 이름을 가리키게 됨), `aria-label`도 새 라벨에 맞춰 갱신. `index.html`(라벨 2곳+aria-label 2곳)+`js/app.js`(설명 alert 3개+확인 confirm 1개 문구) 수정. Claude 1차 확인(Playwright, 라이트/다크+390px 좁은 화면): 라벨 3개 정확히 반영 확인 → **길어진 라벨("클라우드 문장으로 복원")에도 390px에서 가로 오버플로우 없는 것**(`scrollWidth === clientWidth`) 확인 → "?" 3개 alert 문구+복원 confirm 문구 전부 정확히 새 문구로 교체된 것 확인 → 스크린샷으로 레이아웃 확인 → 콘솔 에러 없음(gstatic 차단 에러 제외) → **사용자 실기기 2차 확인 완료(2026-09-15, "확인 완료")**
  - **저장 공간 안내** (2026-08-23): "데이터 관리" 섹션 맨 위에 배치(`#storage-usage-info`). 문장 개수 텍스트(`.storage-usage-count`) + 진행률 막대바(`.storage-usage-bar-track`/`.storage-usage-bar-fill`) + 용량 텍스트(`.storage-usage-detail`, 우측 정렬) 3단 구성. 문장 데이터를 `Blob` 크기로 계산해 KB 단위로 표시, 5MB를 통상 참고치로 두고 비율 계산. 실사용량이 거의 항상 1% 미만이라 막대가 안 보이는 것처럼 보일 수 있어 `.storage-usage-bar-fill`에 `min-width: 3%` 최소 시각 보정을 둠. 설정 화면에 진입할 때마다(`settingsOpenBtn` 클릭 시) `renderStorageUsage()`로 다시 계산 — 실시간 반영은 아님
  - **"백업 내보내기"/"초기화"에 "?" 설명 추가** (2026-09-15): 클라우드 백업 섹션 버튼들에 "?" 설명을 붙인 김에 "마찬가지로 백업내보내기/초기화에도 물음표와 도움말 넣는게 좋겠어"라는 요청 — 같은 `.settings-action-group`/`.settings-action-main`/`.settings-info-btn` 패턴 재사용(색상 변경은 없음, "기본문장 삭제"는 범위 밖). Claude가 두 버튼의 실제 동작(백업 내보내기=TSV 파일로 로컬 저장, 비파괴적 / 초기화=이 기기 문장 전체 삭제, 되돌릴 수 없음)에 맞춰 문구를 제안 → 사용자 "진행" 승인. `index.html`(두 버튼을 그룹으로 재구성+"?" 버튼 2개 신규)+`js/app.js`(`export-backup-info-btn`/`reset-info-btn` 클릭 시 `alert()` 2개 신규) 수정. Claude 1차 확인(Playwright, 라이트/다크+390px): "?" 클릭 시 정확한 설명 alert만 뜨고 실제 내보내기/초기화 동작(문장 개수 불변)은 실행되지 않는 것 확인, 오버플로우 없음 확인, 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인 완료")**
  - **기본문장 숨기기 토글** (2026-08-22): 이 앱 최초의 on/off 토글 스위치 컴포넌트(`.settings-toggle`/`.settings-toggle-thumb`), 켜짐 상태는 `--color-brand` 배경 + 흰 원이 오른쪽으로 이동. `getOrderedSentences()`가 이 설정을 반영해 걸러내므로 카드 학습 순서와 문장관리 목록 양쪽에 자동으로 같이 적용됨(별도로 두 곳을 각각 손댈 필요 없음)
- **문장추가 화면**: 기존 팝업 모달에서 다른 화면(문장관리/설정/도움말)과 같은 전체화면 구조(`.screen`+`.screen-topbar`)로 전환(2026-08-21) — 직접 문장을 입력하는 앱 성격상 입력창을 더 넓게 쓰고 싶다는 요청 반영. 상단바는 왼쪽 뒤로가기(취소 역할 겸함)+타이틀("문장 추가"/"문장 수정")만 배치, 기존에 하단에 있던 "취소" 버튼은 뒤로가기와 중복이라 제거하고 제출 버튼만 하단에 전체 너비로 남김
  - 본문 상단에 [폼 입력 / 파일 가져오기 / 텍스트 붙여넣기] 3탭(`#add-modal-tabs`, 2026-09-08까지는 "빠른입력" 4번째 탭도 있었으나 제거됨 — 위 19-1 참고). 탭 버튼 자체 스타일(`.add-modal-tab-btn`)은 문장관리 필터 탭(`.list-filter-btn`)과 완전히 공유 — 아래 "문장관리 필터 탭" 항목 참고. 수정 모드(`openAddModal(sentence)`가 있을 때)에서는 탭을 숨기고 폼만 노출, 추가 모드에서는 탭을 보여주고 "폼 입력"으로 초기화
  - 폼 입력창(한국어/학습할 언어, 2026-09-11까지는 "영어" 라벨이었으나 위 "최종 출시 전 점검사항" 참고해 일반화) `<textarea class="add-form-textarea">`는 고정 줄 수 대신 `flex:1`로 남는 세로 공간을 두 입력창이 나눠 채움(리사이즈 비활성), 자동 포커스 없음(화면 전환 시 모바일 자판이 자동으로 뜨지 않음)
    - **입력창 모양 — 무테두리 → 테두리형 박스로 전환** (2026-09-15): "폼입력탭의 폼을 보통 폼의 모양으로 바꾸어야겠어, 음성메모에서 그런 인풋폼 모양으로"라는 요청 — 기존엔 `.add-form-textarea`가 배경만 있고 테두리가 없는 edge-to-edge 스타일(다크모드에서만 옅은 테두리 추가, 포커스 시 안쪽 box-shadow 링으로 표시)이었는데, 음성 메모 입력칸(`.voice-memo-preview`)처럼 항상 보이는 테두리(`1.5px solid var(--gray-200)`)+둥근 모서리(`border-radius: 8px`)가 있는 일반적인 폼 박스 모양으로 통일. 테두리가 항상 보이므로 다크모드 전용 보정, 포커스 전용 box-shadow 트릭 둘 다 불필요해져 함께 제거(음성 메모 입력칸도 별도 포커스 스타일 없이 브라우저 기본 아웃라인에 의존 — 그대로 맞춤). `.add-form-field`가 갖고 있던 `.add-body` 좌우 패딩 상쇄용 음수 마진(`margin: 0 -20px`, edge-to-edge 시절의 흔적)도 함께 제거 — 이 값이 오늘 화면 여백 16px 통일 작업 이후 20px 기준 그대로 남아있어 실제로는 4px 과도하게 상쇄되고 있던 잠재적 버그였음(테두리형 박스로 바뀌며 애초에 이 트릭 자체가 필요 없어져 자연히 해소됨). 같은 클래스를 공유하는 AI 변형 화면(15-2)의 `.ai-variation-textarea`는 이미 자체적으로 테두리·패딩·둥근모서리를 갖고 있어(문장추가와 달리 처음부터 테두리형으로 재정의돼 있었음, "AI 변형 화면" 섹션 참고) 이제 기본 클래스와 값이 완전히 겹쳐 중복 선언이 된 부분을 정리(불필요해진 `padding`/`border`/`border-radius` 제거, `flex`/`min-height`만 유지). `css/style.css`만 수정, JS 변경 없음. Claude 1차 확인(Playwright, 라이트/다크): 입력창이 정확한 테두리·둥근모서리·패딩으로 렌더링되는 것 확인, 가로 오버플로우 없는 것(음수 마진 제거 후 정상) 확인, 실제 문장 추가 동작 회귀 없음(제출 후 개수 정상 증가) 확인, AI 변형 프롬프트 화면도 기존과 동일하게 보이는 것(회귀 없음) 확인, 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
    - **설정 글자크기와 연동** (같은 날): 사용자가 "폼안에 써지는 글자크기는 설정의 글자크기 영향을 안받아?"라고 질문 → 고정 16px라 영향받지 않는다고 답변 → "그럼 폼 입력창도 글자크기 설정에 연동시켜줘" 요청 → `.add-form-textarea`의 `font-size: 16px` 고정값을 `var(--kr-font-size)`(카드 화면 한국어 텍스트와 같은 변수, 5단계 16~28px)로 교체 — 한국어/학습할 언어 두 칸 모두 동일하게 연동(별도 변수 구분 없이 하나의 폼이라 통일). 같은 클래스를 공유하는 AI 변형 화면의 `.ai-variation-textarea`도 자동으로 함께 연동됨(부가 효과, 별도 작업 불필요). Claude 1차 확인(Playwright): 기본(3단계) 상태 22px 확인 → 설정에서 5단계(최대)로 변경 후 재진입 시 28px로 커지는 것(한국어/학습할 언어 두 칸 모두) 확인, 스크린샷으로 잘림 없이 커진 것 확인, 콘솔 에러 없음 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - 카드 화면·문장관리 화면 양쪽에서 열릴 수 있어, 열린 시점의 화면을 `addModalReturnScreen`에 기억해뒀다가 뒤로가기/제출 시 그 화면으로 복귀
- **카드 화면 하단 구조**: 상단바 이동 후 하단에는 이전/확인/다음(`control-bar`)만 배치. **둥글기**(2026-09-15): 앱 전체 버튼 둥글기 규칙(원형 아이콘 50%/일반 액션 12px/소형 카드·입력 8px/알약형 999px)을 사용자에게 설명하던 중 "이전/확인/다음 버튼의 둥글기를 20으로 변형" 요청 — 기존엔 공용 `.btn` 클래스의 12px를 그대로 상속했는데, `.control-bar .btn`에 `border-radius: 20px`를 별도로 얹어 이 세 버튼만 더 둥글게 조정(다른 화면의 `.btn` 공유 버튼엔 영향 없음, `css/style.css`만 수정). Claude 1차 확인(Playwright, `getComputedStyle`로 세 버튼 20px 확인+다른 `.btn` 사용처가 여전히 12px인 것 확인, 스크린샷) 완료 → **사용자 실기기 2차 확인 완료(2026-09-15, "확인완료")**
  - **마킹 아이콘 바**: 정답 확인 후에만 `.card` 블록 안(정답 아래)에 ☆(중요)/⚑(미암기) 아이콘 노출(`#card-mark-bar`). 문장 텍스트와 한 블록으로 묶어 화면 크기와 무관하게 일정한 간격 유지. 정렬 기준이 "중요한것/못외운것 먼저"일 때 토글하면 순서가 바뀔 수 있어, 토글 직후 `currentIndex`를 그 문장의 새 위치로 맞춰 점프 방지
  - 마킹 아이콘 바에 스피커(발음 듣기, 24번, 2026-09-11) 아이콘을 별표/깃발 옆에, 구분선(`.card-mark-divider`) 뒤에 연필(수정)/휴지통(삭제) 아이콘 배치 — `☆ ⚑ 🔊 | ✏ 🗑` 순서(수정/삭제는 데이터를 바꾸는 액션, 발음 듣기는 별표/깃발처럼 문장과 상호작용하는 성격이라 토글 그룹에 배치). 수정은 `openAddModal`, 삭제는 `confirmDeleteSingle`을 문장관리와 그대로 공유. 카드에서 삭제 시 `currentIndex`가 범위를 벗어날 수 있어 `renderCard()`에 범위 보정 로직 포함(마지막 카드를 삭제해도 이전 카드로 자연스럽게 이동)
- **카드 화면 빈 상태**: 문장이 0개(초기화 직후)이면 안내 문구를 보여주고 이전/확인/다음 버튼을 비활성화. 문장추가로 다시 채워지면 자동 복귀
- **문장관리 → 노트형 학습 진입 버튼**(25번, 2026-09-11): 문장관리 화면 자체는 원래 모습 그대로 유지(한 줄 텍스트+말줄임표, 아이콘 인라인) — 최초엔 안내문·표시토글·2단 레이아웃·스피커를 전부 문장관리에 얹었으나, 사용자가 "너무 복잡하다"고 판단해 관리와 읽기를 분리하는 쪽으로 재설계. 필터 개수 안내 아래 `#list-study-entry-btns`에 진입 버튼만 추가 — "전체"에서는 "노트형 학습하기"(아웃라인) 1개, "중요"/"미암기"에서는 "이 조건으로 플래시카드 학습"(솔리드, 23번 버튼 라벨 변경)과 "이 조건으로 노트형 학습"(아웃라인) 2개를 세로로 쌓음(라벨이 길어 나란히 두면 좁은 화면에서 넘칠 수 있음). 선택 모드에서는 `#list-filter-section` 전체가 숨겨지는 기존 규칙을 그대로 상속해 함께 숨겨짐
- **노트형 학습 화면**(25번, 2026-09-11, `#note-study-screen`): 문장관리와 완전히 분리된 새 `.screen` — 관리 아이콘(별표/깃발/AI/체크박스) 전혀 없이 스피커(발음 듣기, `.note-study-speaker-btn`, 카드 화면 24번의 `speakEnglish()` 재사용)만 있음. 상단에 전체 보기/한글만/영어만 3단 토글(`#note-display-mode-bar`, `.mode-select`/`.mode-select-btn`과 시각적으로 동일하지만 `modeSelectBtns`(화면모드) 전역 셀렉터와 겹치면 안 돼서 완전히 별도 클래스 `.list-display-mode-btn` 재사용 — 최초 구현 시 `padding`/`font-size`가 `.mode-select-btn`보다 살짝 작게 들어가 있었는데, 2026-09-12 "설정 화면모드 버튼이 더 편해 보인다"는 피드백으로 두 클래스의 수치를 완전히 동일하게(`padding: 10px 0`, `font-size: 14px`) 맞춤) — 선택 상태는 `localStorage`(`tuktak_list_display_mode`)로 재방문해도 유지, 항목을 다시 그리지 않고 `#note-study-list`에 `hide-kr`/`hide-en` 클래스만 토글. 텍스트(`.note-study-kr`/`.note-study-en`)는 `--kr-font-size` 연동+줄바꿈 허용으로 문장이 잘리지 않음. 뒤로가기는 문장관리로, 홈 버튼은 카드 화면으로(뒤로가기 2번 이상인 화면이라 홈 버튼 포함). 상시 안내문(`.list-note-intro`)은 2026-09-16 삭제됨 — 아래 "자동재생" 참고. 화면 우하단엔 원형 플로팅 버튼(`.note-autoplay-fab`, 2026-09-16)이 `#note-study-screen`(`position:relative`) 기준으로 고정 — 넓은 화면(PC)에서 `.screen`이 가운데 정렬 패널로 제한되는 규칙과 맞물려 `position:fixed`(뷰포트 기준) 대신 `absolute`를 사용, 패널 밖으로 벗어나지 않게 함
  - **탭-확인(인출연습) 보완**(2026-09-14): "한글만"/"영어만" 모드일 때 `.note-study-text`(항목의 텍스트 영역)를 탭하면 그 문장만 `.note-study-item.revealed` 클래스가 붙어 반대쪽 언어가 임시로 보이고, 다시 탭하면 원복 — CSS는 `#note-study-list.hide-en .revealed .note-study-en{display:block}` + `.revealed .note-study-kr{display:none}` 조합(영어만 모드는 대칭). "전체보기"에서는 이미 둘 다 보여서 탭해도 적용 대상이 없어 무변화. `applyListDisplayMode()`가 모드 전환마다 모든 `.revealed`를 정리해 이전 모드의 확인 상태가 새 모드로 새어나가지 않도록 함. 스피커 버튼은 `textWrap`의 형제 요소라(자식 아님) 클릭해도 이 토글에 영향 없음 — 별도 `stopPropagation` 불필요
  - **버그: flexbox 축소 붕괴**: 안내문+토글+목록을 담은 `.note-study-body`를 `display:flex; flex-direction:column`으로 만들었더니, 목록이 길어 전체 콘텐츠가 화면보다 커지는 순간 토글 바(둥근 모서리용 `overflow:hidden` 있음)가 거의 0px로 찌그러져 탭이 안 되는 버그 발생 — `overflow`가 `visible`이 아닌 flex 아이템은 자동 최소 크기가 콘텐츠 기준이 아니라 0이 되는 flexbox 스펙 때문(카드 화면 24번의 그리드 blowout과는 반대 방향 버그: 그리드는 안 줄어들어서 문제, 플렉스는 너무 잘 줄어서 문제). `.note-study-body`를 flex 대신 일반 블록 레이아웃으로 바꿔 해결
- **문장관리 필터 탭**: 상단바 아래에 전체/중요/미암기 세그먼트(`.list-filter-bar`). 정렬과는 독립적인 별도 축(필터는 조건에 안 맞는 문장을 목록에서 제외). 진입 시 항상 "전체"로 초기화, 선택 모드에서는 숨김
  - **검색 (22번, 2026-09-01 최초 구현 → 같은 날 폼 박스로 재조정)**: 상단바 "선택" 버튼 왼쪽에 돋보기 아이콘(`#list-search-btn`, `.screen-topbar-icon-btn`) 추가. 탭하면 필터를 "전체"로 강제 리셋하고 필터탭(`#list-filter-bar`)을 숨김(선택된 탭이 아니라 전체 대상 검색이라는 결정, 결과 항목엔 ☆⚑ 아이콘이 그대로 남아 어떤 탭에 속하는지 계속 구분됨). 타이틀 자리(`#list-topbar-title`, 절대위치라 평소엔 플렉스 공간을 차지하지 않음)는 검색 폼 박스(`#list-search-box`, 일반 플렉스 흐름 `flex:1`)로 바뀌어 남는 공간을 자연스럽게 채움
    - **폼 박스 스타일** (2026-09-01, "검색칸이 안 보인다"는 피드백으로 배경 없는 텍스트 방식에서 변경): `--color-chip-answer`(하늘색, `.en-text`/`.backup-reminder-banner`와 같은 "테마 무관 항상 밝은 배경" 칩) 배경의 둥근 박스 안에 돋보기 아이콘(`.list-search-box-icon`)+입력창(`.list-search-input`)+X 취소 버튼(`#list-search-clear-btn`, `.list-search-clear-btn`)을 나란히 배치. 밝은 배경 위 고정 요소라 텍스트·아이콘 색은 `.en-text`와 같은 규칙으로 리터럴 값(`#1E2A38`/`#4B5D6B`) 사용
    - **취소 동작 위치** (2026-09-01, "취소가 폼안에 x버튼으로 넣으면 어때" 요청으로 변경): 최초엔 선택모드와 같은 "뒤로가기→취소 텍스트" 패턴을 재사용했으나, 폼 박스 안에 X 버튼이 생기면서 그 역할을 X가 전담하도록 변경 — 뒤로가기 화살표는 검색 중에도 평소와 같은 아이콘 모양·동작(문장관리 화면 자체를 벗어나 카드 화면으로 이동)을 유지, X를 탭하면 `exitSearchMode()`로 검색만 취소하고 문장관리 화면엔 그대로 남음
    - 검색모드·선택모드는 상호배타 — 서로의 진입 버튼(검색 아이콘/선택 아이콘)을 숨겨서 강제
    - **"선택" 버튼도 아이콘으로 통일** (2026-09-01): 텍스트 필(pill)이던 "선택" 버튼이 아이콘인 검색 버튼과 나란히 있으니 무게감이 안 맞아 보인다는 피드백으로 `trash-2` 아이콘 버튼(`.screen-topbar-icon-btn`)으로 교체. 탭해도 즉시 삭제되지 않고 체크박스+비활성화된 하단 "삭제" 버튼으로 전환되는 기존 흐름이 "아직 안 지워짐"을 스스로 보여주므로 추가 안내 UI는 없음
    - **버그 노트**: 최초 구현 시 검색 아이콘과 "선택" 버튼 둘 다 `margin-left:auto`를 쓰면 flexbox가 남는 공간을 두 요소에 절반씩 나눠줘서 그룹이 화면 중간에 붕 뜨는 문제가 있었음(auto margin은 첫 번째 요소만 독식하는 게 아니라 모든 auto-margin 요소에 균등 분배됨) → `margin-left:auto`를 전용 클래스 `.screen-topbar-actions-lead`로 분리해 우측 아이콘 그룹 맨 앞(검색 아이콘)에만 부여, 뒤따르는 아이콘은 `gap`만으로 붙게 정리. "선택"이 텍스트→아이콘으로 바뀌며 같은 함정이 재발할 뻔했음 — 향후 상단바 우측에 아이콘 버튼을 추가할 땐 이 클래스를 새로 붙이지 말 것(맨 앞 요소만 유지)
  - **탭 버튼 스타일** (2026-08-21): 버튼형(테두리 박스+배경 채움)이 아닌 밑줄형. 고정폭(`flex:1`, 글자 길이 무관 동일 너비), 비활성은 `--gray-500`(연한 회색) 글자, 활성은 `--color-brand-dark`(네이비) 글자 + 네이비 밑줄 2px. 상단바·제출 버튼의 브랜드 블루와 함께 파란색이 과하다는 피드백으로 활성 탭을 네이비로 낮춤. `.list-filter-btn`/`.add-modal-tab-btn`(문장추가 탭)이 완전히 동일한 스타일을 공유하므로 하나만 고치면 어긋남 — 항상 같이 수정. 다크모드는 네이비가 배경과 대비 1.8:1로 거의 안 보여 `--color-text-primary`(밝은 색)로 전환(`.stepper-btn`/`.help-accordion-header`와 같은 종류의 다크모드 예외)
  - 필터 탭 아래 구분선(`.list-filter-bar`의 `border-bottom`) 밑에 현재 활성 탭 기준 문장 개수 안내(`#list-count-info`, 예: "전체 5개") 가운데 정렬로 표시. `--color-text-secondary`/13px 사용(처음엔 `--color-text-muted`/12px로 했으나 목록 텍스트보다 흐리고 작아 눈에 거슬린다는 피드백으로 조정, 이어서 탭 위쪽에 뒀다가 "터치될 것 같다"는 피드백으로 구분선 아래로 이동, 모두 2026-08-19). 목록이 바뀔 때마다(`renderSentenceList()`) 함께 갱신, 선택 모드에서는 탭·구분선과 함께(`#list-filter-section`) 숨김
- **문장변형 화면** (2026-08-25 최초 구현, 2026-08-26 목록 카드형 전환 + 상세 화면 여러 차례 개선 + "체험" 제외한 명칭으로 변경): 목록 화면은 최초엔 문장관리 목록과 완전히 같은 클래스를 재사용했으나, "탭하면 변형이 나온다"는 직관이 잘 안 온다는 피드백으로 세로 1열 카드(`.variation-card-list`/`.variation-card`/`.variation-card-kr`/`.variation-card-en`/`.variation-card-footer`)로 교체 — 문장관리 목록(수정 진입용)과 시각적으로 구분되도록 둥근 모서리+테두리+그림자(라이트) 카드로 감싸고, 상단에 안내 문구(`.variation-list-intro`: "문장을 탭하면<br>시제·인칭 등 다양한 변형을 볼 수 있어요", 15px/세미볼드/브랜드 네이비, 다크모드는 `.help-accordion-header`와 같은 패턴으로 `--color-text-primary`) + 카드 우하단에 "N개 변형" 배지·`chevron-right` 화살표를 추가해 탭 가능함과 콘텐츠 양을 함께 암시. 다크모드는 `.help-accordion-content`와 같은 패턴(배경 `--gray-100`, 테두리 없음). 영어 텍스트(목록 카드·상세 원문·상세 항목 전부)는 Pretendard가 영어를 넓게 렌더링해 "자간이 띄엄띄엄해 보인다"는 피드백으로 `letter-spacing: -0.3px` 공통 적용(2026-08-26). 안내 문구는 처음에 `word-break: keep-all`이 빠져 있어 좁은 화면에서 단어 중간에 아무 데서나 줄바꿈되는 문제가 있었음 → keep-all 추가 + "문장을 탭하면" 다음처럼 자연스러운 지점에 `<br>`로 명시적 줄바꿈을 둠(문구 자체는 기존 그대로 유지, 2026-08-26). 정렬은 가운데→왼쪽으로 변경(2026-08-26). 상세 화면의 변형 항목(`.variation-item`) 간 간격은 8px → 20px로 확대(2026-08-26)
  - **"내 문장으로 추가" 버튼** (2026-08-26 최초 구현 — 탭 즉시 추가, 2026-08-27 "선택 후 한 번에 추가" 방식으로 변경): 각 `.variation-item` 상단에 태그와 한 줄로 배치(`.variation-item-header`, flex space-between). 기본 아이콘은 `circle-plus`(`.variation-item-add-btn`, 카드 화면 마킹 아이콘 `.card-mark-btn`을 28px로 축소 재사용한 원형 버튼), 렌더링 시점에 `sentences.some()`으로 이미 같은 kr/en이 있는지 검사해 있으면 처음부터 `check`+`.added`(accent-star 호박색) 상태로 시작(기본문장 100개와 우연히 겹치는 경우 자동 반영), 재선택 불가. 버튼 탭이 항목 전체의 플립 탭과 겹치는 영역에 있어 클릭 핸들러에서 `e.stopPropagation()`으로 분리, 하늘색 하이라이트(`.variation-item-flipped`) 위에서도 보이도록 아이콘 색 별도 처리
    - **2026-08-27 변경**: "여러 개를 눌러도 진짜 다 추가된 건지 확신이 덜 든다"는 사용자 피드백으로, 탭 즉시 `addSentence()` 호출하던 것을 **"선택 후 하단 버튼으로 한 번에 확정 추가"** 방식으로 전환 — AI 변형(15-2) 미리보기 화면에서 먼저 검증된 "체크박스 선택 → 하단 고정 버튼으로 확정" 패턴을 문장변형(15-1)에도 동일하게 적용한 것. 탭하면 즉시 추가되는 대신 `.variation-item-add-btn.selected`(브랜드 블루 채움, `#FFFFFF` 아이콘) 상태로만 토글되고, 화면 하단 고정 버튼(`#variation-add-selected-btn`, `.variation-detail-footer`)이 "선택한 문장 추가하기 (N개)"로 선택 개수를 실시간 표시하다가 탭하면 그제서야 선택된 항목 전체를 한 번에 `addSentence()`로 추가하고 `.selected`→`.added`(체크·호박색)로 전환. `selected`는 이미 `.added`인 항목과 확실히 구분되는 색(브랜드 블루 vs accent-star 호박색)을 써서 "아직 확정 전" 임을 시각적으로 드러냄. 상세 화면에 진입할 때마다(`renderVariationDetail()`) 선택 상태(`variationSelectedIndexes`)는 초기화됨 — 다른 문장으로 이동하면 이전 선택은 유지되지 않음. 항목 탭-플립 인터랙션과는 완전히 독립(`e.stopPropagation()`으로 분리 유지)
  - **글자크기 연동** (2026-08-26): 목록 카드에 이어 상세 화면도 글자크기 설정과 연동. 처음엔 한국어 `--kr-font-size`/영어 `--kr-font-size-revealed`로 영어를 더 작게 뒀으나, "글자크기는 영어와 한국어 동일하게" 요청으로 `.variation-item-kr`/`.variation-item-en` 모두 `--kr-font-size`(16~28px)로 통일. 영어 글자색은 브랜드 네이비에서 `var(--color-text-primary)`(라이트/다크 자동 전환)로 변경. 항목 위 태그(`.variation-item-tag`: 현재시제/과거시제/2인칭/복수 등, "시제" 카테고리만 뜻이 모호해 "~시제" 접미사 추가)도 기존 `--color-text-muted`(대비 3.3:1, 컬러 가이드상 소글씨 금지 색인데 잘못 쓰이고 있었음)가 잘 안 보인다는 피드백으로 `--color-text-secondary`(대비 6.4:1)·13px로 교체
  - **탭해서 한/영 플립** (2026-08-25 "탭하면 영어 추가 노출·계속 유지" → 2026-08-26 세 차례 개선): ① 먼저 탭할 때마다 한/영이 서로 자리를 바꾸는 토글로 전환(항목마다 "탭해서 영어/한국어 보기" 힌트 문구도 상태에 맞게 전환) ② "탭해서 보기 힌트가 한국어 아래→영어 위로 위치가 널뛴다"는 피드백으로 힌트를 항목마다 두지 않고 목록 화면과 같은 방식으로 화면 상단에 한 번(`.variation-list-intro`: "문장을 탭하면 한국어·영어가 전환돼요")만 두는 것으로 정리, 항목별 힌트 요소(`.variation-item-hint`)는 삭제 ③ "글자가 플립하는 애니메이션을 넣어달라"는 요청으로 단순 hidden 토글 대신 CSS 3D 플립 애니메이션 구현 — `.variation-item-flip`(perspective) > `.variation-item-flip-inner`(0.4s rotateY 트랜지션) 안에 `.variation-item-kr`/`.variation-item-en`을 같은 grid 칸에 포개어(컨테이너 높이가 더 긴 쪽에 자동으로 맞춰짐) `backface-visibility:hidden`으로 뒷면을 숨기는 방식(영어가 2줄로 늘어나는 큰 글자크기에서도 잘림 없이 정상 확인). 항목별로 독립 동작, 진도 저장·이전/다음 넘기기 같은 카드 학습 인프라는 재사용하지 않은 가벼운 구현
  - **원문 강조박스 제거 + 하늘색 하이라이트로 대체** (2026-08-26, 두 단계로 정리): 최초 구현 때는 상세 화면 맨 위에 원문을 별도 `.variation-original` 박스(`--color-chip-answer` 하늘색 배경)로 강조했으나, "방금 목록에서 탭한 원문이라 기억할 것이고 화면에 변형문이 이미 다 보인다"는 사용자 판단으로 이 박스를 없앰. ① 처음엔 원문과 내용이 완전히 같은 변형 항목(대부분 "현재시제", 10번은 "과거시제")에 하늘색을 **고정** 적용 — 이 과정에서 4번(스트레스)·9번(날씨) 두 문장은 "원문과 의미가 겹친다"는 이유로 현재완료 변형 자체를 만들지 않아 원문과 일치하는 항목이 아예 없었던 것을 발견, `VARIATION_SENTENCES`에 두 문장의 "현재완료" 항목(원문과 동일한 내용)을 새로 추가해 10개 문장 모두 하이라이트 항목이 1개씩 있도록 통일(4번 8→9개 변형, 9번 5→6개 변형). ② "영어로 보이는 항목이 하늘색이어야지 항상 현재시제만 고정되는 건 이상하다"는 피드백으로 하이라이트를 **원문 일치 여부가 아니라 지금 영어가 보이는(플립된) 상태**에 동적으로 연동하도록 전환 — `.variation-item-flipped` 클래스를 탭 이벤트에서 `.variation-item-flip-inner`의 `flipped`와 함께 토글, 여러 항목을 동시에 영어로 열어두면 전부 하늘색, 한국어로 되돌리면 사라짐(항목별 독립). 원문과 일치하는 항목은 목록에서 이미 원문을 보고 들어온 것이므로 렌더링 시점에 미리 플립된 상태로 시작해 결과적으로 처음엔 하늘색으로 보임. 하늘색 배경 위 텍스트는 항상 밝은 배경이라 리터럴 `#1E2A38` 고정(`.en-text`와 동일 규칙)
- **AI 변형 화면(15-2)** (2026-08-27 최초 구현 → 같은 날 진입 구조 재설계): 처음엔 원문 선택용 전용 화면을 문장관리 목록 스타일 그대로 재사용해 만들었으나, "문장관리에 이미 같은 목록이 있는데 굳이 화면을 하나 더 두나"는 지적으로 그 화면을 완전히 삭제하고 **문장관리 목록 각 항목에 AI 아이콘을 직접 추가**하는 구조로 교체(위 "문장관리 필터 탭" 아래 문단 참고). `.sentence-star-btn`/`.sentence-unfamiliar-btn`/`.sentence-ai-btn` 3개 버튼 크기를 32px→28px로 축소하고(15-1의 `.variation-item-add-btn`과 동일한 크기 규칙), 별/깃발(즉시 토글)과 AI(새 화면 이동)의 무게감 차이를 시각적으로 드러내기 위해 `.sentence-list-actions-divider`(1px 세로선)로 둘을 분리 — 카드 화면 마킹 아이콘 바(`.card-mark-bar`)의 "토글 2개 + 구분선 + 액션 2개" 패턴을 문장관리 행에도 적용한 것. 프롬프트+붙여넣기 화면은 `.add-form-textarea`(문장추가 화면의 edge-to-edge 무테두리 스타일)를 그대로 쓰지 않고 `.ai-variation-textarea`로 테두리 있는 독립 필드로 재정의(문장추가처럼 화면 전체를 채우는 입력이 아니라 한 화면에 2개 필드+버튼이 나란히 있는 구조라 서로 구분되는 경계가 필요). 미리보기 화면은 `.sentence-checkbox`(선택 모드 체크박스와 동일 클래스)를 항상 켜진 채로 재사용하되, 15-1의 "탭하면 플립" 인터랙션은 의도적으로 가져오지 않음 — 이 화면은 AI가 만든 결과를 **검수**하는 목적이라 한/영을 동시에 보여줘야 하기 때문(플립은 "이미 아는 내용을 학습 체험"하는 15-1에만 맞는 상호작용이라는 논의 결과, PROGRESS.md 2026-08-27 참고)
  - **미니 학습 배너**: 카드 화면 상단바 바로 아래 노출(`#mini-session-banner`, 카드 화면 grid를 `auto 1fr auto`→`auto auto 1fr auto`로 확장해 삽입, 평소엔 `.hidden`이라 grid 높이에 영향 없음). 색상은 즐겨찾기와 같은 accent-star 계열(호박색 배경+글자) 재사용 — "표시가 남아있다"는 느낌을 15-1의 "내 문장으로 추가" 완료 상태와 통일
- **로컬 백업 리마인더 배너(17번)** (2026-08-28): 카드 화면 grid를 `auto auto 1fr auto`(상단바-미니학습배너-카드-버튼바)에서 `auto auto auto 1fr auto`(상단바-**백업 리마인더 배너**-미니학습배너-카드-버튼바)로 한 행 더 확장 — 두 배너 모두 평소엔 `.hidden`이라 grid 높이엔 영향 없음, `.card-topbar`/`.card`/`.control-bar`처럼 각 배너도 grid-row를 명시해 자동배치 밀림 버그(2026-08-27에 발견된 패턴, 위 미니 학습 배너 참고)를 피함. 배경은 `--color-chip-answer`(하늘색, 테마와 무관하게 항상 밝음) — `.en-text`/`.variation-item-flipped`와 같은 이유로 내부 텍스트도 라이트/다크 모두 동일하게 보이도록 고정값 사용(제목은 테마 불변 변수인 `--color-brand-dark`, 설명·닫기 아이콘은 `--color-text-secondary`의 라이트모드 값을 리터럴 `#4B5D6B`로 고정 — 이 변수를 그대로 쓰면 다크모드에서 밝은 색으로 바뀌어 밝은 배경 위에서 안 보이는 문제가 있어 구현 중 직접 잡음). 트리거 조건·체크포인트 로직은 위 체크리스트 17번 항목 참고
- **브랜드색 배경 위 고정 흰색 규칙**: 시작 화면(`.start-logo`, `.btn-primary`)과 상단바처럼 배경이 항상 브랜드 블루/네이비로 고정된 요소는 `--color-base` 대신 리터럴 `#FFFFFF` 사용
- **헤더/상태바 색상**: 카드·문장관리·설정 화면의 상단바 기본 배경은 **무채색(`--color-surface`, 2026-09-15 전환 — 위 "헤더 무채색 전환" 참고)**, 로고만 브랜드 블루 유지. 문장관리 선택모드는 배경 대신 텍스트 색(네이비)으로 강조. 상태바(PWA `theme-color`, `manifest.json`의 `theme_color`)는 네이비, 로딩 스플래시 배경(`manifest.json`의 `background_color`)은 블루 — 헤더가 무채색이 된 이후에도 이 둘은 범위 밖으로 남겨둠(앱 진입 시 브랜드 컬러가 살짝 보이는 지점으로 의도적으로 유지)


## 파일 구조


## 코딩 규칙

### HTML
- 시맨틱 태그 우선 사용 (header, nav, main, article, section, footer)
- 주석은 한국어로 작성
- 들여쓰기는 2칸

### CSS
- 클래스명은 명확한 의미 전달
- Vanilla CSS 사용 (프레임워크 사용 금지)
- 주석으로 섹션 구분
- 컬러 시스템은 CSS 변수로 관리 (변수명 임의 변경 금지)

### JavaScript
- ES6+ 문법 사용
- 가독성을 위해 함수는 작게 분리
- 주석은 한국어로 작성

### 참고 문서(워드 .docx) 작성 규칙 (2026-08-30 확정)
- 사용자가 `reference/` 폴더의 워드 문서를 직접 인쇄할 수 있어, **인쇄 최적화**를 기본 원칙으로 함
- **색을 넣지 않음**: 글자색·배경 음영(셀 채우기 등) 전부 사용 금지, 검정 텍스트+흰 배경만 사용. 표 헤더 행 등 강조가 필요하면 배경색 대신 **굵게(bold)**로 구분
- **페이지 번호 필수**: 모든 워드 문서 하단 중앙에 "페이지 N / M" 형식의 페이지 번호 푸터(Word 필드 코드 `PAGE`/`NUMPAGES` 사용, 고정 텍스트로 박아넣지 말 것 — 문서 분량이 바뀌어도 자동 갱신되게 함)
- 신규 문서 작성 시에도 처음부터 이 규칙을 적용할 것. 2026-08-30 당시 존재하던 문서 6개(`pwa_playstore_guide.docx`, `결제방식_검토_...docx`, `구글계정_교체_...docx`, `구글플레이_인앱결제_조사_...docx`, `영어_12시제_정리표_검수본.docx`, `툭탁_수익화_논의_정리_...docx`)에 일괄 소급 적용 완료(색 전부 제거, 페이지 번호 푸터 추가)
- **2026-08-31 사용자가 `reference/` 폴더를 직접 재정리**: 워드/텍스트 문서 대부분을 `reference/docs/` 하위로 이동. 위 6개 문서 중 `구글계정_교체_...docx`(1회성 계정 이전 가이드, 역할 종료로 판단돼 삭제된 것으로 보임)를 제외한 나머지는 내용이 유지·통합된 채 남아있음 — `pwa_playstore_guide.docx`→`reference/docs/구글플레이스토어.docx`, `결제방식_검토_...docx`+`구글플레이_인앱결제_조사_...docx`→`reference/docs/유료화_결제.docx`(하나로 병합), `영어_12시제_정리표_검수본.docx`·`툭탁_수익화_논의_정리_...docx`는 같은 이름으로 `reference/docs/`로 이동. `reference/` 최상위에는 `.md` 참고 자료(`help-content-draft.md`/`tuktak-color-guide.md`/`문장변형_체험판_콘텐츠_초안.md`)만 남음. 새 워드 문서를 참조할 때는 `reference/docs/` 경로 기준으로 작성할 것
