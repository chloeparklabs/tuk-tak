# PROGRESS

작업 기록은 아래에 계속 누적합니다 (기존 기록은 삭제하지 않음).

---

## 2026-08-04

### 진행 내용
- `CLAUDE.md` 내용 확인 및 사용자에게 작업 규칙/체크리스트 요약 보고
- `PROGRESS.md` 없음을 확인 → 이번에 신규 생성
- `reference/` 폴더 자료 확인
  - `idea.txt`: 핵심 스펙 = 한국어 문장 표시 → 정답보기 버튼 → 영어 정답 표시
  - `아이디어브리핑.txt`: 1단계(순수 HTML/CSS/JS 단일 파일) → 2단계(PWA) → 3단계(Supabase 연동, 선택) 확장 로드맵. 오답 재출제, SM-2 스페이스드 리피티션, TTS 등 확장 아이디어 제안됨
  - `프로젝트진행순서.txt`: 배포/출시 관련 메모(개발자 계정, Capacitor, Play Console 등) — 개발 초기 단계와는 별개 참고자료
- 실제 앱 코드(HTML/CSS/JS)는 아직 없음 — 아직 코드 작업 착수 전

### 체크리스트 상태
- [ ] 1. 기본 카드 UI
- [ ] 2. 텍스트 답변 흐름
- [ ] 3. 음성 답변 흐름
- [ ] 4. 구글시트 데이터 연동
- [ ] 5. (진행하며 추가 예정)

### 다음 작업 제안
- 체크리스트 1번(기본 카드 UI)부터 시작 — 사용자의 "시작"/"go" 승인 대기 중

---

## 2026-08-04 (이어서)

### 진행 내용
- 사용자 "시작" 승인 → 체크리스트 **1번(기본 카드 UI) + 2번(텍스트 답변 흐름)**을 하나의 기능으로 묶어 구현
- 레퍼런스 화면(화면1~3)에 맞춰 시작화면 → 한국어 카드 → 확인 버튼 클릭 시 영어 노출 → 이전/다음 이동 흐름 구현
- 신규 파일: `index.html`, `css/style.css`, `js/app.js`
  - `css/style.css`: 브랜드 컬러 변수(`--color-brand` 등) 선언. **Pretendard 폰트 파일이 아직 없어 시스템 폰트로 임시 대체** — 폰트 파일 추가 시 별도로 적용 필요
  - `js/app.js`: 샘플 한/영 문장 5개 하드코딩 (구글시트 연동 전까지 임시), 확인/이전/다음 로직, 문장 목록 처음↔끝 순환
- **1차 확인(Claude, 자동화)**: Playwright(headless Chromium)로 로컬 서버 구동 후 시작→카드 표시→확인(영어 노출)→다음→이전→처음에서 이전 클릭 시 마지막으로 순환 흐름 검증, 콘솔 에러 없음 확인. 테스트에 사용한 임시 파일(node_modules, package.json, 스크립트, 스크린샷)은 확인 후 모두 삭제하여 프로젝트 폴더 정리 완료
- CLAUDE.md 체크리스트 1, 2번 [x] 처리 (2026-08-04)

### 참고 (사용자가 별도로 CLAUDE.md 수정함)
- 프로젝트명: TukTak으로 확정
- 톤 & 비주얼 컨셉: 맑은 파란색 기반 + 레드 계열 포인트 컬러 추가 (한국 국기/단청 색감 모티프)

- **사용자 2차 확인 완료** — 정상 동작 확인됨. 이번 세션은 여기서 종료.

### 다음 작업 제안
- 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동) 중 우선순위 정해서 시작
- 다음 세션 시작 시 이 PROGRESS.md를 먼저 확인하고 이어서 진행

---

## 2026-08-06

### 진행 내용
- 기존 `icons/` 폴더(icon-192x192.png, icon-512x512.png)가 아직 매니페스트에 연결되어 있지 않은 것을 확인
- 사용자 "시작" 승인 → PWA 아이콘 연결 작업 진행 (체크리스트 항목은 아니며, 기술 스택의 "PWA: Web App Manifest" 지원을 위한 보조 작업)
  - 신규 파일: `manifest.json` (name/short_name: TukTak, theme_color: `#378ADD` Brand Blue, icons 2종 등록)
  - `index.html`에 `<link rel="manifest">`, favicon, apple-touch-icon, `<meta name="theme-color">` 태그 추가
- **1차 확인(Claude, 자동화)**: Node 임시 정적 서버로 `index.html`, `manifest.json`, 두 아이콘 파일 모두 HTTP 200 응답 확인. `manifest.json` JSON 유효성 검증 완료. 테스트용 서버 프로세스는 종료 처리

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실기기(폰 브라우저 등)에서 아이콘/매니페스트 정상 인식 확인 후 알려주시면 다음 기능(체크리스트 3번 또는 4번)으로 진행
- 2차 확인 전까지 다음 기능 단계로 넘어가지 않음
- **세션 종료 (2026-08-06)**: 사용자가 2차 확인을 다음 세션으로 미룸. 다음 세션 시작 시 PWA 아이콘/매니페스트 실기기 확인부터 이어서 진행할 것

---

## 2026-08-07

### 진행 내용
- GitHub 저장소 연동 및 배포 진행 (체크리스트 항목은 아니며, 기술 스택의 "배포: Vercel / 버전관리: GitHub" 지원을 위한 인프라 작업)
  - 로컬 프로젝트 폴더에 `git init` → 저장소 `https://github.com/easycodehow/tuk-tak.git`를 origin으로 연결
  - `reference/` 폴더(기획 메모, 레퍼런스 이미지) 포함 여부 및 저장소 공개 범위(Public) 사용자 확인 후 전체 파일 커밋
  - `main` 브랜치로 최초 커밋 push 완료
  - 사용자가 Vercel에서 GitHub 저장소 Import → 배포 완료, URL 확인: `https://tuk-tak-beta.vercel.app/`
  - `CLAUDE.md`의 "배포 URL" 항목에 반영

### 다음 작업 제안
- **PWA 아이콘/매니페스트 2차 확인**: 이제 실제 배포 URL(`https://tuk-tak-beta.vercel.app/`)이 생겼으니, 폰 브라우저에서 이 주소로 접속해 아이콘/매니페스트 정상 인식 여부 확인 (이전 세션부터 미뤄진 확인 항목)
- 2차 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동) 중 우선순위 정해서 다음 기능 시작

---

## 2026-08-07 (이어서 — 시작화면 스타일 수정)

### 진행 내용
- 사용자 요청: 시작화면 배경색을 브랜드 블루로 교체 + "시작하기" 버튼이 세로로 길게 늘어나는 버그 수정
  - 원인 파악: `.btn` 클래스의 `flex: 1`이 flex 컨테이너인 `#start-screen` 안에서 그대로 상속되어 버튼이 화면 세로 전체를 채우던 버그
  - 사용자 확인 후 `css/style.css` 수정
    - `#start-screen` 배경색을 `var(--color-brand)`(브랜드 블루)로 변경
    - `.btn-primary`에 `flex: none` 추가로 버그 수정, 배경색은 `var(--color-point)`(포인트 코럴)로 변경해 파란 배경과 구분되도록 조정
  - **1차 확인(Claude, 자동화)**: Node 임시 정적 서버 + 헤드리스 Chrome 스크린샷으로 배경색/버튼 크기·색상 정상 반영 확인
- **중요 이슈 발견 및 해결**: 수정한 CSS가 로컬에만 있고 커밋/push가 안 되어 실제 배포 사이트(Vercel)에는 반영되지 않고 있었음
  - 사용자에게 원인 설명 (Vercel은 GitHub push 기준으로 재배포됨, 로컬 수정만으로는 반영 안 됨)
  - 커밋 후 `main` 브랜치로 push 완료 → GitHub-Vercel 연동으로 자동 재배포됨

### 다음 작업 제안
- **사용자 2차 확인 필요**: 배포 URL(`https://tuk-tak-beta.vercel.app/`) 재배포 완료 후, 배경색/버튼 스타일이 의도대로 보이는지 + PWA 아이콘 인식 여부 함께 확인
- 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동) 중 우선순위 정해서 다음 기능 시작

---

## 2026-08-07 (이어서 — PWA 아이콘 흰색 표시 문제 수정)

### 진행 내용
- 사용자 보고: 홈 화면에 앱 설치 후 아이콘이 흰색으로 표시됨
- 원인 파악: 아이콘 파일 자체는 배포 서버에서 정상 서빙(HTTP 200, 파란 배경+흰 글씨)되고 있었으나, `manifest.json`의 `"purpose": "any maskable"` 설정이 문제
  - 마스킹 아이콘은 시스템이 원형/둥근사각형 등으로 잘라 표시하는데, 현재 아이콘 디자인이 가장자리 여백(safe zone) 없이 텍스트가 꽉 차 있어 마스킹 처리 시 흰색으로 대체 표시된 것으로 추정
- `manifest.json` 두 아이콘 항목의 `purpose`를 `"any maskable"` → `"any"`로 변경 (마스킹 미적용, 원본 그대로 표시)
- **1차 확인(Claude, 자동화)**: Node로 `manifest.json` JSON 유효성 검증 완료
- 커밋 후 `main` 브랜치로 push 완료 → Vercel 자동 재배포
- 사용자에게 안내: 기존에 홈 화면에 설치된 아이콘은 캐시되어 있어 매니페스트 변경이 자동 반영되지 않으므로, 삭제 후 재설치 필요

### 다음 작업 제안
- **사용자 2차 확인 필요**: 기존 설치 아이콘 삭제 후 재설치하여 파란 배경 아이콘이 정상 표시되는지 확인
- 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동) 중 우선순위 정해서 다음 기능 시작

---

## 2026-08-07 (이어서 — 카드 화면 버튼 위치 조정 + 브레인스토밍)

### 진행 내용
- **카드 학습 화면 버튼 위치 조정**: "글자 탭으로 정답 확인 + 이전/다음 버튼 사이드 배치"라는 사용자 아이디어를 두고 한손/두손 사용성을 논의
  - 논의 결과: 사이드 배치는 한손 사용성이 떨어져 보류, 대신 **세로 모드 유지 + 버튼을 화면 하단 절반을 3등분한 가운데 지점으로 이동**하는 절충안으로 합의 (최하단 1/3은 추후 설정 메뉴 등을 위한 여백으로 남김)
  - `css/style.css` 수정: `#card-screen`을 flex → grid(`grid-template-rows: 2fr 1fr 1fr`)로 변경, `.control-bar`를 세로 중앙 정렬
  - **1차 확인(Claude, 자동화)**: 헤드리스 Chrome 스크린샷으로 버튼이 하단 절반 중간 지점에 정상 배치되고 화면 폭 안에 들어가는 것 확인
  - 커밋 `6094478` push 완료 → Vercel 자동 재배포
  - **사용자 2차 확인은 폰으로 진행 예정 (결과 아직 미보고)**
- **아이콘 파일 문제 발생 및 롤백**: 사용자가 직접 아이콘 파일을 교체하려다 문제가 생김 (`icon-192x192.png` 수정, `icon-512x512.png` 삭제, `TUKTAK 앱아이콘(512).png` 추가) → 아직 커밋 전이라 배포에는 영향 없었음. 사용자 요청으로 `icon-192x192.png`, `icon-512x512.png`를 `git restore`로 마지막 커밋 상태(기존 파란 배경 아이콘)로 되돌림. `TUKTAK 앱아이콘(512).png`는 삭제 없이 미추적 상태로 보존 (임의 삭제 금지 원칙)
  - **아이콘 디자인 자체는 아직 미해결 상태로 보류됨**

### 브레인스토밍: 향후 기능 아이디어 (체크리스트 미확정, 메모 단계)
1. **개인화 입력 폼**: 구글시트 대신 앱 자체에 문장 입력 폼 제공 (모바일/웹 모두 지원) — 기존 체크리스트 4번(구글시트 CSV 연동) 방향과 달라질 수 있어 추후 체크리스트 조정 필요
2. **디스플레이 설정 메뉴**: 글자 크기·색상 등 커스터마이징 — 버튼 아래 남겨둔 여백(위 항목)이 진입점 후보
3. **문장 자동 변형 출제** ⭐ (핵심 고도화 기능으로 별도 기억함): 문장 하나 입력 시 시제(현재/과거/미래/현재완료)·주어 단수복수 등 변형해서 추가 문제로 출제. 규칙 기반(정규 동사·단순문 한정 시 가능, 불규칙 동사·복문은 어려움) vs LLM API 방식(안정적이나 서버·비용·오프라인 불가 트레이드오프) 두 방안을 논의했으나 **미결정 — 실제 착수 시 재논의하기로 함**
4. **문제 출제 순서 선택**: 랜덤 순서 / 오래된 순서 등 선택 가능하게 (1번의 입력일시 메타데이터 필요)
5. **음성/마이크 인식(체크리스트 3번)**: 앱을 다양한 기능보다 특화된 기능 위주로 가져가고 싶다는 방향에 따라 **마이너 목표로 우선순위 하향** (제거는 아님)

### 다음 작업 제안
- **사용자 2차 확인 필요**: 버튼 위치 변경이 폰에서 의도대로 보이는지 확인
- 아이콘 디자인 문제 별도 해결 필요 (보류 중)
- 다음 기능 착수 시, 브레인스토밍 우선순위(1번 개인화 입력 폼 또는 3번 문장 자동 변형이 핵심으로 언급됨, 음성 인식은 후순위)를 참고해 사용자와 우선순위 논의 후 체크리스트 반영

---

## 2026-08-08 (이어서 — 카드 화면 버튼 스타일 추가 조정)

### 진행 내용
- 사용자 2차 확인: 이전 버튼 위치 변경(하단 절반 중간)은 폰에서 정상적으로 보임 확인됨. 다만 "버튼이 하단 네비게이션과 너무 붙어 보인다" + "버튼을 정사각형으로 바꾸고 싶다"는 추가 스타일 요청
- 사용자 "진행" 승인 → `css/style.css` 수정
  - `#card-screen`: 하단 여백 확대 (`padding-bottom` 24px → 40px + safe-area, `grid-template-rows` 마지막 여백 행 비율 1fr → 1.4fr)
  - `.control-bar`: `margin-bottom: 24px` 추가로 버튼 그룹과 화면 하단 사이 간격 추가 확보, `justify-content: center` 추가
  - `.control-bar .btn`(이전/확인/다음 버튼)을 기존 가변 너비 직사각형(`flex:1`, `padding:16px 0`)에서 고정 크기 정사각형(`width:76px`, `aspect-ratio:1/1`)으로 변경. 시작화면의 `.btn-primary`(시작하기 버튼)는 이 규칙 영향 받지 않도록 `.control-bar .btn` 셀렉터로 범위 한정
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드 폴더에 Playwright를 임시 설치해 iPhone 12 뷰포트로 헤드리스 테스트 진행 (프로젝트 폴더에는 흔적 남기지 않음)
  - 시작 → 카드 화면 → 확인/이전/다음 클릭 흐름 정상 동작, 콘솔/페이지 에러 없음 확인
  - 스크린샷으로 버튼이 정사각형으로 렌더링되고 하단 여백이 이전보다 확실히 늘어난 것 확인
  - 테스트에 사용한 임시 Playwright 설치, 임시 서버 스크립트, 스크린샷 모두 삭제하여 정리 완료
- 커밋 후 `main` 브랜치로 push 완료 → Vercel 자동 재배포
- 참고: 작업 트리에 사용자가 별도로 교체 시도 중이던 아이콘 파일(`icons/icon-192x192.png`, `icons/icon-512x512.png`) 변경이 남아 있었으나, 이번 커밋 범위(버튼 스타일)와 무관하여 포함하지 않고 그대로 미커밋 상태로 둠

### 다음 작업 제안
- **사용자 2차 확인 필요**: 배포 URL에서 버튼 하단 여백 및 정사각형 모양이 의도대로 보이는지 확인
- 2차 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동), 혹은 브레인스토밍 항목(개인화 입력 폼/문장 자동 변형) 중 우선순위 논의

---

## 2026-08-08 (이어서 — 아이콘 디자인 반영)

### 진행 내용
- 사용자가 직접 `icons/icon-192x192.png`, `icons/icon-512x512.png`를 새 디자인("TUK TAK" 텍스트 로고, 브랜드 블루 배경, 가장자리 여백 포함)으로 교체
- **1차 확인(Claude)**: 두 파일이 유효한 PNG이고 각 파일명과 일치하는 크기(192x192, 512x512)인지, `manifest.json`의 아이콘 항목(크기·purpose)과 일치하는지 확인. 기존 문제였던 "여백 없이 텍스트가 꽉 찬 디자인"과 달리 이번 디자인은 여백이 있어 마스킹 시 흰색으로 잘리는 문제가 재발할 가능성은 낮아 보임
- 커밋 후 `main` 브랜치로 push 완료 → Vercel 자동 재배포
- 이전에 보류 상태였던 "아이콘 디자인 미해결" 이슈는 이번 반영으로 해소

### 다음 작업 제안
- **사용자 2차 확인 필요**: 기존 홈 화면 설치 아이콘은 캐시되어 있으므로 삭제 후 재설치하여 새 아이콘이 정상 표시되는지 확인 (버튼 스타일 2차 확인과 함께 진행 가능)
- 2차 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동), 혹은 브레인스토밍 항목(개인화 입력 폼/문장 자동 변형) 중 우선순위 논의

---

## 2026-08-08 (이어서 — 버튼 크기·글자 크기 확대 + 반응형 대응)

### 진행 내용
- 사용자 질문: "버튼크기와 글자크기가 얼마야?" → 당시 값(76px 정사각형, 글자 14px) 안내
- 사용자 요청: 버튼 100×100px, 글자 20px로 변경. 폰 화면 크기에 따라 자동으로 달라지는지 질문
  - 현재 방식은 고정 px라 화면 크기와 무관하다는 점, 100px 그대로 고정하면 iPhone SE(375px) 등 좁은 화면에서 버튼 3개가 화면 폭을 넘칠 수 있다는 점을 설명
  - 사용자 선택: **넓은 화면에서만 100px/20px, 좁은 화면은 자동 축소**(clamp 방식) 채택
- 사용자 "시작" 승인 → `css/style.css`의 `.control-bar .btn` 수정
  - `width: 76px` → `width: clamp(64px, 25vw, 100px)`
  - `font-size: 14px` → `font-size: clamp(14px, 5vw, 20px)`
  - 400px 이상 화면에서는 100px/20px로 고정 표시, 그보다 좁은 화면에서는 화면 폭에 비례해 축소, 최소 64px/14px 아래로는 줄지 않도록 하한선 설정
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드 폴더에 Playwright 임시 설치 후 320px(구형 소형 화면)·375px(iPhone SE)·390px(iPhone 12)·430px(대형 화면) 4가지 뷰포트로 헤드리스 테스트
  - 320px: 버튼 80×80px/글자 16px, 375px: 93.8px/18.75px, 390px: 97.5px/19.5px, 430px: 100px/20px(상한 도달) — 모든 경우 가로 스크롤/버튼 잘림 없음 확인
  - 스크린샷으로 좁은 화면(320px)·넓은 화면(430px) 모두 버튼이 화면 폭 안에 정상적으로 들어가는 것 확인
  - 테스트에 사용한 임시 Playwright 설치, 서버 스크립트, 스크린샷 모두 삭제하여 정리 완료

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 버튼/글자 크기가 커진 것이 의도대로 보이는지, 화면 하단 여백과 함께 어색하지 않은지 확인
- 2차 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동), 혹은 브레인스토밍 항목(개인화 입력 폼/문장 자동 변형) 중 우선순위 논의

---

## 2026-08-08 (이어서 — 시작화면 스플래시 논의 + 시작하기 버튼 모양을 레퍼런스대로 변경)

### 진행 내용
- 사용자 요청으로 `reference/시작화면.png`(브랜드 블루 배경 + "툭치면 탁튀어나올때까지" 슬로건 + "TUK TAK" 로고 텍스트) 확인 → 현재 실제 시작화면(index.html의 #start-screen)에는 이 슬로건/로고 텍스트가 아직 없다는 점 공유
- 사용자 질문: "앱이 시작할 때 보여주는 화면으로 쓰려면?" → 확인 결과 **PWA 네이티브 스플래시 화면**(홈화면 설치 후 아이콘 탭 시 잠깐 뜨는 로딩 화면)을 의미
  - 설명: 네이티브 스플래시는 Android/iOS 모두 `manifest.json`의 `background_color` + 아이콘 조합으로 브라우저가 자동 생성하며, 슬로건 같은 임의 텍스트는 Android에서는 애초에 표시 불가, iOS는 기기별 `apple-touch-startup-image` 정적 이미지를 따로 만들면 가능하나 작업량이 큼
  - 현재 `manifest.json`의 `background_color`가 `#FFFFFF`(흰색)로 되어 있어 브랜드 블루가 아닌 흰색 스플래시가 뜨고 있을 가능성 확인 (아직 수정 전, 방향 결정 대기 중)
  - **아직 결론/작업 미착수 상태** — 다음 세션에서 (A) manifest 배경색만 브랜드 블루로 수정 + 슬로건은 인앱 시작화면에 추가, 또는 (B) iOS 전용 커스텀 스플래시 이미지 제작 중 방향 결정 필요
- 별도로 사용자가 레퍼런스 폴더의 `화면 1.png`(시작하기 버튼 와이어프레임: 각진 사각형 + 테두리만, 채움 없음)를 보고 "시작하기" 버튼 모양을 레퍼런스대로 바꿔달라고 요청
  - 범위 확인 질문 후 **모서리+채우기+위치 모두 레퍼런스대로** 적용하기로 확정 (색상은 레퍼런스의 흑백이 아니라 브랜드 컬러 체계 유지, 채움 없는 테두리 스타일만 차용)
  - `css/style.css`의 `.btn-primary` 수정: `border-radius: 12px→0`(각진 사각형), `background-color: var(--color-point)→transparent`(채움 제거), `border: 2px solid var(--color-base)`(흰색 테두리) 추가, 텍스트 색도 흰색으로 변경. 위치는 기존에도 화면 중앙 정렬이라 별도 변경 없음
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 시작화면 스크린샷 확인 — 각진 흰색 테두리 버튼이 파란 배경 위에 중앙 정렬되어 정상 렌더링, "시작하기" 클릭 시 카드 화면으로 정상 전환, 콘솔/페이지 에러 없음 확인. 테스트 흔적(임시 설치, 서버 스크립트, 스크린샷) 모두 삭제
- 커밋 완료, **아직 push 전** (사용자에게 push 여부 확인 예정)

### 다음 작업 제안
- **사용자 2차 확인 필요**: push 후 배포 URL에서 "시작하기" 버튼이 각진 흰색 테두리 스타일로 잘 보이는지 확인
- **스플래시 화면 방향 결정 필요**: 위 (A)/(B) 중 선택 후 진행
- 2차 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동), 혹은 브레인스토밍 항목(개인화 입력 폼/문장 자동 변형) 중 우선순위 논의

---

## 2026-08-08 (이어서 — 시작화면을 실제 레퍼런스 이미지 기준으로 재수정 + 포인트 코럴 사용 보류)

### 진행 내용
- 사용자가 `reference/시작버튼.png`(실제 디자인 목업: 브랜드 블루 배경 + "TUK TAK" 로고 텍스트 + 둥근 모서리·반투명 흰색 채움·흰색 테두리 버튼)를 레퍼런스 폴더에 추가
  - 직전에 초기 와이어프레임(`화면 1.png`) 기준으로 적용했던 "각진 사각형" 스타일은 이 실제 목업과 달라 다시 수정
- 사용자 "진행" 승인 → `css/style.css`, `index.html` 수정
  - `.btn-primary`: `border-radius: 0` → `12px`(다시 둥글게), `background: transparent` → `rgba(255,255,255,0.15)`(반투명 흰색 채움)로 변경, 흰색 테두리는 유지
  - `#start-screen`에 `gap: 24px` 추가, `.start-logo` 클래스 신설(흰색, 40px, 굵게)
  - `index.html`의 `#start-screen`에 `<p class="start-logo">TUK TAK</p>` 로고 텍스트 요소 추가 (버튼 위)
- **추가 요청**: 포인트 코럴(`--color-point`) 사용을 당분간 보류. 필요한 곳이 생기면 그때 다시 검토하고, 너무 튀면 아예 제외할 수도 있음 → 지금은 브랜드 블루/네이비/화이트 + 그레이스케일로만 디자인
  - `CLAUDE.md`의 컬러 시스템 표에 Point Coral 사용 보류 상태와 사유를 각주로 기록 (변수 자체는 삭제하지 않음)
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 시작화면 스크린샷 확인 — 로고 텍스트+둥근 반투명 버튼이 레퍼런스와 유사하게 렌더링됨, "시작하기" 클릭 시 카드 화면 정상 전환, 콘솔 에러 없음. 테스트 흔적 모두 삭제
  - 로고 텍스트 크기가 레퍼런스 대비 살짝 작아 보이는 정도의 미세한 차이는 있음 (필요시 추후 미세 조정 가능)
- 커밋(`a187302`) 후 사용자 "진행" 승인으로 push 완료 → Vercel 자동 재배포

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 시작화면(로고+버튼)이 레퍼런스와 비슷하게 보이는지, 로고 텍스트 크기가 적당한지 확인
- **스플래시 화면 방향 결정 필요**: 여전히 미해결 (A: manifest 배경색만 수정 + 인앱 시작화면 활용 / B: iOS 전용 커스텀 스플래시 이미지 제작)
- 2차 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동), 혹은 브레인스토밍 항목(개인화 입력 폼/문장 자동 변형) 중 우선순위 논의

---

## 2026-08-08 (이어서 — 로딩화면 배경색 변경 + 512 아이콘 재디자인)

### 진행 내용
- 사용자 요청: "로딩화면의 배경색을 브랜드 블루로 교체" + "아이콘 위치를 일단은 하단에 붙여줘"
  - 배경색: `manifest.json`의 `background_color`를 `#FFFFFF` → `#378ADD`(브랜드 블루)로 변경 → 스플래시 방향 (A) 채택 확정
  - 아이콘 위치(하단 고정): Android 네이티브 스플래시는 브라우저가 아이콘을 항상 화면 중앙에 고정 배치하며 manifest로 위치 조정이 불가능하다는 제약을 설명, 구현 방식(인앱 화면에 직접 배치 vs iOS 전용 커스텀 이미지) 확인 질문을 드렸으나 **아직 답변/결정 없이 보류 상태**
- 별도로 사용자가 `icons/icon-512x512.png`를 직접 새 디자인(흰색 배경 + "툭!"·"탁!" 텍스트, 화면 왼쪽 중간쯤 배치)으로 교체
  - `icons/icon-192x192.png`(기존 파란 배경 "TUK TAK")와 디자인이 상이해 두 아이콘 간 통일성 우려를 공유했으나, 사용자가 "디자인은 다르게 했어. 이 상태로 반영해줘"라고 확인 → 의도된 변경으로 그대로 반영
- **1차 확인(Claude)**: `manifest.json` JSON 유효성 검증 완료
- 커밋(`c255469`) 후 push 완료 → Vercel 자동 재배포

### 다음 작업 제안
- **아이콘 위치(하단 고정) 방향 결정 필요**: 인앱 시작화면에 아이콘 이미지 추가해서 하단 배치 vs iOS 전용 커스텀 스플래시 이미지 제작 — 미해결
- **사용자 2차 확인 필요**: 배포 후 로딩화면 배경색(브랜드 블루)과 새 512 아이콘이 의도대로 보이는지 확인
- 2차 확인 완료 후 체크리스트 3번(음성 답변 흐름) 또는 4번(구글시트 연동), 혹은 브레인스토밍 항목(개인화 입력 폼/문장 자동 변형) 중 우선순위 논의

---

## 2026-08-08 (이어서 — 카드 화면 버튼 위치 하단 고정으로 임시 복귀)

### 진행 내용
- 사용자 요청: "버튼들을 하단에 다시 붙여줘. 나중에 위치 확정할때까지." — 이전 세션에서 화면 하단 절반 중간으로 옮겼던 버튼 위치를 최종 위치 확정 전까지 임시로 화면 맨 아래로 되돌림
- `css/style.css` 수정
  - `#card-screen`: `grid-template-rows: 2fr 1fr 1.4fr` → `1fr auto` (카드 영역이 남은 공간을 모두 차지하고, 버튼 바는 필요한 높이만큼만 차지하며 맨 아래 배치), `padding-bottom`도 여분 여백(40px) 제거하고 기본 24px + safe-area로 축소
  - `.control-bar`의 `margin-bottom: 24px` 제거
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 확인 — 버튼 그룹이 화면 하단(패딩 24px만 남기고) 바로 붙어 배치됨을 좌표로 확인, 스크린샷으로도 확인. 테스트 흔적 삭제하여 정리 완료

### 다음 작업 제안
- ~~사용자 2차 확인 필요~~ → **사용자 2차 확인 완료** ("확인했어, 잘 보여")
- **버튼 최종 위치는 여전히 임시 상태**: 하단 고정 vs 이전 중간 배치(margin) 중 최종안은 아직 미확정 (나중에 다시 논의 예정)

---

## 2026-08-08 (이어서 — 데이터 입력 방법 기획 논의: 폼 입력 + CSV/TSV 가져오기 통합안)

### 진행 내용 (기획만 진행, 코드 변경 없음)
- 사용자 질문: 데이터 입력 방법을 구글시트 하나로 한정하지 않고 여러 개(구글시트, 폼입력 등) 제공하면 어떤지 논의
  - Claude 의견: 두 개를 "동시에 살아있는 원본"으로 병렬 운영하면 동기화/중복 문제가 복잡해지므로, **앱 자체 폼 입력을 기본 단일 저장소로 먼저 만들고, 구글시트는 나중에 "대량 가져오기" 보조 기능으로 얹는 방향**을 제안 → 이전 브레인스토밍 우선순위(개인화 입력 폼이 핵심)와도 일치
- 사용자 아이디어: 외부에서 ChatGPT에게 영어 단문을 주고 기본 문형 변형(시제 등)을 만들게 한 뒤, 결과를 파일로 다운받아 앱에 가져오는 방식은 어떤지 질문
  - Claude 의견: 유효한 방법. 문장 자동 변형을 앱 내부(규칙 기반/API)로 구현하지 않고 **외부 ChatGPT 수작업 + 파일 가져오기**로 우회하면, 이전에 미결정이었던 "문장 자동 변형" 브레인스토밍 항목을 사실상 해결. 다만 앱 내 자동 생성이 아니라 수동 왕복(ChatGPT에서 생성 → 다운로드 → 앱에 업로드)이 필요하다는 점은 트레이드오프로 안내
- 형식 논의: JSON vs 다른 형식 질문 → **CSV/TSV 추천**
  - 이유: 이미 계획된 구글시트 "웹에 게시(CSV)" 가져오기 기능과 **파서를 공유**할 수 있어 별도 스키마/파서를 안 만들어도 됨
  - 순수 CSV는 문장에 쉼표가 포함될 경우(예: "Yesterday, I went home.") 깨질 위험이 있어, **TSV(탭 구분)**가 더 안전하다고 안내
- 사용자 확인 질문: "정리하면 폼입력/구글시트/ChatGPT 자료를 모두 커버하는 방법이 맞는지" → 명확화
  - 폼 입력은 파일이 아니라 UI 직접 입력이라 CSV/TSV 파싱과는 별개 경로. 구글시트 + ChatGPT 자료 두 가지가 "파일/URL 기반 CSV·TSV 파싱"으로 하나의 공통 파서를 공유하는 것이고, **세 경로 모두 최종적으로는 같은 데이터 저장소(구조)로 합쳐지는 것**이 핵심이라고 정리

### 구현 계획 (합의됨, 실행은 다음 세션)
- **데이터 모델**: `sentences: { id, kr, en, createdAt }[]`, LocalStorage에 저장 (기존 하드코딩 샘플 5개는 최초 실행 시 시드 데이터로 유지)
- **공통 파서**: TSV(기본)/CSV 텍스트 → `{kr, en}` 배열로 변환하는 함수 1개, 구글시트 가져오기와 파일 가져오기(ChatGPT용)가 공유
- **입력 경로 3가지**
  1. 폼 입력: 앱 내 입력 화면/모달(한국어·영어 필드 + 추가 버튼) → 바로 저장소에 추가
  2. 파일 가져오기(ChatGPT용): `<input type="file">`로 CSV/TSV 파일 선택 → FileReader로 읽어 공통 파서로 파싱 → 병합
  3. 구글시트 연동(체크리스트 4번): "웹에 게시" URL 입력 → fetch → 공통 파서로 파싱 → 병합, 실패 시 LocalStorage 캐시로 폴백
- **진행 순서 제안**(범위가 커서 기능 단위로 분할, 매 단계 승인 필요)
  1. 데이터 저장소 + 폼 입력 (나머지 두 경로의 토대)
  2. CSV/TSV 파일 가져오기 (공통 파서 포함)
  3. 구글시트 URL 연동 (파서 재사용, 체크리스트 4번 완료 처리)
- 위 작업을 하려면 진입점 UI(더보기 메뉴/설정 화면 등)가 새로 필요 — 지금은 없어서 1단계에서 간단한 형태로 함께 만들 계획
- **체크리스트 4번 문구 조정 여부는 아직 미확정** (사용자에게 물었으나 답변 전) — 다음 세션에서 확인 필요
- 사용자 결정: **이번 세션은 기획까지만, 실제 구현(코드 작성)은 다음 세션에서 진행**

### 다음 작업 제안
- 다음 세션 시작 시 이 계획 검토 후, "1단계: 데이터 저장소 + 폼 입력"부터 "시작" 승인받아 진행
- 체크리스트 4번 문구를 이 통합 방향에 맞게 조정할지 여부 확인
- 그 외 미해결 항목: 아이콘 하단 고정 방향(인앱 배치 vs iOS 커스텀 스플래시), 버튼 최종 위치(하단 고정 vs 중간 배치)도 함께 확인 가능

---

## 2026-08-11 (이어서 — 1단계: 데이터 저장소 + 폼 입력 구현)

### 진행 내용
- 사용자 "1단계 시작" 승인 → 데이터 저장소(LocalStorage) + 문장 추가 폼 UI 구현
  - `js/app.js`
    - 하드코딩 문장 5개를 `SEED_SENTENCES`로 전환, `{ id, kr, en, createdAt }` 구조로 LocalStorage(`tuktak_sentences`)에 저장
    - `loadSentences()`: 저장된 데이터 있으면 로드, 없으면(최초 실행) 시드 데이터로 초기화 후 저장
    - `saveSentences()`, `addSentence(kr, en)` 함수 추가
    - 문장 추가 모달 열기/닫기, 제출(빈 값 검증 포함) 이벤트 핸들러 추가
  - `index.html`: 카드 화면에 "+ 문장 추가" 진입점 버튼과 문장 추가 모달(한국어/영어 입력 + 추가/취소 버튼) 마크업 추가
  - `css/style.css`
    - 그레이스케일 CSS 변수(gray-50/100/200/300/500/800/900) `:root`에 추가 (CLAUDE.md 문서에는 있었으나 코드에는 미선언 상태였음)
    - 진입점 버튼, 모달 오버레이/박스/입력창/버튼 스타일 추가
    - `#card-screen`의 `grid-template-rows`를 `1fr auto` → `auto 1fr auto`로 조정 (진입점 버튼이 추가되며 grid 자식이 3개가 되어 레이아웃 깨짐 방지)
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 테스트
  - 진입점 버튼 표시, 모달 열기/닫기, 빈 값 제출 시 무시(검증 정상), 문장 추가 후 LocalStorage에 6번째 항목으로 저장 확인
  - 새로고침 후에도 LocalStorage 데이터 유지 확인 (영속성 정상)
  - 콘솔/페이지 에러 없음, 모달 UI 스크린샷으로 정상 렌더링 확인
  - 테스트에 사용한 임시 Playwright 설치, 스크립트, 스크린샷 모두 삭제하여 정리 완료
- 커밋(`4dd8031`) 후 사용자 요청으로 push 완료 → Vercel 자동 재배포
- **사용자 2차 확인 완료** — 배포 URL에서 문장 추가 정상 동작 확인됨 ("문장 추가돼")

### 다음 작업 제안
- **2단계: CSV/TSV 파일 가져오기** 진행 (공통 파서 구현 포함) — 사용자 "시작" 승인 대기
- 그 외 미해결 항목: 체크리스트 4번 문구 조정 여부, 아이콘 하단 고정 방향, 카드 화면 버튼 최종 위치

---

## 2026-08-11 (이어서 — 2단계: CSV/TSV 파일 가져오기 구현 + 파서 버그 수정)

### 진행 내용
- 사용자 "2단계 시작" 승인 → CSV/TSV 파일 가져오기(공통 파서 포함) 구현
  - `js/app.js`
    - `parseSentencesText(text)` 공통 파서 추가: 탭(TSV) 우선, 없으면 쉼표(CSV)로 구분해 `{kr, en}` 배열로 변환. 첫 줄이 헤더("한국어" 등)로 보이면 자동 스킵
    - 파일 선택 시 `FileReader`로 텍스트를 읽어 파서로 변환 후 `addSentence()`로 목록에 병합
  - `index.html`: 카드 화면에 "파일 가져오기" 버튼과 숨겨진 `<input type="file">`(csv/tsv/txt) 추가, 기존 "+ 문장 추가" 버튼과 한 줄에 배치
  - `css/style.css`: 두 버튼을 담는 `.screen-actions` 레이아웃, 공용 `.text-link-btn` 스타일 추가
- **1차 확인(Claude, 자동화)**: 임시 Playwright로 TSV(헤더 포함)·CSV(헤더 없음) 파일을 각각 가져와 헤더는 스킵되고 데이터 행만 정상 추가되는지 확인, LocalStorage 병합 확인, 콘솔 에러 없음
- **사용자에게 예제 TSV/CSV 다운로드 아티팩트를 만들어 안내했으나, 사용자 환경에서 아티팩트 링크가 "Page not found"로 열리지 않음** → 대신 채팅에 예시 문장(탭 구분) 텍스트와 메모장 저장 방법(파일 형식 "모든 파일", 확장자 `.tsv`, 인코딩 UTF-8)을 직접 안내
- **버그 발견**: 사용자가 폰(LAN IP `172.30.1.12:5501`)에서 메모장으로 만든 TSV 파일을 가져오기 했으나 "0개 문장을 추가했습니다" 발생 (스크린샷으로 확인)
  - 원인 추정: 메모장 등에 텍스트를 붙여넣는 과정에서 탭 문자가 스페이스로 바뀌어, 기존 파서가 탭도 쉼표도 없는 줄을 구분하지 못하고 전체 줄을 kr 필드 하나로만 인식 → en이 빈 문자열이 되어 스킵됨
  - `js/app.js`의 `parseSentencesText` 수정: 탭/쉼표가 모두 없는 줄은 **연속 공백 2칸 이상**을 구분자로 시도하는 폴백 로직 추가 (일반 문장의 단일 스페이스는 구분자로 오인하지 않도록 2칸 이상만 인정)
  - **1차 확인(Claude, 자동화)**: 탭/쉼표/연속공백(2~4칸) 케이스 모두 정상 파싱, 단일 스페이스만 있는 애매한 케이스는 의도대로 계속 0개 처리되는 것을 브라우저 컨텍스트에서 직접 `parseSentencesText` 호출로 검증
  - 테스트에 사용한 임시 Playwright 설치, 스크립트 모두 삭제하여 정리 완료
- **사용자 2차 확인 완료** — 같은 방식(메모장 저장 → 파일 가져오기)으로 재시도해 정상 동작 확인 ("성공")
- 커밋(`bdcac9a`) 후 push 완료 → Vercel 자동 재배포
- 참고: 대화 중 생성된 아티팩트(다운로드 페이지)는 사용자 환경에서 접근이 안 되는 것으로 확인되어 이후에는 이런 보조 자료가 필요하면 채팅 텍스트로 바로 제공하는 방식을 우선 고려할 것

### 다음 작업 제안
- **3단계: 구글시트 URL 연동** (체크리스트 4번, 공통 파서 재사용) 진행 여부 논의
- 그 외 미해결 항목: 체크리스트 4번 문구 조정 여부, 아이콘 하단 고정 방향(인앱 배치 vs iOS 커스텀 스플래시), 카드 화면 버튼 최종 위치(하단 고정 vs 중간 배치)
- 브레인스토밍 항목 중 "문장 목록 보기 + 수정/삭제" 기능은 아직 미구현 상태(사용자가 이번 세션에 질문으로 확인함) — 별도 기능 단위로 추후 진행 가능

- **세션 종료 (2026-08-11)**: 사용자 요청으로 여기서 세션 마무리. 위 "다음 작업 제안" 항목들이 다음 세션 시작점.

---

## 2026-08-11 (이어서 — 3단계: 구글시트 URL 연동, 체크리스트 4번 완료)

### 진행 내용
- 사용자 "시작" 승인 → 구글시트 CSV 연동(체크리스트 4번) 구현
  - `index.html`: 카드 화면 진입점(`.screen-actions`)에 "구글시트 연동" 버튼 추가 (기존 "+ 문장 추가", "파일 가져오기"와 한 줄에 배치), 구글시트 URL 입력 모달(`#sheet-modal`, 기존 `modal-*` 클래스 재사용) 추가
  - `js/app.js`: `sheet-submit-btn` 클릭 시 입력한 URL을 `fetch` → 응답 텍스트를 기존 공통 파서 `parseSentencesText()`로 파싱 → `addSentence()`로 기존 목록에 병합. 실패(네트워크 오류/404 등) 시 에러 alert만 띄우고 기존 LocalStorage 데이터는 변경하지 않음(폴백)
  - `css/style.css`: `.screen-actions`에 `flex-wrap: wrap` 추가 (버튼 3개로 늘어나며 좁은 화면에서 줄바꿈 가능하도록)
  - URL 저장(재방문 시 자동 재사용) 기능은 이번 범위에서 제외, 매번 입력하는 방식으로 최소 범위 구현
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치, `page.route`로 실제 구글시트 대신 모킹한 CSV 응답(성공 케이스)과 404(실패 케이스)를 준비해 iPhone 12 뷰포트로 테스트
  - 성공 케이스: 모킹 CSV 2건 fetch → 파싱 → 기존 5건에 병합되어 7건, 모달 자동 닫힘 확인
  - 실패 케이스: 404 응답 시 alert만 뜨고 문장 수 변화 없음(7건 유지) 확인
  - 새로고침 후에도 7건 유지(영속성 정상) 확인
  - 콘솔 에러 없음(테스트용 의도된 404 리소스 로그 제외), 스크린샷으로 3개 진입점 버튼 한 줄 배치 확인
  - 테스트에 사용한 임시 Playwright 설치, 스크립트, 스크린샷 모두 삭제하여 정리 완료
- **참고**: 실제 구글시트 "웹에 게시(CSV)" URL로는 아직 테스트하지 못함 (모킹 fetch로만 검증) — 실제 구글시트 CORS 허용 여부는 사용자 2차 확인에서 실제 URL로 함께 확인 필요
- CLAUDE.md 체크리스트 4번 [x] 처리 (2026-08-11)
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 구글시트에서 "파일 > 공유 > 웹에 게시" → CSV 링크를 발급받아, 앱에서 실제로 가져오기 동작하는지 확인 (모킹 테스트만으로는 실제 구글시트 CORS/응답 형식 이슈를 완전히 배제할 수 없음)
- 2차 확인 완료 후 커밋/push 진행
- 그 외 미해결 항목: 아이콘 하단 고정 방향(인앱 배치 vs iOS 커스텀 스플래시), 카드 화면 버튼 최종 위치(하단 고정 vs 중간 배치), 문장 목록 보기+수정/삭제 기능(미구현)

---

## 2026-08-11 (이어서 — 구글시트 연동 2차 확인 중 버그 발견/수정, 최종 확인 완료)

### 진행 내용
- 사용자 2차 확인 중 여러 문제가 순차적으로 드러남
  1. **저장소 분리 문제**: 브라우저 탭에서 가져온 문장이 홈 화면 설치 앱(별도 저장소)에는 안 보임 → 앱 안에서 직접 가져오기 하도록 안내해 해결
  2. **갱신 미반영 문제**: 구글시트에 새 문장을 추가해도 앱에 반영이 안 됨 → 원인 진단 과정에서 "자동 게시" 체크박스는 이미 켜져 있음을 확인(스크린샷), 시크릿 창 테스트로 구글 게시본 자체는 최신 데이터를 담고 있음을 확인 → **브라우저의 HTTP 캐시가 이전 fetch 응답을 재사용**하고 있던 것이 원인으로 좁혀짐
- 사용자 "시작" 승인 → `js/app.js`의 구글시트 fetch 로직 수정
  - `fetch(url)` → `fetch(noCacheUrl, { cache: 'no-store' })`로 변경, URL에 매번 다른 타임스탬프(`_cb=Date.now()`) 쿼리 파라미터를 붙여 브라우저가 항상 새로 요청하도록 강제
- **1차 확인(Claude, 자동화)**: 임시 Playwright로 서버 응답에 강한 캐시 헤더(`max-age=3600`)를 걸어도 매 요청이 실제로 서버에 도달하고(호출 횟수 2회 확인), 갱신된 내용이 정상 반영되는지 검증. 테스트 흔적 정리 완료
- 커밋(`4cb33ba`) 후 push 완료 → Vercel 자동 재배포
- **사용자 2차 확인 완료**: 홈 화면 설치 앱에서 "구글시트 연동"으로 새로 추가한 문장(5개)이 모두 정상 반영됨을 확인 — 처음 문제 삼았던 "홈 화면 앱" 환경에서도 검증 완료
- **체크리스트 4번(구글시트 데이터 연동) 최종 완료 확인됨**

### 다음 작업 제안
- 미해결 항목: 아이콘 하단 고정 방향(인앱 배치 vs iOS 커스텀 스플래시), 카드 화면 버튼 최종 위치(하단 고정 vs 중간 배치), 문장 목록 보기+수정/삭제 기능(미구현)
- 다음 기능 착수 시 브레인스토밍 우선순위(개인화 입력 폼은 이미 완료, 문장 자동 변형이 핵심 고도화 기능으로 남음) 참고해 논의

---

## 2026-08-11 (이어서 — 구글시트 연동 기능 제거, 파일 가져오기로 일원화)

### 진행 내용
- 사용자가 구글시트 실시간 연동 방식의 실효성에 의문 제기: 문장을 추가할 때마다 매번 링크를 다시 붙여넣어야 해 번거롭고, 게시 갱신 지연/캐싱 문제도 반복됨
- 대안 논의: 엑셀/구글시트로 표를 작성한 뒤 "웹에 게시" 대신 **CSV로 다운로드**해서 기존 "파일 가져오기" 기능으로 불러오는 방식이 훨씬 안정적이라고 판단 (다운로드 시점 스냅샷이라 캐싱/갱신 지연 문제 자체가 없음). 메모장으로 직접 작성하는 방법도 함께 안내
- 사용자 "시작" 승인 → **구글시트 연동 기능 전체 제거**
  - `index.html`: "구글시트 연동" 버튼, URL 입력 모달(`#sheet-modal`) 삭제
  - `js/app.js`: 구글시트 관련 DOM 참조, 모달 열기/닫기 함수, fetch/캐시버스팅 로직 전부 삭제. 공통 파서 `parseSentencesText()`와 파일 가져오기 기능은 그대로 유지(파일 가져오기가 이 파서를 계속 재사용)
  - `CLAUDE.md`: 체크리스트 4번을 "구글시트 데이터 연동" → **"문서(엑셀/메모장 등)로 작성 후 파일로 가져오기"**로 문구 변경, 제거 사유를 각주로 기록. 기술 스택의 "데이터 입력" 항목도 동일하게 수정
- **1차 확인(Claude, 자동화)**: 임시 Playwright로 (1) 구글시트 버튼/모달이 DOM에서 완전히 사라졌는지, (2) 기존 카드 확인/이전/다음, 문장 추가 폼, 파일 가져오기용 파서가 모두 정상 동작하는지 확인. 콘솔 에러 없음. 테스트 흔적 정리 완료
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

- 커밋(`de5bece`) 후 push 완료 → Vercel 자동 재배포
- **사용자 2차 확인 완료** — 배포 사이트에서 구글시트 버튼이 사라지고 파일 가져오기만 남은 것 확인됨

### 다음 작업 제안
- 그 외 미해결 항목: 아이콘 하단 고정 방향(인앱 배치 vs iOS 커스텀 스플래시), 카드 화면 버튼 최종 위치(하단 고정 vs 중간 배치), 문장 목록 보기+수정/삭제 기능(미구현)
- 다음 기능 착수 시 브레인스토밍 우선순위(문장 자동 변형이 핵심 고도화 기능으로 남음) 참고해 논의

---

## 2026-08-11 (이어서 — 데이터 입력 방식 최종 정리 + 서버 도입 여부 논의, 세션 종료)

### 논의 내용 (기획만 진행, 코드 변경 없음)
- 사용자가 "파일 옮기는 게 번거롭지 않냐"는 질문에서 출발해, 실제 사용 시나리오를 다음과 같이 정리
  - **문장을 하나씩 입력**: 핸드폰 앱의 **폼 입력**(+ 문장 추가) 사용
  - **문장을 대량으로 입력**: 데스크탑에서 엑셀/구글시트로 편하게 작성 → CSV로 다운로드 → **구글 드라이브**에 올려서 → 폰에서 "파일 가져오기"로 선택 (안드로이드 파일 선택 화면에서 구글 드라이브가 바로 보여 다운로드 없이 선택 가능)
- Claude 의견: 두 경로 모두 이미 구현되어 있고 안정적으로 검증된 방식이라 합리적인 조합이라고 판단. 유일한 트레이드오프는 대량 입력 시 "다운로드 → 드라이브 업로드 → 폰에서 선택" 몇 단계를 거쳐야 한다는 점이지만, 서버 없는 구조에서는 자연스러운 비용이라고 정리
- 사용자가 "서버를 쓰면 어떤 장단점이 있는지" 질문 → Claude 답변
  - 장점: 모든 기기 자동 실시간 동기화(파일 이동 자체가 불필요해짐), 추후 여러 사용자·기기 간 학습 상태 공유 용이
  - 단점: 로그인/인증·서버 운영·보안 등 복잡도 증가, 오프라인 사용을 위한 별도 동기화 로직 필요, 무료 티어 초과 시 비용 발생
  - Claude 추천: 지금은 개인용 소량 데이터라 서버 없이 파일 가져오기로 충분 — 초기 기획 문서(`아이디어브리핑.txt`)에서도 서버(Supabase) 연동은 3단계 선택 사항으로 남겨뒀던 부분과 일치. 여러 기기 자동 동기화가 꼭 필요해지는 시점이 오면 그때 재검토
- **사용자 최종 결정**: 위 방향(폼 입력 + 구글드라이브 경유 파일 가져오기)으로 확정, **서버 도입은 지금 하지 않음** (추후 필요 시 재논의)

### 세션 종료 (2026-08-11)
- 사용자 요청으로 여기서 세션 마무리. 이번 세션에 결정된 사항 요약:
  1. ~~구글시트 실시간 연동~~ 기능은 제거하고, **파일 가져오기(CSV/TSV/TXT)**로 데이터 입력 방식을 일원화 (완료, 배포됨)
  2. 실사용 워크플로: **문장 1개 = 폼 입력(앱) / 대량 = 데스크탑 작성 → 구글드라이브 → 폰에서 파일 가져오기**로 확정 (별도 코드 작업 불필요, 이미 구현된 기능으로 충분)
  3. **서버(백엔드) 도입은 보류** — 여러 기기 자동 동기화가 실제로 필요해지기 전까지는 현재의 LocalStorage + 파일 가져오기 구조 유지

### 다음 작업 제안
- 다음 세션 시작 시 이 PROGRESS.md를 먼저 확인하고, 아래 미해결 항목 중 우선순위를 정해 진행
  - 체크리스트 3번(음성 답변 흐름) — 마이너 목표로 후순위 합의된 상태
  - 브레인스토밍 핵심 항목: **문장 자동 변형 출제** (규칙 기반 vs LLM API, 미결정 — 착수 시 재논의)
  - 그 외: 아이콘 하단 고정 방향, 카드 화면 버튼 최종 위치, 문장 목록 보기+수정/삭제 기능(미구현)

---

## 2026-08-12 (체크리스트 5번·6번 추가 + 5번 글자 크기 설정 구현)

### 진행 내용
- 사용자 요청으로 `CLAUDE.md` 체크리스트에 신규 항목 2개 추가
  - 5번: 글자 크기 설정
  - 6번: 다크모드 (`[data-theme="dark"]` 변수는 컬러 시스템 문서에 자리만 있고 값은 미정 상태)
  - 기존 5번("이후 단계 추가 예정")은 7번으로 밀림
- 사용자 "시작" 승인 → **체크리스트 5번(글자 크기 설정)** 구현
  - 범위 확정(질문으로 확인): 카드 화면 텍스트(한국어/영어)만 적용, 작게/보통/크게 3단계 프리셋, 새 "설정" 메뉴 신설(추후 다크모드도 같은 모달에 추가 가능한 구조)
  - `index.html`: 카드 화면 진입점(`.screen-actions`)에 "설정" 버튼 추가, 설정 모달(`#settings-modal`)에 글자 크기 프리셋 버튼(작게/보통/크게) 마크업 추가
  - `css/style.css`
    - `:root`에 `--kr-font-size`(22px)·`--kr-font-size-revealed`(16px)·`--en-font-size`(24px) 변수 추가(기본값 = 보통), `[data-font-size="small"]`/`[data-font-size="large"]` 셀렉터로 프리셋별 값 오버라이드
    - `.kr-text`/`.card.revealed .kr-text`/`.en-text`의 고정 `font-size` 값을 위 변수 참조로 변경
    - `.font-size-options`, `.font-size-btn`(선택 시 `.active`로 브랜드 블루 강조) 스타일 추가
  - `js/app.js`: `FONT_SIZE_KEY`로 LocalStorage(`tuktak_font_size`)에 선택값 저장, `applyFontSize()`가 `<html data-font-size="...">` 속성을 갱신해 CSS 변수 적용 및 프리셋 버튼 `.active` 표시 동기화, 앱 로드 시 저장된 값(없으면 기본 medium) 적용
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 테스트
  - 초기 기본값(보통, 22px) 확인, 설정 모달 열기 확인
  - "크게" 선택 → kr-text 26px로 변경 및 해당 버튼에 `.active` 표시 확인
  - 새로고침 후에도 "large" 설정과 26px가 유지됨을 확인 (영속성 정상)
  - "작게" 선택 + 정답 노출 상태에서 en-text 20px, kr-text(revealed) 14px로 정상 반영 확인
  - 콘솔/페이지 에러 없음
  - 테스트에 사용한 임시 Playwright 설치, 스크립트 모두 삭제하여 정리 완료
- `CLAUDE.md` 체크리스트 5번 [x] 처리 (2026-08-12)
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 설정 메뉴 진입, 글자 크기 3단계 전환, 새로고침 후 유지 여부 확인
- 2차 확인 완료 후 커밋/push 진행
- 그 외 미해결 항목: 체크리스트 3번(음성 답변, 후순위), 6번(다크모드), 문장 자동 변형 출제, 아이콘 하단 고정 방향, 카드 화면 버튼 최종 위치, 문장 목록 보기+수정/삭제 기능

---

## 2026-08-12 (이어서 — 글자 크기 UI를 3버튼 프리셋 → 5단계 도트+스테퍼 방식으로 변경)

### 진행 내용
- 사용자가 참고 이미지(`temp/image copy 5.png` — 메모앱 설정 화면: "글자크기" 항목이 도트 인디케이터 + `−`/`+` 스테퍼 버튼, "다크모드"는 별도 토글)를 제시하며 두 가지 요청
  1. 글자 크기 단계를 3단계 → **5단계**로 확대
  2. 기존 "작게/보통/크게" 3개 버튼 방식(클릭할 때마다 버튼 테두리·배경이 바뀌어 "화면이 바뀌는 것처럼 보인다"는 피드백)을 참고 이미지처럼 **도트 인디케이터 + `−`/`+` 스테퍼** 방식으로 교체
  - 다크모드 토글은 이번 범위에서 제외 (체크리스트 6번은 별도 승인 필요, 사용자도 글자 크기 관련 요청만 함)
- 사용자 "진행" 승인 → 구현
  - `index.html`: 설정 모달의 글자 크기 영역을 버튼 3개 → 도트 5개(`.font-size-dot`, `data-index="1"~"5"`) + `−`/`+` 스테퍼 버튼(`#font-size-dec-btn`/`#font-size-inc-btn`)으로 교체
  - `css/style.css`: `.font-size-options`/`.font-size-btn` 스타일 제거, `.font-size-row`/`.font-size-dots`/`.font-size-dot(.active)`/`.stepper`/`.stepper-btn(:disabled)` 스타일 추가. 기존 `[data-font-size="small"/"large"]` 속성 셀렉터 블록은 제거(5단계부터는 JS가 CSS 변수를 인라인으로 직접 갱신하는 방식으로 전환)
  - `js/app.js`: `FONT_SIZE_LEVELS`(5단계 배열, 각 단계별 kr/revealed/en px 값) 도입, 레벨 인덱스(1~5, 기본 3=보통) 기준으로 관리. `applyFontSize()`가 `document.documentElement.style.setProperty()`로 CSS 변수를 직접 갱신 + 활성 도트 표시 + 최소/최대 도달 시 해당 스테퍼 버튼 비활성화. LocalStorage(`tuktak_font_size`)에는 레벨 인덱스(숫자)를 저장
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 테스트
  - 초기 레벨(3, 22px)과 활성 도트 확인, 이 상태에서 `−`/`+` 버튼 모두 활성화 상태임을 확인
  - `+` 2회 클릭 → 레벨5(28px)까지 정상 도달, 도달 시 `+` 버튼이 `disabled` 처리됨을 확인, 그 상태에서 추가 클릭해도 값이 변하지 않음(상한 clamp 정상) 확인
  - `−` 4회 클릭 → 레벨1(16px)까지 정상 도달, `−` 버튼도 동일하게 비활성화됨 확인
  - 새로고침 후에도 레벨1(16px) 유지 확인 (영속성 정상)
  - 레벨1 상태에서 정답 노출 시 en-text 18px, kr-text(revealed) 12px로 정상 반영 확인
  - 콘솔/페이지 에러 없음
  - 테스트에 사용한 임시 Playwright 설치, 스크립트 모두 삭제하여 정리 완료
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 도트+스테퍼 UI가 참고 이미지처럼 자연스럽게 보이는지, `−`/`+` 클릭 시 이전처럼 "화면이 바뀌는" 느낌 없이 부드럽게 전환되는지 확인
- 2차 확인 완료 후 커밋/push 진행 (이전 3버튼 방식 커밋은 아직 없었으므로 이번이 글자 크기 기능의 최초 커밋이 됨)
- 그 외 미해결 항목: 체크리스트 3번(음성 답변, 후순위), 6번(다크모드 — 참고 이미지의 토글 스타일 참고 가능), 문장 자동 변형 출제, 아이콘 하단 고정 방향, 카드 화면 버튼 최종 위치, 문장 목록 보기+수정/삭제 기능

---

## 2026-08-12 (이어서 — 설정 패널을 중앙 모달 → 하단 시트로 변경)

### 진행 내용
- 사용자 요청: "모달형식말고 화면을 볼수있게 해줘" — 기존 설정 모달이 화면 전체를 어두운 배경으로 덮어 카드 텍스트가 가려지는 문제 지적 (글자 크기를 조절하며 실시간으로 카드에 반영되는 걸 보고 싶은데 모달에 가려 안 보임)
- 사용자에게 패널 스타일 옵션 확인 → **하단 시트**(화면 아래에서 올라오는 패널, 카드 텍스트는 그대로 노출) 방식 선택
- 사용자 "진행" 승인 → 구현 (체크리스트 5번 범위 내 UI 방식 변경, 문장 추가 모달은 기존 중앙 모달 방식 그대로 유지)
  - `index.html`: `#settings-modal`/`.modal-box`에 `settings-sheet-overlay`/`settings-sheet-box` 클래스 추가 (마크업 구조는 그대로, 스타일만 별도 클래스로 분리)
  - `css/style.css`: `.settings-sheet-overlay`(배경 투명, `align-items: flex-end`로 하단 정렬, `padding:0`), `.settings-sheet-box`(위쪽 모서리만 둥글게, 하단 safe-area 패딩, 그림자) 추가. 기존 `.modal-overlay`/`.modal-box` 공용 스타일은 변경하지 않아 문장 추가 모달에는 영향 없음
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 테스트
  - 설정 오버레이 배경이 투명(`rgba(0,0,0,0)`)임을 확인
  - 카드 텍스트(`#kr-text`)와 설정 시트의 좌표를 비교해 서로 겹치지 않음을 확인, 시트가 화면 맨 아래에 붙어 있음을 확인
  - 시트가 열린 상태에서도 글자 크기 `+`/`−` 조절이 정상 동작함을 확인
  - 닫기 버튼 정상 동작 확인
  - **회귀 확인**: 문장 추가 모달(`#add-modal`)은 배경이 여전히 어두운 반투명(`rgba(20,23,28,0.5)`)으로 기존 중앙 모달 방식 그대로 유지되는지 확인해 이번 변경이 다른 모달에 영향 주지 않았음을 검증
  - 콘솔/페이지 에러 없음
  - 테스트에 사용한 임시 Playwright 설치, 스크립트 모두 삭제하여 정리 완료
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 설정 패널이 하단에서 올라오고, 카드 텍스트를 보며 글자 크기를 조절할 수 있는지 확인
- 2차 확인 완료 후 커밋/push 진행 (글자 크기 기능 전체의 최초 커밋)
- 그 외 미해결 항목: 체크리스트 3번(음성 답변, 후순위), 6번(다크모드), 문장 자동 변형 출제, 아이콘 하단 고정 방향, 카드 화면 버튼 최종 위치, 문장 목록 보기+수정/삭제 기능

---

## 2026-08-12 (이어서 — 문장추가/파일가져오기/설정 진입점을 하단 네비게이션 바로 이동)

### 진행 내용
- 사용자 요청: 카드 화면 상단에 있던 문장추가/파일가져오기/설정 텍스트 링크를 하단 네비게이션 바 형태로 옮겨달라는 요청
- 세부 방식 확인(질문으로 결정)
  1. 표현 방식: **아이콘 + 텍스트 라벨** (아이콘 없이 텍스트만인 방식 대신 선택)
  2. 배치: 기존 이전/확인/다음 버튼(`control-bar`)은 그대로 유지하고, 그 **아래에 새 줄로 네비게이션 바 추가**
- 아이콘 구현 방식: 새 라이브러리(lucide npm/CDN)를 설치하지 않고, 필요한 아이콘 3개(circle-plus/upload/settings)만 SVG를 직접 인라인으로 삽입 — 오프라인 PWA 특성상 외부 로드 의존 없이 동작하도록 함
- 사용자 "진행" 승인 → 구현 (기존 3개 기능의 진입점 위치만 이동, 새 기능 추가 아님 — 체크리스트 5번 범위 내 UI 개편)
  - `index.html`: 상단 `.screen-actions` 제거, `.control-bar` 아래 `<nav class="bottom-nav">` 신설(아이콘 SVG + 라벨 3개: 문장추가/파일가져오기/설정). 기존 버튼 id(`add-open-btn`, `import-open-btn`, `settings-open-btn`)와 숨김 파일 입력은 그대로 이 안으로 이동해 `js/app.js`는 수정 불필요
  - `css/style.css`: `#card-screen`의 `grid-template-rows`를 `auto 1fr auto`(상단 액션/카드/버튼) → `1fr auto auto`(카드/버튼/하단 네비)로 변경, `.control-bar`에 `margin-bottom: 20px` 추가로 두 버튼 줄 사이 간격 확보. 기존 `.screen-actions`/`.text-link-btn` 스타일을 `.bottom-nav`(상단 구분선)/`.bottom-nav-item`(아이콘 위·라벨 아래, 3등분, 클릭 시 브랜드 블루로 색 변경)으로 교체
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 320px(소형)·390px(iPhone 12) 두 뷰포트로 테스트
  - 하단 네비게이션 바가 화면 안에 들어오고 이전/확인/다음 버튼 아래에 위치함을 좌표로 확인
  - 문장추가 클릭 → 모달 열림, 설정 클릭 → 하단 시트 열림, 파일가져오기 클릭 → 네이티브 파일 선택 동작(에러 없음) 확인
  - 스크린샷으로 아이콘(원+플러스, 업로드 화살표, 톱니바퀴)이 라벨과 함께 정상 렌더링되는 것 육안 확인
  - 콘솔/페이지 에러 없음
  - 테스트에 사용한 임시 Playwright 설치, 스크립트, 스크린샷 모두 삭제하여 정리 완료
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 하단 네비게이션 바(아이콘+라벨)가 잘 보이고, 세 버튼이 정상 동작하는지 확인 (이전 세션들의 글자 크기 2차 확인과 함께 진행 가능)
- 2차 확인 완료 후 커밋/push 진행 (이번이 글자 크기 + 하단 네비게이션 UI 개편의 최초 커밋이 됨)
- 그 외 미해결 항목: 체크리스트 3번(음성 답변, 후순위), 6번(다크모드), 문장 자동 변형 출제, 아이콘 하단 고정 방향(PWA 스플래시), 문장 목록 보기+수정/삭제 기능

- **사용자 2차 확인 완료** (폰에서 확인) → 커밋(`ab29840`) + push 완료. 이어서 CLAUDE.md에 아이콘 인라인 SVG 방식/UI 레이아웃 규칙(하단 네비 구조, 모달 vs 하단 시트 구분) 문서화 요청 → 반영 후 별도 커밋(`bbe1c7c`) + push 완료

---

## 2026-08-12 (이어서 — 하단 버튼 과밀 문제로 상단바+더보기 메뉴 도입)

### 진행 내용
- 사용자 피드백: "하단에 너무 많은 버튼들이 모여 있어서 불편감이 있어" — 이전/확인/다음(3개) + 문장추가/파일가져오기/설정(3개), 총 6개 버튼이 하단에 몰려있는 문제 제기. "상단에 바를 만들고 왼쪽에 로고, 오른쪽에 더보기 메뉴를 만들어서 하단 버튼들을 이동하면 어떨지" 아이디어 제시, 점검 및 대안 요청
- Claude 검토 의견: 하단에는 학습 핵심(이전/확인/다음)만 남기고 문장추가/파일가져오기/설정은 상단 로고+더보기 메뉴로 옮기는 사용자 안을 추천. 트레이드오프는 유틸 기능 접근이 1탭→2탭(더보기 열기→선택)으로 늘어난다는 점. 대안(문장추가/파일가져오기를 설정 시트 안 "데이터 관리" 섹션으로 합쳐 하단에 설정 아이콘 하나만 남기는 방법)도 제시했으나, 문장추가처럼 자주 쓰는 기능이 설정 안에 묻히는 단점이 있어 상단바+더보기 안을 최종 추천 → 사용자가 추천안 채택
- 사용자 "진행" 승인 → 구현 (체크리스트 5번 범위 내 UI 개편, 각 기능의 동작 자체는 변경 없이 진입점만 이동)
  - `index.html`: `#card-screen` 상단에 `<header class="card-topbar">`(왼쪽 "TUK TAK" 로고, 오른쪽 더보기 아이콘 버튼 `#more-open-btn`) 추가. 기존 `.bottom-nav`를 제거하고 그 안의 버튼 3개(`add-open-btn`/`import-open-btn`/`settings-open-btn`, id 유지) + 숨김 파일 입력을 더보기 버튼 아래 드롭다운 `#more-menu`로 이동
  - `css/style.css`: `#card-screen` grid를 `1fr auto auto`(카드/버튼/하단네비) → `auto 1fr auto`(상단바/카드/버튼)로 변경. `.card-topbar`/`.topbar-logo`/`.topbar-more-btn` 및 드롭다운 `.more-menu`(더보기 버튼 우측 하단에 앵커, 그림자, 아이콘+라벨 세로 목록)/`.more-menu-item` 스타일 신규 작성. 기존 `.bottom-nav`/`.bottom-nav-item` 스타일과 `.control-bar`의 여분 `margin-bottom`(하단 네비와의 간격용이었음) 제거
  - `js/app.js`: `moreOpenBtn`/`moreMenu` 참조 추가, 더보기 버튼 클릭 시 메뉴 토글, 메뉴 안 항목(문장추가/파일가져오기/설정) 클릭 시 자동 닫힘, 메뉴 바깥(`.topbar-more` 영역 밖) 클릭 시 자동 닫힘 로직 추가
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 테스트
  - `.bottom-nav` 요소가 DOM에서 완전히 사라졌음을 확인, 로고/더보기 버튼 정상 노출 확인
  - 더보기 클릭 시 메뉴 열림, 메뉴가 화면 안에 정상 위치(더보기 버튼 우측 하단)함을 좌표로 확인
  - 카드 바깥 영역 클릭 시 메뉴 자동 닫힘 확인
  - 문장추가 클릭 → 모달 열림 + 메뉴 자동 닫힘, 설정 클릭 → 하단 시트 열림 + 메뉴 자동 닫힘 확인
  - 설정 시트가 열린 상태에서 글자 크기 `+` 조절도 기존과 동일하게 정상 동작함을 확인(회귀 없음)
  - 스크린샷으로 상단바(로고+더보기 아이콘)와 드롭다운 메뉴(아이콘+라벨 3개) 육안 확인
  - 콘솔/페이지 에러 없음
  - 테스트에 사용한 임시 Playwright 설치, 스크립트, 스크린샷 모두 삭제하여 정리 완료
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 상단바(로고/더보기)와 드롭다운 메뉴가 잘 보이고, 하단에는 이전/확인/다음 버튼만 남아 여유로워졌는지 확인
- 2차 확인 완료 후 커밋/push 진행
- 그 외 미해결 항목: 체크리스트 3번(음성 답변, 후순위), 6번(다크모드), 문장 자동 변형 출제, 아이콘 하단 고정 방향(PWA 스플래시), 문장 목록 보기+수정/삭제 기능

---

## 2026-08-12 (이어서 — 상단바 브랜드 컬러화 + 설정을 더보기 메뉴에 플랫 통합 + 다크모드 신규 구현)

### 진행 내용
- 사용자가 참고 이미지(`temp/image copy 6.png` — "SPARK" 메모앱 실제 화면: 진한 네이비 상단바 + 흰색 로고/더보기 아이콘, 더보기 드롭다운 안에 메모내보내기/메모가져오기/글자크기(도트+스테퍼)/다크모드(토글)가 한 목록에 플랫하게 배치됨)를 공유
- 사용자 확인: 상단바를 참고 이미지처럼 브랜드 네이비 배경 + 흰색 로고/아이콘으로 변경하기로 결정
- 사용자 추가 요청: 기존 "설정" 항목을 눌러 별도 하단 시트로 이동하는 방식 대신, 참고 이미지처럼 글자크기/다크모드를 더보기 메뉴 안에서 바로 조절할 수 있게 하고, 다크모드도 새로 추가해달라는 요청. "설정 버튼을 아코디언처럼 펼치는 게 나을지" 질문
  - Claude 검토 의견: 항목이 글자크기·다크모드 2개뿐이라 아코디언보다 플랫 목록이 클릭 수가 적어 더 낫다고 판단, 참고 이미지와 동일한 플랫 구조 추천 → 사용자 채택
- 사용자 "진행" 승인 → 구현 (체크리스트 5번 UI 마무리 + 6번 다크모드 함께 진행)
  1. **상단바 브랜드 컬러화**: `.card-topbar`에 카드 화면 패딩(24px) 밖으로 확장하는 음수 마진을 줘서 화면 좌우 끝까지 꽉 차게 만들고, 배경을 `--color-brand-dark`(네이비)로 설정
  2. **더보기 메뉴 플랫 통합**: `#settings-modal`(하단 시트) 마크업을 완전히 제거. 더보기 메뉴(`#more-menu`) 안에 기존 문장추가/파일가져오기 버튼에 이어 글자크기 로우(도트 5개+스테퍼, 기존 것 재사용)와 다크모드 토글 로우를 구분선과 함께 바로 배치. 메뉴 폭을 168px→250px로 확장해 새 로우가 들어갈 공간 확보
  3. **다크모드 신규 구현**: `css/style.css`에 `[data-theme="dark"]` 팔레트 새로 정의(배경/그레이스케일 반전, 브랜드 블루·네이비·코럴은 유지), 라이트 모드에서 `--color-brand-dark`를 텍스트색으로 쓰던 요소(`body`, `.kr-text`, `.modal-title`, `.btn-outline`, `.stepper-btn`)는 다크 배경 위 시인성 문제로 다크모드에서 `--color-gray-900`(밝은 값)로 개별 재지정. 토글 스위치(`.theme-toggle`) 신규 스타일. `js/app.js`에 `THEME_KEY`로 LocalStorage에 테마 저장, `applyTheme()`가 `<html data-theme="...">` 전환 + 토글 `aria-checked` 동기화, 앱 로드 시 저장된 테마(없으면 light) 적용
- **버그 발견 및 수정(1차 확인 중)**: 다크모드 토글 후 상단바 로고/더보기 아이콘이 배경(네이비)과 거의 구분 안 되는 어두운 색으로 바뀌는 문제 발견
  - 원인: `.topbar-logo`/`.topbar-more-btn`/`.start-logo`/`.btn-primary`/`.btn-outline:active`/`.modal-btn-submit`/`.theme-toggle-thumb`가 "브랜드색 배경 위 흰색 텍스트"를 표현하려고 `var(--color-base)`를 재사용하고 있었는데, 이번에 `--color-base`를 다크모드 페이지 배경 토큰(라이트 흰색 ↔ 다크 `#14171C`)으로 재정의하면서 상단바처럼 **항상 브랜드색 배경인 요소의 흰색도 함께 어두워져 버림**
  - 수정: 위 요소들의 `var(--color-base)` 사용을 전부 리터럴 `#FFFFFF`로 교체 (테마와 무관하게 항상 흰색 고정). `body`/`.modal-box`/`.modal-input`/`.more-menu`/`.stepper-btn`/`.btn-outline`(기본 상태) 배경처럼 실제로 테마에 따라 바뀌어야 하는 곳은 `var(--color-base)` 그대로 유지
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 테스트
  - `#settings-modal` 요소가 DOM에서 완전히 사라졌음을 확인
  - 더보기 메뉴가 확장된 폭(250px)으로 화면 안에 정상 위치, 글자크기 스테퍼가 메뉴 안에서 바로 동작하고 메뉴가 닫히지 않음을 확인
  - 다크모드 토글 클릭 → `data-theme="dark"` 전환, body 배경/텍스트·kr-text 색상 모두 정상 반전 확인, 메뉴는 유지됨(설정 조절 중 메뉴가 안 닫히는 것 확인), 새로고침 후에도 다크모드 유지 확인, 다시 클릭 시 라이트로 정상 복귀 확인
  - 버그 수정 후 상단바 배경(네이비, 테마 무관 고정) + 로고 색(흰색, `rgb(255,255,255)`) 정상 확인
  - 스크린샷으로 라이트 모드 메뉴, 다크 모드 메뉴, 다크 모드 카드 화면 모두 참고 이미지와 유사하게 육안 확인 (상단바 네이비 유지, 카드/버튼/텍스트 전체 다크 반전, 대비 양호)
  - 콘솔/페이지 에러 없음
  - 테스트에 사용한 임시 Playwright 설치, 스크립트, 스크린샷 모두 삭제하여 정리 완료
- `CLAUDE.md` 업데이트: 체크리스트 5번 설명을 "더보기 드롭다운 안 상시 노출" 방식으로 수정 + 하단 시트 제거 이력 각주 추가, 6번(다크모드) `[x]` 처리 (2026-08-12), 컬러 시스템에 Dark Mode 표 신규 작성, Base White 행에 다크모드 전환/리터럴 흰색 사용 규칙 각주 추가, 아이콘 목록에서 더 이상 안 쓰는 `settings` 제거하고 `ellipsis-vertical` 추가, UI 레이아웃 규칙에 상단바/더보기 플랫 메뉴/브랜드색 배경 위 고정 흰색 규칙 신규 작성
- 아직 커밋/push 전 — 사용자 확인 후 진행 예정

### 다음 작업 제안
- **사용자 2차 확인 필요**: 실제 폰에서 상단바가 네이비로 꽉 차 보이는지, 더보기 메뉴 안에서 글자크기/다크모드를 바로 조절할 수 있는지, 다크모드 전환 시 전체 화면(카드/버튼/모달 등)이 자연스럽게 반전되는지 확인
- 2차 확인 완료 후 커밋/push 진행
- 그 외 미해결 항목: 체크리스트 3번(음성 답변, 후순위), 문장 자동 변형 출제, 아이콘 하단 고정 방향(PWA 스플래시), 문장 목록 보기+수정/삭제 기능

- **사용자 2차 확인 완료** → 커밋(`8bf38e1`) + push 완료 → Vercel 자동 재배포

### 세션 종료 (2026-08-12)
이번 세션(2026-08-12)에서 완료된 사항 요약
1. **체크리스트 5번(글자 크기 설정) 완료**: 5단계(도트+스테퍼) 조절, 최종적으로 상단바 더보기 드롭다운 메뉴 안에 상시 노출되는 형태로 정착
2. **체크리스트 6번(다크모드) 신규 완료**: `[data-theme="dark"]` 팔레트 정의, 더보기 메뉴 안 토글로 라이트/다크 전환, LocalStorage에 저장되어 재방문 시 유지
3. **UI 개편**: 카드 화면 하단에 버튼 6개가 몰려있던 문제를 상단바(로고+더보기)로 해결 — 하단에는 이전/확인/다음만 남음. 상단바는 브랜드 네이비 배경 + 흰색 로고/아이콘으로 화면 끝까지 꽉 차게 표시
4. 과정에서 `--color-base`를 다크모드 배경 토큰과 "브랜드색 배경 위 고정 흰색"에 동시에 재사용해 발생한 버그(다크모드 시 상단바 로고가 안 보이던 문제)를 발견해 리터럴 흰색으로 분리 수정
5. CLAUDE.md 체크리스트(5·6번), 컬러 시스템(다크모드 표), 아이콘/UI 레이아웃 규칙 문서 최신화 완료
6. 총 4회 커밋 + push (`ab29840`, `bbe1c7c`, `8bf38e1` 등), 매 기능 단위마다 1차(자동화)·2차(실기기) 확인 완료

### 다음 작업 제안
- 다음 세션 시작 시 이 PROGRESS.md를 먼저 확인하고, 아래 미해결 항목 중 우선순위를 정해 진행
  - 체크리스트 3번(음성 답변 흐름) — 마이너 목표로 후순위 합의된 상태
  - 브레인스토밍 핵심 항목: **문장 자동 변형 출제** (규칙 기반 vs LLM API, 미결정 — 착수 시 재논의)
  - 그 외: 아이콘 하단 고정 방향(PWA 스플래시), 문장 목록 보기+수정/삭제 기능(미구현)

---

## 2026-08-13 (체크리스트 7번: 문장 목록 보기 + 수정/삭제 구현)

### 진행 내용
- 사용자 "구현 시작" 요청 → 세부 UI 방식이 미정이라 질문으로 먼저 범위 확정
  1. 목록 화면 형태: **전체화면(새 화면)**
  2. 수정 방식: **기존 "문장 추가" 모달 재사용**(항목 탭 시 프리필 후 저장)
  3. 삭제 방식: **각 항목 삭제 아이콘 버튼**(확인창 후 삭제)
- 사용자 확정 답변 받은 후 계획 설명 → 구현 진행 (체크리스트 신규 7번)
  - `index.html`: 더보기 메뉴에 "문장목록" 항목 추가, 전체화면 `#list-screen`(상단 뒤로가기+타이틀, 목록 컨테이너) 신설, 기존 `#add-modal`의 `<h2>`에 `id="add-modal-title"` 추가해 수정 모드에서 타이틀을 JS로 전환할 수 있게 함
  - `js/app.js`
    - `updateSentence(id, kr, en)`, `deleteSentence(id)` 함수 추가(LocalStorage 저장 및 `currentIndex` 범위 보정 포함)
    - `openAddModal()`을 확장해 문장 객체를 인자로 받으면 "문장 수정" 모드(타이틀/버튼 텍스트 전환, 입력칸 프리필)로 동작하도록 변경, 제출 시 `editingId` 유무로 추가/수정 분기
    - `renderSentenceList()`: 목록 항목을 `textContent` 기반으로 DOM 생성(사용자 입력 데이터를 다루므로 `innerHTML` 대신 사용해 XSS 위험 원천 차단), 항목 탭 시 수정 모달 오픈, 삭제 아이콘 클릭 시 `confirm()` 확인 후 삭제 — 단, **문장이 1개만 남은 경우 삭제를 막고 안내 alert** 표시(빈 목록 상태의 카드 화면 깨짐을 방지하기 위한 최소 안전장치)
  - `css/style.css`: 목록 화면 헤더(브랜드 네이비, 항상 흰색 고정 — 기존 상단바 규칙과 동일)와 목록 항목(한국어/영어 미리보기 + 삭제 아이콘) 스타일 신규 추가. 삭제 아이콘 강조색은 Point Coral 대신 그레이스케일(`--color-gray-800`) 사용(Point Coral 사용 보류 방침 유지)
- **1차 확인(Claude, 자동화)**: 임시 스크래치패드에 Playwright 임시 설치 후 iPhone 12 뷰포트로 테스트
  - 목록 진입 시 시드 5개 정상 노출
  - 항목 탭 → 수정 모달이 "문장 수정"/"저장" 라벨로 전환되고 기존 값이 프리필됨 확인, 값 변경 후 저장 시 목록에 즉시 반영 확인
  - 삭제 아이콘 클릭 → confirm 다이얼로그 노출, 수락 시 목록에서 제거(개수 감소) 확인
  - 뒤로가기로 카드 화면 복귀 정상 확인
  - 새로고침 후에도 수정/삭제 내용이 유지됨(영속성) 확인
  - 1개만 남을 때까지 반복 삭제 후, 마지막 1개 삭제 시도 시 삭제되지 않고 "최소 1개의 문장은 있어야 합니다" 알림이 뜨는지 확인
  - 콘솔/페이지 에러 없음
  - 테스트에 사용한 임시 Playwright 설치, 서버 스크립트 모두 삭제하여 정리 완료 (정리 중 이전 세션에서 남아있던 임시 서버 프로세스 잔여물도 함께 종료)
- `CLAUDE.md` 체크리스트 7번 [x] 처리 (2026-08-13)
- **사용자 2차 확인 완료** → 커밋(`d462aed`) + push 완료 → Vercel 자동 재배포

### 다음 작업 제안
- 다음 세션 시작 시 이 PROGRESS.md를 먼저 확인하고, 아래 미해결 항목 중 우선순위를 정해 진행
  - 체크리스트 3번(음성 답변 흐름) — 마이너 목표로 후순위 합의된 상태
  - 브레인스토밍 핵심 항목: **문장 자동 변형 출제** (규칙 기반 vs LLM API, 미결정 — 착수 시 재논의)
  - 그 외: 아이콘 하단 고정 방향(PWA 스플래시)

---

## 2026-08-13 (이어서 — 체크리스트 8번: 설정 화면 신설 + 데이터 관리)

### 진행 내용
- 사용자가 `temp/setting.jpg`(다른 메모앱의 설정 화면 캡처: 화면모드/글자크기/글씨체/글씨굵기/저장공간/데이터관리)를 공유하며 참고 의견 요청
- 여러 차례 대화로 범위 확정
  - **풀스크린 설정 화면**으로 신설하기로 결정(더보기 드롭다운은 항목이 늘면 답답해짐)
  - 필수 포함: **글자크기·화면모드**(더보기에서 이동), **데이터 관리**(신규 — CLAUDE.md 기술스택에 있었지만 미구현 상태였던 백업 기능을 이번에 구현)
  - **보류 결정**(이유 포함, 아래 "오늘 미구현 아이디어" 참고): 글씨체 선택, 글씨 굵기, 저장 공간 표시는 이번 범위에서 제외. 화면모드의 "시스템 연동"은 처음엔 보류 후보였다가 이후 대화에서 사용자가 명시적으로 원한다고 확정해 포함됨
  - 더보기 메뉴 재구성: **문장추가 / 문장관리(기존 "문장목록" 이름 변경) / 설정** 3항목만 남기기로 확정
  - 데이터 관리 백업 형식 논의: 처음 "JSON 내보내기"를 제안했으나, 기존 "파일가져오기"가 CSV/TSV만 지원해 복원 호환이 안 되는 문제를 발견 → **TSV로 통일**해 별도 복원 로직 없이 기존 파일가져오기를 그대로 재사용하는 방식으로 결정 (CLAUDE.md 기술스택의 "JSON 백업" 계획을 이번에 TSV로 정정)
  - 초기화 범위 확인: "문장만 삭제"(설정값은 유지), 결과는 **완전히 빈 상태(0개)**로 확정 → 카드 화면이 0개를 전제하지 않던 문제를 발견해 빈 상태 UI를 함께 설계
- 사용자 "진행" 승인 → 구현
  - `index.html`: 더보기 메뉴 3항목으로 축소, 신규 전체화면 `#settings-screen`(글자크기/화면모드 이동 + "데이터 관리" 섹션: 파일가져오기 이동, 백업 내보내기 신규 버튼, 초기화 신규 버튼), 카드 화면에 빈 상태 안내 문구(`#empty-state-msg`) 추가
  - `css/style.css`: 문장관리 화면과 설정 화면이 공유하는 `.screen-topbar`/`.screen-back-btn`/`.screen-topbar-title` 공용 클래스로 정리(중복 방지), 데이터 관리 버튼/섹션 타이틀/빈 상태 문구 스타일 신규
  - `js/app.js`
    - `updateSentence`/`deleteSentence`에 이어 `exportBackup`(문장을 TSV로 만들어 Blob 다운로드), `resetOpenBtn` 핸들러(confirm 확인 후 문장 전체 삭제) 추가
    - `renderCard()`/`goToSentence()`에 문장 0개 가드 추가: 안내 문구 노출 + 이전/확인/다음 버튼 비활성화, 문장이 다시 생기면 자동 복귀
    - 글자크기/화면모드(다크모드 토글) 로직은 id 기반 참조라 위치만 옮기고 코드 변경 없이 그대로 재사용
  - `CLAUDE.md` 체크리스트 8번 신규 기재, 기술스택 Backup 항목(JSON→TSV) 정정, 아이콘 목록·UI 레이아웃 규칙 갱신
- **1차 확인(Claude, 자동화)**: 임시 Playwright로 더보기 메뉴 3항목, 설정 화면 진입/뒤로가기, 글자크기·다크모드 새 위치에서도 정상 동작(회귀 없음), 데이터관리 3버튼, TSV 백업 다운로드(탭 구분·문장 5개), 문장관리 화면 타이틀 "문장 관리", 초기화 confirm→alert, 초기화 후 빈 상태 안내+버튼 비활성화, 문장 재추가 시 정상 복귀까지 모두 확인. 콘솔 에러 없음. 테스트 흔적 정리 완료
- **사용자가 커밋/push 전에 2차 확인을 요청하자 "실기기는 배포 URL로 확인하니 먼저 push해야 한다"고 지적** → 순서 실수를 인지하고 바로 커밋(`8f33d9d`) + push 진행 → Vercel 자동 재배포
- **사용자 2차 확인 완료** ("확인했어")

### 다음 작업 제안
- 다음 세션 시작 시 이 PROGRESS.md를 먼저 확인
- 브레인스토밍 핵심 항목: **문장 자동 변형 출제** (미결정), 체크리스트 3번(음성 답변, 후순위), 아이콘 하단 고정 방향(PWA 스플래시)
- 오늘 보류된 아이디어(아래 "오늘 미구현 아이디어" 참고)도 필요 시 재논의

---

## 2026-08-13 (이어서 — 설정 화면 보완: 글자크기 견본 + 화면모드 3단 선택 + 앱 정보 푸터)

### 진행 내용
- 사용자 요청 1: 글자크기 조절에 **견본**(여러 개 아니고 글자 하나로 실시간 크기 확인)을 보여달라
- 사용자 요청 2: 화면모드를 라이트/다크 토글이 아니라 **시스템/라이트/다크 3단 선택**으로 바꿔달라 (참고 이미지 `temp/setting.jpg`의 세그먼트 버튼 형태)
- 사용자 "진행" 승인 → 구현 (체크리스트 8번 보완)
  - `index.html`: 글자크기 행에 견본 글자(`#font-size-sample`, "가") 추가, 기존 `.theme-toggle` 스위치를 `.mode-select`(시스템/라이트/다크 3버튼, `role="radiogroup"`)로 교체
  - `css/style.css`: 견본 글자는 `font-size: var(--kr-font-size)`로 선언해 JS 추가 코드 없이 기존 `applyFontSize()`가 그대로 실시간 반영. 기존 `.theme-toggle`/`.theme-toggle-thumb` 스위치 스타일은 더 이상 안 쓰여 삭제하고 `.mode-select`/`.mode-select-btn(.active)` 신규 작성
  - `js/app.js`: `THEME_KEY` 저장값을 기존 `'dark'/'light'` 외에 `'system'`까지 포함하도록 확장(기존 저장값과 호환), `window.matchMedia('(prefers-color-scheme: dark)')`로 시스템 설정 감지, `resolveTheme(mode)`가 system일 때 실제 라이트/다크를 판정. `change` 이벤트 리스너를 추가해 "시스템" 선택 중에는 기기 설정이 바뀌면 앱도 실시간으로 따라가도록 구현 (다크/라이트를 명시적으로 고른 경우는 시스템 변경에 영향받지 않음)
- **1차 확인(Claude, 자동화)**: 임시 Playwright로 글자크기 조절 시 견본 글자가 22px→28px→22px로 실시간 반영되는지, 화면모드 기본값(라이트)·다크 선택·새로고침 후 유지 확인. `page.emulateMedia`로 시스템 다크/라이트 전환을 시뮬레이션해 "시스템" 모드일 때만 실시간 반영되고 "다크"를 명시적으로 고른 상태에서는 시스템이 바뀌어도 영향받지 않는 것을 확인. 콘솔 에러 없음. 테스트 흔적 정리 완료
- 커밋(`5a7f8ed`) + push 완료 → Vercel 자동 재배포
- 사용자가 "설정화면에 앱정보 제작자등 간단정보를 넣기도 해?" 질문 → Claude 의견: 개인용 앱이라 "제작자 정보"는 실용성이 낮지만, PWA 캐싱 문제를 겪은 적이 있어 **버전/최근 업데이트 날짜** 정도는 나중에 "이게 최신 버전 맞나?" 확인할 때 도움이 된다고 추천. 사용자 "진행" 승인
  - `index.html`: 설정 화면 데이터 관리 섹션 아래에 `<p class="settings-footer">TukTak · 최근 업데이트 2026-08-13</p>` 추가 (버전 번호 체계는 없어 날짜 기준으로 수동 갱신)
  - `css/style.css`: `.settings-footer`(`margin-top: auto`로 설정 화면 하단에 고정, 작은 회색 텍스트) 스타일 추가
  - **1차 확인(Claude, 자동화)**: Playwright로 푸터 텍스트/노출/화면 하단 위치(스크린샷으로 육안 확인 포함), 다크모드에서도 정상 노출 확인. 콘솔 에러 없음
  - 커밋(`5b8f067`) + push 완료 → Vercel 자동 재배포

### 오늘(2026-08-13) 미구현 아이디어 (설정 화면 관련, 보류)
- **글씨체 선택**(고딕/궁서/귀여운 등): CLAUDE.md에 기본 폰트가 Pretendard로 브랜드 정체성처럼 고정돼 있어 충돌, 폰트 파일도 추가로 구해야 해서 보류
- **글씨 굵기**(보통/굵게): 글자크기 기능과 목적이 겹쳐 우선순위 낮음으로 보류
- **저장 공간 표시**: 개인이 문장 몇백 개 저장하는 용도라 5MB 한도에 부딪힐 일이 거의 없어 정보성 대비 효용이 낮다고 판단해 보류
- **정렬/출제 순서 기능**(랜덤/오래된순 등, 예전부터 브레인스토밍에 있던 항목): 이번 범위에서는 제외. 나중에 만들 경우 **설정 화면 안에 새 행으로 추가**하는 게 자연스럽다고 정리됨(데이터관리와는 성격이 달라 별도 그룹). "카드 학습 화면 출제 순서"인지 "문장관리 목록 정렬"인지는 착수 시 다시 정할 것

### 다음 작업 제안
- 다음 세션 시작 시 이 PROGRESS.md를 먼저 확인
- 브레인스토밍 핵심 항목: **문장 자동 변형 출제** (미결정), 체크리스트 3번(음성 답변, 후순위), 아이콘 하단 고정 방향(PWA 스플래시)
- 위 "오늘 미구현 아이디어" 중 필요해지면 재논의 (글씨체/글씨굵기/저장공간표시/정렬기능)
